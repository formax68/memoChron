---
phase: quick-260515-l3j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - package.json
  - versions.json
autonomous: true
requirements:
  - REVERT-VERSION-01
must_haves:
  truths:
    - "manifest.json on main reports version 1.14.0 (matches the latest published GitHub release tag)"
    - "package.json reports version 1.14.0, keeping it in sync with manifest.json per BRAT/version-bump.mjs contract"
    - "versions.json no longer contains the 1.15.0-beta.1 entry (the 1.14.0 entry is preserved)"
    - "Obsidian users no longer fail to install/update because the manifest version corresponds to a real published release"
  artifacts:
    - path: "manifest.json"
      provides: "Plugin manifest with version 1.14.0"
      contains: '"version": "1.14.0"'
    - path: "package.json"
      provides: "npm manifest with version 1.14.0"
      contains: '"version": "1.14.0"'
    - path: "versions.json"
      provides: "Version-to-minAppVersion map without the beta entry"
      contains: '"1.14.0": "1.8.9"'
  key_links:
    - from: "manifest.json#version"
      to: "GitHub release tag"
      via: "Obsidian update fetcher / BRAT"
      pattern: 'Tag for version in manifest must exist as a published (non-draft) release'
    - from: "package.json#version"
      to: "manifest.json#version"
      via: "version-bump.mjs (npm version script)"
      pattern: "Both files must declare the same version string"
---

<objective>
Revert the version metadata on `main` from `1.15.0-beta.1` back to `1.14.0` across the three release-tracking files. This fixes the Obsidian install/update failure caused by `manifest.json` advertising version `1.15.0-beta.1` while no published GitHub release exists for that tag.

Purpose: Obsidian reads `manifest.json` on `main` to detect available versions and fetches release assets by tag. With `1.15.0-beta.1` referenced but no published release at that tag, every install/update attempt 404s. Reverting to `1.14.0` (which has a real published release) restores working installs.

Output: Three modified files committed as a single atomic commit. No code changes, no tag changes, no workflow changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@manifest.json
@package.json
@versions.json

<background>
Commit 4696020 bumped `manifest.json` / `package.json` / `versions.json` to `1.15.0-beta.1` to exercise the CI attestation flow for a Phase 6 DIR-12 acceptance check. The release workflow (.github/workflows/release.yml) creates GitHub releases with `--draft`, and the draft for `1.15.0-beta.1` was either never published or has been deleted — there is no published GitHub release for tag `1.15.0-beta.1`.

CLAUDE.md captures the rule that this revert restores: "DON'T update manifest.json in main branch until official release." The `1.15.0-beta.1` git tag itself is intentionally left in place as a historical CI test artifact (out of scope).
</background>

<current_state>
- `manifest.json` line 4: `"version": "1.15.0-beta.1"` → target `"1.14.0"`
- `package.json` line 3: `"version": "1.15.0-beta.1"` → target `"1.14.0"`
- `versions.json` line 46: `"1.15.0-beta.1": "1.8.9"` → must be removed
- `versions.json` line 45: `"1.14.0": "1.8.9"` → must be preserved untouched
- Note: removing the last entry also requires removing the trailing comma on the preceding line (line 45) so the JSON remains valid.
</current_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert version metadata to 1.14.0 and commit</name>
  <files>manifest.json, package.json, versions.json</files>
  <action>
Apply three targeted edits — do NOT run `npm version`, do NOT touch `version-bump.mjs`, do NOT modify anything under `src/`, `.github/`, or any other file.

1) `manifest.json` — change the `"version"` field value from `"1.15.0-beta.1"` to `"1.14.0"`. Leave all other fields (`id`, `name`, `minAppVersion`, `description`, `author`, `isDesktopOnly`) untouched. Preserve the existing tab indentation.

2) `package.json` — change the `"version"` field value from `"1.15.0-beta.1"` to `"1.14.0"`. Leave all other fields untouched. Preserve the existing 2-space indentation.

3) `versions.json` — remove the final `"1.15.0-beta.1": "1.8.9"` entry (currently line 46) AND fix the trailing comma on the preceding `"1.14.0": "1.8.9"` line (currently line 45) so the file remains valid JSON. After the edit, `"1.14.0": "1.8.9"` must be the final entry and must NOT end with a comma. Do NOT modify any other line in the file.

After the three edits, stage exactly these three files and create one atomic commit. Use a message in the repo's existing style — recent commits use the `chore(release): ...` / `fix(...): ...` / `docs(...): ...` form. Use:

    chore(release): revert version to 1.14.0

with a body explaining the revert briefly: the 1.15.0-beta.1 bump on main caused Obsidian update failures because no published GitHub release exists for that tag; reverting manifest, package, and versions.json restores working installs while leaving the historical git tag in place.

Per `CLAUDE.md`: the commit message MUST NOT reference Claude or AI assistance. Do not include a `Co-Authored-By` trailer. Do not run `git push`.
  </action>
  <verify>
    <automated>
# All three checks below must pass.

# 1. manifest.json version is exactly 1.14.0
grep -n '"version"' manifest.json | grep -q '"1.14.0"' && echo "manifest OK"

# 2. package.json version is exactly 1.14.0
grep -n '"version"' package.json | grep -q '"1.14.0"' && echo "package OK"

# 3. versions.json no longer mentions 1.15.0-beta.1, and still has 1.14.0
! grep -q '1.15.0-beta.1' versions.json && grep -q '"1.14.0": "1.8.9"' versions.json && echo "versions OK"

# 4. versions.json is still valid JSON (no trailing comma fallout)
node -e "JSON.parse(require('fs').readFileSync('versions.json','utf8')); console.log('versions.json parses OK')"

# 5. manifest.json and package.json are still valid JSON
node -e "JSON.parse(require('fs').readFileSync('manifest.json','utf8')); console.log('manifest.json parses OK')"
node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('package.json parses OK')"

# 6. Commit exists, touches exactly these three files, and does not reference Claude/AI
git log -1 --name-only --pretty=format:'%s%n%b%n---FILES---' | tee /tmp/last_commit.txt
grep -E '^manifest\.json$|^package\.json$|^versions\.json$' /tmp/last_commit.txt | sort -u | wc -l | grep -q '^3$' && echo "commit touches all 3 files"
! git log -1 --pretty=format:'%s%n%b' | grep -iE 'claude|anthropic|ai-generated|co-authored-by' && echo "commit message clean"
    </automated>
  </verify>
  <done>
- `manifest.json` declares `"version": "1.14.0"`.
- `package.json` declares `"version": "1.14.0"`.
- `versions.json` no longer contains `"1.15.0-beta.1"`; the `"1.14.0": "1.8.9"` entry is preserved as the final entry; the file is still valid JSON.
- A single new commit on `main` touches exactly these three files with a `chore(release): revert version to 1.14.0` message (or equivalent in the repo's commit style) and no Claude/AI references.
- No other files modified; no git tag created or deleted; no push performed.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes, the verify block above proves all required state. No additional phase-level verification needed — this is a three-file content revert with a single atomic commit.

Manual confirmation the developer can do later (optional, not required for plan completion):
- Pull `main` into an Obsidian vault using BRAT or the community plugin updater and confirm the update completes without 404.
</verification>

<success_criteria>
- `grep -n '"version"' manifest.json package.json` shows `1.14.0` in both files (and only `1.14.0`).
- `grep '1.15.0-beta.1' versions.json` returns no matches.
- `versions.json`, `manifest.json`, and `package.json` all parse as valid JSON (`node -e "JSON.parse(...)"` exits 0 for each).
- Exactly one new commit was created; `git show --stat HEAD` lists only `manifest.json`, `package.json`, and `versions.json`.
- `git log -1 --pretty=%B` contains no references to Claude, Anthropic, or AI authorship.
</success_criteria>

<output>
Create `.planning/quick/260515-l3j-revert-manifest-to-1-14-0-to-fix-obsidia/260515-l3j-SUMMARY.md` summarizing:
- Files changed (with old → new version values)
- Commit SHA of the revert
- Confirmation that verify checks 1–6 all passed
</output>
