---
phase: 08-type-hygiene-conventions
plan: 05
type: execute
wave: 4
depends_on:
  - 08-04
files_modified:
  - .planning/codebase/CONVENTIONS.md
  - CLAUDE.md
autonomous: false
requirements:
  - DOC-02

must_haves:
  truths:
    - "D-01: Canonical do/don't list lives in `.planning/codebase/CONVENTIONS.md`; `CLAUDE.md` links to it via the pointer section. Single source of truth, no duplicated rules list — drift risk eliminated"
    - "D-02: Inside CONVENTIONS.md, the `## Directory Compliance` section is grouped by rule CLUSTER (DOM API, Lifecycle & Compatibility, Type Hygiene, Release & Docs) — NOT by individual DIR-NN finding"
    - "D-05: This is commit 5 of the locked 5-commit ordering — the CLOSING commit of Phase 8 AND milestone v1.15 (DOC-02 lands last so the do/don't list reflects what the code actually does after v1.15)"
    - "`.planning/codebase/CONVENTIONS.md` contains a `## Directory Compliance` section appended at the end, with four `###` cluster sub-sections: DOM API, Lifecycle & Compatibility, Type Hygiene, Release & Docs"
    - "Each cluster has a 1-sentence intro stating which scorecard findings it closes, followed by rule blocks in the locked 4-line `Don't:` / `Do:` / `Why:` / `Docs:` format per D-03"
    - "Each `Docs:` URL was verified live by the researcher (see RESEARCH.md §Sources) — links go to canonical Obsidian / ESLint / typescript-eslint docs"
    - "`CLAUDE.md` lines 182-236 (the `## TODO: Code Quality Issues to Address` block) are replaced by a `## Directory Compliance` pointer section per D-04"
    - "`CLAUDE.md` `### Memory Reminders` (lines 238-242 in the pre-edit file) is preserved verbatim, immediately following the new `## Directory Compliance` section"
    - "`CLAUDE.md` `### Beta Release Strategy` section (line 66 in pre-edit file, BRAT workflow) is preserved verbatim — no edits in this commit affect CLAUDE.md lines 1-181, per D-04 which scopes the replacement to lines 182-236 only"
    - "`CLAUDE.md` does NOT contain the strings 'TODO: Code Quality Issues' or 'High Priority Issues' or 'Medium Priority Issues' anywhere after this commit"
    - "Manual 60-second Obsidian smoke test (D-06 / RESEARCH.md §'Smoke-Test Plan') executed and recorded in the commit message body"
    - "`npm run lint` exits 0 and `npm run build` exits 0 (no source changes in this commit; this is a sanity check that nothing regressed since Plan 04)"
  artifacts:
    - path: .planning/codebase/CONVENTIONS.md
      provides: "`## Directory Compliance` section with 4 sub-clusters, 14 rule blocks total, all in 4-line `Don't:`/`Do:`/`Why:`/`Docs:` format"
    - path: CLAUDE.md
      provides: "`## Directory Compliance` pointer section (replaces the stale TODO block); `### Memory Reminders` and `### Beta Release Strategy` preserved verbatim"
  key_links:
    - from: "CLAUDE.md `## Directory Compliance`"
      to: ".planning/codebase/CONVENTIONS.md#directory-compliance"
      via: "markdown link in the pointer section"
      pattern: "\\.planning/codebase/CONVENTIONS\\.md#directory-compliance"
---

<objective>
Closing commit of v1.15. Append the `## Directory Compliance` section to `.planning/codebase/CONVENTIONS.md` per D-01/D-02/D-03 using the verbatim cluster templates from RESEARCH.md §"DOC-02 Section Templates". Replace the stale `## TODO: Code Quality Issues to Address` block in `CLAUDE.md` with a `## Directory Compliance` pointer section per D-04, preserving `### Memory Reminders` verbatim AND preserving the `### Beta Release Strategy` section (line 66 in pre-edit CLAUDE.md) verbatim — the bounded replacement only touches lines 182-236.

Run the manual 60-second Obsidian smoke test per D-06 and record the result in the commit message body.

Purpose: Single source of truth for the directory-compliance rules going forward. Future plans land compliant by default because the rules are documented next to the codebase intel docs and CLAUDE.md (loaded into every session) points to them.

Output: 2 markdown files modified (no source changes); milestone v1.15 closes with this commit.

This task is `autonomous: false` because it includes a manual smoke-test checkpoint that pauses for the user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-type-hygiene-conventions/08-CONTEXT.md
@.planning/phases/08-type-hygiene-conventions/08-RESEARCH.md
@.planning/phases/08-type-hygiene-conventions/08-VALIDATION.md
@.planning/phases/08-type-hygiene-conventions/08-04-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append `## Directory Compliance` to CONVENTIONS.md (DOC-02 part A)</name>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (read the end of the file — confirm where to append; the new section sits at the very bottom)
    - .planning/phases/08-type-hygiene-conventions/08-RESEARCH.md §"DOC-02 Section Templates" → "CONVENTIONS.md skeleton" (lines 717-885) — the verbatim section to paste
  </read_first>
  <action>
    Append the `## Directory Compliance` section to the END of `.planning/codebase/CONVENTIONS.md`. Use the **verbatim text** from `08-RESEARCH.md` lines 719-885 (everything inside the outer ```markdown fenced block in §"CONVENTIONS.md skeleton (verbatim, ready to paste)"). The section consists of:

    1. A 4-line intro paragraph (lines 722-726 of RESEARCH.md).
    2. **`### DOM API`** sub-section — closes DIR-02, DIR-03, DIR-04. 3 rule blocks (innerHTML/outerHTML, inline `.style.*`, `document.createElement`).
    3. **`### Lifecycle & Compatibility`** sub-section — closes DIR-05, DIR-06, DIR-07, DIR-08. 4 rule blocks (view refs in plugin, popout-window helpers, `instanceof TFile`, floating promises).
    4. **`### Type Hygiene`** sub-section — closes DIR-01, DIR-09, DIR-10. 6 rule blocks (`console.*`, `any`, `no-case-declarations`, `no-useless-escape`, `??`-with-constant-LHS, unused vars/imports).
    5. **`### Release & Docs`** sub-section — closes DIR-11, DIR-12, DOC-01. 3 rule blocks (manifest punctuation, release attestation, lint gate).
    6. **`### Verifying compliance`** sub-section — a code block with 5 verification commands.

    Format constraints (D-03):
    - Each rule block uses EXACTLY the 4-line `Don't:` / `Do:` / `Why:` / `Docs:` shape, bolded labels.
    - Cluster intros are 1 sentence, NOT paragraphs.
    - Rule blocks do NOT include full before/after code examples (the codebase shows the compliant pattern at every call site after Phase 6/7).

    Paste integrity:
    - Copy the markdown content from RESEARCH.md lines 720-885 EXACTLY. Do not rewrite. Do not paraphrase. Do not reorder rules within a cluster.
    - The closing fence at line 885 of RESEARCH.md (` ``` `) is the end of the OUTER `markdown` fence — do NOT include that closing fence in the appended content. Stop at line 884 (the closing fence of the inner `bash` code block in `### Verifying compliance`).
    - Confirm the appended content starts with `## Directory Compliance` and ends with the closing fence of the `### Verifying compliance` bash code block.

    Do NOT modify any other content in CONVENTIONS.md. The existing 2026-05-09 analysis sections (Naming Patterns, Code Style, Import Organization, Access Modifiers, Error Handling, Logging, Comments, Function Design, Module Design, TypeScript Usage) stay intact above.

    Optional one-line addition to CONVENTIONS.md `## TypeScript Usage` section: if the planner judges it useful, add a short note "See `## Directory Compliance` below for the v1.15 rule on `any` — the documented `(window as any).moment` cast is now obsolete (typed `import { moment } from \"obsidian\"` per Amendment A1)." This is a one-line addition, not a rewrite of the §TypeScript Usage section. Default: SKIP this addition. The compliance section's cross-reference to §TypeScript Usage is implicit; explicit forward links bloat the section.
  </action>
  <verify>
    <automated>
      cd /Users/mike/code/memoChron && \
      grep -c '^## Directory Compliance' .planning/codebase/CONVENTIONS.md ; \
      # must be 1
      grep -cE '^### (DOM API|Lifecycle & Compatibility|Type Hygiene|Release & Docs)' .planning/codebase/CONVENTIONS.md ; \
      # must be 4
      grep -cE '^### Verifying compliance' .planning/codebase/CONVENTIONS.md ; \
      # must be 1
      grep -cE '^\*\*Don\x27t:\*\*' .planning/codebase/CONVENTIONS.md
      # must be at least 14 (rule blocks across all 4 clusters)
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Directory Compliance' .planning/codebase/CONVENTIONS.md` returns 1
    - `grep -cE '^### (DOM API|Lifecycle & Compatibility|Type Hygiene|Release & Docs)' .planning/codebase/CONVENTIONS.md` returns 4
    - `grep -cE '^### Verifying compliance' .planning/codebase/CONVENTIONS.md` returns 1
    - `grep -cE "^\\*\\*Don't:\\*\\*" .planning/codebase/CONVENTIONS.md` returns at least 14 (one per rule block: 3 DOM + 4 Lifecycle + 6 Type Hygiene + 3 Release/Docs = 16, but if researcher's split differs slightly the count may be 14-16; minimum 14 acceptable)
    - `grep -cE "^\\*\\*Do:\\*\\*" .planning/codebase/CONVENTIONS.md` returns the same count as `Don't:`
    - `grep -cE "^\\*\\*Why:\\*\\*" .planning/codebase/CONVENTIONS.md` returns the same count
    - `grep -cE "^\\*\\*Docs:\\*\\*" .planning/codebase/CONVENTIONS.md` returns the same count
    - The new section is at the END of CONVENTIONS.md (no content appears after the closing ```bash fence of `### Verifying compliance`)
    - No content above the new section was modified (confirmed via `git diff .planning/codebase/CONVENTIONS.md` — the diff should be append-only)
  </acceptance_criteria>
  <done>`## Directory Compliance` section is in CONVENTIONS.md with all 4 clusters, 14+ rule blocks, and the verification snippet.</done>
</task>

<task type="auto">
  <name>Task 2: Replace `## TODO: Code Quality Issues to Address` block in CLAUDE.md (DOC-02 part B)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (read lines 178-244 — must see the full TODO block + the `### Memory Reminders` subsection that follows AND any content immediately after, to confirm the replacement boundaries)
    - CLAUDE.md (read lines 60-130 — confirm `### Beta Release Strategy` is at line 66 and its content runs through line ~125; this section MUST NOT be touched)
    - .planning/phases/08-type-hygiene-conventions/08-RESEARCH.md §"DOC-02 Section Templates" → "CLAUDE.md replacement block" (lines 887-908) — the verbatim replacement
  </read_first>
  <action>
    Replace the `## TODO: Code Quality Issues to Address` section in `CLAUDE.md`. Boundaries:

    - **Start of replacement:** the line `## TODO: Code Quality Issues to Address` (currently CLAUDE.md line 182).
    - **End of replacement (inclusive):** the line ending the `### Notes` paragraph at the end of the TODO section (currently line 236, ending with "...code quality, security, and user experience.").
    - **Preserved verbatim AFTER the replacement:** the `### Memory Reminders` subsection (currently lines 238-242, including the blank line at 237 as the separator).
    - **PRESERVED VERBATIM BEFORE the replacement:** EVERYTHING from CLAUDE.md line 1 through line 181, INCLUDING the `### Beta Release Strategy` BRAT workflow section starting at line 66. This commit MUST NOT edit, paraphrase, or reformat any of lines 1-181 of CLAUDE.md.

    Read CLAUDE.md again before editing to confirm the exact line numbers (they may have shifted since the planning snapshot if any unrelated edits landed).

    The replacement content is the verbatim text from `08-RESEARCH.md` lines 890-904 (everything inside the fenced ```markdown block in §"CLAUDE.md replacement block"). Specifically:

    ```
    ## Directory Compliance

    v1.15 closed all Obsidian community-plugin scorecard findings from the v1.13.1 report.
    The canonical do/don't list — one short rule per finding with rationale and docs URL —
    lives in [.planning/codebase/CONVENTIONS.md#directory-compliance](.planning/codebase/CONVENTIONS.md#directory-compliance).

    Index of rule clusters:
    - **DOM API** — `innerHTML`/`outerHTML`, inline `element.style.*`, raw `document.createElement` (closes DIR-02 / DIR-03 / DIR-04)
    - **Lifecycle & Compatibility** — view refs in plugin, popout-window helpers, `instanceof TFile`, floating promises (closes DIR-05 / DIR-06 / DIR-07 / DIR-08)
    - **Type Hygiene** — `console.*`, `any` types, `case`-block declarations, useless escapes, unused vars (closes DIR-01 / DIR-09 / DIR-10)
    - **Release & Docs** — `manifest.json` punctuation, release attestation, ESLint + CI gate (closes DIR-11 / DIR-12 / DOC-01)

    ESLint enforces every rule above (`eslint.config.mjs`). CI runs `npm run lint`
    on every push and PR via `.github/workflows/lint.yml`.
    ```

    Final ordering verification: after the edit, `CLAUDE.md` reads (top-down):
    1. (everything from line 1 to line 181 — unchanged, INCLUDING `### Beta Release Strategy` at line 66)
    2. `## Directory Compliance` (new section, ~15 lines)
    3. blank line
    4. `### Memory Reminders` (preserved verbatim — 3 bullet items about no Claude references)
    5. blank line
    6. `<!-- GSD:project-start source:PROJECT.md -->` and everything below (unchanged)

    The `### Memory Reminders` subsection sits UNDER the new `## Directory Compliance` heading because it's `h3`. RESEARCH.md notes both this nesting and the alternative of promoting it to its own `## Project Conventions` `h2`; default keeps it under Directory Compliance for minimum diff. Either works — but DO NOT change the heading level in this commit.

    Do NOT touch any other content in CLAUDE.md.
  </action>
  <verify>
    <automated>
      cd /Users/mike/code/memoChron && \
      grep -c '^## TODO: Code Quality Issues to Address' CLAUDE.md ; \
      # must be 0
      grep -c '^## Directory Compliance' CLAUDE.md ; \
      # must be 1
      grep -c '^### Memory Reminders' CLAUDE.md ; \
      # must be 1
      grep -c '^### Beta Release Strategy' CLAUDE.md ; \
      # must be 1 (BRAT section preserved verbatim per D-04)
      grep -c 'CONVENTIONS.md#directory-compliance' CLAUDE.md ; \
      # must be at least 1 (the markdown link in the new pointer section)
      grep -c 'High Priority Issues\|Medium Priority Issues\|Low Priority Issues' CLAUDE.md
      # must be 0 (old TODO subsection headers gone)
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## TODO: Code Quality Issues to Address' CLAUDE.md` returns 0
    - `grep -c '^## Directory Compliance' CLAUDE.md` returns 1
    - `grep -c '^### Memory Reminders' CLAUDE.md` returns 1 (preserved)
    - `grep -c '^### Beta Release Strategy' CLAUDE.md` returns 1 (BRAT workflow section preserved verbatim per D-04)
    - `grep -c 'Using GitHub Pre-releases with BRAT' CLAUDE.md` returns 1 (BRAT subsection marker preserved — confirms the section's body content is intact, not just the heading)
    - `grep -cE '(High|Medium|Low) Priority Issues' CLAUDE.md` returns 0 (stale TODO subsections gone)
    - `grep -c 'CONVENTIONS.md#directory-compliance' CLAUDE.md` returns at least 1 (the new pointer)
    - `grep -c "Lifecycle and Cleanup\\|Security and Validation\\|Error Handling" CLAUDE.md` returns at most 0-1 (Error Handling appears elsewhere in CLAUDE.md as a top-level concept; the count check is to ensure the SPECIFIC TODO subsection that contained "Error Handling" with checkboxes is gone — confirm by grepping `^#### .* Error Handling$` which should be 0)
    - `grep -c 'don.t mention claude' CLAUDE.md` returns at least 1 (the existing `### Memory Reminders` bullets preserved — one of them reads "don't mention claude in commits")
    - The order in `CLAUDE.md` (verifiable via `grep -n '^## \\|^### ' CLAUDE.md`): `### Beta Release Strategy` appears BEFORE `## Directory Compliance` AND `## Directory Compliance` appears BEFORE `### Memory Reminders` AND `### Memory Reminders` appears BEFORE the next `## ` heading (whatever follows it)
    - `git diff CLAUDE.md` shows NO changes in the range corresponding to pre-edit lines 1-181 (i.e., the diff hunks all originate at or after pre-edit line 182)
  </acceptance_criteria>
  <done>CLAUDE.md TODO block replaced; Memory Reminders preserved; Beta Release Strategy preserved verbatim; pointer link present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 60-second Obsidian smoke test (D-06)</name>
  <what-built>
    All Phase 8 commits have landed:
    - Commit 1 (Plan 01): unused vars deleted (DIR-10)
    - Commit 2 (Plan 02): TypeScript hygiene tightened (DIR-09); `(window as any).moment` → typed `import { moment }`; lint exclusion for `**/*.d.ts`
    - Commit 3 (Plan 03): 33 console deletions + 6 gated forensic sites
    - Commit 4 (Plan 04): Phase-8 ESLint override block deleted
    - Commit 5 (this plan, Tasks 1+2): DOC-02 conventions doc + CLAUDE.md pointer

    No user-facing behavior change is intentional. The smoke test verifies that none of the mechanical edits broke the plugin in Obsidian. Per Phase 8 D-06, this is the closing-phase verification — its result is recorded in the commit message body, not in a separate HUMAN-UAT.md file.
  </what-built>
  <how-to-verify>
    Run the 8-step Obsidian smoke test from RESEARCH.md §"Smoke-Test Plan" (lines 910-942). Approximate time: 60 seconds.

    Before opening Obsidian, in the repo root:
    1. `npm run build` — confirm exits 0. This produces a fresh `main.js`.
    2. Copy `main.js`, `manifest.json`, and `styles.css` to the dev vault's MemoChron plugin folder (path: `<dev-vault>/.obsidian/plugins/memochron/`). If you symlink the source folder, no copy is needed.

    In Obsidian (with the dev vault that contains MemoChron):

    1. Toggle MemoChron OFF in Settings → Community Plugins. Confirm no errors in the developer console.
    2. Toggle MemoChron ON. Confirm sidebar reload works (no errors).
    3. Open the sidebar calendar (Cmd/Ctrl-P → "MemoChron: Open calendar"). Confirm the month grid renders and the agenda below it renders.
    4. Click the right arrow to navigate one month forward. Click the left arrow to navigate back. Confirm grid + agenda update correctly.
    5. Click an event in the agenda. Confirm a note is created (or opened, if it already exists).
    6. Open Settings → MemoChron. Confirm the calendar list renders and the "Add calendar" flow opens correctly.
    7. Toggle the "Hide calendar" setting. Confirm the calendar hides in the sidebar. Toggle again. Confirm it returns.
    8. Open a test note containing a `memochron-calendar` code block. Confirm the embedded calendar renders.

    **If any step fails:** stop, capture the error, and revert the offending commit. Do NOT proceed to the commit step.

    **If all 8 steps pass:** type "approved" to proceed to the commit step. The result will be recorded in the commit message body.

    **Optional UAT artifact (Claude's Discretion per D-06):** the planner judged a HUMAN-UAT.md file unnecessary because Phase 8 is code-internal hygiene with no user-visible behavior change intended. If the user disagrees and wants an artifact, type "create UAT.md" and the executor will write `.planning/phases/08-type-hygiene-conventions/08-HUMAN-UAT.md` with the 8 steps and the result before committing.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps pass. Type "issue: <description>" if any step fails. Type "create UAT.md" to also write a UAT file before commit.</resume-signal>
  <files>(no files modified — human verification only)</files>
  <action>Pause execution. Present the 8-step smoke-test checklist above to the user. The executor performs no edits during this task — the human runs Obsidian, walks through each step, and reports the result via the resume-signal. If the human types `create UAT.md`, the executor writes `.planning/phases/08-type-hygiene-conventions/08-HUMAN-UAT.md` with the 8 steps + result before resuming. If the human types `approved`, proceed to Task 4 (final commit). If the human types `issue: ...`, halt and surface the failure to the user for next-step decision (revert commit, fix forward, etc.).</action>
  <verify>
    <human-check>User confirms all 8 smoke-test steps passed (or reports a specific failure)</human-check>
  </verify>
  <done>Human responded to resume-signal; outcome (approved / issue / create UAT.md) recorded for use in Task 4's commit body.</done>
</task>

<task type="auto">
  <name>Task 4: Final lint + build sanity check, commit, and milestone close</name>
  <files>(no source files — verification + git only)</files>
  <read_first>
    - .planning/phases/08-type-hygiene-conventions/08-VALIDATION.md (DOC-02 rows + Build sanity row)
  </read_first>
  <action>
    Final commit. Per D-05 commit 5 — closing commit of Phase 8 AND milestone v1.15.

    1. **Final sanity:** run `npm run lint` and `npm run build`. Both must exit 0. (No source changes in this plan, but Plan 04 was the last source-affecting plan and lint passed there; this is a double-check.)

    2. **Capture the smoke-test result** from Task 3's resume-signal. Record:
       - "Obsidian smoke test (8 steps): all green" if Task 3 returned `approved`.
       - "Obsidian smoke test (8 steps): FAILED at step N — <description>" if Task 3 returned an issue (and in that case, the executor should NOT be at this commit step — they should have reverted and re-planned).

    3. **Lint-site resolution summary** to include in the commit body:
       - 39 no-console sites resolved (33 deleted, 6 gated) — Plan 03
       - 16 no-explicit-any sites resolved (1 ambient site tightened, 5 ambient covered by `**/*.d.ts` lint-exclude, 5 typed-import migrations per A1, 2 jCal shim additions, 1 unknown narrowing, 2 real-type substitutions) — Plan 02
       - 18 no-unused-vars sites resolved — Plan 01
       - 1 no-case-declarations + 1 no-useless-escape resolved — Plan 02
       - 0 ??-with-constant-LHS sites found in executable code (1 documentation-comment hit at SettingsTab.ts:263 unchanged) — Plan 02 audit
       - Phase-8 eslint.config.mjs override block deleted — Plan 04

    4. **Stage and commit** with this EXACT message (no Claude/AI references):
       ```
       docs(08): add Directory Compliance conventions (DOC-02)

       Closes v1.15 Directory Compliance milestone.

       Adds `## Directory Compliance` section to
       .planning/codebase/CONVENTIONS.md with 4 clusters (DOM API,
       Lifecycle & Compatibility, Type Hygiene, Release & Docs) — one
       4-line Don't/Do/Why/Docs block per rule, links to canonical
       Obsidian / ESLint / typescript-eslint docs.

       Replaces stale `## TODO: Code Quality Issues to Address` block in
       CLAUDE.md (lines 182-236) with a `## Directory Compliance` pointer
       section that links to the canonical doc. `### Memory Reminders`
       and `### Beta Release Strategy` (BRAT workflow) both preserved
       verbatim — bounded replacement only touches lines 182-236.

       Lint sites resolved across Phase 8:
       - 18 no-unused-vars (Plan 01)
       - 16 no-explicit-any + 1 no-case-declarations + 1 no-useless-escape (Plan 02)
       - 39 no-console (33 deleted, 6 gated behind `const DEBUG = false`) (Plan 03)
       - 0 ??-with-constant-LHS in executable code (audit clean)
       - Phase-8 eslint.config.mjs override block deleted (Plan 04)

       Verification:
       - npm run lint: clean
       - npm run build: clean
       - Obsidian smoke test (8 steps): all green
       ```

    5. **Confirm `git status` is clean** after commit.

    6. **Phase 8 done; milestone v1.15 closed.** STATE.md update is handled by post-phase orchestration (not this plan).
  </action>
  <verify>
    <automated>
      cd /Users/mike/code/memoChron && \
      npm run lint && npm run build && \
      git log -1 --pretty=%B | grep -ciE 'claude|anthropic|claude.ai|generated with' \
      # must be 0 — commit message has no AI/Claude references
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - `git log -1 --pretty=%B` shows the commit message above
    - `git log -1 --pretty=%B | grep -ciE 'claude|anthropic'` returns 0 (no AI references)
    - `git log -1 --pretty=%B | grep -c 'Obsidian smoke test'` returns at least 1 (smoke-test result recorded)
    - `git status` is clean
    - `git log --oneline -5` shows the last 5 commits on `main` are Phase 8's commits 1-5 in order (most recent first: docs(08), chore(lint), refactor(logs), refactor(types), refactor:)
  </acceptance_criteria>
  <done>Phase 8 commit 5 landed. Milestone v1.15 ships when the user tags and releases.</done>
</task>

</tasks>

<verification>
- `## Directory Compliance` section in CONVENTIONS.md with 4 clusters, all rule blocks in 4-line format
- CLAUDE.md TODO block replaced; `### Memory Reminders` preserved verbatim; `### Beta Release Strategy` (BRAT workflow) preserved verbatim (no edits to CLAUDE.md lines 1-181)
- `npm run lint` and `npm run build` both exit 0
- 8-step Obsidian smoke test passed (recorded in commit body)
- Commit message contains no AI/Claude references
- 5 phase commits landed in order on `main`
</verification>

<success_criteria>
- DOC-02 closed at the acceptance level. Milestone v1.15 closes with this commit.
- Phase 8 success criterion #5 satisfied: "CLAUDE.md and `.planning/codebase/CONVENTIONS.md` carry a 'Directory Compliance' do/don't section with one short rule per scorecard finding, each with a one-line rationale and a link to the relevant Obsidian docs page."
</success_criteria>

<output>
Create `.planning/phases/08-type-hygiene-conventions/08-05-SUMMARY.md` when done, including:
- Number of rule blocks per cluster (DOM API / Lifecycle / Type Hygiene / Release & Docs)
- The smoke-test outcome (all 8 steps green, or list of failures)
- Any deviations from the verbatim template (e.g., a docs URL the executor verified was 404 and substituted)
- Whether the optional UAT.md artifact was created
- Confirmation that CLAUDE.md `### Beta Release Strategy` section was preserved verbatim (no diff in lines 1-181)
- Commit SHA
- Final milestone state: "v1.15 Directory Compliance closed; ready to tag release"
</output>
</content>
</invoke>