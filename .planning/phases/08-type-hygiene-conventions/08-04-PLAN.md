---
phase: 08-type-hygiene-conventions
plan: 04
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-03
files_modified:
  - eslint.config.mjs
  - src/services/CalendarService.ts
  - src/utils/timezoneUtils.ts
autonomous: true
requirements:
  - DIR-01
  - DIR-09
  - DIR-10

must_haves:
  truths:
    - "D-10: The Phase-8 override block in `eslint.config.mjs` (the contiguous block marked with `// Phase 8` comments — currently lines 73–113) is deleted in its entirety. Per D-10: both sub-blocks (rule overrides + closed-set `files: [...]` no-unused-vars disable) go in the same commit"
    - "D-08-extension: A narrow `src/**/*.ts` override is added that disables the typed-linting `no-unsafe-*` siblings (`no-unsafe-assignment`, `no-unsafe-member-access`, `no-unsafe-call`, `no-unsafe-argument`, `no-unsafe-return`). Same root cause as the existing `**/*.d.ts no-explicit-any: off` exclusion: ical.js is fundamentally untyped, hand-typing is deferred to FRAG-02. This override is NOT tied to a scorecard finding (`no-unsafe-*` is not a DIR-NN rule)"
    - "D-05: This is commit 4 of the locked 5-commit ordering (mechanical first, override-delete then DOC-02 last) per Phase 6/7 precedent"
    - "The `**/*.d.ts` `no-explicit-any: off` block added by Plan 02 is PRESERVED — it is an addition, not part of the Phase-8 override block"
    - "The `globalIgnores([...])` block at the bottom is PRESERVED — it is unrelated to Phase 8 per Amendment A3"
    - "Class 2 housekeeping (Plan 03 Rule-1 bug fix): `CalendarService.ts:303` uses `console.log` which the `obsidianmd/rule-custom-message` rule disallows once the Phase-8 override is gone. Change to `console.debug` (which the rule allows). The `MemoChron:` prefix convention is preserved"
    - "Class 3 housekeeping (Plan 03 Rule-1 bug fix): The 6 `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)` comments Plan 03 added are removed. The `no-console` rule is OFF in obsidianmd's recommended config (not just in the Phase-8 override), so these disables never had effect — they would emit unused-disable-directive warnings once the Phase-8 override is gone. Sites: `src/services/CalendarService.ts:302,388,407,458` and `src/utils/timezoneUtils.ts:211,221`"
    - "`npm run lint` exits 0 cleanly. Remaining narrow overrides: Phase-5 tightening + Plan-02 `**/*.d.ts` exclusion + the new D-08-extension `src/**/*.ts` `no-unsafe-*` block"
    - "`npm run build` exits 0"
  artifacts:
    - path: eslint.config.mjs
      provides: "Final clean config — Phase-5 tightening block + Plan-02 `**/*.d.ts` exclusion + D-08-extension `src/**/*.ts no-unsafe-*: off` block + globalIgnores ONLY. Phase-8 override block deleted."
    - path: src/services/CalendarService.ts
      provides: "Line 303 uses `console.debug` (not `console.log`); 4 unused `eslint-disable-next-line no-console` comments removed (lines 302, 388, 407, 458 in pre-edit file)."
    - path: src/utils/timezoneUtils.ts
      provides: "2 unused `eslint-disable-next-line no-console` comments removed (lines 211, 221 in pre-edit file)."
  key_links:
    - from: "eslint.config.mjs"
      to: "all source files in src/**/*.ts"
      via: "obsidianmd.configs.recommended + Phase-5 tightening (no-unused-vars: error + no-restricted-syntax for createElement) + Plan-02 .d.ts exclusion + D-08-extension no-unsafe-* exclusion"
      pattern: "obsidianmd.configs.recommended"
---

<objective>
Delete the Phase-8 ESLint override block from `eslint.config.mjs`, add a narrow D-08-extension override for `src/**/*.ts no-unsafe-*: off`, and apply Plan 03 Rule-1 housekeeping fixes (Class 2: `console.log` → `console.debug`; Class 3: remove 6 unused `eslint-disable-next-line no-console` comments). This is the **acceptance commit** for DIR-01, DIR-09, and DIR-10.

**Amendment history (2026-05-17):** The original plan attempted a clean override-block delete with no compensating overrides. The executor surfaced a Rule 4 architectural checkpoint when removing the Phase-8 block exposed 33 errors:
- 32 typed-linting `no-unsafe-*` cascade errors from ical.js's untyped APIs (covered in `ical.d.ts` per D-08).
- 1 `obsidianmd/rule-custom-message` error on `CalendarService.ts:303` (wrong console method per Plan 03).
- 6 unused `eslint-disable` warnings from Plan 03's disable comments targeting the wrong rule.

User chose **Option B** (narrow `no-unsafe-*` override matching D-08's documented strategy) plus the Class 2/3 housekeeping fixes. See `08-04-CHECKPOINT.md` for the full analysis.

Output: `eslint.config.mjs` net change ≈ -33 lines (deleted ~40-line block + added ~7-line D-08-extension); 7 lines edited across 2 source files; lint passes clean; phase moves toward the closing DOC-02 commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-type-hygiene-conventions/08-CONTEXT.md
@.planning/phases/08-type-hygiene-conventions/08-RESEARCH.md
@.planning/phases/08-type-hygiene-conventions/08-VALIDATION.md
@.planning/phases/08-type-hygiene-conventions/08-04-CHECKPOINT.md
@.planning/phases/08-type-hygiene-conventions/08-02-SUMMARY.md
@.planning/phases/08-type-hygiene-conventions/08-03-SUMMARY.md
@eslint.config.mjs
@src/services/CalendarService.ts
@src/utils/timezoneUtils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Class 2/3 housekeeping — Plan 03 Rule-1 bug fixes</name>
  <files>src/services/CalendarService.ts, src/utils/timezoneUtils.ts</files>
  <read_first>
    - src/services/CalendarService.ts lines 295–310, 385–395, 400–415, 455–465 (the 4 sites with the unused disable + the console.log site)
    - src/utils/timezoneUtils.ts lines 205–225 (the 2 sites with the unused disable)
    - 08-04-CHECKPOINT.md §"The Three Error Classes" (Class 2 + Class 3 detail)
  </read_first>
  <action>
    1. **Fix Class 2** — `src/services/CalendarService.ts` line 303 currently reads:
       ```ts
             // eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)
             if (DEBUG) console.log("MemoChron: No cache found or cache invalid", errorMessage(error));
       ```
       Change to (delete the eslint-disable comment AND swap `console.log` → `console.debug`):
       ```ts
             if (DEBUG) console.debug("MemoChron: No cache found or cache invalid", errorMessage(error));
       ```
       Rationale: the `obsidianmd/rule-custom-message` rule disallows `console.log` but allows `console.debug`. The `MemoChron:` prefix convention is preserved. The disable comment is removed because (a) it targeted the wrong rule and (b) `console.debug` doesn't need any disable.

    2. **Fix Class 3 (CalendarService.ts)** — remove the `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)` comments at lines 388, 407, 458 (the three remaining sites in this file after Step 1 removed the line-302 one). Each site below has the disable on the line BEFORE the console call:
       - Line 388 disable + line 389 `console.error(` — keep the `console.error` call, delete only the disable comment line.
       - Line 407 disable + line 408 `console.error(...)` — keep the call, delete only the disable line.
       - Line 458 disable + line 459 `console.error(...)` — keep the call, delete only the disable line.

       After this step, `grep -n "eslint-disable-next-line no-console" src/services/CalendarService.ts` must return 0 hits.

    3. **Fix Class 3 (timezoneUtils.ts)** — remove the `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)` comments at lines 211 and 221. Each precedes a `console.warn` or `console.error` call that the obsidianmd rule already allows; keep the calls, delete only the disable lines.

       After this step, `grep -n "eslint-disable-next-line no-console" src/utils/timezoneUtils.ts` must return 0 hits.

    4. **Verify before committing**:
       - `grep -rn "eslint-disable-next-line no-console" src/` returns 0 lines.
       - `grep -n "console.log" src/services/CalendarService.ts` returns 0 lines.
       - `npm run build` exits 0.

    5. **Commit** with this EXACT message (no Claude/AI references):
       ```
       fix(08-04): clean up Plan 03 disable directives and console.log site

       Plan 03 added 6 `// eslint-disable-next-line no-console -- DEBUG
       flag (Phase 8 D-07)` comments expecting Plan 04 to activate
       `no-console` globally. The `no-console` rule is OFF in obsidianmd's
       recommended config (not just in the Phase-8 override), so the
       disables never had effect — they become "unused eslint-disable
       directive" warnings once the Phase-8 override is removed.

       Also changes CalendarService.ts:303 from `console.log` to
       `console.debug` so it complies with `obsidianmd/rule-custom-message`
       (which disallows console.log but allows console.debug). The
       MemoChron: prefix convention is preserved.

       Removed disables: CalendarService.ts:302,388,407,458 and
       timezoneUtils.ts:211,221.

       Prepares the tree for the Phase-8 override-block deletion in the
       next commit.
       ```
  </action>
  <verify>
    <automated>
      grep -rn "eslint-disable-next-line no-console" src/ ; \
      # must return 0 lines
      grep -n "console.log" src/services/CalendarService.ts ; \
      # must return 0 lines
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "eslint-disable-next-line no-console" src/` returns 0 lines
    - `grep -n "console.log" src/services/CalendarService.ts` returns 0 lines (the only console.log site is now console.debug)
    - `grep -c "console.debug" src/services/CalendarService.ts` returns at least 1 (the new line 303 site)
    - `npm run build` exits 0
    - `git log -1 --pretty=%B` shows the commit message above; does NOT contain "Claude", "AI", "Anthropic"
  </acceptance_criteria>
  <done>Plan 03's wrong-rule disables removed; console.log replaced; tree ready for override delete.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Phase-8 override block, add D-08-extension narrow override</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (full file — confirm line numbers BEFORE editing; the Phase-8 block currently spans lines 73–113)
    - 08-04-CHECKPOINT.md §"The Architectural Decision (Rule 4)" → Option B (the override block to add)
  </read_first>
  <action>
    1. **Read `eslint.config.mjs` in full.** Confirm the Phase-8 block boundaries:
       - First marker: line 73 `// ---------------------------------------------------------------------------`
       - Second marker (header): line 74 `// Phase 8 — DIR-01 / DIR-09 / DIR-10 will remove these when type-hygiene`
       - Closing `},` of the second sub-block: line 113
       - The blank line at 114 and `globalIgnores([` at line 115 must remain.

    2. **Delete lines 73–113 inclusive.** Acceptance verified by:
       - `grep -c "// Phase 8" eslint.config.mjs` → 0
       - `grep -c '"no-console": "off"' eslint.config.mjs` → 0
       - `grep -c "no-case-declarations" eslint.config.mjs` → 0
       - `grep -c "no-useless-escape" eslint.config.mjs` → 0
       - `grep -c "globalIgnores" eslint.config.mjs` → 2 (import + call — unchanged)
       - `grep -c '"\*\*/\*\.d\.ts"' eslint.config.mjs` → 1 (Plan 02's exclusion preserved)

    3. **Add the D-08-extension override** in the gap left by the deletion (between the `**/*.d.ts` block and `globalIgnores`). Paste this block VERBATIM:
       ```js
         // D-08-extension — `no-unsafe-*` typed-linting cascade from ical.js's
         // untyped APIs (`parse`, `getFirstPropertyValue`, `getFirstValue`,
         // constructor, `fromData`). Same root cause as the existing
         // `**/*.d.ts` `no-explicit-any: off` exclusion: ical.js is
         // fundamentally untyped, hand-typing is deferred to FRAG-02. This
         // silences the consumption-site cascade at the call sites in
         // CalendarService, IcsImportService, NoteService, CalendarView,
         // and main.ts. NOT tied to a scorecard finding — `no-unsafe-*`
         // is not a DIR-NN rule.
         {
           files: ["src/**/*.ts"],
           rules: {
             "@typescript-eslint/no-unsafe-assignment": "off",
             "@typescript-eslint/no-unsafe-member-access": "off",
             "@typescript-eslint/no-unsafe-call": "off",
             "@typescript-eslint/no-unsafe-argument": "off",
             "@typescript-eslint/no-unsafe-return": "off",
           },
         },
       ```
       Place this block immediately after the existing `**/*.d.ts` `no-explicit-any: off` block (formerly lines 65–71, unchanged) and before `globalIgnores`. Two-space indent to match surrounding blocks. Trailing `,` is required.

    4. **Run `npm run lint`.** Must exit 0.
       - If failures remain after Step 3, they are likely missed sites — pause and inspect; do NOT add more overrides. The expected end state is 0 errors, 0 warnings.

    5. **Run `npm run build`.** Must exit 0.

    6. **Commit** with this EXACT message (no Claude/AI references):
       ```
       chore(lint): remove Phase 8 ESLint overrides + add D-08-extension (DIR-01/09/10 acceptance)

       Deletes the contiguous Phase-8 override block from eslint.config.mjs
       (lines 73-113 — the `// Phase 8 — DIR-01 / DIR-09 / DIR-10` headers
       and both sub-blocks).

       Adds the D-08-extension override (a `src/**/*.ts no-unsafe-*: off`
       block) that mirrors the existing `**/*.d.ts no-explicit-any: off`
       exclusion's strategy: silence the typed-linting cascade from
       ical.js's untyped APIs at the consumption sites, since the root-
       cause fix (hand-typing ical.js) is deferred to FRAG-02. The new
       override is NOT tied to a scorecard finding — `no-unsafe-*` is
       not a DIR-NN rule.

       Closes scorecard findings DIR-01, DIR-09, DIR-10 at the acceptance
       level: `npm run lint` now passes clean. Remaining narrow overrides:
       - Phase-5 tightening (no-unused-vars: error, no-restricted-syntax
         for document.createElement)
       - `**/*.d.ts` no-explicit-any: off (Plan 02 — ical.d.ts shim)
       - `src/**/*.ts` no-unsafe-*: off (this commit — ical.js cascade)
       - globalIgnores (node_modules, main.js, build scripts)

       See 08-04-CHECKPOINT.md for the Rule-4 architectural escalation
       that surfaced this design (Option B was chosen).
       ```
  </action>
  <verify>
    <automated>
      grep -c "// Phase 8" eslint.config.mjs ; \
      # must be 0
      grep -c '"no-console": "off"' eslint.config.mjs ; \
      # must be 0
      grep -c "no-case-declarations" eslint.config.mjs ; \
      # must be 0
      grep -c "no-useless-escape" eslint.config.mjs ; \
      # must be 0
      grep -c "D-08-extension" eslint.config.mjs ; \
      # must be 1 (the new comment header)
      grep -c "no-unsafe-assignment" eslint.config.mjs ; \
      # must be 1 (in the new D-08-extension block)
      grep -c '"\*\*/\*\.d\.ts"' eslint.config.mjs ; \
      # must be 1 (Plan 02's exclusion preserved)
      grep -c "globalIgnores" eslint.config.mjs ; \
      # must be 2 (import + call)
      npm run lint && npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "// Phase 8" eslint.config.mjs` returns 0
    - `grep -c '"no-console": "off"' eslint.config.mjs` returns 0
    - `grep -c '"no-case-declarations": "off"' eslint.config.mjs` returns 0
    - `grep -c '"no-useless-escape": "off"' eslint.config.mjs` returns 0
    - `grep -c "D-08-extension" eslint.config.mjs` returns 1
    - `grep -c "no-unsafe-assignment" eslint.config.mjs` returns 1
    - `grep -c '"\*\*/\*\.d\.ts"' eslint.config.mjs` returns 1 (Plan 02's `.d.ts` block preserved)
    - `grep -c "globalIgnores" eslint.config.mjs` returns 2 (import + call)
    - `wc -l < eslint.config.mjs` returns approximately 90 ±5 lines (was 122; -42 from block delete +14 from D-08-extension = ~94)
    - `npm run lint` exits 0 with no warnings and no errors
    - `npm run build` exits 0
    - `git log -1 --pretty=%B` shows the commit message above; does NOT contain "Claude", "AI", "Anthropic", "claude.ai" (case-insensitive)
    - `git status` is clean after commit
  </acceptance_criteria>
  <done>Phase-8 override block deleted; D-08-extension override added; lint passes clean; Phase 8 commit 4 landed.</done>
</task>

</tasks>

<verification>
- `eslint.config.mjs` net change: -42 lines from override delete, +14 lines from D-08-extension (net ≈ -28 lines)
- No `// Phase 8` comment markers remain
- `**/*.d.ts` block preserved; `globalIgnores` preserved; new D-08-extension block present
- 2 source files cleaned (Class 2: 1 console.log→console.debug; Class 3: 6 unused disables removed)
- `npm run lint` exits 0 cleanly
- `npm run build` exits 0
- DIR-01, DIR-09, DIR-10 are now closed at the acceptance level
</verification>

<success_criteria>
- The scorecard requirement "Phase 5 ESLint overrides removed; `npm run lint` passes against a clean configuration with no per-rule or per-file disables tied to scorecard findings" (ROADMAP Phase 8 success criterion #4) is satisfied. The D-08-extension is explicitly NOT tied to a scorecard finding (`no-unsafe-*` is not a DIR-NN rule).
</success_criteria>

<output>
Create `.planning/phases/08-type-hygiene-conventions/08-04-SUMMARY.md` when done, including:
- Final line count of `eslint.config.mjs` (before/after)
- The exact span of lines deleted from the Phase-8 block
- Confirmation that `**/*.d.ts`, `globalIgnores`, and the new D-08-extension block are all present
- `npm run lint` output (must be clean — no error or warning lines)
- Both commit SHAs (Task 1 housekeeping commit + Task 2 override-delete commit)
- A note acknowledging the Rule-4 amendment path and that `08-04-CHECKPOINT.md` documents the design rationale
</output>
