---
phase: 08-type-hygiene-conventions
plan: 04
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-03
files_modified:
  - eslint.config.mjs
autonomous: true
requirements:
  - DIR-01
  - DIR-09
  - DIR-10

must_haves:
  truths:
    - "D-10: The Phase-8 override block in `eslint.config.mjs` (the contiguous block marked with `// Phase 8` comments — formerly lines 65-105, may have shifted to ~69-109 after Plan 02 added the `**/*.d.ts` exclusion block) is deleted in its entirety. Per D-10: both sub-blocks (rule overrides + closed-set `files: [...]` no-unused-vars disable) go in the same commit"
    - "D-05: This is commit 4 of the locked 5-commit ordering (mechanical first, override-delete then DOC-02 last) per Phase 6/7 precedent"
    - "The `**/*.d.ts` `no-explicit-any: off` block added by Plan 02 is PRESERVED — it is an addition, not part of the Phase-8 override block"
    - "The `globalIgnores([...])` block (currently at lines ~107-114) is PRESERVED — it is unrelated to Phase 8 per Amendment A3"
    - "`npm run lint` exits 0 cleanly without any per-rule or per-file disables tied to scorecard findings (the only remaining narrow overrides are the Phase-5 tightening block and the `**/*.d.ts` ambient-shim exclusion)"
    - "`npm run build` exits 0"
  artifacts:
    - path: eslint.config.mjs
      provides: "Final clean config — Phase-5 tightening block + Plan-02 `**/*.d.ts` exclusion + globalIgnores ONLY. Phase-8 override block deleted."
  key_links:
    - from: "eslint.config.mjs"
      to: "all source files in src/**/*.ts"
      via: "obsidianmd.configs.recommended + Phase-5 tightening (no-unused-vars: error + no-restricted-syntax for createElement) + Plan-02 .d.ts exclusion"
      pattern: "obsidianmd.configs.recommended"
---

<objective>
Delete the Phase-8 ESLint override block from `eslint.config.mjs`. This is the **acceptance commit** for DIR-01, DIR-09, and DIR-10 — by removing the rule suppressions and verifying `npm run lint` passes clean, the directory-scorecard finding for each of those three requirements is closed.

Per Amendment A3, the override block is at lines **65-105** in the CONTEXT.md-snapshot state of `eslint.config.mjs`. However, Plan 02 inserted a 4-line `**/*.d.ts` exclusion block at approximately the same location (between the Phase-5 tightening block and the Phase-8 override block), so the override block may have shifted by +4 lines. **The executor MUST re-read `eslint.config.mjs` first** and locate the override block by its `// Phase 8` comment anchors rather than by literal line numbers.

Purpose: Fourth commit of Phase 8. Pure config cleanup. No source changes. The fact that lint passes after the override deletion is the proof that DIR-01/09/10 are closed.

Output: `eslint.config.mjs` is ~40 lines shorter; lint passes clean; phase moves toward the closing DOC-02 commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-type-hygiene-conventions/08-CONTEXT.md
@.planning/phases/08-type-hygiene-conventions/08-RESEARCH.md
@.planning/phases/08-type-hygiene-conventions/08-VALIDATION.md
@.planning/phases/08-type-hygiene-conventions/08-02-SUMMARY.md
@.planning/phases/08-type-hygiene-conventions/08-03-SUMMARY.md
@eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read eslint.config.mjs, locate the Phase-8 override block, delete it, run lint</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (full file — must read to confirm line numbers BEFORE deleting; line numbers may have shifted after Plan 02)
    - .planning/phases/08-type-hygiene-conventions/08-RESEARCH.md §"eslint.config.mjs Line Range Verification" (lines 653-670)
    - 08-CONTEXT.md "Amendments" section A3 (corrects line range to 65-105 from CONTEXT.md's original 66-109)
  </read_first>
  <action>
    1. **Read `eslint.config.mjs` in full.** Locate the contiguous Phase-8 block:
       - Starts with a comment line containing `// Phase 8` (the block begins with a horizontal rule separator and a `// Phase 8 — DIR-01 / DIR-09 / DIR-10 …` comment header).
       - First sub-block: `files: ["src/**/*.ts"]` with rule overrides for `obsidianmd/rule-custom-message`, `no-console`, `@typescript-eslint/no-explicit-any` (and 5 `no-unsafe-*` siblings), `no-case-declarations`, `no-useless-escape`. Closes with `},`.
       - Then a second `// Phase 8 — DIR-01 will remove these …` comment header.
       - Second sub-block: `files: [...]` with a closed-set list of 7 file paths disabling `@typescript-eslint/no-unused-vars`. Closes with `},`.

    2. **Identify the exact line span.** Per Amendment A3, in the CONTEXT.md-snapshot the span was 65-105. After Plan 02 added a 4-line `**/*.d.ts` exclusion block AFTER the Phase-5 tightening block but BEFORE the Phase-8 override block, the Phase-8 block likely starts at approximately line 69 now. The closing `},` is followed (after a blank line) by `globalIgnores([` — that's the boundary marker.

       Use `grep -n "// Phase 8" eslint.config.mjs` to find the comment markers. Use `grep -n "globalIgnores" eslint.config.mjs` to find the next-block start. The Phase-8 span is everything from the first `// Phase 8` comment (and the preceding `// ---...` separator line if present) down through the closing `},` of the second sub-block (just before the blank line that precedes `globalIgnores`).

    3. **Verify the `**/*.d.ts` block from Plan 02 is OUTSIDE the Phase-8 span.** The `.d.ts` block was placed AFTER the Phase-5 tightening block (which ends around line 63) and BEFORE the Phase-8 override block. After deletion, the `.d.ts` block must remain. Visual check: `grep -n '"\\*\\*/\\*\\.d\\.ts"' eslint.config.mjs` should return 1 hit BOTH before and after the deletion.

    4. **Verify `globalIgnores` is OUTSIDE the Phase-8 span.** Per Amendment A3: `globalIgnores([...])` at the bottom of the file is NOT Phase-8 and MUST be preserved. Visual check: `grep -n "globalIgnores" eslint.config.mjs` should return 1 hit BOTH before and after the deletion.

    5. **Delete the Phase-8 override block.** Edit `eslint.config.mjs` and remove every line from the first `// Phase 8` comment (inclusive of any preceding `// ---...` separator) through the closing `},` of the second override sub-block (inclusive). The blank line that precedes `globalIgnores` may stay or be cleaned up — either is acceptable.

    6. **Run `npm run lint`.** Must exit 0.
       - If lint FAILS, the failure is one of:
         - (a) a leftover violation Plan 01/02/03 missed (e.g., a `no-console` site that wasn't gated or deleted; a `no-unused-vars` site that crept back in)
         - (b) the override-block delete accidentally removed too much (e.g., snipped into the `**/*.d.ts` block or the `globalIgnores` block)
       - For (a), pause and inspect the failing site; if it's an actual Phase 8 regression, fix the source file and re-run lint.
       - For (b), restore `eslint.config.mjs` and re-apply the deletion more carefully.

    7. **Run `npm run build`.** Must exit 0.

    8. **Stage and commit** with this EXACT message (no Claude/AI references):
       ```
       chore(lint): remove Phase 8 ESLint overrides (DIR-01/09/10 acceptance)

       Deletes the contiguous Phase-8 override block from eslint.config.mjs
       (formerly lines 65-105 per Amendment A3; shifted by +4 after Plan 02
       added the **/*.d.ts no-explicit-any: off exclusion).

       Closes scorecard findings DIR-01, DIR-09, DIR-10 at the acceptance
       level: `npm run lint` now passes clean without any per-rule or
       per-file suppressions tied to scorecard findings.

       Preserved:
       - Phase-5 tightening block (no-unused-vars: error, no-restricted-syntax
         for document.createElement)
       - **/*.d.ts no-explicit-any: off exclusion (covers the 5 remaining
         ambient sites in ical.d.ts that aren't worth hand-typing)
       - globalIgnores([node_modules, main.js, esbuild.config.mjs,
         version-bump.mjs, versions.json])
       ```
  </action>
  <verify>
    <automated>
      cd /Users/mike/code/memoChron && \
      # Phase-8 markers gone
      grep -c "// Phase 8" eslint.config.mjs ; \
      # must be 0
      # .d.ts exclusion preserved
      grep -c '"\*\*/\*\.d\.ts"' eslint.config.mjs ; \
      # must be 1
      # globalIgnores preserved
      grep -c "globalIgnores" eslint.config.mjs ; \
      # must be 1 (or 2 if both the import and call match)
      # Clean lint + build
      npm run lint && npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "// Phase 8" eslint.config.mjs` returns 0 (no Phase-8 comment markers remain)
    - `grep -c '"no-console": "off"' eslint.config.mjs` returns 0
    - `grep -c '"@typescript-eslint/no-explicit-any": "off"' eslint.config.mjs` returns 1 (the `**/*.d.ts` exclusion remains — and nothing else suppresses no-explicit-any)
    - `grep -c "no-unsafe-assignment" eslint.config.mjs` returns 0
    - `grep -c '"no-case-declarations": "off"' eslint.config.mjs` returns 0
    - `grep -c '"no-useless-escape": "off"' eslint.config.mjs` returns 0
    - `grep -c "globalIgnores" eslint.config.mjs` returns at least 1 (the call expression at the bottom of the file)
    - `grep -c '"\\*\\*/\\*\\.d\\.ts"' eslint.config.mjs` returns 1 (Plan 02's addition preserved)
    - `wc -l < eslint.config.mjs` returns approximately 75 ±10 lines (was ~115 before; deleted ~40 lines from the override block)
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - `git log -1 --pretty=%B` shows the commit message above; does NOT contain "Claude", "AI", "Anthropic", or "claude.ai" (case-insensitive)
    - `git status` is clean after commit
  </acceptance_criteria>
  <done>Override block deleted; lint passes clean; Phase 8 commit 4 landed.</done>
</task>

</tasks>

<verification>
- `eslint.config.mjs` is ~40 lines shorter
- No `// Phase 8` comment markers remain
- `**/*.d.ts` block preserved; `globalIgnores` preserved
- `npm run lint` exits 0 cleanly (no overrides hiding violations)
- `npm run build` exits 0
- DIR-01, DIR-09, DIR-10 are now closed at the acceptance level
</verification>

<success_criteria>
- The scorecard requirement "Phase 5 ESLint overrides removed; `npm run lint` passes against a clean configuration with no per-rule or per-file disables tied to scorecard findings" (ROADMAP Phase 8 success criterion #4) is satisfied.
</success_criteria>

<output>
Create `.planning/phases/08-type-hygiene-conventions/08-04-SUMMARY.md` when done, including:
- Final line count of `eslint.config.mjs` (before/after)
- The exact span of lines deleted
- Confirmation that `**/*.d.ts` and `globalIgnores` are preserved
- `npm run lint` output (must be clean — no error lines)
- Commit SHA
</output>
