---
phase: 08-type-hygiene-conventions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/CalendarService.ts
  - src/services/IcsImportService.ts
  - src/settings/SettingsTab.ts
  - src/settings/types.ts
  - src/utils/viewRenderers.ts
  - src/views/CalendarView.ts
  - src/views/EmbeddedCalendarView.ts
autonomous: true
requirements:
  - DIR-10

must_haves:
  truths:
    - "`npm run lint` reports zero `@typescript-eslint/no-unused-vars` violations across `src/` (live count goes from 18 → 0)"
    - "All 18 named symbols flagged at research time (CalendarService.ts:12, :531; IcsImportService.ts:5; SettingsTab.ts:6, :7, :16, :1174; types.ts:4; viewRenderers.ts:2, :3 [4 names]; CalendarView.ts:1, :14, :200, :1101; EmbeddedCalendarView.ts:15, :109) are either deleted or genuinely consumed"
    - "Two unused catch-bindings (`CalendarService.ts:531`, `SettingsTab.ts:1174`) are converted to `catch { ... }` per D-09 default (bodies do not consume the error)"
    - "Build remains clean (`npm run build` exits 0) — no symbol referenced elsewhere was accidentally removed"
  artifacts:
    - path: src/services/CalendarService.ts
      provides: "Unused `convertTimezone` import removed from line 12; unused `error` catch binding at line 531 converted to `catch { ... }`"
    - path: src/services/IcsImportService.ts
      provides: "Unused `Property` import removed from line 5"
    - path: src/settings/SettingsTab.ts
      provides: "Three unused imports removed (`TextAreaComponent`, `DropdownComponent`, `CalendarNotesSettings`); unused `error` catch binding at line 1174 converted to `catch { ... }`"
    - path: src/settings/types.ts
      provides: "Unused `DEFAULT_CALENDAR_URLS` symbol removed from line 4 (verify no external import first)"
    - path: src/utils/viewRenderers.ts
      provides: "Four unused imports removed (`MemoChronSettings`, `TFile`, `Notice`, `App`)"
    - path: src/views/CalendarView.ts
      provides: "Four unused symbols removed/inlined (`DropdownComponent`, `DateElements` interface, `controls` local var, `title` local var)"
    - path: src/views/EmbeddedCalendarView.ts
      provides: "Two unused symbols removed/inlined (`CalendarEvent` import, `title` local var)"
  key_links:
    - from: "src/utils/errors.ts"
      to: "src/services/CalendarService.ts catch sites + src/settings/SettingsTab.ts catch sites"
      via: "`errorMessage(error)` already used at 18 other catch sites; D-09 default for the two unused-`error` sites is `catch { ... }` (no binding), not consumption via `errorMessage`"
      pattern: "catch \\{ "
---

<objective>
Delete or inline every `@typescript-eslint/no-unused-vars` violation in `src/` (DIR-10). 18 live ESLint sites identified by `npx eslint src/ --rule '{"@typescript-eslint/no-unused-vars":["error",{"args":"none"}]}'` at research time (08-RESEARCH.md §"DIR-10 Unused Vars Inventory"). Two of those are catch-binding errors that get converted to `catch { ... }` per D-09 default.

Purpose: First commit of Phase 8. Smallest mechanical commit — no behavior change, no type change, just deletion. Builds confidence for the larger DIR-09 and DIR-01 commits that follow.

Output: 7 source files with unused symbols deleted; `npm run lint` shows 0 `no-unused-vars` errors (still has the override in place — the override removal lands in Plan 04, but the violations themselves disappear here).

Note on the per-file ESLint override: the `eslint.config.mjs:92-105` block currently disables `no-unused-vars` for 7 specific files. This plan deletes the violations themselves so that when Plan 04 deletes that override block, `npm run lint` continues to pass. To verify the deletions actually closed the violations (despite the override being in place), each task runs `npx eslint <file> --rule '{"@typescript-eslint/no-unused-vars":["error",{"args":"none"}]}'` (rule-only invocation that ignores the file-level override) on the changed file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-type-hygiene-conventions/08-CONTEXT.md
@.planning/phases/08-type-hygiene-conventions/08-RESEARCH.md
@.planning/phases/08-type-hygiene-conventions/08-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@src/utils/errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused symbols in services and settings (12 of 18 sites)</name>
  <files>src/services/CalendarService.ts, src/services/IcsImportService.ts, src/settings/SettingsTab.ts, src/settings/types.ts</files>
  <read_first>
    - src/services/CalendarService.ts (read lines 1-20 and lines 525-540 before touching)
    - src/services/IcsImportService.ts (read lines 1-10)
    - src/settings/SettingsTab.ts (read lines 1-20 and lines 1170-1185)
    - src/settings/types.ts (read full file — small)
    - .planning/phases/08-type-hygiene-conventions/08-RESEARCH.md §"DIR-10 Unused Vars Inventory" (lines 580-619) and §"Catch-Binding Recommendations" (lines 621-651)
    - .planning/phases/08-type-hygiene-conventions/08-RESEARCH.md §"Pitfall 5: Deleting `convertTimezone` import" (lines 260-272) — confirms the grep verification pattern
  </read_first>
  <action>
    Execute these edits in order, running `grep -c "<symbol>" <file>` BEFORE deleting each import to confirm only one hit (the import line) exists:

    1. **`src/services/CalendarService.ts:12`** — Remove `convertTimezone` from the import list. Current shape: `import { convertIcalTimeToDate, convertTimezone } from "../utils/timezoneUtils";` → `import { convertIcalTimeToDate } from "../utils/timezoneUtils";`. Verify: `grep -c "convertTimezone" src/services/CalendarService.ts` returns 0 after the edit.

    2. **`src/services/CalendarService.ts:531`** — Convert `} catch (error) {` to `} catch {` (no binding) per D-09 default. The catch body returns a fixed `{ status: 404, text: "Cannot read file: ..." }` and does not reference `error` (verified at RESEARCH.md §"Catch-Binding Recommendations" lines 625-636).

    3. **`src/services/IcsImportService.ts:5`** — Remove `Property` from the import list. Verify: `grep -c "Property" src/services/IcsImportService.ts` returns 0 after the edit.

    4. **`src/settings/SettingsTab.ts:6`** — Remove `TextAreaComponent` from the import. Verify: `grep -c "TextAreaComponent" src/settings/SettingsTab.ts` returns 0.

    5. **`src/settings/SettingsTab.ts:7`** — Remove `DropdownComponent` from the import (NOTE: this same name also appears at `CalendarView.ts:1` and is handled in Task 2). Verify in SettingsTab.ts only: `grep -c "DropdownComponent" src/settings/SettingsTab.ts` returns 0.

    6. **`src/settings/SettingsTab.ts:16`** — Remove `CalendarNotesSettings` from the import. Verify: `grep -c "CalendarNotesSettings" src/settings/SettingsTab.ts` returns 0.

    7. **`src/settings/SettingsTab.ts:1174`** — Convert `} catch (error) {` to `} catch {` (no binding) per D-09 default. The catch body renders a generic "Invalid template format" notice and does not reference `error` (verified at RESEARCH.md lines 638-649).

    8. **`src/settings/types.ts:4`** — Delete the `DEFAULT_CALENDAR_URLS` export. BEFORE deleting, verify zero external consumers: `grep -rn "DEFAULT_CALENDAR_URLS" src/` should show only the declaration line. If anything else references it, do NOT delete — escalate to user instead. (RESEARCH.md confirms it's dead.)

    Do NOT modify any other lines. Do NOT reorder imports. Do NOT touch viewRenderers.ts, CalendarView.ts, or EmbeddedCalendarView.ts in this task — those are Task 2.
  </action>
  <verify>
    <automated>
      cd /Users/mike/code/memoChron && \
      npx eslint src/services/CalendarService.ts src/services/IcsImportService.ts src/settings/SettingsTab.ts src/settings/types.ts \
        --rule '{"@typescript-eslint/no-unused-vars":["error",{"args":"none"}]}' \
        --no-eslintrc 2>&1 | grep -c "no-unused-vars" ; \
      # The count above must equal 0. If it errors due to --no-eslintrc not being available in flat-config mode, alternative:
      npx eslint src/services/CalendarService.ts src/services/IcsImportService.ts src/settings/SettingsTab.ts src/settings/types.ts 2>&1 | grep -c "no-unused-vars"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "convertTimezone" src/services/CalendarService.ts` returns 0
    - `grep -c "Property" src/services/IcsImportService.ts` returns 0
    - `grep -c "TextAreaComponent" src/settings/SettingsTab.ts` returns 0
    - `grep -c "CalendarNotesSettings" src/settings/SettingsTab.ts` returns 0
    - `grep -c "DEFAULT_CALENDAR_URLS" src/settings/types.ts` returns 0
    - `grep -rn "DEFAULT_CALENDAR_URLS" src/` returns NO matches (zero remaining references anywhere)
    - `grep -c "} catch {" src/services/CalendarService.ts` is at least 1 (the new no-binding catch at the former line 531)
    - `grep -c "} catch {" src/settings/SettingsTab.ts` is at least 1 (the new no-binding catch at the former line 1174)
    - `grep -v '^#' src/services/CalendarService.ts | grep -c "DropdownComponent"` is 0 IF this file did not previously use DropdownComponent; planner should grep before/after to be sure (this symbol IS in scope for SettingsTab.ts:7 only in this task)
    - Running `npx eslint src/services/CalendarService.ts src/services/IcsImportService.ts src/settings/SettingsTab.ts src/settings/types.ts` produces no `no-unused-vars` lines in the output (the override in eslint.config.mjs lines 92-105 may suppress it; the rule-explicit invocation in the verify command above bypasses that)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 8 sub-edits applied; grep verifications pass; build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete unused symbols in utils and views (6 of 18 sites)</name>
  <files>src/utils/viewRenderers.ts, src/views/CalendarView.ts, src/views/EmbeddedCalendarView.ts</files>
  <read_first>
    - src/utils/viewRenderers.ts (read lines 1-10)
    - src/views/CalendarView.ts (read lines 1-25 and lines 195-210 and lines 1095-1110)
    - src/views/EmbeddedCalendarView.ts (read lines 1-20 and lines 100-130)
    - .planning/phases/08-type-hygiene-conventions/08-RESEARCH.md §"DIR-10 Unused Vars Inventory" (lines 580-619) — note `controls` and `title` are LOCAL VARS not imports; the fix is to drop the `const X = ` prefix on the assignment (bare expression call) so the return value is discarded
  </read_first>
  <action>
    Execute these edits in order:

    1. **`src/utils/viewRenderers.ts:2`** — Remove `MemoChronSettings` and `TFile` from the import on line 2. Verify: `grep -c "MemoChronSettings" src/utils/viewRenderers.ts` returns 0 AND `grep -c "TFile" src/utils/viewRenderers.ts` returns 0.

    2. **`src/utils/viewRenderers.ts:3`** — Remove `Notice` and `App` from the import on line 3. Verify both return 0 via grep.

    3. **`src/views/CalendarView.ts:1`** — Remove `DropdownComponent` from the obsidian import on line 1. Verify: `grep -c "DropdownComponent" src/views/CalendarView.ts` returns 0.

    4. **`src/views/CalendarView.ts:14`** — Delete the unused `DateElements` interface block in its entirety (per RESEARCH.md line 599, "DELETE the interface block at lines 14-16 entirely"). The exact span is the `interface DateElements { ... }` declaration around line 14. After delete: `grep -c "DateElements" src/views/CalendarView.ts` returns 0.

    5. **`src/views/CalendarView.ts:200`** — Convert `const controls = this.createControls(container);` to a bare call: `this.createControls(container);` (drop `const controls = ` only; keep the call). Verify the return value `controls` is not used downstream by greppping the rest of the file: `grep -c "controls" src/views/CalendarView.ts` after the edit should match only any `createControls` method NAME hits (i.e., the method declaration), not the variable.

    6. **`src/views/CalendarView.ts:1101`** — Convert `const title = ...` to a bare call (drop the `const title = ` prefix). The surrounding code does not read `title` downstream (verified at RESEARCH.md line 601). After: `grep -nE "\\btitle\\b" src/views/CalendarView.ts` shows no hits at or near line 1101 corresponding to the deleted binding.

    7. **`src/views/EmbeddedCalendarView.ts:15`** — Remove `CalendarEvent` from the import on line 15. Verify: `grep -c "CalendarEvent" src/views/EmbeddedCalendarView.ts` returns 0.

    8. **`src/views/EmbeddedCalendarView.ts:109`** — Convert `const title = header.createEl(...)` to a bare call: `header.createEl(...)` (drop `const title = `). RESEARCH.md confirms `title` is not read downstream (line 603).

    Do NOT modify any other lines. Do NOT delete the catch-binding `error` symbols (those were handled in Task 1).
  </action>
  <verify>
    <automated>
      cd /Users/mike/code/memoChron && \
      npx eslint src/utils/viewRenderers.ts src/views/CalendarView.ts src/views/EmbeddedCalendarView.ts 2>&1 | grep -c "no-unused-vars"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MemoChronSettings" src/utils/viewRenderers.ts` returns 0
    - `grep -c "TFile" src/utils/viewRenderers.ts` returns 0
    - `grep -c "Notice" src/utils/viewRenderers.ts` returns 0
    - `grep -c "App" src/utils/viewRenderers.ts` returns 0
    - `grep -c "DropdownComponent" src/views/CalendarView.ts` returns 0
    - `grep -c "interface DateElements" src/views/CalendarView.ts` returns 0
    - `grep -c "DateElements" src/views/CalendarView.ts` returns 0
    - `grep -c "CalendarEvent" src/views/EmbeddedCalendarView.ts` returns 0
    - `grep -nE "^\\s*const controls\\s*=" src/views/CalendarView.ts` returns no match (the `const controls = ...` assignment is gone)
    - `grep -nE "^\\s*const title\\s*=" src/views/CalendarView.ts` returns no match at line ~1101 (the assignment near 1101 is gone)
    - `grep -nE "^\\s*const title\\s*=" src/views/EmbeddedCalendarView.ts` returns no match at line ~109 (the assignment near 109 is gone)
    - Running `npx eslint src/utils/viewRenderers.ts src/views/CalendarView.ts src/views/EmbeddedCalendarView.ts` produces no `no-unused-vars` lines in the output
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 8 sub-edits applied; grep verifications pass; build is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Verify zero DIR-10 violations remain, build clean, and commit</name>
  <files>(no source files — verification + git only)</files>
  <read_first>
    - .planning/phases/08-type-hygiene-conventions/08-VALIDATION.md (re-confirm the DIR-10 row expectation)
  </read_first>
  <action>
    Verification + commit step. Per D-05 commit 1 wording.

    1. Run `npm run lint` and capture the full output. The current `eslint.config.mjs:92-105` override block suppresses `no-unused-vars` for 7 specific files; that override is removed in Plan 04. For now, also run a rule-explicit pass that bypasses the override: `npx eslint src/ --rule '@typescript-eslint/no-unused-vars: ["error", { args: "none" }]'` (or the equivalent flat-config invocation). The rule-explicit pass MUST report zero `no-unused-vars` errors across `src/`.

    2. Run `npm run build`. Must exit 0. If it fails with a TypeScript error about an unused symbol used in a type position (per RESEARCH.md Pitfall 2), restore that symbol — the import was needed for type usage, not value usage.

    3. Stage the modified files and commit with this EXACT message (no Claude/AI references per CLAUDE.md):
       ```
       refactor: remove unused vars and imports (DIR-10)

       Closes 18 @typescript-eslint/no-unused-vars sites:
       - CalendarService.ts:12 (convertTimezone import), :531 (catch binding)
       - IcsImportService.ts:5 (Property import)
       - SettingsTab.ts:6 (TextAreaComponent), :7 (DropdownComponent),
         :16 (CalendarNotesSettings), :1174 (catch binding)
       - settings/types.ts:4 (DEFAULT_CALENDAR_URLS)
       - viewRenderers.ts:2-3 (MemoChronSettings, TFile, Notice, App)
       - CalendarView.ts:1 (DropdownComponent), :14 (DateElements interface),
         :200 (controls local), :1101 (title local)
       - EmbeddedCalendarView.ts:15 (CalendarEvent import), :109 (title local)

       Catch bindings at CalendarService.ts:531 and SettingsTab.ts:1174 use
       `catch { }` (no binding) per D-09 default — bodies do not consume
       the error.

       Phase-8 eslint.config.mjs override remains in place; Plan 04
       removes it once DIR-09 and DIR-01 also land.
       ```
  </action>
  <verify>
    <automated>
      cd /Users/mike/code/memoChron && \
      npm run lint && \
      npm run build && \
      npx eslint 'src/**/*.ts' --rule '@typescript-eslint/no-unused-vars: ["error", { "args": "none" }]' 2>&1 | grep -c "no-unused-vars" \
      # The grep above must produce 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - Rule-explicit pass `npx eslint 'src/**/*.ts' --rule '@typescript-eslint/no-unused-vars: ["error", { "args": "none" }]'` produces zero `no-unused-vars` lines
    - `git log -1 --pretty=%B` shows the commit message above; the message does NOT contain "Claude", "AI", "Anthropic", or "claude.ai" (case-insensitive)
    - `git status` is clean after commit
  </acceptance_criteria>
  <done>DIR-10 violations are all gone; commit 1 of Phase 8 is on `main`; no AI references in commit message.</done>
</task>

</tasks>

<verification>
- `npm run lint` exits 0
- `npm run build` exits 0
- Rule-explicit pass shows zero `no-unused-vars` across `src/`
- All 18 RESEARCH.md-listed sites verified absent via per-symbol greps (acceptance criteria above)
- Commit message contains no AI/Claude references
</verification>

<success_criteria>
- Wave 1 is complete: DIR-10 closed at the violation level. Override block stays put for now (removed in Plan 04). Phase 8 commit 1 landed.
</success_criteria>

<output>
Create `.planning/phases/08-type-hygiene-conventions/08-01-SUMMARY.md` when done, including:
- Symbols deleted (per-file count)
- Catch-binding sites converted (2)
- Build + lint output (rule-explicit invocation captures the actual `no-unused-vars` count, now 0)
- Commit SHA
- Any deviations from the plan (e.g., a symbol the planner thought was dead but was used in a type position)
</output>
