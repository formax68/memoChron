---
phase: 08-type-hygiene-conventions
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/main.ts
  - src/services/CalendarService.ts
  - src/services/NoteService.ts
  - src/services/IcsImportService.ts
  - src/settings/SettingsTab.ts
  - src/utils/timezoneUtils.ts
  - src/views/CalendarView.ts
  - src/views/EmbeddedCalendarView.ts
  - src/views/EmbeddedAgendaView.ts
autonomous: true
requirements:
  - DIR-01

must_haves:
  truths:
    - "Rule-explicit `npx eslint src/ --rule 'no-console: error'` reports zero `no-console` violations across `src/`"
    - "33 of 39 `console.*` sites are deleted outright (default per D-07)"
    - "6 sites are GATED behind a compile-time `const DEBUG = false` constant at the top of the owning file. The 4 gated sites in `CalendarService.ts` are at lines `:302, :387, :406, :457` (per RESEARCH.md §'DIR-01 Console Audit', lines 431-464). The 2 gated sites in `timezoneUtils.ts` are at lines `:215, :224`."
    - "Per-file `const DEBUG = false` declared at top of `CalendarService.ts` and `timezoneUtils.ts` only (D-07 default: per-file unless 3+ files gated). No shared `src/utils/debug.ts`."
    - "Each gated site uses the exact pattern `if (DEBUG) { console.X(...) }` (per D-07 — tree-shakeable when DEBUG=false)"
    - "Zero loss of user-visible signal: every removed `console.error` was paired with a `Notice` (verified per-site in RESEARCH.md §'DIR-01 Console Audit')"
    - "Live source-tree reconciliation: `CalendarService.ts` has 14 `console.*` sites (live grep), not 15 (CONTEXT.md was off by one). Aggregate stays at 39 (= 33 DELETE + 6 GATE) across all 11 files. Commit body notes the per-file delta."
  artifacts:
    - path: src/services/CalendarService.ts
      provides: "4 GATED forensic sites (cache misses / fetch failures) behind file-top `const DEBUG = false`; 10 other console sites deleted (14 total sites in file)"
    - path: src/utils/timezoneUtils.ts
      provides: "2 GATED forensic sites (invalid TZ + catastrophic TZ failure) behind file-top `const DEBUG = false`; 3 other console sites deleted"
    - path: src/main.ts
      provides: "2 console.warn sites deleted (invalid color logging)"
    - path: src/services/NoteService.ts
      provides: "6 console sites deleted (all paired with Notice or silent recovery)"
    - path: src/services/IcsImportService.ts
      provides: "2 console sites deleted"
    - path: src/settings/SettingsTab.ts
      provides: "2 console sites deleted"
    - path: src/views/CalendarView.ts
      provides: "6 console sites deleted (all paired with Notice or silent recovery)"
    - path: src/views/EmbeddedCalendarView.ts
      provides: "1 console site deleted"
    - path: src/views/EmbeddedAgendaView.ts
      provides: "1 console site deleted"
  key_links:
    - from: "Each remaining gated console site"
      to: "file-top `const DEBUG = false`"
      via: "`if (DEBUG) { console.X(...) }` wrapper (D-07 pattern)"
      pattern: "if \\(DEBUG\\) \\{?\\s*console\\."
---

<objective>
Close DIR-01: 39 `console.*` sites across 11 files. Default action is delete (33 sites) — every flagged site is either paired with a `Notice`, lifecycle noise, or silent debug. Six forensic sites in two files get GATED behind a compile-time `const DEBUG = false` at file top, per D-07 default.

Purpose: Third commit of Phase 8. Largest commit by site count, but mechanically simple — each site is a 1-line delete or 1-line wrap. No behavior change visible to users (every deleted `console.error` was already paired with a `Notice`).

Output: 9 source files modified; rule-explicit lint pass shows zero `no-console` errors. Override at `eslint.config.mjs:65-105` still suppresses the lint rule globally; Plan 04 removes the override, at which point `npm run lint` natively reports clean.

**Per-site classification is locked by RESEARCH.md §"DIR-01 Console Audit" (lines 418-468).** Executor MUST cross-reference each site against that table before deleting or gating. If the executor disagrees with a classification, the rule from D-07 is: where a `console.*` is paired with a user-visible `Notice`, DELETE without exception. Gating is reserved for forensic-only sites with no user-visible counterpart.

**Count reconciliation:** CONTEXT.md per-file list for `CalendarService.ts` showed `(15)` in the summary header but enumerated 14 line numbers (`:60, 256, 267, 302, 344, 346, 381, 384, 387, 398, 406, 457, 545, 923`). Live grep confirms 14. The aggregate 39 = 33 DELETE + 6 GATE still holds: CONTEXT.md's enumerated lines were correct; only the `(15)` parenthetical was off. Use 14 consistently in Task 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-type-hygiene-conventions/08-CONTEXT.md
@.planning/phases/08-type-hygiene-conventions/08-RESEARCH.md
@.planning/phases/08-type-hygiene-conventions/08-VALIDATION.md
@.planning/phases/08-type-hygiene-conventions/08-01-SUMMARY.md
@.planning/phases/08-type-hygiene-conventions/08-02-SUMMARY.md

<interfaces>
<!-- Gate pattern (D-07, per-file top-of-file). Tree-shakeable when DEBUG=false. -->
<!-- Used in CalendarService.ts and timezoneUtils.ts only. -->

Pattern at top of file (after imports, before first class/function):
```typescript
// Forensic-only flag for cache/fetch/timezone debugging. Flip to true
// locally; tree-shakes to nothing in production builds when false.
const DEBUG = false;
```

Pattern at each gated site:
```typescript
// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)
if (DEBUG) console.error("MemoChron: ...", err);
```

The `MemoChron: ` prefix follows the §Logging convention in CONVENTIONS.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalendarService.ts — gate 4 forensic sites, delete 10 others (14 total)</name>
  <files>src/services/CalendarService.ts</files>
  <read_first>
    - src/services/CalendarService.ts (read each site ±10 lines: 60, 256, 267, 302, 344, 346, 381, 384, 387, 398, 406, 457, 545, 923 — 14 sites)
    - .planning/phases/08-type-hygiene-conventions/08-RESEARCH.md §"DIR-01 Console Audit" (lines 418-468) — the per-site classification table for CalendarService.ts
  </read_first>
  <action>
    Per RESEARCH.md §"DIR-01 Console Audit" classifications for CalendarService.ts:

    **GATE (4 sites)** — wrap each in `if (DEBUG) console.X(...)` with a per-line `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)` comment IMMEDIATELY ABOVE the `if (DEBUG) ...` line:
    - `:302` — `console.log` (cache miss) — useful for diagnosing cache freshness
    - `:387` — `console.error` (other HTTP error) — no Notice at this branch; forensic only
    - `:406` — `console.error` (fetch caught) — caller's CORS/network Notice is generic; this adds detail
    - `:457` — `console.error` (Outlook HTML response) — Outlook quirk debugging

    **DELETE (10 sites):**
    - `:60` (no enabled sources warn — caller's Notice covers it)
    - `:256` (fetch error — paired with Notice at :257)
    - `:267` (background refresh started — lifecycle noise)
    - `:344` (cache saved — lifecycle noise)
    - `:346` (cache save failed — silent acceptable; next save retries)
    - `:381` (403 — paired with Notice at :382)
    - `:384` (404 — paired with Notice at :385)
    - `:398` (invalid calendar data — paired with Notice at :399)
    - `:545` (local file read error — caller surfaces via Notice)
    - `:923` (platform info debug — one-shot diagnostic; planner's call — DELETE per D-07 default)

    Total: 4 GATE + 10 DELETE = **14 sites** (matches live grep count for CalendarService.ts).

    Execution order:

    1. **Add `const DEBUG = false;`** at the top of the file after the imports and before the first class declaration. Add a one-line comment above explaining it's a forensic flag (see `<interfaces>` block above for the exact text).

    2. **Process each site** by reading the line ±5 (or grep+view), then either:
       - Delete the entire `console.*` line (and its `eslint-disable-next-line` comment if any).
       - For gated sites, replace the line with the pattern shown in `<interfaces>`: a per-line `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)` comment, then the `if (DEBUG) console.X("MemoChron: ...", ...)` line. If the existing log string already starts with `"MemoChron: "`, keep it; otherwise add the prefix per CONVENTIONS.md §Logging.

    3. **After all 14 sites**, verify:
       - `grep -nE '^\s*console\.' src/services/CalendarService.ts` returns 0 matches (because every gated `console.X` is preceded by `if (DEBUG)` on the SAME line, so the bare `console.` no longer starts a line).
       - To enumerate gated sites: `grep -cE 'if \\(DEBUG\\) console\\.' src/services/CalendarService.ts` returns 4.
       - `grep -c "const DEBUG = false" src/services/CalendarService.ts` returns 1.

    Do NOT touch any other site or any other file in this task.
  </action>
  <verify>
    <automated>
cd /Users/mike/code/memoChron && npx eslint src/services/CalendarService.ts --rule 'no-console: error' 2>&1 | grep -v '^#' | grep -c 'no-console' || echo 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const DEBUG = false" src/services/CalendarService.ts` returns 1
    - `grep -cE "if \\(DEBUG\\) console\\." src/services/CalendarService.ts` returns 4 (one per gated site)
    - Bare un-gated `console.*` lines: 0 (every surviving `console.X` is on a line that ALSO contains `if (DEBUG)`)
    - For each of the 10 DELETE sites listed above, the line at that approximate position no longer matches `console.`
    - For each of the 4 GATE sites (lines :302, :387, :406, :457 — approximate, will shift after deletions), the `console.X(...)` call is gated by `if (DEBUG)` AND has a per-line `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)` comment IMMEDIATELY ABOVE it
    - Rule-explicit lint check passes (see verify block; expected output: 0)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>14 sites in CalendarService.ts processed (4 gated, 10 deleted); DEBUG flag in place; per-line eslint-disable on gated sites; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: timezoneUtils.ts — gate 2 forensic sites, delete 3 others (5 total)</name>
  <files>src/utils/timezoneUtils.ts</files>
  <read_first>
    - src/utils/timezoneUtils.ts (read sites ±5: 179, 200, 215, 224, 266)
    - .planning/phases/08-type-hygiene-conventions/08-RESEARCH.md §"DIR-01 Console Audit" lines 452-456
  </read_first>
  <action>
    Per RESEARCH.md classifications for timezoneUtils.ts:

    **GATE (2 sites)** — wrap each in `if (DEBUG) console.X(...)` with per-line `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)`:
    - `:215` — `console.warn` (invalid timezone) — first thing to enable when users report wrong times
    - `:224` — `console.error` (full TZ conversion failure) — catastrophic; useful for support

    **DELETE (3 sites):**
    - `:179` (toJSDate failed — falls back; warn doesn't help)
    - `:200` (toJSDate failed for custom TZID — falls back)
    - `:266` (invalid TZ in convertTimezone — falls back; less useful than :215)

    Execution:

    1. **Add `const DEBUG = false;`** at the top of `src/utils/timezoneUtils.ts` after imports. Comment same as Task 1.

    2. **For each site**, delete or wrap-in-`if (DEBUG)` per the lists above. Gated sites get a per-line `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)` above the `if (DEBUG) console.X(...)` line.

    3. **Verify:**
       - `grep -c "const DEBUG = false" src/utils/timezoneUtils.ts` returns 1.
       - `grep -cE "if \\(DEBUG\\) console\\." src/utils/timezoneUtils.ts` returns 2.
  </action>
  <verify>
    <automated>
cd /Users/mike/code/memoChron && npx eslint src/utils/timezoneUtils.ts --rule 'no-console: error' 2>&1 | grep -v '^#' | grep -c 'no-console' || echo 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const DEBUG = false" src/utils/timezoneUtils.ts` returns 1
    - `grep -cE "if \\(DEBUG\\) console\\." src/utils/timezoneUtils.ts` returns 2
    - Each gated site has a per-line `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)` comment immediately above
    - Rule-explicit lint check passes (expected output: 0)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>5 sites in timezoneUtils.ts processed; DEBUG flag in place; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Delete console.* in remaining 7 files (19 sites total)</name>
  <files>src/main.ts, src/services/NoteService.ts, src/services/IcsImportService.ts, src/settings/SettingsTab.ts, src/views/CalendarView.ts, src/views/EmbeddedCalendarView.ts, src/views/EmbeddedAgendaView.ts</files>
  <read_first>
    - .planning/phases/08-type-hygiene-conventions/08-RESEARCH.md §"DIR-01 Console Audit" (lines 426-464) — the table classifying every site as DELETE for these 7 files
    - Each file at the relevant line numbers (read ±5 to confirm pairing with Notice before deleting)
  </read_first>
  <action>
    Per RESEARCH.md: ALL 19 sites across these 7 files are classified DELETE. No GATE sites. Per-file:

    - **`src/main.ts`** — 2 sites: `:112` (invalid color in loadSettings warn), `:123` (invalid dailyNoteColor warn). DELETE both.
    - **`src/services/NoteService.ts`** — 6 sites: `:81, :133, :172, :316, :338, :596`. DELETE all (each is silent recovery or paired with Notice elsewhere).
    - **`src/services/IcsImportService.ts`** — 2 sites: `:38` (timezone already registered — already silent intent), `:40` (timezone error — recovers). DELETE both.
    - **`src/settings/SettingsTab.ts`** — 2 sites: `:1537` (custom settings missing — UI shows it), `:1767` (refreshing custom settings — lifecycle noise). DELETE both.
    - **`src/views/CalendarView.ts`** — 6 sites: `:153, :174, :347, :833, :927, :1059`. DELETE all (each is paired with Notice or silent UI fallback).
    - **`src/views/EmbeddedCalendarView.ts`** — 1 site: `:249` (paired with Notice at :250). DELETE.
    - **`src/views/EmbeddedAgendaView.ts`** — 1 site: `:398` (paired with Notice at :399). DELETE.

    Execution:

    1. For each file, grep all `console.` sites: `grep -nE '^\s*console\.' <file>`. Confirm the count matches the per-file count above.

    2. For each site, read the surrounding ±5 lines. Verify the pairing-with-Notice claim (or the silent-recovery claim) before deleting. If the executor finds a site where the deletion would lose user-visible signal (e.g., a `console.error` that ISN'T paired with a Notice), keep the `console.error` and ADD a `new Notice(errorMessage(error))` instead — use the `errorMessage` helper from `src/utils/errors.ts` per CONVENTIONS.md §Error Handling. This is the only acceptable deviation from the DELETE classification.

    3. Delete each line (including any `eslint-disable-next-line` comment immediately above it if any).

    4. Verify after each file:
       - `grep -cE '^\s*console\.' <file>` returns 0.
       - No file in this task gets a `const DEBUG = false` (no sites are gated in these files).

    5. **No** `const DEBUG = false` declarations in any of these 7 files — they have no gated sites. Only CalendarService.ts (Task 1) and timezoneUtils.ts (Task 2) have the flag.
  </action>
  <verify>
    <automated>
cd /Users/mike/code/memoChron && for f in src/main.ts src/services/NoteService.ts src/services/IcsImportService.ts src/settings/SettingsTab.ts src/views/CalendarView.ts src/views/EmbeddedCalendarView.ts src/views/EmbeddedAgendaView.ts; do echo "$f: $(grep -cE '^\s*console\.' $f)"; done
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\\s*console\\.' src/main.ts` returns 0
    - `grep -cE '^\\s*console\\.' src/services/NoteService.ts` returns 0
    - `grep -cE '^\\s*console\\.' src/services/IcsImportService.ts` returns 0
    - `grep -cE '^\\s*console\\.' src/settings/SettingsTab.ts` returns 0
    - `grep -cE '^\\s*console\\.' src/views/CalendarView.ts` returns 0
    - `grep -cE '^\\s*console\\.' src/views/EmbeddedCalendarView.ts` returns 0
    - `grep -cE '^\\s*console\\.' src/views/EmbeddedAgendaView.ts` returns 0
    - `grep -c "const DEBUG = false" src/main.ts src/services/NoteService.ts src/services/IcsImportService.ts src/settings/SettingsTab.ts src/views/CalendarView.ts src/views/EmbeddedCalendarView.ts src/views/EmbeddedAgendaView.ts` returns 0 (no DEBUG flag in any of these files)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>19 sites in 7 files deleted; build clean.</done>
</task>

<task type="auto">
  <name>Task 4: Full source-tree audit, lint, build, commit</name>
  <files>(no source files — verification + git only)</files>
  <read_first>
    - .planning/phases/08-type-hygiene-conventions/08-VALIDATION.md (DIR-01 row)
  </read_first>
  <action>
    Verification + commit step. Per D-05 commit 3.

    1. **Full-tree console audit:**
       ```
       grep -rnE '^\s*console\.' src/
       ```
       Expected: 0 lines (every surviving `console.X` is on a line that begins with `if (DEBUG)`, so `^\s*console\.` doesn't match).

       To enumerate the 6 gated sites:
       ```
       grep -rcE 'if \(DEBUG\) console\.' src/
       ```
       Expected: `src/services/CalendarService.ts:4` and `src/utils/timezoneUtils.ts:2`; all other files 0.

    2. **Rule-explicit lint:** `npx eslint 'src/**/*.ts' --rule 'no-console: error'`. Per-line `// eslint-disable-next-line no-console -- DEBUG flag (Phase 8 D-07)` comments added in Tasks 1 & 2 cover the 6 gated sites, so this pass produces 0 errors.

    3. **`npm run lint`:** must exit 0 (the override at `eslint.config.mjs` still disables `no-console`, plus the per-line comments cover the gated sites for when the override is removed in Plan 04).

    4. **`npm run build`:** must exit 0.

    5. **Stage and commit** with this EXACT message (no Claude/AI references):
       ```
       refactor(logs): remove or gate console.* per DIR-01

       Closes 39 no-console sites:
       - DELETE: 33 sites (paired with Notice, lifecycle noise, or silent recovery)
       - GATE: 6 forensic sites behind compile-time `const DEBUG = false`
         (CalendarService.ts cache/fetch/Outlook: 4 sites; timezoneUtils.ts
         TZ resolution: 2 sites)

       Per D-07 default (per-file unless 3+ files gated): file-top `const DEBUG
       = false` declared in CalendarService.ts and timezoneUtils.ts only. No
       shared src/utils/debug.ts.

       Each gated site has `// eslint-disable-next-line no-console -- DEBUG
       flag (Phase 8 D-07)` so the gated sites satisfy `no-console` when
       Plan 04 removes the global override.

       MemoChron: prefix preserved on retained logs per CONVENTIONS.md
       §Logging.

       Note: CONTEXT.md per-file summary listed CalendarService.ts at 15
       sites; live grep finds 14 (the enumerated line list — :60, 256, 267,
       302, 344, 346, 381, 384, 387, 398, 406, 457, 545, 923 — was correct;
       only the parenthetical count was off). Aggregate 39 (= 33 DELETE +
       6 GATE) across all 11 files holds.

       Phase-8 eslint.config.mjs override block remains in place; Plan 04
       removes it now that DIR-09 (Plan 02) and DIR-01 (this commit) are clean.
       ```
  </action>
  <verify>
    <automated>
cd /Users/mike/code/memoChron && npm run lint && npm run build && npx eslint 'src/**/*.ts' --rule 'no-console: error' 2>&1 | grep -v '^#' | grep -c 'no-console' || echo 0
    </automated>
  </verify>
  <acceptance_criteria>
    - Full-tree audit: `grep -rcE '^\\s*console\\.' src/` returns 0 lines that contain a positive count (every surviving `console.X` is on the same line as `if (DEBUG)`)
    - Gated-site enumeration: `grep -rcE 'if \\(DEBUG\\) console\\.' src/` shows ONLY `src/services/CalendarService.ts:4` and `src/utils/timezoneUtils.ts:2`; all other source files report 0
    - Rule-explicit `npx eslint 'src/**/*.ts' --rule 'no-console: error'` produces 0 errors
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - `git log -1 --pretty=%B` shows the commit message above; does NOT contain "Claude", "AI", "Anthropic", or "claude.ai" (case-insensitive)
    - `git status` is clean after commit
  </acceptance_criteria>
  <done>DIR-01 violations are all gone at the source level; commit 3 of Phase 8 is on `main`; no AI references in commit message.</done>
</task>

</tasks>

<verification>
- 33 sites deleted, 6 sites gated (4 in CalendarService.ts, 2 in timezoneUtils.ts) — confirmed by greps
- Per-file `const DEBUG = false` in exactly 2 files
- Each gated site has per-line `// eslint-disable-next-line no-console` so the gated sites survive Plan 04's override removal
- `npm run lint` exits 0; `npm run build` exits 0
- Rule-explicit `no-console` pass produces 0 errors
- Commit message contains no AI/Claude references
- Per-file count for CalendarService.ts reconciled: 14 sites live (10 DELETE + 4 GATE), aggregate still 39
</verification>

<success_criteria>
- Wave 3 is complete: DIR-01 closed at the violation level. Phase 8 commit 3 landed.
</success_criteria>

<output>
Create `.planning/phases/08-type-hygiene-conventions/08-03-SUMMARY.md` when done, including:
- Per-file deleted count + gated count
- Any deviations from RESEARCH.md classifications (sites where DELETE was downgraded to GATE or vice versa)
- The 3-line DEBUG flag pattern as actually committed (comment + const + `if (DEBUG) console...` usage)
- Build + lint output snippets
- Commit SHA
- Confirmation that the aggregate 33 DELETE + 6 GATE = 39 holds against live grep totals
</output>
</content>
</invoke>