---
phase: 04-ux-enhancements
plan: 07
type: execute
mode: gap_closure
wave: 1
depends_on: []
files_modified:
  - styles.css
autonomous: true
requirements:
  - ENH-03
gaps_addressed:
  - CR-01

must_haves:
  truths:
    - "ENH-03 / SC#3 / Truth #3 — The optional note-exists corner-square indicator on the calendar grid is visible on a day-cell that is BOTH `.selected` AND contains an event with a note. The user's currently-selected day no longer silently hides the indicator (closes the 04-VERIFICATION.md truth #3 regression / CR-01)."
  artifacts:
    - path: "styles.css"
      provides: "Combined-state CSS override that re-colors the note-indicator corner-square to var(--text-on-accent) when its parent day cell is .selected — survives the accent background, identical pattern to the WR-02 today-ring resolution shipped in 04-06"
      contains: ".memochron-day.selected .memochron-note-indicator"
  key_links:
    - from: "styles.css .memochron-day.selected .memochron-note-indicator"
      to: ".memochron-note-indicator (preceding base rule at styles.css:221-230)"
      via: "CSS selector specificity (descendant selector with two classes on the parent > single-class child rule)"
      pattern: "\\.memochron-day\\.selected\\s+\\.memochron-note-indicator\\s*\\{[^}]*text-on-accent"
---

<objective>
Close the single remaining gap surfaced by `04-VERIFICATION.md` (truth #3 / ENH-03 / CR-01) so a re-run of `/gsd-verify-phase 4` flips truth #3 from FAILED back to VERIFIED and moves the phase score from 5/6 to 6/6.

Purpose: Plan 04-04 shipped functional code for ENH-03 (the optional, toggleable grid corner-square that marks days containing at least one event with a note). Plan 04-06 closed the analogous today-ring contrast bug (WR-02). The post-04-06 code review (`04-REVIEW.md`) surfaced **CR-01** — the same zero-contrast bug class, applied to the note-indicator corner-square instead of the today-ring: when a day with a note is also the user's `.selected` day, `var(--interactive-accent)` on the indicator matches `var(--interactive-accent)` on the cell background, and the indicator silently disappears on the user's most-interrogated day.

Output: A single additive CSS rule in `styles.css`, inserted immediately after the existing `.memochron-note-indicator` block (around line 230). No new files, no new dependencies, no settings additions, no TypeScript changes, no test additions (no test framework in this milestone per CLAUDE.md scope).

Mode: gap_closure. This plan does NOT supersede plans 04-01..04-06 — it patches their output. The WR-02 fix in 04-06 (the today-ring analog) remains the authoritative pattern; this plan applies that same pattern to the note-indicator.

Scope boundary (explicit non-goals — do NOT address in this plan):
- 04-REVIEW.md WR-01 (embedded-agenda try/catch parity) — out of scope per 04-VERIFICATION.md "Non-blocking observations"
- 04-REVIEW.md WR-02 / IN-03 (rAF unload cleanup) — out of scope per 04-VERIFICATION.md "Non-blocking observations"
- 04-REVIEW.md IN-01 (dead `--color-text-today` CSS variable) — out of scope per 04-VERIFICATION.md "Non-blocking observations"
- 04-REVIEW.md IN-02 (closure-shadow `createdFile` pattern) — out of scope per 04-VERIFICATION.md "Non-blocking observations"
- 04-REVIEW.md IN-03 (per-surface duplication of `renderEventItem`) — out of scope per 04-VERIFICATION.md "Non-blocking observations"
- 04-REVIEW.md IN-04 (optional vs unconditional `hasNote` gating) — out of scope per 04-VERIFICATION.md "Non-blocking observations"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ux-enhancements/04-CONTEXT.md
@.planning/phases/04-ux-enhancements/04-VERIFICATION.md
@.planning/phases/04-ux-enhancements/04-REVIEW.md
@.planning/phases/04-ux-enhancements/04-04-SUMMARY.md
@.planning/phases/04-ux-enhancements/04-06-SUMMARY.md

<interfaces>
<!-- Concrete code coordinates the executor needs. Sourced from direct read of styles.css and from 04-VERIFICATION.md Gaps Summary. -->

styles.css:146-148 — preexisting selected-state cell background (DO NOT modify; this is the colliding-color source):
  .memochron-day.selected {
    background-color: var(--interactive-accent);
  }

styles.css:161-169 — preexisting ENH-01 today-ring rules (DO NOT modify; the WR-02 fix shipped in 04-06 is verified clean and is the structural analog this plan replicates):
  /* ENH-01: persistent today indicator — accent ring when today is not selected */
  .memochron-day.today {
    box-shadow: inset 0 0 0 2px var(--interactive-accent);
  }

  /* ENH-01: contrasting ring when today is also selected — survives the accent background */
  .memochron-day.today.selected {
    box-shadow: inset 0 0 0 2px var(--text-on-accent);
  }

styles.css:220-230 — preexisting ENH-03 base rule (DO NOT modify; this rule stays untouched and continues to paint the accent-colored square when the cell is NOT selected):
  /* ENH-03: corner-square indicator for day cells that have at least one event with a note */
  .memochron-note-indicator {
    position: absolute;
    top: 2px;
    right: 2px;
    width: 6px;
    height: 6px;
    border-radius: 1px;
    background-color: var(--interactive-accent);
    pointer-events: none;
  }

Insertion point: a new rule on the line immediately after styles.css:230 (the closing `}` of `.memochron-note-indicator`) and BEFORE the blank line at styles.css:231 / before the `/* Week View */` comment at styles.css:232.

Exact rule to insert (verbatim text from 04-VERIFICATION.md Gaps Summary):
  /* ENH-03: contrasting indicator when the day is also selected — survives the accent background */
  .memochron-day.selected .memochron-note-indicator {
    background-color: var(--text-on-accent);
  }

Decision-source references (locked, NON-NEGOTIABLE):
  04-VERIFICATION.md Gap 1 (truth #3 / ENH-03 / CR-01): the literal fix block above is the prescribed remedy
  04-REVIEW.md CR-01: identifies the same root cause (`var(--interactive-accent)` on indicator == `var(--interactive-accent)` on `.selected` background)
  04-06-SUMMARY.md WR-02 resolution: established the `--text-on-accent` Option-A pattern that this plan replicates
  04-CONTEXT.md D-05: corner-square placement and color decisions (palette is theme-aware; no hex literals, no new color settings)
  CLAUDE.md: commit messages and release notes must NOT reference Claude or AI assistance
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add combined-state CSS override for note-indicator on selected day (CR-01 / ENH-03)</name>
  <files>styles.css</files>
  <read_first>
    - styles.css:140-235 (the selected/today/note-indicator rule cluster — confirm the base `.memochron-note-indicator` rule is at lines 221-230 and that `.memochron-day.selected` at lines 146-148 uses `var(--interactive-accent)`)
    - .planning/phases/04-ux-enhancements/04-VERIFICATION.md (Gap 1 — the only remaining gap; the literal fix block in "Gaps Summary" is the prescribed CSS to add)
    - .planning/phases/04-ux-enhancements/04-REVIEW.md (CR-01 — root cause and the WR-02 analog pattern)
    - .planning/phases/04-ux-enhancements/04-06-PLAN.md Task 1 (WR-02 / today-ring fix) — same pattern, applied to the today-ring; this plan applies the same pattern to the note-indicator
    - .planning/phases/04-ux-enhancements/04-CONTEXT.md (D-05 — corner-square design decisions; D-03 — theme-aware color tokens)
  </read_first>
  <action>
    Insert a single new CSS rule into `styles.css` immediately after the closing `}` of the existing `.memochron-note-indicator` rule (currently at line 230). The new rule is a combined-state override that re-colors the indicator to `var(--text-on-accent)` when its parent day cell is `.selected`, mirroring exactly the WR-02 today-ring resolution shipped in 04-06.

    The literal text to insert (matching the fix block prescribed in 04-VERIFICATION.md "Gaps Summary" verbatim):

    A blank line, followed by:
    - Comment line: `/* ENH-03: contrasting indicator when the day is also selected — survives the accent background */`
    - Selector line: `.memochron-day.selected .memochron-note-indicator {`
    - Declaration line: `  background-color: var(--text-on-accent);`
    - Closing brace: `}`

    Insertion position constraints:
    - The new rule MUST appear AFTER the existing `.memochron-note-indicator` rule (currently spanning styles.css:221-230).
    - The new rule MUST appear BEFORE the `/* Week View */` comment (currently at styles.css:232) and its `.memochron-week-grid` block (currently at styles.css:233).
    - One blank line between the existing `.memochron-note-indicator` closing brace and the new rule's comment, for readability consistency with the surrounding file style.

    Implementation choice rationale (record in the eventual commit body, NOT in code): this is the descendant-selector form of the WR-02 fix shipped in 04-06 (`.memochron-day.today.selected` — multi-class selector on the same element). Both forms achieve the same goal: when the parent cell is also `.selected`, switch the child's painted color from `var(--interactive-accent)` to `var(--text-on-accent)` so the child remains visible against the accent-colored parent background. The descendant-selector form is the natural choice here because the indicator is a child element of the day cell, not a state class on the cell itself.

    Do NOT modify the existing `.memochron-note-indicator` rule at styles.css:221-230 — the base rule remains correct for the "has-note but not selected" case (the indicator stays accent-colored against the default cell background).

    Do NOT modify the `.memochron-day.selected` rule at styles.css:146-148 — that rule is the source of the colliding cell background, but changing it would regress the verified-clean "selected-day cell is accent-colored" appearance.

    Do NOT modify the WR-02 fix rules at styles.css:161-169 — those are verified clean and are not the subject of this gap.

    Do NOT touch any TypeScript file. The bug is purely a CSS contrast defect; the render-path code that creates the `div.memochron-note-indicator` element (`viewRenderers.ts:297-300` and `CalendarView.ts:694-700`) is verified correct and unchanged.
  </action>
  <acceptance_criteria>
    - `styles.css` contains the literal substring `.memochron-day.selected .memochron-note-indicator` exactly once (the new combined-state selector).
    - Within the same CSS rule block (between the new selector's `{` and its matching `}`), the file contains the literal substring `background-color: var(--text-on-accent)`.
    - The new rule appears AFTER the existing `.memochron-note-indicator` rule (the line containing `.memochron-day.selected .memochron-note-indicator` must have a strictly greater line number than the line containing the bare `.memochron-note-indicator {` selector that currently lives at styles.css:221).
    - The existing base rule `.memochron-note-indicator { ... background-color: var(--interactive-accent); ... }` remains in the file UNCHANGED (the substring `background-color: var(--interactive-accent)` still appears at least once inside a `.memochron-note-indicator` block).
    - The existing `.memochron-day.selected { background-color: var(--interactive-accent); }` rule at styles.css:146-148 remains UNCHANGED (the substring `.memochron-day.selected {` still appears followed by `background-color: var(--interactive-accent)`).
    - The existing WR-02 fix rules `.memochron-day.today` (styles.css:162-164) and `.memochron-day.today.selected` (styles.css:167-169) remain UNCHANGED — both selectors still present, both `box-shadow: inset 0 0 0 2px ...` declarations still present.
    - `npm run build` exits 0 (CSS is bundled as a sibling file but the build must still succeed without TypeScript regressions).
    - No TypeScript source file under `src/` is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '\.memochron-day\.selected \.memochron-note-indicator' styles.css)" = "1" &amp;&amp; awk '/\.memochron-day\.selected \.memochron-note-indicator\s*\{/,/\}/' styles.css | grep -q 'background-color: var(--text-on-accent)' &amp;&amp; test "$(grep -n '^\.memochron-note-indicator {' styles.css | head -1 | cut -d: -f1)" -lt "$(grep -n '\.memochron-day\.selected \.memochron-note-indicator' styles.css | head -1 | cut -d: -f1)" &amp;&amp; awk '/^\.memochron-note-indicator\s*\{/,/\}/' styles.css | grep -q 'background-color: var(--interactive-accent)' &amp;&amp; grep -q '\.memochron-day\.today\.selected' styles.css &amp;&amp; grep -q '\.memochron-day\.today {' styles.css &amp;&amp; test -z "$(git diff --name-only -- 'src/**/*.ts' 'src/**/*.tsx' 2>/dev/null)" &amp;&amp; npm run build</automated>
  </verify>
  <done>The CSS file contains a new `.memochron-day.selected .memochron-note-indicator` rule with `background-color: var(--text-on-accent)`, inserted immediately after the existing `.memochron-note-indicator` base rule. The base rule and the `.memochron-day.selected` cell-background rule are unchanged. The WR-02 today-ring rules from 04-06 are unchanged. `npm run build` is green. VERIFICATION truth #3 will flip from FAILED to VERIFIED on the next `/gsd-verify-phase 4` run.</done>
</task>

</tasks>

<verification>
After Task 1 completes, perform three phase-level checks:

1. **Re-run verifier:** `/gsd-verify-phase 4` should report all six truths as VERIFIED with no remaining BLOCKED or PARTIAL items. Score moves from 5/6 to 6/6.

2. **No regression in shipped scope (in-scope artifacts from earlier plans must remain intact):**
   - `grep -c '\.memochron-day\.today {' styles.css` returns 1 (ENH-01 base rule from 04-01 intact)
   - `grep -c '\.memochron-day\.today\.selected' styles.css` returns 1 (WR-02 fix from 04-06 intact)
   - `grep -c '\.memochron-event-note-indicator' styles.css` returns ≥ 1 (WR-01 fix from 04-06 intact)
   - `grep -c 'showNoteIndicatorOnGrid' src/settings/types.ts src/settings/SettingsTab.ts` returns ≥ 2 (ENH-03 setting wiring from 04-04 intact)
   - `grep -c 'view.file?.path === file.path' src/views/CalendarView.ts src/views/EmbeddedAgendaView.ts` returns 2 (WR-03 fix from 04-06 intact)
   - `grep -c 'extractCursorMarker' src/services/NoteService.ts` returns ≥ 1 (ENH-06 marker-stripping from 04-05 intact)

3. **Build is green:** `npm run build` exits 0. TypeScript `tsc -noEmit -skipLibCheck` succeeds (it is part of the build script).
</verification>

<success_criteria>
- VERIFICATION.md truth #3 (note-indicator visible on selected day) flips FAILED → VERIFIED on re-run.
- 04-REVIEW.md CR-01 (the BLOCKER) is fully addressed and can be moved to "resolved" status on the next review pass.
- All non-blocking observations from 04-VERIFICATION.md remain explicitly out of scope (REVIEW.md WR-01 embedded-agenda try/catch parity, IN-03/WR-02 rAF unload cleanup, IN-01..IN-04 code-quality smells).
- No new files created. No settings added. No new dependencies. No TypeScript source modified.
- Total diff is contained to: `styles.css` (one additive CSS rule of approximately 4 lines including the leading comment).
- `npm run build` is green.
- Commit message contains NO references to Claude or AI assistance (per CLAUDE.md project rule).
</success_criteria>

<commit_strategy>
One atomic commit. Suggested commit message (no Claude / AI references per CLAUDE.md):

`fix(styles): contrasting note-indicator when day is also selected (CR-01)`

Commit body should reference the gap ID (CR-01), the VERIFICATION.md truth number (#3), and the ROADMAP success criterion (#3 / ENH-03). It should explicitly note that the pattern is the descendant-selector analog of the WR-02 today-ring fix shipped in commit `12e9f3b` (04-06 Task 1). No AI provenance.
</commit_strategy>

<deferred_ideas>
- **04-REVIEW.md WR-01** (embedded-agenda try/catch parity in `EmbeddedAgendaView.handleEventClick`) — surface divergence from `CalendarView.addEventClickHandler`; does not block any SC. Defer to a future hardening plan that touches `EmbeddedAgendaView` error-handling.
- **04-REVIEW.md WR-02 / IN-03** (rAF unload cleanup) — companion robustness item to WR-03; the existing active-file guard makes the wrong-file write impossible at the user-visible site. Defer to a future hardening plan.
- **04-REVIEW.md IN-01** (dead `--color-text-today: var(--interactive-accent)` CSS variable) — declared but unused; cleanup item. Defer to a future cleanup pass.
- **04-REVIEW.md IN-02** (closure-shadow `createdFile` pattern in `CalendarView.ts:959-962` and `EmbeddedAgendaView.ts:418-421`) — readability nit; behavior is correct. Defer to a future code-quality pass.
- **04-REVIEW.md IN-03** (per-surface duplication of `renderEventItem`) — known debt from the original ENH-02 work; both surfaces are exercised, duplication is debt-not-defect. Defer to a future refactor.
- **04-REVIEW.md IN-04** (optional vs unconditional `hasNote` gating) — latent risk if a future caller forgets to provide `hasNote`; no caller currently omits it. Defer to a future code-quality pass.

All six are documented for future hardening passes and do not affect any of the six ROADMAP success criteria. None overlap with this plan's single scope (the CR-01 CSS override).
</deferred_ideas>

<output>
After completion, create `.planning/phases/04-ux-enhancements/04-07-SUMMARY.md` documenting:
- Which single gap was closed (CR-01 / ENH-03 truth #3)
- The exact file/line diff (insertion of the new `.memochron-day.selected .memochron-note-indicator` rule immediately after styles.css:230)
- Confirmation that `/gsd-verify-phase 4` is the next workflow step (expected score: 6/6)
- Confirmation that all 04-REVIEW.md non-blocking observations (WR-01, WR-02/IN-03, IN-01, IN-02, IN-03, IN-04) were intentionally deferred per 04-VERIFICATION.md "Non-blocking observations" scoping
- Reference to the analog WR-02 fix in 04-06 (commit `12e9f3b`) as the structural precedent
</output>
