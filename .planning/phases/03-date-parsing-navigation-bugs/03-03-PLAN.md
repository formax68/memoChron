---
phase: 03-date-parsing-navigation-bugs
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/utils/viewRenderers.ts
  - .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md
autonomous: true
requirements:
  - BUG-04
must_haves:
  truths:
    - "D-11: The DD-MM-YYYY/MM-DD-YYYY branch of `parseDateFromFilename` carries a code comment that names issue #56, the post-#58 fix, the Phase 3 BUG-01 closure, and the concrete `29-01-2026 → 2026-01-29 local` outcome — short, single-line, greppable"
    - "D-13: The unreachable duplicate regex at `viewRenderers.ts:433` (`/(\\d{2}-\\d{2}-\\d{4})/` — same regex as line 431) is deleted; the `formats` array drops from six entries to five"
    - "D-13: The DD-MM-YYYY branch's dual-parse fallback (DD-MM-YYYY → MM-DD-YYYY) inside the matched branch at lines 456-464 is preserved — the deletion is hygienic dead-code removal, not a behavior change"
    - "D-12: A `.planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` artifact exists matching the shape of `01-HUMAN-UAT.md` and `02-HUMAN-UAT.md`, covering the four ROADMAP success criteria for Phase 3 (including the BUG-04 walkthrough: rename/create `29-01-2026.md`, confirm agenda selects 29 January 2026 not 20 January 2029)"
    - "D-13: After the formats array is reduced, `parseDateFromFilename(\"29-01-2026.md\")` still returns a Date with `.getDate() === 29` and `.getMonth() === 0` — same observable behavior as before the deletion, because the surviving DD-MM-YYYY regex at the prior line 431 matches and the in-branch dual-parse handles disambiguation"
  artifacts:
    - path: "src/utils/viewRenderers.ts"
      provides: "parseDateFromFilename with five-entry formats array and BUG-04 closure comment"
      contains: "#56"
    - path: ".planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md"
      provides: "Human verification artifact covering all four Phase 3 ROADMAP success criteria"
      contains: "29-01-2026"
  key_links:
    - from: "src/utils/viewRenderers.ts parseDateFromFilename"
      to: "issue tracker (#56, #58, #59) and Phase 3 BUG-01"
      via: "single-line greppable comment on DD-MM-YYYY branch"
      pattern: "#56.*#58.*BUG-01"
    - from: ".planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md"
      to: "ROADMAP.md Phase 3 success criteria 1-4"
      via: "four numbered test entries"
      pattern: "29-01-2026"
---

<objective>
Close BUG-04 with a verification artifact and a hygiene cleanup:

- **Verification artifact (D-11):** add a single greppable code comment to the DD-MM-YYYY branch of `parseDateFromFilename` that names issue #56, the post-#58 fix, and the Phase 3 BUG-01 closure. This is the "documented" half of ROADMAP success criterion #4. A future maintainer who greps for `#56` or `BUG-01` lands directly on the code that handles `29-01-2026 → 2026-01-29 local`.
- **Hygiene cleanup (D-13):** delete the unreachable duplicate regex at `src/utils/viewRenderers.ts:433`. Lines 431 and 433 currently both hold `/(\d{2}-\d{2}-\d{4})/`; the second one is shadowed by the first and never matches. The DD-MM vs MM-DD disambiguation is handled inside the matched branch by the dual-parse fallback at lines 456-464, so deleting the duplicate is purely dead-weight removal — no behavior change.
- **HUMAN-UAT artifact (D-12, plus CONTEXT.md `<specifics>` directive):** create `.planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` with four numbered tests, one per ROADMAP Phase 3 success criterion. This is the user-facing verification artifact that confirms BUG-01 through BUG-04 close from the user's perspective. Same template as `01-HUMAN-UAT.md` and `02-HUMAN-UAT.md`.

This plan depends on plan **03-01** because it modifies the same function (`parseDateFromFilename`) that 03-01 rewrote. Landing it as Wave 2 prevents merge-conflict noise with the BUG-01 diff. The BUG-04 comment text also references "BUG-01 fix in Phase 3" — that reference is only accurate after 03-01 has landed.

Purpose: close the four-bug phase with a clean trailing artifact a future maintainer can find, and remove the one piece of dead code in the file that the BUG-01 fix exposes. Phase 3 closes when this plan lands.

Output:
- `src/utils/viewRenderers.ts` `parseDateFromFilename` with:
  - The duplicate regex at line 433 removed (formats array drops from 6 to 5 entries)
  - A new single-line comment above the DD-MM-YYYY branch naming #56, #58, BUG-01, and the `29-01-2026 → 2026-01-29 local` concrete outcome
- `.planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` — new file, frontmatter + four numbered tests + summary block (same shape as `02-HUMAN-UAT.md`)
- One commit covering the source change + the UAT artifact, or two atomic commits — Claude's discretion per D-12
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-date-parsing-navigation-bugs/03-CONTEXT.md
@.planning/phases/03-date-parsing-navigation-bugs/03-PATTERNS.md
@.planning/phases/03-date-parsing-navigation-bugs/03-01-PLAN.md
@.planning/phases/02-security-correctness/02-HUMAN-UAT.md
@.planning/phases/01-foundation/01-HUMAN-UAT.md
@CLAUDE.md

<interfaces>
<!-- Established patterns the executor must follow exactly. -->

From src/utils/viewRenderers.ts:423-436 (the `formats` array currently with the duplicate at line 433 — content BEFORE 03-01 lands; the array body should be untouched by 03-01):
```typescript
const formats = [
  // YYYY-MM-DD
  /(\d{4}-\d{2}-\d{2})/,
  // YYYY_MM_DD
  /(\d{4}_\d{2}_\d{2})/,
  // YYYY.MM.DD
  /(\d{4}\.\d{2}\.\d{2})/,
  // DD-MM-YYYY
  /(\d{2}-\d{2}-\d{4})/,
  // MM-DD-YYYY                ← line 433 — UNREACHABLE; same regex as line 431
  /(\d{2}-\d{2}-\d{4})/,
  // YYYYMMDD
  /(\d{8})/,
];
```
After this plan, the `MM-DD-YYYY` comment AND the duplicate regex below it (the lines tagged "UNREACHABLE") are both deleted. The remaining `// DD-MM-YYYY` comment above the surviving regex is rewritten to reflect that this branch handles both DD-MM and MM-DD via in-branch dual-parse (see new comment text below).

From src/utils/viewRenderers.ts:456-464 (the DD-MM-YYYY in-branch dual-parse fallback — POST-03-01 shape, where `new Date(`${parts[2]}-${parts[1]}-${parts[0]}`)` has been replaced with `new Date(year, month - 1, day)`-form construction; this is the branch that handles `29-01-2026`):

After 03-01 lands, this branch tries DD-MM-YYYY first (`parts[0]` is day, `parts[1]` is month), and if that produces an invalid Date (e.g. month > 12), falls back to MM-DD-YYYY (`parts[0]` is month, `parts[1]` is day). The dual-parse is unchanged by this plan — only the surrounding regex array and the comment change.

For `29-01-2026`:
- DD-MM-YYYY interpretation: day=29, month=01, year=2026 → valid Date (January 29, 2026). Returns this. ✓
- MM-DD-YYYY interpretation would be: month=29, day=01, year=2026 → invalid (month > 12). Never reached because the first interpretation succeeded.

So `29-01-2026.md` → 29 January 2026. The BUG-04 outcome.

From src/services/CalendarService.ts:39, 49, 76 (greppable issue/bug comment pattern — analog for the new comment):
```typescript
// BUG-06 (D-12): a single in-flight Promise deduplicates concurrent callers.
// The Promise's non-null state IS the "fetch in flight" signal.
```
```typescript
// BUG-06 (D-12): concurrent callers receive the in-flight promise.
// Eliminates the double-render race where two callers each kick off a fetch.
```
```typescript
// BUG-06 (D-12): hold the in-flight promise so concurrent callers see it.
```
Pattern: `// <issue-ref> <decision-ref>: <one-line summary including before/after concrete value>`. Single line preferred; multi-line acceptable when the rationale needs it.

From .planning/phases/02-security-correctness/02-HUMAN-UAT.md (the UAT template — frontmatter + numbered tests + summary):
```yaml
---
status: partial
phase: 02-security-correctness
source: [02-VERIFICATION.md]
started: <ISO8601 timestamp>
updated: <ISO8601 timestamp>
---

## Current Test

[awaiting human testing]

## Tests

### 1. <short title>

expected: <one-paragraph description of setup + observation + pass criterion>
result: [pending]

### 2. ...

## Summary

total: N
passed: 0
issues: 0
pending: N
skipped: 0
blocked: 0

## Gaps
```
Phase 3's UAT has FOUR tests (one per ROADMAP success criterion). The `source:` value is `[03-VERIFICATION.md]` even though that file does not exist yet — that is the pattern matching Phase 1 and Phase 2, where the verification file is generated by a later step in the gsd flow.

Project rule (CLAUDE.md):
- No Claude / AI references in commit messages or in the UAT body
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove duplicate regex and add BUG-04 closure comment in parseDateFromFilename</name>
  <files>src/utils/viewRenderers.ts</files>
  <read_first>
    - src/utils/viewRenderers.ts (lines 418-477 — confirm the post-03-01 shape of `parseDateFromFilename`; the `formats` array at lines 423-436 should still contain six entries because 03-01 did not touch it; the DD-MM-YYYY in-branch dual-parse at lines 456-464 should now use the numeric constructor)
    - src/utils/viewRenderers.ts (lines 425-435 — the six-entry `formats` array; the deletion targets line 433 + its preceding `// MM-DD-YYYY` comment at line 432)
    - src/services/CalendarService.ts (lines 38-77 — the BUG-06 / D-12 greppable comment pattern; this is the analog for the BUG-04 closure comment)
    - .planning/phases/03-date-parsing-navigation-bugs/03-01-PLAN.md (the plan that landed first in Wave 1; confirm the BUG-01 diff is committed before starting this task — `git log --oneline | grep -c BUG-01` returns >= 1)
    - .planning/phases/03-date-parsing-navigation-bugs/03-CONTEXT.md (decisions D-11, D-13)
    - .planning/phases/03-date-parsing-navigation-bugs/03-PATTERNS.md (Site 2 — duplicate regex removal; Site 1 — code comment placement on the DD-MM-YYYY branch)
  </read_first>
  <action>
Make two narrow edits to `src/utils/viewRenderers.ts`.

**1. Delete the duplicate regex at line 433 (and its preceding comment at line 432).** In the `formats` array (currently lines 423-436), find the block:

```typescript
  // DD-MM-YYYY
  /(\d{2}-\d{2}-\d{4})/,
  // MM-DD-YYYY                ← delete THIS comment line
  /(\d{2}-\d{2}-\d{4})/,       ← delete THIS regex line
```

After the deletion, the array drops from six entries to five. The surviving `// DD-MM-YYYY` comment above the first occurrence of `/(\d{2}-\d{2}-\d{4})/` is REPLACED with the new BUG-04 closure comment described in step 2 below — the comment now describes both BUG-04 closure context AND the dual-parse semantics of the branch.

**2. Add the BUG-04 closure comment above the surviving DD-MM-YYYY regex.** Per D-11, the comment is a single line that names issue #56, the post-#58 fix, the Phase 3 BUG-01 closure, and the concrete `29-01-2026 → 2026-01-29 local` outcome:

```typescript
    // BUG-04 (D-11): #56 regression closed post-#58 (and BUG-01 fix in Phase 3 — local-day construction). 29-01-2026 → 2026-01-29 local. Handles DD-MM-YYYY and MM-DD-YYYY via in-branch dual-parse below.
    /(\d{2}-\d{2}-\d{4})/,
```

This comment replaces what was previously `// DD-MM-YYYY` above the surviving regex AND absorbs the semantic note from the now-deleted `// MM-DD-YYYY` comment (because the dual-parse covers both formats from the single regex). After the edit, the `formats` array shape is:

```typescript
  const formats = [
    // YYYY-MM-DD
    /(\d{4}-\d{2}-\d{2})/,
    // YYYY_MM_DD
    /(\d{4}_\d{2}_\d{2})/,
    // YYYY.MM.DD
    /(\d{4}\.\d{2}\.\d{2})/,
    // BUG-04 (D-11): #56 regression closed post-#58 (and BUG-01 fix in Phase 3 — local-day construction). 29-01-2026 → 2026-01-29 local. Handles DD-MM-YYYY and MM-DD-YYYY via in-branch dual-parse below.
    /(\d{2}-\d{2}-\d{4})/,
    // YYYYMMDD
    /(\d{8})/,
  ];
```

**Important comment-text notes:**
- The exact strings `#56`, `#58`, and `BUG-01` MUST appear in the comment — they are the greppable anchors for a future maintainer. Verify with `grep -c '#56' src/utils/viewRenderers.ts` returning >= 1 and same for `#58` and `BUG-01`.
- The concrete date transform `29-01-2026 → 2026-01-29 local` MUST appear verbatim. This is the canonical example from D-11.
- Single line is preferred (matches the BUG-06 D-12 analog), but if the line gets uncomfortably long for a 100-column terminal, splitting into two `//` lines is acceptable — the first carries the issue references and date transform, the second carries the dual-parse explanation.
- The comment uses the project's 4-space indentation inside the array (matching the existing entries at lines 425/427/429/431/435).

**3. Verify the in-branch dual-parse at lines 456-464 is UNCHANGED.** The 03-01 plan rewrote the Date construction inside this branch (string-form → numeric form). This plan does NOT touch that logic. The branch still tries DD-MM-YYYY first (returning if valid) and MM-DD-YYYY second (returning if valid). Read the branch in the post-03-01 shape and confirm.

**Do NOT modify in this task:**
- Any other format branch in `parseDateFromFilename`
- `parseDate` (lines 385-416)
- Any other function in `viewRenderers.ts`
- The function signature, exports, or call-site contract

**Behavioral assertion:** because the deleted regex was unreachable (shadowed by the identical regex above it), removing it changes no observable behavior. Verify by running the same Node REPL spot-checks from plan 03-01's acceptance criteria after this edit — every test case still returns the same Date.

**Commit:** single commit titled `chore(viewRenderers): close BUG-04 — comment + remove dead duplicate regex (#56)`. Body briefly notes that #56's `29-01-2026 → 2026/01/20` mis-parse is closed post-#58 and confirmed by the BUG-01 fix from plan 03-01. No Claude / AI references.
  </action>
  <verify>
    <automated>npx tsc -noEmit && npm run build && awk '/const formats = \[/,/\];/' src/utils/viewRenderers.ts | grep -cE '^\s*/\(' && grep -c '#56' src/utils/viewRenderers.ts && grep -c 'BUG-01' src/utils/viewRenderers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -noEmit` exits 0
    - `npm run build` exits 0
    - The `formats` array now has exactly five regex entries: `awk '/const formats = \[/,/\];/' src/utils/viewRenderers.ts | grep -cE '^\s*/\('` returns 5 (down from 6)
    - The duplicate regex line is gone — the file contains only ONE occurrence of `/(\d{2}-\d{2}-\d{4})/`: `grep -c '/(\\d{2}-\\d{2}-\\d{4})/' src/utils/viewRenderers.ts` returns 1
    - The BUG-04 closure comment names all three references: `grep -c '#56' src/utils/viewRenderers.ts` returns >= 1, `grep -c '#58' src/utils/viewRenderers.ts` returns >= 1, `grep -c 'BUG-01' src/utils/viewRenderers.ts` returns >= 1
    - The concrete date transform appears verbatim: `grep -c '29-01-2026' src/utils/viewRenderers.ts` returns >= 1, `grep -c '2026-01-29' src/utils/viewRenderers.ts` returns >= 1
    - The in-branch dual-parse is preserved: `awk '/^\s*\} else if \(\/\^\\\\d\{2\}-\\\\d\{2\}-\\\\d\{4\}\\\\\\$\//, /^\s*\} else if/' src/utils/viewRenderers.ts | grep -c 'parts\[' ` returns >= 2 (both day/month index orderings still parsed)
    - **Behavior assertion (Node REPL spot-check, same fixtures as plan 03-01):**
      - `parseDateFromFilename("29-01-2026.md").getDate()` returns 29 and `.getMonth()` returns 0
      - `parseDateFromFilename("01-29-2026.md").getDate()` returns 29 and `.getMonth()` returns 0 (MM-DD-YYYY fallback inside the matched branch)
      - `parseDateFromFilename("2026-01-15.md").getDate()` returns 15 and `.getMonth()` returns 0 (unchanged from plan 03-01)
      - All other format branches (YYYY-MM-DD, YYYY_MM_DD, YYYY.MM.DD, YYYYMMDD) return the same Dates as before
    - Commit message contains `BUG-04` and `#56`; does NOT contain `Claude` or `AI`
  </acceptance_criteria>
  <done>
    `parseDateFromFilename` has a five-entry `formats` array (duplicate at line 433 deleted), the surviving DD-MM-YYYY regex carries a single-line greppable comment naming `#56`, `#58`, `BUG-01`, and `29-01-2026 → 2026-01-29 local`. `tsc -noEmit` and `npm run build` exit 0. Node REPL spot-checks show identical Date outputs versus the post-03-01 baseline.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 03-HUMAN-UAT.md covering all four Phase 3 ROADMAP success criteria</name>
  <files>.planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 58-67 — the four Phase 3 success criteria, one per BUG)
    - .planning/phases/02-security-correctness/02-HUMAN-UAT.md (full file — the canonical template for the UAT artifact shape, frontmatter, test format, and summary block)
    - .planning/phases/01-foundation/01-HUMAN-UAT.md (full file — confirms the cross-phase shape; Phase 1's UAT has only one test, Phase 2's has five — Phase 3's will have four)
    - .planning/phases/03-date-parsing-navigation-bugs/03-CONTEXT.md (decision D-12 — the HUMAN-UAT for BUG-04 walks through renaming/creating `29-01-2026.md` and confirming the agenda selects 29 January)
    - .planning/phases/03-date-parsing-navigation-bugs/03-CONTEXT.md (the `<specifics>` block — "HUMAN-UAT entries are the verification artifact this milestone")
  </read_first>
  <action>
Create a new file at `.planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md`. Match the shape and tone of `02-HUMAN-UAT.md` exactly: YAML frontmatter, `## Current Test` block, `## Tests` with one numbered test per ROADMAP success criterion, `## Summary` block, `## Gaps` placeholder.

Frontmatter:

```yaml
---
status: partial
phase: 03-date-parsing-navigation-bugs
source: [03-VERIFICATION.md]
started: <ISO8601 timestamp at file-creation moment, e.g. 2026-05-12T07:00:00Z>
updated: <same timestamp>
---
```

Note: the `source: [03-VERIFICATION.md]` entry mirrors the convention in Phase 1 (`source: [01-VERIFICATION.md]`) and Phase 2 (`source: [02-VERIFICATION.md]`). The verification file does not exist at UAT-creation time; it is generated downstream by the gsd flow.

Body:

```markdown
## Current Test

[awaiting human testing]

## Tests

### 1. BUG-01: daily-note filename in non-UTC timezone selects the correct local day

expected: With Obsidian running on macOS or Linux in `America/New_York` (UTC-5) or any other timezone west of UTC, open the vault, locate or create a daily note named `2026-01-15.md` (no folder requirement; the file just needs to exist in the vault). Click the daily note in the file explorer. The MemoChron agenda pane selects January 15 — not January 14. The calendar grid highlights the January 15 cell. Repeat the test by renaming an existing note to `2026-06-15.md` (mid-year, DST boundary considerations) and confirm June 15 selects (not June 14).
result: [pending]

### 2. BUG-02: month and week navigation arrows feel instantaneous

expected: With at least one configured iCal source enabled and the calendar pane open in month-mode, click the next-month arrow ten times in rapid succession. Each click repaints the grid immediately — no spinner, no perceptible pause between click and paint. With DevTools network panel open, confirm that AT MOST one outbound calendar fetch fires during the ten clicks (not ten). Repeat in 1-week mode by clicking the next-week arrow ten times: same instant repaint, same single-fetch behavior. Confirms ROADMAP success criterion #2 and confirms the Phase 2 `fetchInFlight` dedup is collapsing concurrent triggers.
result: [pending]

### 3. BUG-03: Today button recenters across all view modes after a drag-resize

expected: With the calendar pane in month-mode (default), drag the resize handle UP until the calendar collapses to 1-week mode (the view-mode dropdown should read "Week" or the active week-mode label). Click the next-week arrow several times until today is no longer in the visible week. Click the Today button. The calendar repaints with today's week visible; today's cell is selected; the view-mode dropdown still reads "Week" (the view mode did not change). Repeat in 2-week mode: drag-resize to 2-week height, navigate away, click Today — today's week is centered within the 2-week range.
result: [pending]

### 4. BUG-04: 29-01-2026.md filename parses to 29 January 2026 (not 20 January 2029)

expected: Create (or rename an existing daily note to) a file named `29-01-2026.md` in the vault. Click it in the file explorer. The MemoChron agenda pane selects 29 January 2026. The calendar grid highlights the January 29, 2026 cell (NOT January 20, 2029 — the pre-#58 mis-parse). With code-review verification: `grep -n "#56" src/utils/viewRenderers.ts` returns the BUG-04 closure comment naming `#56`, `#58`, `BUG-01`, and `29-01-2026 → 2026-01-29 local`. Confirms ROADMAP success criterion #4 — closed AND documented.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
```

**Notes:**
- Each test maps 1:1 to a ROADMAP success criterion in the order BUG-01, BUG-02, BUG-03, BUG-04 (same order as ROADMAP.md lines 63-66).
- The `expected:` paragraphs describe BOTH the setup the human must perform AND the observation they must confirm. They are written so a tester (the user, not the executor agent) can follow them without re-reading the plan or context.
- Test 4 includes a code-review element (`grep -n "#56" src/utils/viewRenderers.ts`) because BUG-04 is half "verify in the running plugin" and half "verify the documentation artifact landed" — same dual-shape as D-11/D-12.
- `Gaps` is left as an empty heading per the Phase 1/2 convention; if any test fails during human UAT, the failures are recorded under this heading by a later step in the gsd flow.

**Do NOT:**
- Reference Claude or AI in the UAT body
- Add tests beyond the four ROADMAP success criteria — out of scope
- Mark any test as `[passed]` — the executor agent cannot pass them; they are explicitly user-facing

**Commit:** single commit titled `docs(phase-03): add HUMAN-UAT artifact for BUG-01..BUG-04`. Per D-12 commit-granularity discretion, this commit can be combined with Task 1's commit, or kept separate — Claude's discretion. Keeping separate makes git-bisect cleaner. No Claude / AI references.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md && grep -c '^### [1-4]\. BUG-0' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md && grep -c 'total: 4' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` exits 0
    - Frontmatter parses as YAML: file starts with `---`, contains `phase: 03-date-parsing-navigation-bugs`, `status: partial`, `source: [03-VERIFICATION.md]`, `started:` and `updated:` ISO timestamps
    - Four numbered tests present: `grep -c '^### [1-4]\. BUG-0' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` returns 4
    - Each test names its BUG: `grep -c 'BUG-01' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` >= 1, same for BUG-02, BUG-03, BUG-04
    - Test 1 (BUG-01) names the timezone scenario: `grep -c 'America/New_York' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` >= 1
    - Test 4 (BUG-04) names the specific filename: `grep -c '29-01-2026' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` >= 1 and `grep -c '#56' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` >= 1
    - Each test has `result: [pending]`: `grep -c 'result: \[pending\]' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` returns 4
    - Summary block: `grep -c 'total: 4' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` returns 1, `grep -c 'pending: 4' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` returns 1
    - Gaps placeholder: `grep -c '^## Gaps' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` returns 1
    - No Claude / AI references: `grep -ciE 'claude|anthropic' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` returns 0 and `grep -cE '\\bAI\\b' .planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` returns 0
    - Commit message does NOT contain `Claude` or `AI`
  </acceptance_criteria>
  <done>
    `.planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` exists, follows the Phase 2 template, has four numbered tests (one per ROADMAP Phase 3 success criterion), and is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan deletes a dead-code regex line and adds a comment. The HUMAN-UAT file is project documentation, not executable. No data crosses any trust boundary as a result of these changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | (no applicable STRIDE category) | source comment + array entry deletion + documentation file | accept | No new attack surface. Removing an unreachable regex changes no observable behavior. Adding a comment is inert. Creating a markdown UAT file under `.planning/` adds no executable surface. |

**Conclusion:** No new attack surface — pure hygiene and documentation. No `high`-severity findings; nothing to block on.
</threat_model>

<verification>
- `tsc -noEmit` and `npm run build` both exit 0 after Task 1
- `formats` array in `parseDateFromFilename` has five entries (down from six)
- The surviving DD-MM-YYYY regex carries a single-line comment naming `#56`, `#58`, `BUG-01`, and `29-01-2026 → 2026-01-29 local`
- Node REPL spot-checks confirm `parseDateFromFilename("29-01-2026.md")` returns January 29, 2026 (unchanged behavior from the post-03-01 baseline)
- `.planning/phases/03-date-parsing-navigation-bugs/03-HUMAN-UAT.md` exists, follows the Phase 2 template shape, and has four numbered tests
- No Claude / AI references in any commit or artifact
</verification>

<success_criteria>
- BUG-04 closed: the post-#58 fix is now documented in-code with a greppable single-line comment AND verified by the HUMAN-UAT entry
- ROADMAP success criterion #4 met: `29-01-2026.md` → January 29, 2026 (confirmed via code path that the BUG-01 fix already routes through the numeric Date constructor) AND documented via the in-code comment
- Hygiene win: the unreachable duplicate regex is gone; the `formats` array is honest
- Phase 3 ships with a complete `03-HUMAN-UAT.md` artifact covering all four ROADMAP success criteria
- Phase 3 is complete after this plan lands; Phase 4 can begin
</success_criteria>

<output>
After completion, create `.planning/phases/03-date-parsing-navigation-bugs/03-03-SUMMARY.md` capturing:
- The before/after diff of the `formats` array (six entries → five entries)
- The exact text of the BUG-04 closure comment that landed
- Confirmation that the in-branch dual-parse at lines 456-464 is unchanged from the post-03-01 shape
- Node REPL spot-check output for `29-01-2026.md`, `01-29-2026.md`, and `2026-01-15.md` (confirming no behavior regression versus the post-03-01 baseline)
- The path and frontmatter timestamps of the new `03-HUMAN-UAT.md`
- `tsc -noEmit` and `npm run build` exit codes
- The commit SHA(s) — one if combined, two if separate
</output>
