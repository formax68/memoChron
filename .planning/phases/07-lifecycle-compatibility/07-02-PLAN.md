---
phase: 07-lifecycle-compatibility
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/views/CalendarView.ts
  - src/views/EmbeddedAgendaView.ts
  - src/settings/SettingsTab.ts
  - src/utils/viewRenderers.ts
  - src/utils/colorValidation.ts
autonomous: true
requirements: [DIR-06]
tags: [obsidian-plugin, popout-window, activeDocument, window-timers, prefer-active-doc]

must_haves:
  truths:
    - "Every `getComputedStyle(document.documentElement)` call across `src/` is replaced by `getComputedStyle(activeDocument.documentElement)` per D-04 + D-05 — `git ls-files src/ | xargs grep -nE 'getComputedStyle\\(document\\.documentElement\\)'` returns zero matches"
    - "Every bare `setTimeout`, `setInterval`, `clearTimeout`, `clearInterval`, `requestAnimationFrame` call in `src/` has a `window.` prefix per amendment A2 (supersedes D-06) — non-comment grep returns zero non-`window.` matches"
    - "The 11 `getComputedStyle` sites grep-confirmed at HEAD are migrated: `SettingsTab.ts:170, 612, 636, 670, 681, 705`; `CalendarView.ts:658, 767`; `EmbeddedAgendaView.ts:259`; `viewRenderers.ts:144`; `colorValidation.ts:46` (CONTEXT D-05 wording said `14 sites`; live grep at planning time returned 11 — if ESLint surfaces additional sites at execution, fix those too)"
    - "The 2 `requestAnimationFrame` sites surfaced by research (`CalendarView.ts:967`, `EmbeddedAgendaView.ts:425`) gain a `window.` prefix per A2 site-by-site reconciliation"
    - "The 2 bare `setTimeout` sites in `SettingsTab.ts:1381, 1783` gain a `window.` prefix per A2 site-by-site reconciliation"
    - "`src/main.ts:202, 227` and the 3 `clear*` callsites in `main.ts` are LEFT UNCHANGED per A2 — all already use `window.*` prefix"
    - "`src/views/CalendarView.ts:79` (startup `window.setTimeout`) is LEFT UNCHANGED per A2 — already correct"
    - "`npm run lint` exits 0 and `npm run build` exits 0 at HEAD after this plan's commit (Phase 7 override block at `eslint.config.mjs:65-91` still active; per-rule sampling per VALIDATION.md confirms count does not regress vs HEAD~1)"
    - "No new abstraction is introduced (no `readAccentColor()` helper) — the 11 reads stay inline per Claude's-Discretion default (CONTEXT.md `<decisions>` Claude's Discretion 4th bullet)"
    - "D-07: No `app.workspace.activeWindow` / `app.workspace.activeDocument` indirection — every DOM read uses the bare `activeDocument` global (per D-04) and every timer uses the bare `window.*` (per A2). `git ls-files src/ | xargs grep -nE 'app\\.workspace\\.active(Window|Document)'` returns zero matches after this plan's commit."
  artifacts:
    - path: "src/views/CalendarView.ts"
      provides: "Two `getComputedStyle(activeDocument.documentElement)` reads at the former 658, 767 sites; one `window.requestAnimationFrame` at the former 967 site"
      pattern: "activeDocument\\.documentElement|window\\.requestAnimationFrame"
    - path: "src/views/EmbeddedAgendaView.ts"
      provides: "One `getComputedStyle(activeDocument.documentElement)` read; one `window.requestAnimationFrame`"
      pattern: "activeDocument\\.documentElement|window\\.requestAnimationFrame"
    - path: "src/settings/SettingsTab.ts"
      provides: "Six `getComputedStyle(activeDocument.documentElement)` reads; two `window.setTimeout` calls"
      pattern: "activeDocument\\.documentElement|window\\.setTimeout"
    - path: "src/utils/viewRenderers.ts"
      provides: "One `getComputedStyle(activeDocument.documentElement)` read"
      pattern: "activeDocument\\.documentElement"
    - path: "src/utils/colorValidation.ts"
      provides: "One `getComputedStyle(activeDocument.documentElement)` read"
      pattern: "activeDocument\\.documentElement"
  key_links:
    - from: "view code reads CSS custom properties (e.g., --interactive-accent)"
      to: "popout window's <html> element"
      via: "activeDocument.documentElement — follows the user's focused window per prefer-active-doc rule"
      pattern: "getComputedStyle\\(activeDocument\\.documentElement\\)"
    - from: "view code schedules timers (setTimeout, setInterval, requestAnimationFrame)"
      to: "the calling realm's timer ID pool"
      via: "window.* prefix — per prefer-window-timers rule, NOT activeWindow.* (which the rule forbids)"
      pattern: "window\\.(setTimeout|setInterval|clearTimeout|clearInterval|requestAnimationFrame)"
---

<objective>
Second commit of Phase 7. Close DIR-06 (popout-window compatibility) by adopting `activeDocument` for all `getComputedStyle(document.documentElement)` reads (per D-04 + D-05) AND adding the `window.` prefix to all bare `setTimeout`, `setInterval`, `clearTimeout`, `clearInterval`, `requestAnimationFrame` calls (per amendment A2 — supersedes D-06). The two rules have an intentional asymmetry: `activeDocument` for DOM ops (so popout windows render with the correct theme), `window.*` for timers (per `obsidianmd/prefer-window-timers` — `activeWindow.*` is FORBIDDEN; the rule auto-fixes `activeWindow.setTimeout` to `window.setTimeout`).

Purpose: ROADMAP Phase 7 success criterion #2 ("`git ls-files src/ | xargs grep -nE '(^|[^.a-zA-Z])document\\.|window\\.set(Timeout|Interval)\\b'` audited: every match in a view context uses `activeDocument` / `window.setTimeout` / `window.setInterval`"). Per RESEARCH §"Conflict 2: D-04 / D-06 vs. obsidianmd/prefer-window-timers", the rule sources were read line-by-line and the canonical end-state is `activeDocument` for DOM, `window.*` for timers. The asymmetry is already inline-documented at `src/main.ts:215-222` (iOS WKWebView timer-ID pool comment).

Output: 5 source files modified (`CalendarView.ts`, `EmbeddedAgendaView.ts`, `SettingsTab.ts`, `viewRenderers.ts`, `colorValidation.ts`); one git commit with subject `refactor(views): adopt activeDocument and add window prefix for timers (DIR-06)`. `src/main.ts:202` and `src/main.ts:227` are no-ops per A2 (already correct).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-lifecycle-compatibility/07-CONTEXT.md
@.planning/phases/07-lifecycle-compatibility/07-RESEARCH.md
@.planning/phases/07-lifecycle-compatibility/07-PATTERNS.md
@.planning/phases/07-lifecycle-compatibility/07-01-SUMMARY.md
@CLAUDE.md
@src/views/CalendarView.ts
@src/views/EmbeddedAgendaView.ts
@src/settings/SettingsTab.ts
@src/utils/viewRenderers.ts
@src/utils/colorValidation.ts
@src/main.ts

<interfaces>
<!-- 11 grep-confirmed sites for D-05. CONTEXT.md said "14 sites" — live grep at planning time returned 11. If ESLint surfaces additional sites at execution, the lint output is authoritative; fix those with the same one-token swap. -->

D-05 sites — `getComputedStyle(document.documentElement)` → `getComputedStyle(activeDocument.documentElement)`:

  src/settings/SettingsTab.ts:170 — daily-note-color initialization
  src/settings/SettingsTab.ts:612 — color-picker overlay theme read
  src/settings/SettingsTab.ts:636 — color-picker overlay theme read
  src/settings/SettingsTab.ts:670 — color-picker overlay theme read
  src/settings/SettingsTab.ts:681 — color-picker overlay theme read
  src/settings/SettingsTab.ts:705 — color-picker overlay theme read
  src/views/CalendarView.ts:658  — calendar-grid daily-note-dot color resolution
  src/views/CalendarView.ts:767  — agenda daily-note color resolution
  src/views/EmbeddedAgendaView.ts:259 — embedded-agenda daily-note color resolution
  src/utils/viewRenderers.ts:144 — shared daily-note color resolution
  src/utils/colorValidation.ts:46 — inside `defaultDailyNoteColor()` (no plugin ref; uses global `activeDocument` per D-04)

A2 timer sites — `window.` prefix:

  src/main.ts:202 — `window.setInterval` ✓ ALREADY CORRECT — NO CHANGE per A2
  src/main.ts:210 — `window.clearInterval` ✓ ALREADY CORRECT
  src/main.ts:225 — `window.clearTimeout` ✓ ALREADY CORRECT
  src/main.ts:227 — `window.setTimeout` ✓ ALREADY CORRECT — NO CHANGE per A2
  src/main.ts:235 — `window.clearTimeout` ✓ ALREADY CORRECT
  src/views/CalendarView.ts:79 — `window.setTimeout` ✓ ALREADY CORRECT — verify; no change if prefix present
  src/settings/SettingsTab.ts:1381 — `setTimeout(...)` (bare) — ADD `window.` PREFIX
  src/settings/SettingsTab.ts:1783 — `setTimeout(...)` (bare) — ADD `window.` PREFIX
  src/views/CalendarView.ts:967 — `requestAnimationFrame(...)` (bare) — ADD `window.` PREFIX
  src/views/EmbeddedAgendaView.ts:425 — `requestAnimationFrame(...)` (bare) — ADD `window.` PREFIX

Net edits per file:

  src/main.ts: NO CHANGES (verify only — A2 confirms 202, 227 already use `window.*`)
  src/views/CalendarView.ts: 3 edits (lines 658 + 767 active-doc swap; line 967 add `window.` prefix to `requestAnimationFrame`)
  src/views/EmbeddedAgendaView.ts: 2 edits (line 259 active-doc swap; line 425 add `window.` prefix to `requestAnimationFrame`)
  src/settings/SettingsTab.ts: 8 edits (6 active-doc swaps + 2 add `window.` prefix to bare `setTimeout`)
  src/utils/viewRenderers.ts: 1 edit (active-doc swap)
  src/utils/colorValidation.ts: 1 edit (active-doc swap)

Total: 15 edits across 5 files (D-05: 11 sites, A2: 4 sites that need additions; A2: 6 already-correct sites verified).

Identifier reference (no imports needed):
  `activeDocument` and `window` are both globals registered by `obsidianmd.configs.recommended` (see eslint.config.mjs:33-41 comment and PATTERNS Pattern 3 net-new flag). No `import` statement required at any of the 5 files.

Cross-reference to leave untouched (per PATTERNS Pattern 3):
  `src/views/CalendarView.ts:1112, 1207` use `getComputedStyle(this.calendar)` (an element reference, not `document.documentElement`). These are NOT `prefer-active-doc` targets — MUST NOT be edited.
  `src/settings/SettingsTab.ts:562, 567, 585` use `document.createElementNS(SVG_NS, ...)` for SVG creation — Phase 6 D-12 documented these as leave-alone (rule does not flag `createElementNS`). DO NOT edit.

Per Claude's-Discretion (CONTEXT.md `<decisions>` block 4th bullet): keep the 11 reads INLINE; do NOT introduce a `readAccentColor()` helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate the 11 getComputedStyle(document.documentElement) sites to activeDocument (D-05)</name>
  <files>
    src/settings/SettingsTab.ts,
    src/views/CalendarView.ts,
    src/views/EmbeddedAgendaView.ts,
    src/utils/viewRenderers.ts,
    src/utils/colorValidation.ts
  </files>
  <read_first>
    - .planning/phases/07-lifecycle-compatibility/07-CONTEXT.md (D-04 — globals over workspace-instance; D-05 — site list; Claude's-Discretion 4th bullet — keep inline, no helper)
    - .planning/phases/07-lifecycle-compatibility/07-RESEARCH.md (Pattern 4 — verified one-token swap shape; §"Conflict 2" — `prefer-active-doc` skips timers, so the rule only fires on DOM reads)
    - .planning/phases/07-lifecycle-compatibility/07-PATTERNS.md (Pattern 3 — net-new pattern flag; the rule's auto-fix shape IS the template; representative excerpt at SettingsTab.ts:168-173)
    - All 5 source files in `<files>` — confirm the current line numbers (PATTERNS line numbers captured 2026-05-15; plan 07-01 only touched `src/main.ts`, so these should not have shifted)
  </read_first>
  <action>
    Make a one-token swap at each of the 11 sites listed in `<interfaces>`. Replace `document.documentElement` with `activeDocument.documentElement` inside the `getComputedStyle(...)` call. Do NOT change any other code on the surrounding lines — keep the trailing `.getPropertyValue("...").trim()` chain, the `|| "#7c3aed"` fallback (where present), and the surrounding variable assignment unchanged.

    Site-by-site:
      1. `src/settings/SettingsTab.ts:170` — change `document.documentElement` → `activeDocument.documentElement` on that single line inside the daily-note-color initialization block.
      2. `src/settings/SettingsTab.ts:612` — same one-token swap inside the ternary branch starting with `? getComputedStyle(...)`.
      3. `src/settings/SettingsTab.ts:636` — same shape as site 2.
      4. `src/settings/SettingsTab.ts:670` — same swap (unwrapped, no `?`).
      5. `src/settings/SettingsTab.ts:681` — same shape as site 2 (ternary branch).
      6. `src/settings/SettingsTab.ts:705` — same shape as site 2 (ternary branch).
      7. `src/views/CalendarView.ts:658` — same one-token swap inside `dailyNoteColor` resolution for calendar-grid dot.
      8. `src/views/CalendarView.ts:767` — same one-token swap inside `renderDailyNoteEntry` for agenda dot.
      9. `src/views/EmbeddedAgendaView.ts:259` — same one-token swap inside embedded-agenda `renderDailyNoteEntry`.
      10. `src/utils/viewRenderers.ts:144` — same one-token swap inside the shared `renderDailyNoteEntry` helper.
      11. `src/utils/colorValidation.ts:46` — same one-token swap inside `defaultDailyNoteColor()`. This file has NO plugin reference; the global `activeDocument` per D-04 is used directly. No import required.

    SANITY CHECK after all 11 edits:
      `git ls-files src/ | xargs grep -nE 'getComputedStyle\(document\.documentElement\)'` must return ZERO matches.
      `git ls-files src/ | xargs grep -nE 'getComputedStyle\(activeDocument\.documentElement\)'` must return at least 11 matches.
      `git ls-files src/ | xargs grep -nE 'getComputedStyle\(this\.calendar\)'` must STILL return matches at `src/views/CalendarView.ts:1112` and `:1207` (intentional leave-alone — element refs, not document refs; cross-reference PATTERNS Pattern 3).
      `git ls-files src/ | xargs grep -nE 'document\.createElementNS'` must STILL return matches at `src/settings/SettingsTab.ts:562, 567, 585` (SVG creation, Phase 6 D-12 leave-alone).

    If ESLint surfaces additional `obsidianmd/prefer-active-doc` violations beyond these 11 sites (CONTEXT.md mentioned "14 sites" — the +3 may be other shapes of `document.X` reads), apply the same `document` → `activeDocument` swap at each.

    Do NOT add any helper function. Do NOT change any timer call in this task (that is Task 2). Do NOT touch `src/main.ts`.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; ! git ls-files src/ | xargs grep -nE 'getComputedStyle\(document\.documentElement\)' &amp;&amp; [ "$(git ls-files src/ | xargs grep -cE 'getComputedStyle\(activeDocument\.documentElement\)' | awk -F: '{s+=$2} END {print s+0}')" -ge 11 ] &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files src/ | xargs grep -nE 'getComputedStyle\(document\.documentElement\)'` returns ZERO matches
    - `git ls-files src/ | xargs grep -nE 'getComputedStyle\(activeDocument\.documentElement\)'` returns at least 11 matches (one per site)
    - `git ls-files src/ | xargs grep -nE 'getComputedStyle\(this\.calendar\)'` still returns at least 2 matches at `src/views/CalendarView.ts:1112` and `:1207` (intentional leave-alone)
    - `git ls-files src/ | xargs grep -nE 'document\.createElementNS'` still returns at least 3 matches at `src/settings/SettingsTab.ts` (SVG leave-alone per Phase 6 D-12)
    - `npm run build` exits 0 (`tsc --noEmit` accepts `activeDocument` as a registered global — if it does NOT, the executor adds a `declare const activeDocument: Document;` ambient declaration, but the obsidianmd recommended config registers this global so this is unlikely)
    - No helper function (e.g., `readAccentColor`) was added — verified by `git diff` not introducing any new exported function
  </acceptance_criteria>
  <done>
    All 11 `getComputedStyle(document.documentElement)` sites are migrated to `getComputedStyle(activeDocument.documentElement)`. Element-reference reads at `CalendarView.ts:1112, 1207` are untouched. SVG `createElementNS` sites at `SettingsTab.ts:562, 567, 585` are untouched. `npm run build` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add window. prefix to the 4 bare timer sites per amendment A2</name>
  <files>
    src/settings/SettingsTab.ts,
    src/views/CalendarView.ts,
    src/views/EmbeddedAgendaView.ts
  </files>
  <read_first>
    - .planning/phases/07-lifecycle-compatibility/07-CONTEXT.md (amendment A2 — site-by-site reconciliation list; D-07 — no workspace-instance indirection)
    - .planning/phases/07-lifecycle-compatibility/07-RESEARCH.md (§"Conflict 2" — rule source `preferWindowTimers.js` read line-by-line; rule auto-fix is `activeWindow.X` → `window.X`; the `prefer-active-doc` rule explicitly skips timer functions; intentional asymmetry inline-documented at `src/main.ts:215-222`)
    - .planning/phases/07-lifecycle-compatibility/07-PATTERNS.md ("`window.*` timer prefix" subsection of Shared Patterns — canonical owned-handle shape at `src/main.ts:202, 227`)
  </read_first>
  <action>
    Add a `window.` prefix at each of the 4 bare-timer sites listed below. Confirm the already-prefixed site at `CalendarView.ts:79` does not need a change.

    Site 1 — `src/settings/SettingsTab.ts:1381`:
      Inside the `blur` registerDomEvent callback for an input's autocomplete suggestions container. Replace `setTimeout(` with `window.setTimeout(` on the line that currently reads `      setTimeout(() => {`. No other change. Do NOT track the timer handle (fire-and-forget 200ms delay; not owned-handle territory).

    Site 2 — `src/settings/SettingsTab.ts:1783`:
      Same shape as Site 1 — bare `setTimeout` inside a different `blur` callback for autocomplete. Same action: `setTimeout(` → `window.setTimeout(`.

    Site 3 — `src/views/CalendarView.ts:967`:
      Inside `handleEventClick` (or similar), bare `requestAnimationFrame` call following a daily-note `openFile`. Replace `requestAnimationFrame(` with `window.requestAnimationFrame(` on the line that currently reads `        requestAnimationFrame(() => {`. No other change.

    Site 4 — `src/views/EmbeddedAgendaView.ts:425`:
      Same shape as Site 3 — bare `requestAnimationFrame` after `openFile`. Action: `requestAnimationFrame(` → `window.requestAnimationFrame(`. No other change.

    VERIFY (do NOT change) — already-prefixed sites:
      `src/main.ts:202` — `window.setInterval` ✓
      `src/main.ts:210` — `window.clearInterval` ✓
      `src/main.ts:225` — `window.clearTimeout` ✓
      `src/main.ts:227` — `window.setTimeout` ✓
      `src/main.ts:235` — `window.clearTimeout` ✓
      `src/views/CalendarView.ts:79` — `window.setTimeout` ✓ (PATTERNS confirms; if grep at execution time shows it missing the prefix, add it; otherwise no change)

    SANITY CHECK after all 4 edits, use this command (it filters out single-line and multi-line comment references):

      git ls-files src/ | xargs grep -nE '\b(setTimeout|setInterval|clearTimeout|clearInterval|requestAnimationFrame)\(' | grep -v 'window\.' | grep -vE '^[^:]*:[0-9]+:\s*(//|\*|/\*)'

    Must return ZERO matches. Comment-only references at `main.ts:218-220` (jsdoc explaining iOS WKWebView rationale), `CalendarService.ts:199-200`, and `CalendarView.ts:75-78` are excluded by the trailing `grep -vE` filter.

    Do NOT track timer handles for the 4 sites edited. Per RESEARCH §"Alternatives Considered", these are not owned-handle cases — they are fire-and-forget delays / RAF callbacks tied to the calling view's lifetime. The owned-handle pattern (with explicit `clearTimeout`) is reserved for the auto-refresh / background-refresh timers that survive across user interactions.

    Do NOT touch `src/main.ts` in this task. Per A2 site-by-site reconciliation, all 5 timer sites in `main.ts` are already correct.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; bash -c '! git ls-files src/ | xargs grep -nE "\b(setTimeout|setInterval|clearTimeout|clearInterval|requestAnimationFrame)\(" | grep -v "window\." | grep -vE "^[^:]*:[0-9]+:\s*(//|\*|/\*)" | grep .' &amp;&amp; grep -c 'window\.setTimeout' src/settings/SettingsTab.ts &amp;&amp; grep -c 'window\.requestAnimationFrame' src/views/CalendarView.ts &amp;&amp; grep -c 'window\.requestAnimationFrame' src/views/EmbeddedAgendaView.ts &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The non-comment timer grep (see Task action) returns ZERO non-`window.` matches
    - `grep -c 'window\.setTimeout' src/settings/SettingsTab.ts` returns at least 2 (sites 1381 + 1783 prefixed)
    - `grep -c 'window\.requestAnimationFrame' src/views/CalendarView.ts` returns at least 1 (site 967 prefixed)
    - `grep -c 'window\.requestAnimationFrame' src/views/EmbeddedAgendaView.ts` returns at least 1 (site 425 prefixed)
    - `grep -n 'window\.setInterval' src/main.ts` still returns the existing match at line 202 (no-op — already correct per A2)
    - `grep -n 'window\.setTimeout' src/main.ts` still returns the existing match at line 227 (no-op — already correct per A2)
    - `npm run build` exits 0
    - `npm run lint` exits 0 (Phase 7 override block at `eslint.config.mjs:65-91` still active; both `obsidianmd/prefer-active-doc` and `obsidianmd/prefer-window-timers` are suppressed at this commit — the override removal lands in plan 07-05; count must not regress vs HEAD~1 per VALIDATION.md sampling rate)
  </acceptance_criteria>
  <done>
    All 4 bare timer sites have a `window.` prefix. The 5 already-prefixed `main.ts` sites are unchanged. The `CalendarView.ts:79` `window.setTimeout` is unchanged. `npm run build && npm run lint` both exit 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit DIR-06 changes with the exact D-11 commit 2 subject line</name>
  <files>(git commit only — no source changes in this task)</files>
  <read_first>
    - .planning/phases/07-lifecycle-compatibility/07-CONTEXT.md (D-11 step 2 — exact commit subject; A2 commit-placement note — body should explain `window.*` is the rule-correct shape for timers and `activeDocument` for DOM reads)
    - CLAUDE.md (Memory Reminders: NO Claude / AI references in commits)
  </read_first>
  <action>
    Stage the 5 modified files from Tasks 1 + 2:
      - `src/settings/SettingsTab.ts`
      - `src/views/CalendarView.ts`
      - `src/views/EmbeddedAgendaView.ts`
      - `src/utils/viewRenderers.ts`
      - `src/utils/colorValidation.ts`

    Create a single commit with the exact subject line from D-11 step 2 (using the orchestrator's adjusted form that incorporates A2):

      refactor(views): adopt activeDocument and add window prefix for timers (DIR-06)

    Commit body (4-6 sentences, NO Claude / AI references per CLAUDE.md):
      Paragraph 1: Names DIR-06 closure. References D-04 + D-05. Lists the 11 `getComputedStyle(document.documentElement)` → `getComputedStyle(activeDocument.documentElement)` swaps across `SettingsTab.ts` (×6), `CalendarView.ts` (×2), `EmbeddedAgendaView.ts` (×1), `viewRenderers.ts` (×1), `colorValidation.ts` (×1).
      Paragraph 2: Names amendment A2 (supersedes D-06). Reports the 4 bare-timer sites that gained a `window.` prefix: `SettingsTab.ts:1381, 1783` (`setTimeout`), `CalendarView.ts:967` and `EmbeddedAgendaView.ts:425` (`requestAnimationFrame`). One-line rationale: per `obsidianmd/prefer-window-timers` (rule source read line-by-line in research), timer functions must use `window.*`, NOT `activeWindow.*` — the rule auto-fixes `activeWindow.setTimeout` to `window.setTimeout`. The asymmetry vs `prefer-active-doc` is intentional and inline-documented at `src/main.ts:215-222` (iOS WKWebView timer-ID pool comment).
      Paragraph 3: Confirms no helper added (per CONTEXT Claude's-Discretion — keep inline); the 11 reads remain inline. `main.ts` is intentionally untouched in this commit (per A2 site-by-site reconciliation — the 5 timer sites in `main.ts` are already correct).

    Use HEREDOC for the commit message body. Do NOT use `--no-verify`. Do NOT include `Co-Authored-By:` or any trailer referencing Claude / AI / assistant.

    BEFORE committing, run `npm run lint && npm run build` one more time and confirm both exit 0. If lint regresses (more violations than HEAD~1), inspect the new violations and fix per Task 1/2 rules.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; git log -1 --pretty=%s | grep -qxF 'refactor(views): adopt activeDocument and add window prefix for timers (DIR-06)' &amp;&amp; ! git log -1 --pretty=%B | grep -iE 'claude|AI assist|Co-Authored-By' &amp;&amp; [ "$(git diff HEAD~1 --name-only | sort | tr '\n' ' ')" = "src/settings/SettingsTab.ts src/utils/colorValidation.ts src/utils/viewRenderers.ts src/views/CalendarView.ts src/views/EmbeddedAgendaView.ts " ] &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` exactly matches `refactor(views): adopt activeDocument and add window prefix for timers (DIR-06)`
    - `git diff HEAD~1 --name-only | sort` lists exactly these 5 files (no more, no fewer): `src/settings/SettingsTab.ts`, `src/utils/colorValidation.ts`, `src/utils/viewRenderers.ts`, `src/views/CalendarView.ts`, `src/views/EmbeddedAgendaView.ts`
    - `git log -1 --pretty=%B` contains NO occurrence of "claude", "Claude", "AI", "assistant", "Co-Authored-By" (case-insensitive)
    - `npm run lint` exits 0 at HEAD
    - `npm run build` exits 0 at HEAD
    - The DIR-06 grep set at HEAD:
      - `git ls-files src/ | xargs grep -nE 'getComputedStyle\(document\.documentElement\)'` returns zero matches
      - `git ls-files src/ | xargs grep -nE '\b(setTimeout|setInterval|clearTimeout|clearInterval|requestAnimationFrame)\(' | grep -v 'window\.' | grep -vE '^[^:]*:[0-9]+:\s*(//|\*|/\*)'` returns zero non-comment matches
  </acceptance_criteria>
  <done>
    The DIR-06 commit exists at HEAD with the exact D-11 step 2 subject line, contains exactly the 5 source files, has no Claude / AI / Co-Authored-By references, and `npm run lint && npm run build` exit 0 at HEAD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 7 ships no new attack surface. This commit is internal runtime-context hardening:
- `activeDocument` ensures CSS-variable reads follow the user's focused window (popout window theme accuracy) rather than the main window's stale state
- `window.*` timer prefix follows the rule-correct shape — timers fire in the calling realm, avoiding the iOS WKWebView timer-ID-pool ambiguity noted at `src/main.ts:215-222`

| Boundary | Description |
|----------|-------------|
| View code → DOM (active document) | Reads of CSS custom properties now follow the user's focused window. No write surface change. |
| View code → timer ID pool | Timers are scheduled on the calling window's realm. No new attack surface; the iOS WKWebView ID-pool quirk is already managed by the existing owned-handle pattern in `main.ts`. |

## STRIDE Threat Register

Severity: **none**. RESEARCH §"Security Domain" verified no security implication. The change is a runtime-context refactor.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | `getComputedStyle(activeDocument.documentElement)` reads | accept | Reads CSS custom properties only. No PII, no secrets, no DOM mutations through this path. The change reduces "stale theme on popout window" UX bug surface; not a security control. |
| T-07-04 | Tampering | Timer scheduling with `window.*` prefix | accept | Timers fire on the calling window's realm — same behavior as before for the main window case. Popout windows now correctly schedule timers on their own realm. iOS WKWebView timer-ID pool semantics handled by the existing owned-handle pattern (Phase 1 IN-02 / WR-01); unchanged in this commit. |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint` exits 0 (Phase 7 override block at `eslint.config.mjs:65-91` still active; per-rule sampling per VALIDATION.md confirms count does not regress vs HEAD~1)
- `git ls-files src/ | xargs grep -nE 'getComputedStyle\(document\.documentElement\)'` returns zero matches
- `git ls-files src/ | xargs grep -nE 'getComputedStyle\(activeDocument\.documentElement\)'` returns at least 11 matches
- `git ls-files src/ | xargs grep -nE '\b(setTimeout|setInterval|clearTimeout|clearInterval|requestAnimationFrame)\(' | grep -v 'window\.' | grep -vE '^[^:]*:[0-9]+:\s*(//|\*|/\*)'` returns zero non-comment matches
- `git log -1 --pretty=%s` matches the D-11 step 2 subject exactly
- `git log -1 --pretty=%B` contains no Claude / AI / Co-Authored-By references
- `git diff HEAD~1 --name-only` lists exactly the 5 source files
</verification>

<success_criteria>
- DIR-06 closed at the source level (all 11 DOM reads use `activeDocument`; all timer calls have `window.` prefix)
- ROADMAP Phase 7 success criterion #2 satisfied at the source level (audit grep documented; non-view contexts intentionally unchanged: `main.ts` timers already correct, SVG `createElementNS` left alone per Phase 6 D-12, element-ref `getComputedStyle(this.calendar)` left alone)
- HEAD commit matches D-11 step 2 subject line exactly; no Claude / AI references
- No new abstraction introduced (no `readAccentColor()` helper) per Claude's-Discretion
</success_criteria>

<output>
After completion, create `.planning/phases/07-lifecycle-compatibility/07-02-SUMMARY.md` with:
- Per-file edit count (Tasks 1 + 2: 5 files, 15 total edits)
- The 11 active-doc swaps listed with `file:line` references
- The 4 timer-prefix additions listed with `file:line` references
- The 6 verified-no-change sites in `main.ts` + `CalendarView.ts:79` (with file:line references)
- The git commit SHA and subject line
- The `npm run lint` violation-count delta vs HEAD~1 (must not regress)
- Confirmation that the commit message has no Claude / AI references
- Hand-off to plan 07-03 (DIR-07 instanceof TFile narrowing)
</output>
