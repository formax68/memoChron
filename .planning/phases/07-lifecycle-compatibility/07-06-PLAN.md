---
phase: 07-lifecycle-compatibility
plan: 06
type: execute
wave: 6
depends_on: [07-05]
files_modified:
  - .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md
autonomous: false
requirements: [DIR-05, DIR-06, DIR-07, DIR-08, BUG-07]
tags: [human-uat, popout-window, settings-modal, daily-note-open, lifecycle-acceptance, verification]

must_haves:
  truths:
    - "`.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md` exists at HEAD and follows the Phase 6 `06-HUMAN-UAT.md` template structure per CONTEXT D-12 + PATTERNS Pattern 7"
    - "The 6 mandatory UAT steps from CONTEXT D-12 are documented as a checklist with explicit Pass/Fail criteria each: popout-window walkthrough (DIR-06 / SC #6), daily-note open path 3-surface check (DIR-07), settings-modal persistence on plugin toggle (BUG-07 / SC #5), sidebar parity vs v1.14.0 (regression check), embedded-view parity (D-10 lifecycle rewrite), lint clean (D-12 step 6)"
    - "The reviewer executes the UAT walkthrough in a built/installed plugin and records Pass/Fail evidence inline in `07-HUMAN-UAT.md`"
    - "No before/after screenshot PNG artifacts are committed under `.planning/phases/07-*/` per D-12 (mirrors Phase 6 D-15)"
    - "If UAT step 3 (BUG-07) PASSES (modal stays open), the BUG-07 closure is either folded into plan 07-01's commit body OR noted in this UAT step's result"
    - "If UAT step 3 (BUG-07) FAILS (modal still closes), plan 07-07 lands `BUG-07-CLOSURE.md` with reproduction steps, Obsidian version, OS, and evidence that core plugins reproduce the same Obsidian-side bug"
    - "Mobile UAT footer per D-13: 'Desktop-only verification; mobile audit deferred to v1.16 if regression reported'"
    - "`07-HUMAN-UAT.md` frontmatter `status: complete` (set ONLY after Overall Acceptance verdict is PASS)"
    - "HEAD commit subject matches D-11 step 7: `docs(07): record Phase 7 human UAT`"
    - "No Claude / AI references in the file content, commit subject, commit body, or any trailer"
  artifacts:
    - path: ".planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md"
      provides: "Human-executed UAT evidence-of-record for Phase 7 acceptance (ROADMAP Phase 7 success criteria #5 + #6)"
      pattern: "DIR-05|DIR-06|DIR-07|DIR-08|BUG-07"
  key_links:
    - from: ".planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md"
      to: "Phase 7 acceptance closure"
      via: "6-step UAT walkthrough confirms popout-window rendering, daily-note open paths, settings-modal persistence, sidebar parity, embedded-view parity, lint clean"
      pattern: "Pass|Fail"
---

<objective>
Create `.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md` and execute its 6-step walkthrough against a built/installed plugin to confirm Phase 7 acceptance per CONTEXT D-12 + ROADMAP Phase 7 success criteria #5 (BUG-07 / modal persistence) and #6 (popout-window rendering). This is the FINAL commit of Phase 7 per D-11 step 7 (unless plan 07-07 conditionally lands BUG-07-CLOSURE.md). It closes the manual-verification leg of DIR-05 / DIR-06 / DIR-07 / DIR-08 / BUG-07 that lint + grep cannot exercise.

Purpose: ROADMAP Phase 7 success criteria #5 and #6 require manual UAT. SC #5: "disabling MemoChron in Obsidian's Community Plugins list does NOT close the Settings modal (or a written closure note is committed under `.planning/phases/07-*/` with reproduction steps and Obsidian-version evidence)." SC #6: "opening the plugin in an Obsidian popout window renders calendar grid, navigation works, timers continue to fire — verifying DIR-06's `activeDocument` adoption (timers use `window.*` per A2)." Per CONTEXT D-12 + Phase 6 D-15 precedent, the verification artifact is `07-HUMAN-UAT.md` (no test suite this milestone per PROJECT.md Out of Scope; no PNG baselines per D-12 mirroring Phase 6 D-15).

Output: `07-HUMAN-UAT.md` with the 6 mandatory UAT steps documented and executed (Pass/Fail evidence inline); one git commit with subject `docs(07): record Phase 7 human UAT`. If UAT step 3 FAILS, hand off to plan 07-07 (BUG-07-CLOSURE.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-lifecycle-compatibility/07-CONTEXT.md
@.planning/phases/07-lifecycle-compatibility/07-RESEARCH.md
@.planning/phases/07-lifecycle-compatibility/07-PATTERNS.md
@.planning/phases/07-lifecycle-compatibility/07-01-SUMMARY.md
@.planning/phases/07-lifecycle-compatibility/07-02-SUMMARY.md
@.planning/phases/07-lifecycle-compatibility/07-03-SUMMARY.md
@.planning/phases/07-lifecycle-compatibility/07-04-SUMMARY.md
@.planning/phases/07-lifecycle-compatibility/07-05-SUMMARY.md
@.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md
@CLAUDE.md

<interfaces>
<!-- 6 mandatory UAT steps from CONTEXT D-12 (authoritative). Mirror the Phase 6 `06-HUMAN-UAT.md` structure verbatim per PATTERNS Pattern 7. -->

D-12 step list (verbatim):
  1. **Popout window full walkthrough** (SC #6): right-click sidebar tab → "Move to new window"; in the popout, confirm (a) calendar grid renders with correct accent colors (verifies `activeDocument.documentElement` reads), (b) month-prev / month-next navigation works, (c) drag-resize between month-view and week-view works (verifies `window.setTimeout` for the startup timer + drag handlers), (d) wait long enough for the auto-refresh interval and confirm one refresh fires (verifies `window.setInterval` for the background-refresh timer).
  2. **Daily-note open path** (DIR-07 verification): in the agenda, click an event whose date has a daily note; confirm the daily note opens correctly. Repeat for an embedded calendar code block and an embedded agenda code block (covers all 3 `instanceof TFile` openFile sites).
  3. **Settings-modal persistence on plugin toggle** (SC #5 / BUG-07): open Obsidian Settings → Community Plugins, locate MemoChron, click the toggle to disable; confirm the Settings modal STAYS OPEN. Re-enable, confirm the modal stays open. If the modal still closes: plan 07-07 lands `BUG-07-CLOSURE.md` capturing reproduction steps, Obsidian version (`Obsidian → About`), OS, and an evidence note that the Obsidian Insider build (if accessible) reproduces the same behavior.
  4. **Sidebar parity check:** in the main sidebar, confirm calendar grid + agenda render identically to v1.14.0 / post-Phase-6 baseline. No regression from the active-doc / active-window changes.
  5. **Embedded-view parity check:** open a note containing `memochron-calendar` and `memochron-agenda` code blocks; confirm both render and the daily-note-click path works (covers EmbeddedCalendarView / EmbeddedAgendaView async-lifecycle rewrite from D-10).
  6. **Lint clean:** `npm run lint` exits zero with the Phase 7 override block deleted (verifies plan 07-05).

D-13: Mobile UAT is deferred to v1.16. Footer line: "Desktop-only verification; mobile audit deferred to v1.16 if regression reported" (Phase 6 D-15 precedent).

Phase 6 template at `.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md` (verified to exist; 8.2KB; established the no-screenshot live-walkthrough precedent). Top-level structure to mirror:
  - Frontmatter: `phase` (set to `07-lifecycle-compatibility`), `date`, `status` (start `pending`, set `complete` only when Overall Acceptance is PASS)
  - Body sections (in this order):
    - Title: `# Phase 7 — Human UAT Evidence`
    - Intro paragraph: 2-3 sentences. References ROADMAP Phase 7 success criteria #5 + #6 + the DIR-05/06/07/08 + BUG-07 verification leg.
    - Per CONTEXT D-12 note: "No PNG screenshots are committed; the walkthrough is live and the evidence is text inline."
    - `---`
    - `## Pre-Conditions`: bulleted list — Plans 01-05 of Phase 7 are merged, `npm run lint` exits 0 (recorded in 07-05-SUMMARY.md), all Phase 7 success-criterion greps return zero matches (recorded in 07-01/02/03/04 SUMMARYs), freshly built `main.js` + `manifest.json` + `styles.css` installed in test vault, Obsidian version recorded at UAT time, comparison baseline = v1.14.0 release.
    - `---`
    - `## UAT Step 1 — Popout Window Walkthrough` with **Verifies / Steps / Result / Notes** subsections per D-12 step 1
    - `## UAT Step 2 — Daily-Note Open Paths` with same subsection structure per D-12 step 2
    - `## UAT Step 3 — Settings-Modal Persistence on Plugin Toggle` with same subsection structure per D-12 step 3
    - `## UAT Step 4 — Sidebar Parity vs v1.14.0` with same subsection structure per D-12 step 4
    - `## UAT Step 5 — Embedded-View Parity` with same subsection structure per D-12 step 5
    - `## UAT Step 6 — Lint Clean` with same subsection structure per D-12 step 6
    - `## Overall Acceptance`: `ROADMAP Phase 7 success criterion #5 + #6 status: [ ] PASS / [ ] FAIL`; if FAIL list failing step(s) + planned closure path
    - Mobile UAT footer per D-13 mirroring Phase 6 D-15
    - Trailer: `*Phase: 07-lifecycle-compatibility*`, `*UAT executed: [date]*`, `*Reviewer: [reviewer handle]*`

Conditional handling per CONTEXT D-12 step 3 + amendment A1 BUG-07 implication:
  - **If UAT step 3 PASSES** (Settings modal stays open when MemoChron is toggled):
    - Record "Pass" in step 3 Result
    - Note in step 3 Notes that BUG-07 is closed by amendment A1 (deletion of `detachLeavesOfType` from `onunload`)
    - If plan 07-01's commit body did NOT already fold in BUG-07 closure, the reviewer EITHER amends 07-01's commit body OR records the closure in this UAT step's Notes — either is acceptable per D-11 step 1
    - Plan 07-07 is SKIPPED (no `BUG-07-CLOSURE.md` needed)
  - **If UAT step 3 FAILS** (Settings modal still closes):
    - Record "Fail" in step 3 Result
    - Note in step 3 Notes the reproduction steps + Obsidian version + OS
    - Plan 07-07 LANDS with `BUG-07-CLOSURE.md` per D-12 step 3 + D-11 step 5 (conditional commit 5)
    - Plan 07-07's commit comes AFTER plan 07-06's commit (D-11 ordering: closure note → UAT — but if the UAT defines whether closure is needed, the closure commit naturally follows)

No before/after screenshot artifacts committed under `.planning/phases/07-*/` (verified by `ls .planning/phases/07-lifecycle-compatibility/*.{png,jpg,jpeg,gif,bmp,webp} 2>/dev/null | wc -l` returning 0).

Per CLAUDE.md memory reminders: NO Claude / AI references in the file content, commit subject, commit body, or any trailer.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Build the plugin and prepare for UAT</name>
  <files>(no source changes — build verification only)</files>
  <read_first>
    - .planning/phases/07-lifecycle-compatibility/07-05-SUMMARY.md (confirms `npm run lint` exits 0 and the Phase 7 override block is deleted at HEAD)
  </read_first>
  <what-built>
    Plans 07-01 through 07-05 have completed:
    - DIR-05 (`registerView` callback is pure factory; no `plugin.calendarView` field; `getCalendarView()` helper)
    - A1 (`detachLeavesOfType` deleted from `onunload`)
    - DIR-06 (`activeDocument.documentElement` for all `getComputedStyle` reads in view code; `window.*` prefix on all timers)
    - DIR-07 (all 4 `as TFile` casts replaced with `instanceof TFile` guards)
    - DIR-08 (sync `onload(): void` wrapper on Embedded*View files; every floating + misused promise classified per D-09 buckets)
    - Phase 7 ESLint override block at `eslint.config.mjs:65-91` deleted
    - `npm run lint` exits 0 with no Phase 7 rule suppressions
    - `npx eslint --fix src/` is a no-op

    What remains is the human verification of:
    - DIR-06 SC #6 (popout-window correctness)
    - DIR-07 (daily-note open paths in 3 surfaces)
    - BUG-07 / SC #5 (settings modal persistence on plugin toggle)
    - Regression checks (sidebar parity, embedded-view parity)
    - Lint clean confirmation
  </what-built>
  <how-to-verify>
    1. Run `npm run build` and confirm exit 0 (sanity check — was verified across plans 01-05 but re-run for fresh `main.js`).
    2. Install the freshly built plugin into a test Obsidian vault:
        - Locate `main.js`, `manifest.json`, `styles.css` in the repo root
        - Copy them to `<test-vault>/.obsidian/plugins/memochron/`
        - Reload Obsidian or use "Reload app without saving" developer command
    3. Open Obsidian, enable MemoChron in Community Plugins if not already, and confirm the plugin loads without errors in the dev console (Cmd+Opt+I / Ctrl+Shift+I → Console tab).
    4. If the build, install, and load all succeed, proceed to Task 2 (write the UAT skeleton).

    Resume signal: Type "build ready" once the plugin is installed and loads cleanly in the test vault.
  </how-to-verify>
  <resume-signal>Type "build ready" once the plugin is installed and loads cleanly in the test vault; if build or load fails, describe the failure for diagnosis.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the 07-HUMAN-UAT.md file skeleton with the 6 mandatory UAT steps</name>
  <files>.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md (template structure — frontmatter, one section per UAT step, evidence inline, no screenshots)
    - .planning/phases/07-lifecycle-compatibility/07-CONTEXT.md (D-12 — exact 6 UAT steps; D-13 — mobile UAT footer)
  </read_first>
  <action>
    Create `.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md` with the structure specified below. Use the exact heading levels and content. Leave bracketed placeholders for Task 3 to fill in during the walkthrough.

    Frontmatter (matching Phase 6 precedent):
      ---
      phase: 07-lifecycle-compatibility
      date: <current date in YYYY-MM-DD when the walkthrough runs>
      status: pending
      ---

    Body — copy the structure verbatim, adapting per D-12:

      # Phase 7 — Human UAT Evidence

      This file is the verification-of-record for Phase 7 (Lifecycle & Compatibility), consistent with Phases 1-6 practice. It captures the acceptance evidence for ROADMAP Phase 7 success criteria #5 (settings-modal persistence on plugin toggle / BUG-07) and #6 (popout-window rendering / DIR-06), and closes the DIR-05 / DIR-06 / DIR-07 / DIR-08 + BUG-07 manual-verification leg.

      Per CONTEXT D-12, no PNG screenshots are committed; the walkthrough is live and the evidence is text inline.

      ---

      ## Pre-Conditions

      - Plans 07-01 through 07-05 of Phase 7 are merged (see 07-0N-SUMMARY.md files).
      - `npm run lint` exits 0 with Phase 7 override block deleted (recorded in 07-05-SUMMARY.md).
      - `npx eslint --fix src/` is a no-op (recorded in 07-05-SUMMARY.md).
      - All Phase 7 success-criterion greps return zero matches at HEAD (recorded in 07-01/02/03/04/05 SUMMARYs).
      - Freshly built `main.js` + `manifest.json` + `styles.css` installed in the reviewer's test vault.
      - Obsidian version: [reviewer fills in — `Obsidian → Help → About → App version`]
      - OS: [reviewer fills in — macOS / Windows / Linux + version]
      - Comparison baseline: v1.14.0 release (post-Phase-6 baseline for sidebar parity check)

      ---

      ## UAT Step 1 — Popout Window Walkthrough

      **Verifies:** DIR-06 / SC #6 — `activeDocument.documentElement` reads in popout window; `window.setTimeout` / `window.setInterval` timer scheduling correct under popout focus; drag-resize startup-timer behavior survives popout move.

      **Steps:**
      1. Open the sidebar calendar view (left-side ribbon icon or `Cmd/Ctrl+P → MemoChron: Open calendar view`).
      2. Right-click the calendar tab header → select "Move to new window". Obsidian opens a new browser-window-style popout containing the calendar view.
      3. In the popout, confirm the calendar grid renders with the correct accent colors (event dots in each calendar source's color; today/selected day highlights match theme). Verifies `activeDocument.documentElement` follows the popout's `<html>` element for CSS-variable reads.
      4. Click month-prev and month-next navigation arrows. Confirm transitions are instantaneous and the grid updates cleanly.
      5. Drag the resize handle DOWN to switch to week-view; drag UP to switch back to month-view. Confirm the height transition is smooth (verifies the `window.setTimeout`-based startup timer survives the popout context).
      6. Leave the popout open. Wait for one auto-refresh interval (per the user's configured refresh-interval setting, default is 5 minutes — for UAT, the reviewer may temporarily lower the interval to ~1 minute via MemoChron settings, then restore after the test). Confirm one auto-refresh fires (event data updates if changed; otherwise visually unchanged but no errors in the dev console). Verifies `window.setInterval` continues to fire under popout focus.
      7. Close the popout. Confirm the calendar leaf returns to the main window or terminates cleanly.

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in any observed deviation; if Pass, leave blank or write "no deviation observed"]

      ---

      ## UAT Step 2 — Daily-Note Open Paths

      **Verifies:** DIR-07 — `instanceof TFile` narrowing across the 3 daily-note open surfaces (sidebar agenda, embedded calendar code block, embedded agenda code block).

      **Steps:**
      1. In the main sidebar calendar view, navigate to a date that has an existing daily note (or create one first via Obsidian's `Cmd/Ctrl+P → Daily notes: Open today's daily note`).
      2. In the sidebar agenda for that date, click the daily-note entry (the "Daily Note" row with the accent-colored dot). Confirm the daily note opens in a new tab. Verifies the `instanceof TFile` narrow at `CalendarView.ts:828` (former).
      3. In a markdown note (or create a new one), insert a fenced code block: ```memochron-calendar``` (with the language tag `memochron-calendar`). Confirm the embedded calendar renders. Navigate to the same daily-note date and click the date cell — the calendar's day-click handler should open the daily note. Verifies the `instanceof TFile` narrow at `EmbeddedCalendarView.ts:234` (former).
      4. In the same or another note, insert ```memochron-agenda``` (with the language tag `memochron-agenda`). Confirm the embedded agenda renders. Click the daily-note entry. Verifies the `instanceof TFile` narrow at `EmbeddedAgendaView.ts:383` (former).
      5. (Optional regression check) Click a non-daily-note event in any of the 3 surfaces. Confirm it opens / creates the event note correctly (`getAbstractFileByPath` + the existing `instanceof TFile` narrow at `NoteService.ts:73` — pre-existing Phase 2 analog).

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in]

      ---

      ## UAT Step 3 — Settings-Modal Persistence on Plugin Toggle

      **Verifies:** BUG-07 / SC #5 — amendment A1 (deletion of `detachLeavesOfType` from `onunload`) eliminates the plugin-side trigger for the modal-close path. Open question per RESEARCH §"Conflict 1": the forum bug report reproduces with **core** plugins too, suggesting an Obsidian-side bug. If the modal still closes here, that confirms Obsidian-side root cause.

      **Steps:**
      1. Open Obsidian Settings (`Cmd/Ctrl + ,` or settings cog).
      2. Click "Community plugins" in the left navigation.
      3. Locate MemoChron in the installed-plugins list.
      4. Click the toggle to disable MemoChron.
      5. **Observe:** does the Settings modal STAY OPEN, or does it CLOSE?
          - **STAYS OPEN** → BUG-07 is closed. Record Pass below; note in Notes that A1's deletion of `detachLeavesOfType` from `onunload` eliminated the modal-close trigger.
          - **CLOSES** → BUG-07 is Obsidian-side. Record Fail below; note in Notes the reproduction steps + Obsidian version + OS. Plan 07-07 will land `BUG-07-CLOSURE.md` with the closure rationale.
      6. (If modal stayed open) Re-enable MemoChron. Confirm the Settings modal still stays open. The toggle should re-load MemoChron without closing the modal.
      7. (If modal closed in step 5) Re-open Obsidian Settings → Community plugins → re-enable MemoChron. Observe whether the same modal-close happens on enable.

      **Result:** [ ] Pass (modal stays open — BUG-07 closed by A1) / [ ] Fail (modal still closes — closure goes to plan 07-07)
      **Notes:** [reviewer fills in:
        - The exact behavior observed on toggle (modal stayed / closed)
        - Whether the same behavior occurs on enable as on disable
        - If Fail: a brief sentence confirming this matches the Obsidian 1.12.2 forum-thread report (https://forum.obsidian.md/t/settings-modal-closes-when-disabling-a-plugins-actively-focused-view/111479) — and a one-line note indicating plan 07-07 will land the closure note]

      ---

      ## UAT Step 4 — Sidebar Parity vs v1.14.0

      **Verifies:** No visual regression in the sidebar calendar grid or agenda after the `activeDocument` + `window.*` adoption and the floating-promise + lifecycle rewrites. Mirror Phase 6 D-15 (no screenshot baseline; live visual inspection).

      **Steps:**
      1. Side-by-side or memory-compare the sidebar calendar grid against the v1.14.0 baseline (the reviewer's prior install or recollection of the post-Phase-6 state):
          - Event dots in source colors render identically
          - Today indicator (border / background) renders identically
          - Selected-day highlight renders identically
          - Month-prev / month-next transitions instant; no flicker
          - Drag-resize between month-view and week-view works (no broken handle)
      2. Confirm the agenda below the grid renders identically:
          - Per-source color indicators on events
          - Daily-note entry (if enabled in settings) renders in accent color
          - Past events visually de-emphasized
          - "No events scheduled" placeholder renders when applicable
      3. Click an event in the agenda — confirm it opens / creates the linked note correctly (regression check on the promise-hygiene rewrites in `CalendarView.ts`).

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in]

      ---

      ## UAT Step 5 — Embedded-View Parity

      **Verifies:** D-10 — `EmbeddedCalendarView` and `EmbeddedAgendaView` `onload(): void` synchronous wrappers do not regress the rendering or interaction behavior. The async work (former `await this.render()`) is now wrapped in `void this.loadAndRender()` with internal `try/catch` + `Notice`.

      **Steps:**
      1. Open or create a note containing a `memochron-calendar` fenced code block with default params.
      2. Confirm the embedded calendar renders as a month grid identical to the pre-Phase-7 baseline. (The `void this.loadAndRender()` chain should complete and render within the same observable timeframe — there is no synchronization difference for the user.)
      3. In the same or sibling note, embed a `memochron-agenda` code block.
      4. Confirm the embedded agenda renders the day's events. Click a daily-note entry — confirm it opens via the new `instanceof TFile` narrow.
      5. (Negative test, optional) Temporarily corrupt the plugin's calendar URL (e.g., set an obviously invalid URL via settings), reload the note's code block. The new `try/catch` inside `loadAndRender()` should surface the error as a Notice (verifies the error path of the D-10 rewrite). Restore the valid URL after testing.

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in; if step 5's negative test was performed, note whether the Notice fired correctly]

      ---

      ## UAT Step 6 — Lint Clean

      **Verifies:** Plan 07-05 deleted the Phase 7 override block; `npm run lint` exits 0 with the 8 Phase 7 rules now actively enforced.

      **Steps:**
      1. From the repository root, run `npm run lint`.
      2. Confirm exit code is 0 (no errors, no warnings related to Phase 7 rules).
      3. Run `grep -nE 'Phase 7\b' eslint.config.mjs` — confirm zero matches (override block deleted).
      4. Run `npx eslint --fix src/` — confirm `git status -s` reports zero source-side diff (the rules are already satisfied; `--fix` is a no-op).

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in:
        - `npm run lint` output summary (e.g., "0 errors, 0 warnings")
        - `grep -nE 'Phase 7\b' eslint.config.mjs` result
        - `git status -s` after `npx eslint --fix src/` result]

      ---

      ## Overall Acceptance

      ROADMAP Phase 7 success criterion #5 (BUG-07) status: [ ] PASS / [ ] FAIL (closure note in plan 07-07)
      ROADMAP Phase 7 success criterion #6 (popout-window) status: [ ] PASS / [ ] FAIL

      If FAIL on any step: [list the failing step(s) and what specifically deviated; create gap-closure follow-up notes if needed]

      If PASS on all steps (or if BUG-07 step 3 fails but is closed via plan 07-07): Phase 7 is complete; Phase 8 may begin.

      ---

      Mobile UAT footer per CONTEXT D-13: Desktop-only verification; mobile audit deferred to v1.16 if regression reported.

      ---

      *Phase: 07-lifecycle-compatibility*
      *UAT executed: [date]*
      *Reviewer: [reviewer handle]*

    Save the file. Do NOT execute the walkthrough yet — that's Task 3. Just create the skeleton.

    Do NOT include any Claude / AI references in the file content (per CLAUDE.md memory reminders).
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; [ -f .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md ] &amp;&amp; grep -q '## UAT Step 1 — Popout Window Walkthrough' .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md &amp;&amp; grep -q '## UAT Step 6 — Lint Clean' .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md &amp;&amp; grep -q 'status: pending' .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md &amp;&amp; grep -q 'BUG-07' .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md &amp;&amp; grep -q 'DIR-07' .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md &amp;&amp; grep -q 'desktop-only' .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md &amp;&amp; ! grep -iE 'claude|AI assist' .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md &amp;&amp; [ "$(ls .planning/phases/07-lifecycle-compatibility/*.png 2>/dev/null | wc -l)" -eq 0 ] &amp;&amp; [ "$(ls .planning/phases/07-lifecycle-compatibility/*.jpg 2>/dev/null | wc -l)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md` exists
    - The file contains exactly 6 mandatory UAT step headings (`## UAT Step 1` through `## UAT Step 6`)
    - The frontmatter contains `phase: 07-lifecycle-compatibility` and `status: pending`
    - The 5 requirement IDs (DIR-05, DIR-06, DIR-07, DIR-08, BUG-07) all appear in the body
    - The mobile UAT footer per D-13 is present (the string `desktop-only verification; mobile audit deferred to v1.16` or similar)
    - `grep -iE 'claude|AI assist' .planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md` returns zero matches
    - No PNG / JPG / image binary files exist under `.planning/phases/07-lifecycle-compatibility/` (per D-12, mirroring Phase 6 D-15). Verified by `ls .planning/phases/07-lifecycle-compatibility/*.{png,jpg,jpeg,gif,bmp,webp} 2>/dev/null | wc -l` returning 0.
  </acceptance_criteria>
  <done>
    `07-HUMAN-UAT.md` exists as a structured skeleton with all 6 mandatory UAT steps. Status is `pending`. No image artifacts are committed. No Claude / AI references.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute the 6-step UAT walkthrough and fill in Pass/Fail evidence</name>
  <files>.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md</files>
  <what-built>
    Task 2 created the `07-HUMAN-UAT.md` skeleton. Task 1 confirmed the freshly built plugin loads in the reviewer's test vault. The reviewer now executes the 6 mandatory UAT steps, filling in:
    - **Result:** [x] Pass for steps that match expected behavior; [x] Fail if any deviation observed
    - **Notes:** specific observation text per step (or "no deviation observed" for Pass)
    - The Obsidian version and OS in the Pre-Conditions section
    - The **Overall Acceptance** verdicts for SC #5 and SC #6 at the bottom
    - The reviewer handle and date at the bottom
    - Change frontmatter `status: pending` → `status: complete` if Overall Acceptance is PASS on both SC #5 and SC #6
    - If UAT step 3 (BUG-07) FAILS, leave a note in Step 3 Notes that plan 07-07 will land `BUG-07-CLOSURE.md`

    Critical decision in step 3:
    - PASS (modal stays open) → BUG-07 closed in this commit's UAT record. Plan 07-07 is SKIPPED. Optionally amend plan 07-01's commit body to mention BUG-07 closure, OR leave the closure recorded here in step 3 Notes.
    - FAIL (modal still closes) → BUG-07 closure goes to plan 07-07's `BUG-07-CLOSURE.md`. Step 3 Notes record reproduction details for plan 07-07 to reference.

    All other failures (steps 1, 2, 4, 5, 6) should be rare given that plans 07-01 through 07-05 closed every lint/grep gate. If any of these fail, the reviewer documents the deviation and STOPS to notify the orchestrator that gap closure may be needed.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md` in the editor.
    2. Fill in the Pre-Conditions: Obsidian version (`Obsidian → Help → About → App version`), OS.
    3. Execute UAT Step 1 (Popout Window) — right-click pane → Move to new window, then walk through items (a)-(d) per D-12. Fill in Result + Notes.
    4. Execute UAT Step 2 (Daily-Note Open Paths) — 3 surfaces. Fill in Result + Notes.
    5. Execute UAT Step 3 (Settings-Modal Persistence). CRITICAL — observe whether the modal stays open or closes on plugin toggle. Fill in Result + Notes accordingly.
    6. Execute UAT Step 4 (Sidebar Parity). Fill in Result + Notes.
    7. Execute UAT Step 5 (Embedded-View Parity). Fill in Result + Notes.
    8. Execute UAT Step 6 (Lint Clean). Run `npm run lint`, `grep -nE 'Phase 7\b' eslint.config.mjs`, `npx eslint --fix src/` + `git status -s`. Fill in Result + Notes.
    9. Fill in the Overall Acceptance verdicts for SC #5 and SC #6.
    10. If PASS on all (or BUG-07 step 3 Fail with explicit plan-07-07 handoff): change frontmatter `status: pending` → `status: complete`. Fill in date and reviewer handle.
    11. If any non-BUG-07 step FAILS: leave `status: pending`, document the deviation, STOP and notify the orchestrator. Phase 7 closure is blocked.

    Resume signal: Type one of:
      - "UAT complete pass" — all 6 steps PASS, status set to complete
      - "UAT complete BUG-07 fail" — steps 1, 2, 4, 5, 6 PASS but step 3 FAIL; plan 07-07 must land `BUG-07-CLOSURE.md`
      - "UAT complete fail: <brief>" — any non-BUG-07 step failed; phase closure blocked; orchestrator decides next steps
  </how-to-verify>
  <resume-signal>Type "UAT complete pass" or "UAT complete BUG-07 fail" or "UAT complete fail: <brief reason>".</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit the HUMAN-UAT.md with the exact D-11 commit 7 subject line</name>
  <files>(git commit only — no source changes in this task)</files>
  <read_first>
    - .planning/phases/07-lifecycle-compatibility/07-CONTEXT.md (D-11 step 7 — exact commit subject)
    - CLAUDE.md (Memory Reminders: NO Claude / AI references in commits)
  </read_first>
  <action>
    PRECONDITION: Task 3 returned either "UAT complete pass" or "UAT complete BUG-07 fail". If Task 3 returned "UAT complete fail: ..." (non-BUG-07 failure), DO NOT execute this task — the orchestrator handles the gap-closure path.

    Stage ONLY `.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md`. Create a single commit with the exact subject line from D-11 step 7:

      docs(07): record Phase 7 human UAT

    Commit body (3-5 sentences, NO Claude / AI references per CLAUDE.md):
      Paragraph 1: References the 6 UAT steps executed. Names the Overall Acceptance verdicts for SC #5 and SC #6.
      Paragraph 2: If "UAT complete pass" — confirms Phase 7 closure on all 6 success criteria. If "UAT complete BUG-07 fail" — notes that step 3 failed with the modal still closing on plugin toggle (confirming Obsidian-side root cause per RESEARCH §"Pitfall 3" + forum thread evidence), and that plan 07-07 will land `BUG-07-CLOSURE.md`.
      Paragraph 3: Confirms `npm run lint` exits 0 at HEAD (UAT step 6) with the override block deleted.
      Paragraph 4 (optional): Names the post-Phase-7 next step — Phase 8 (Type Hygiene & Conventions / DOC-02).

    Use HEREDOC for the commit message body. Do NOT use `--no-verify`. Do NOT include `Co-Authored-By:` or any trailer referencing Claude / AI / assistant.

    After commit, the Phase 7 commit graph at HEAD should show the canonical 6-commit sequence per D-11 (with BUG-07-CLOSURE.md being commit 5 conditional from plan 07-07 if applicable):
      HEAD            docs(07): record Phase 7 human UAT
      HEAD~1          chore(lint): remove Phase 7 ESLint overrides (DIR-05/06/07/08 acceptance)
      HEAD~2          refactor(views): fix floating promises and MarkdownRenderChild lifecycle return types (DIR-08)
      HEAD~3          refactor(views): narrow TAbstractFile via instanceof TFile (DIR-07)
      HEAD~4          refactor(views): adopt activeDocument and add window prefix for timers (DIR-06)
      HEAD~5          refactor(main): fix view-in-registerView memory leak (DIR-05)

    If plan 07-07 (BUG-07-CLOSURE.md) runs after this plan, the Phase 7 graph extends by 1 commit at HEAD with the closure note (`docs(07): close BUG-07 with Obsidian-side root cause note`). That commit lands as plan 07-07's output, NOT here.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; git log -1 --pretty=%s | grep -qxF 'docs(07): record Phase 7 human UAT' &amp;&amp; ! git log -1 --pretty=%B | grep -iE 'claude|AI assist|Co-Authored-By' &amp;&amp; git diff HEAD~1 --name-only | grep -qx '.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md' &amp;&amp; [ "$(git diff HEAD~1 --name-only | wc -l)" -eq 1 ] &amp;&amp; git log --oneline -6</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` exactly matches `docs(07): record Phase 7 human UAT`
    - `git diff HEAD~1 --name-only` is exactly `.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md` (one file changed)
    - `git log -1 --pretty=%B` contains NO occurrence of "claude", "Claude", "AI", "assistant", "Co-Authored-By"
    - `git log --oneline -6` shows the 6 canonical Phase 7 commits in D-11 ordering (or 5 if 07-06 is HEAD without a yet-pending 07-07 BUG-07-CLOSURE.md)
    - The file `07-HUMAN-UAT.md` at HEAD has `status: complete` in frontmatter (if all 6 UAT steps passed OR step 3 fail is being routed to plan 07-07) AND Overall Acceptance is recorded
  </acceptance_criteria>
  <done>
    The Phase 7 UAT closure commit exists at HEAD with the exact D-11 step 7 subject line, contains only `07-HUMAN-UAT.md`, has no Claude / AI / Co-Authored-By references, and the Phase 7 commit sequence matches D-11 ordering. If BUG-07 fell out empirically (UAT step 3 PASS), plan 07-07 is SKIPPED. If BUG-07 still reproduces (step 3 FAIL), plan 07-07 is the next plan to run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer's eyes → markdown evidence | The visual-parity + popout-correctness + modal-persistence verdicts are human-judged. Mitigation: each UAT step is scoped to a specific feature with explicit Pass/Fail criteria, so the reviewer is checking against a definite list rather than a generic "everything still works." |

## STRIDE Threat Register

Severity: **none**. Phase 7 has no new attack surface. This commit records human verification of a refactor.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Repudiation | UAT evidence | mitigate | Each UAT step is documented with explicit Pass/Fail checkboxes, Notes field, per-step verification claim. Reviewer handle + date + Obsidian version + OS are required fields. The Overall Acceptance verdict at the bottom is the formal record. |
| T-07-11 | Tampering | UAT walkthrough completeness | mitigate | The 6 mandatory steps map 1:1 to CONTEXT D-12 + ROADMAP Phase 7 success criteria #5 + #6. Each step's "Verifies" line names the specific CONTEXT D-NN decision(s) being verified. |
</threat_model>

<verification>
- `.planning/phases/07-lifecycle-compatibility/07-HUMAN-UAT.md` exists at HEAD with `status: complete` (or `pending` if step 3 failed and plan 07-07 is next; in either case the UAT walkthrough is recorded)
- All 6 mandatory UAT step results are filled in (no remaining `[ ] Pass / [ ] Fail` placeholders)
- HEAD commit subject matches D-11 commit 7 exactly: `docs(07): record Phase 7 human UAT`
- The Phase 7 commit sequence matches D-11 ordering
- ROADMAP Phase 7 success criteria #5 + #6 are recorded as PASS (or FAIL-with-handoff to plan 07-07) in the HUMAN-UAT.md
- No PNG / JPG image artifacts under `.planning/phases/07-lifecycle-compatibility/`
- No Claude / AI references in the file or commit message
</verification>

<success_criteria>
- ROADMAP Phase 7 success criteria #5 + #6 are satisfied and documented (or, for SC #5, the FAIL routes to plan 07-07's BUG-07-CLOSURE.md)
- All 6 ROADMAP Phase 7 success criteria are closed (grep #1, #2, #3 + lint #4 + UAT #5 + #6)
- HEAD commit matches D-11 step 7 exactly; no Claude / AI references
- Phase 7 is fully complete (if plan 07-07 is not needed) OR awaiting plan 07-07 (BUG-07-CLOSURE.md conditional)
</success_criteria>

<output>
After completion, create `.planning/phases/07-lifecycle-compatibility/07-06-SUMMARY.md` with:
- Link to `07-HUMAN-UAT.md` and the Overall Acceptance verdicts
- The 6 step verdicts (each as one line: "Step N: PASS — <one-line summary>")
- The git commit SHA and subject
- Confirmation that the Phase 7 commit sequence exists in `git log --oneline -6` matching D-11 ordering
- Confirmation that ROADMAP Phase 7 success criteria #5 + #6 are recorded (PASS or FAIL-routed-to-07-07)
- Confirmation that no Claude / AI references appear in the commit message
- Hand-off to STATE.md update + (conditional) plan 07-07 + Phase 8 planning
</output>
