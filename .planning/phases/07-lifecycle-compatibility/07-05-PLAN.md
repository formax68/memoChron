---
phase: 07-lifecycle-compatibility
plan: 05
type: execute
wave: 5
depends_on: [07-01, 07-02, 07-03, 07-04]
files_modified:
  - eslint.config.mjs
autonomous: true
requirements: [DIR-05, DIR-06, DIR-07, DIR-08]
tags: [obsidian-plugin, eslint, override-removal, phase-acceptance, lint-gate]

must_haves:
  truths:
    - "The Phase 7 ESLint override block at `eslint.config.mjs:65-91` is DELETED entirely (per D-11 commit 6 + PATTERNS Pattern 6) — `grep -nE 'Phase 7\\b' eslint.config.mjs` returns zero matches"
    - "All 8 rule disables previously inside the Phase 7 block are GONE from `eslint.config.mjs` — `grep -E '\"obsidianmd/no-view-references-in-plugin\"\\s*:\\s*\"off\"' eslint.config.mjs` returns zero matches; same for `obsidianmd/no-tfile-tfolder-cast`, `obsidianmd/prefer-active-doc`, `obsidianmd/prefer-window-timers`, `obsidianmd/detach-leaves`, `@typescript-eslint/no-floating-promises`, `@typescript-eslint/no-misused-promises`, `@typescript-eslint/no-unnecessary-type-assertion`"
    - "The adjacent Phase 5 override block (lines 43-63 — Phase 5 DOC-01 hardening) is UNTOUCHED — `grep -E 'no-restricted-syntax|no-unused-vars.*error' eslint.config.mjs` still returns hits"
    - "The adjacent Phase 8 override blocks (lines 93+ — Phase 8 type-hygiene and unused-vars override) are UNTOUCHED — `grep -nE 'Phase 8\\b' eslint.config.mjs` returns at least 2 matches; `grep -E '\"no-console\"\\s*:\\s*\"off\"' eslint.config.mjs` returns at least 1 match"
    - "`npm run lint` exits 0 at HEAD with the Phase 7 override block deleted — no rule is re-disabled, no inline `eslint-disable` shim is added, no per-file or per-rule suppression for the 8 Phase 7 rules remains"
    - "ROADMAP Phase 7 success criterion #4 is met (`npm run lint` reports zero `@typescript-eslint/no-floating-promises` violations and zero `MarkdownRenderChild`-lifecycle `no-misused-promises` findings — the Phase 5 ESLint overrides for these rules are removed); plus the same applies to `obsidianmd/no-view-references-in-plugin`, `obsidianmd/detach-leaves`, `obsidianmd/prefer-active-doc`, `obsidianmd/prefer-window-timers`, `obsidianmd/no-tfile-tfolder-cast`, `@typescript-eslint/no-unnecessary-type-assertion` (all 8 rules now actively enforced)"
    - "`npx eslint --fix src/` is a no-op at HEAD — `git status -s` returns clean (no auto-fix diff produced) per RESEARCH §'Verification probe' — confirming that the source already satisfies the rules and the auto-fix wouldn't rewrite anything"
    - "`globalIgnores` block at the bottom of `eslint.config.mjs` is UNTOUCHED — `grep -E 'globalIgnores\\(' eslint.config.mjs` still returns 1 match"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Phase 7 override block removed (lines 65-91 deleted); file shrinks by approximately 27 lines"
      pattern: "Phase 5|Phase 8"
  key_links:
    - from: "lint output (zero Phase 7 rule violations)"
      to: "Phase 7 acceptance"
      via: "npm run lint exits 0 with no Phase 7 rule suppressions"
      pattern: "Phase 5|Phase 8"
    - from: "CI lint gate (DOC-01 from Phase 5)"
      to: "actively blocks regressions on the 8 Phase 7 rules across all source files"
      via: "obsidianmd recommended config now applies these rules uncondionally to all src/**/*.ts"
      pattern: "obsidianmd\\.configs\\.recommended"
---

<objective>
Fifth commit of Phase 7 (D-11 commit 6). Delete the Phase 7 ESLint override block at `eslint.config.mjs:65-91` per D-11 commit 6 + PATTERNS Pattern 6. After this commit, the 8 rules previously suppressed (the 5 `obsidianmd/*` rules + 3 `@typescript-eslint/*` rules) are enforced across the entire source tree, and `npm run lint` exits 0 because plans 07-01 through 07-04 closed every violation site BEFORE the rules went active. This is the formal acceptance commit for DIR-05 / DIR-06 / DIR-07 / DIR-08 (the rules now actively block any regression on future PRs).

Purpose: ROADMAP Phase 7 success criterion #4 requires that "the Phase 5 ESLint overrides for these rules are removed". Per RESEARCH §"Verification probe (additive to D-12 / success criteria)": after Phase 7 closes, `npx eslint --fix` must be a no-op on the Phase 7 files. The Phase 7 override block at `eslint.config.mjs:65-91` deletes cleanly per D-11 commit 6. This plan IS that commit.

Output: `eslint.config.mjs` shrunk by approximately 27 lines (the 4-line comment header at 65-68 + the 23-line config object at 69-91); one git commit with subject `chore(lint): remove Phase 7 ESLint overrides (DIR-05/06/07/08 acceptance)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-lifecycle-compatibility/07-CONTEXT.md
@.planning/phases/07-lifecycle-compatibility/07-RESEARCH.md
@.planning/phases/07-lifecycle-compatibility/07-PATTERNS.md
@.planning/phases/07-lifecycle-compatibility/07-01-SUMMARY.md
@.planning/phases/07-lifecycle-compatibility/07-02-SUMMARY.md
@.planning/phases/07-lifecycle-compatibility/07-03-SUMMARY.md
@.planning/phases/07-lifecycle-compatibility/07-04-SUMMARY.md
@.planning/phases/06-dom-api-refactor/06-04-PLAN.md
@CLAUDE.md
@eslint.config.mjs

<interfaces>
<!-- Exact bounds of the Phase 7 override block to delete (verified by pattern-mapper at planning time; lines 65-91 inclusive of the 4-line comment header and the 23-line config object). Re-grep at execution time to confirm bounds have not drifted by 0-2 lines due to upstream changes to the Phase 5 block. -->

Block to delete — `eslint.config.mjs:65-91`:

```javascript
  // ---------------------------------------------------------------------------
  // Phase 7 — DIR-05 / DIR-06 / DIR-07 / DIR-08 will remove these when the
  // lifecycle / compatibility cleanup lands.
  // ---------------------------------------------------------------------------
  {
    files: [
      "src/main.ts",
      "src/views/CalendarView.ts",
      "src/views/EmbeddedCalendarView.ts",
      "src/views/EmbeddedAgendaView.ts",
      "src/settings/SettingsTab.ts",
      "src/services/CalendarService.ts",
      "src/services/NoteService.ts",
      "src/utils/colorValidation.ts",
      "src/utils/viewRenderers.ts",
    ],
    rules: {
      "obsidianmd/no-view-references-in-plugin": "off",
      "obsidianmd/no-tfile-tfolder-cast": "off",
      "obsidianmd/prefer-active-doc": "off",
      "obsidianmd/prefer-window-timers": "off",
      "obsidianmd/detach-leaves": "off",
      "@typescript-eslint/no-floating-promises": "off",
      "@typescript-eslint/no-misused-promises": "off",
      "@typescript-eslint/no-unnecessary-type-assertion": "off",
    },
  },
```

DELETE the entire block (the 4-line comment header at 65-68 AND the 23-line config object at 69-91, including the trailing `,` on line 91 that separates this object from the next argument of `tseslint.config(...)`).

After deletion, the file should flow directly from the closing `},` of the Phase 5 rules block (currently around line 63 with the `no-restricted-syntax` rule definition) to the comment header of the Phase 8 block (currently around line 93 with `// Phase 8 — DIR-01 / DIR-09 / DIR-10 will remove these when type-hygiene`).

Adjacent blocks that MUST STAY UNTOUCHED:
  - **Above (lines 43-63):** The Phase 5 / DOC-01 hardening block (`no-unused-vars` bumped to `error`, `no-restricted-syntax` for `document.createElement`). This is a sibling top-level config object — NOT to be touched.
  - **Below (lines 93-116 and 117-133):** The two Phase 8 override blocks. Both stay. The first contains: `obsidianmd/rule-custom-message`, `no-console`, `@typescript-eslint/no-explicit-any`, the `no-unsafe-*` series, `no-case-declarations`, `no-useless-escape`. The second is the narrow `no-unused-vars` override targeting 7 specific files.
  - **Bottom (lines 135-141):** The `globalIgnores([...])` block — UNTOUCHED.

Phase 8 override blocks must remain enforced — their owning phase has not landed yet. DOC-02 (Phase 8 final commit) will remove them.

Mirrors Phase 6 D-13 / D-16 precedent (Phase 6 Plan 04 `06-04-PLAN.md`): single-block delete + lint-must-pass at HEAD.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the Phase 7 ESLint override block at eslint.config.mjs:65-91</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (full file — confirm current line numbers of the Phase 5 / Phase 7 / Phase 8 comment headers; the planner captured 65-91 at 2026-05-15; if prior commits in this phase or upstream merges have shifted the Phase 7 block by ±1 line, re-grep before editing)
    - .planning/phases/07-lifecycle-compatibility/07-CONTEXT.md (D-11 step 6 — exact deletion scope and rationale)
    - .planning/phases/07-lifecycle-compatibility/07-PATTERNS.md (Pattern 6 — structural analog with Phase 5/8 blocks and the exact deletion bounds)
    - .planning/phases/06-dom-api-refactor/06-04-PLAN.md (Phase 6 D-13 / D-16 precedent — same shape, mirror the verification approach)
  </read_first>
  <action>
    Before editing, RECONFIRM the current line numbers by running:
      `grep -nE 'Phase [5678]\b' eslint.config.mjs`
    This returns the 3 (or 4) phase-comment-header lines. The Phase 7 block starts at the line beginning with `// Phase 7 — DIR-05 / DIR-06 / DIR-07 / DIR-08` and ends at the closing `},` immediately before the next phase-comment-header (Phase 8). Use those bounds.

    DELETE the entire Phase 7 override block, exactly matching the `<interfaces>` excerpt above:
      1. The 4-line comment header (the three `// ` lines including the `---` dashes — `Phase 7 — DIR-05 / DIR-06 / DIR-07 / DIR-08 will remove these when the lifecycle / compatibility cleanup lands.`)
      2. The opening `{` line
      3. The `files: [...]` array (9 file paths)
      4. The `rules: { ... }` object (8 rule disables)
      5. The closing `}` line and its trailing `,`

    Approximately 27 lines deleted in total. The exact count depends on the file at execution time; the verification command below confirms net delta.

    After deletion, the file should flow:
      Line N (last Phase 5 line): `},`
      Line N+1 (blank line — preserve it)
      Line N+2: `  // ---------------------------------------------------------------------------`
      Line N+3: `  // Phase 8 — DIR-01 / DIR-09 / DIR-10 will remove these when type-hygiene`

    Confirm after deletion (use grep, not line-count):
      - `grep -nE 'Phase 7\b' eslint.config.mjs` returns ZERO matches
      - `grep -E '"obsidianmd/no-view-references-in-plugin": "off"' eslint.config.mjs` returns ZERO matches
      - `grep -E '"obsidianmd/no-tfile-tfolder-cast": "off"' eslint.config.mjs` returns ZERO matches
      - `grep -E '"obsidianmd/prefer-active-doc": "off"' eslint.config.mjs` returns ZERO matches
      - `grep -E '"obsidianmd/prefer-window-timers": "off"' eslint.config.mjs` returns ZERO matches
      - `grep -E '"obsidianmd/detach-leaves": "off"' eslint.config.mjs` returns ZERO matches
      - `grep -E '"@typescript-eslint/no-floating-promises": "off"' eslint.config.mjs` returns ZERO matches
      - `grep -E '"@typescript-eslint/no-misused-promises": "off"' eslint.config.mjs` returns ZERO matches
      - `grep -E '"@typescript-eslint/no-unnecessary-type-assertion": "off"' eslint.config.mjs` returns ZERO matches

    AND confirm the adjacent blocks are PRESERVED:
      - `grep -nE 'Phase 5\b' eslint.config.mjs` may or may not match (the Phase 5 block has no `Phase 5` comment header by design — Phase 5 is the active baseline; it has a structural comment block but not named that way). Check `grep -n '"no-restricted-syntax"' eslint.config.mjs` → still returns at least 1 match (Phase 5 DOC-01 rule preserved).
      - `grep -nE 'Phase 8\b' eslint.config.mjs` returns at least 1 match (Phase 8 comment header preserved)
      - `grep -E '"@typescript-eslint/no-explicit-any": "off"' eslint.config.mjs` returns at least 1 match (Phase 8 rule preserved)
      - `grep -E '"no-console": "off"' eslint.config.mjs` returns at least 1 match (Phase 8 rule preserved)
      - `grep -E 'globalIgnores\(' eslint.config.mjs` returns 1 match (bottom block preserved)

    Run `npm run lint` AFTER the deletion. It MUST exit 0 — plans 07-01 through 07-04 closed every violation site BEFORE this rule activation. If lint fails with a `obsidianmd/no-view-references-in-plugin`, `obsidianmd/detach-leaves`, `obsidianmd/prefer-active-doc`, `obsidianmd/prefer-window-timers`, `obsidianmd/no-tfile-tfolder-cast`, `@typescript-eslint/no-floating-promises`, `@typescript-eslint/no-misused-promises`, or `@typescript-eslint/no-unnecessary-type-assertion` violation, the executor:
      1. Does NOT re-add the override (the override removal is the entire point of this commit).
      2. Identifies the missing source-side fix in plans 07-01/02/03/04 and applies it inline as a follow-up edit BEFORE committing.
      3. Stages both `eslint.config.mjs` AND any source-side fix as PART OF THIS commit.

    Run `npx eslint --fix src/` AFTER the lint passes. Per RESEARCH §"Verification probe": this command must be a NO-OP — produce no source-side diff. Verify with `git status -s` returning empty (or showing only `eslint.config.mjs` if it was already staged). If `--fix` rewrites any source file, it means a rule's auto-fix shape disagrees with the human-written code; the executor inspects the diff, decides if the auto-fix is acceptable (apply and stage) or if the source-side code is correct (re-investigate the rule).

    Do NOT add ANY new override block, inline `eslint-disable`, or per-file rule disable for the 8 Phase 7 rules. The whole point of the commit is to enforce them. If a genuine edge case requires a localized disable (it should not — RESEARCH found no such cases), the executor flags the issue and asks the orchestrator before proceeding.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; ! grep -nE 'Phase 7\b' eslint.config.mjs &amp;&amp; ! grep -E '"obsidianmd/(no-view-references-in-plugin|no-tfile-tfolder-cast|prefer-active-doc|prefer-window-timers|detach-leaves)": "off"' eslint.config.mjs &amp;&amp; ! grep -E '"@typescript-eslint/(no-floating-promises|no-misused-promises|no-unnecessary-type-assertion)": "off"' eslint.config.mjs &amp;&amp; grep -E '"@typescript-eslint/no-explicit-any": "off"' eslint.config.mjs &amp;&amp; grep -E '"no-console": "off"' eslint.config.mjs &amp;&amp; grep -E 'globalIgnores\(' eslint.config.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Phase 7\b' eslint.config.mjs` returns ZERO matches
    - All 8 rule disables removed:
      - `grep -E '"obsidianmd/no-view-references-in-plugin": "off"' eslint.config.mjs` returns ZERO
      - `grep -E '"obsidianmd/no-tfile-tfolder-cast": "off"' eslint.config.mjs` returns ZERO
      - `grep -E '"obsidianmd/prefer-active-doc": "off"' eslint.config.mjs` returns ZERO
      - `grep -E '"obsidianmd/prefer-window-timers": "off"' eslint.config.mjs` returns ZERO
      - `grep -E '"obsidianmd/detach-leaves": "off"' eslint.config.mjs` returns ZERO
      - `grep -E '"@typescript-eslint/no-floating-promises": "off"' eslint.config.mjs` returns ZERO
      - `grep -E '"@typescript-eslint/no-misused-promises": "off"' eslint.config.mjs` returns ZERO
      - `grep -E '"@typescript-eslint/no-unnecessary-type-assertion": "off"' eslint.config.mjs` returns ZERO
    - Phase 5 / Phase 8 / globalIgnores PRESERVED:
      - `grep -n '"no-restricted-syntax"' eslint.config.mjs` returns at least 1 match (Phase 5 DOC-01 preserved)
      - `grep -nE 'Phase 8\b' eslint.config.mjs` returns at least 1 match (Phase 8 header preserved)
      - `grep -E '"@typescript-eslint/no-explicit-any": "off"' eslint.config.mjs` returns at least 1 match (Phase 8 rule)
      - `grep -E '"no-console": "off"' eslint.config.mjs` returns at least 1 match (Phase 8 rule)
      - `grep -E 'globalIgnores\(' eslint.config.mjs` returns 1 match
    - `npm run lint` exits 0 at end of task
    - `npx eslint --fix src/` is a NO-OP — `git status -s` after `--fix` shows zero source-side diff (only `eslint.config.mjs` if staged)
    - `wc -l eslint.config.mjs` shows a line-count delta of approximately -27 lines vs HEAD~1 (the deleted block size)
  </acceptance_criteria>
  <done>
    The Phase 7 override block is deleted from `eslint.config.mjs`. Phase 5 / Phase 8 / `globalIgnores` blocks are unchanged. `npm run lint` exits 0 and `npx eslint --fix src/` is a no-op. The 8 Phase 7 rules are now actively enforced across `src/`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit override-block deletion with the exact D-11 commit 6 subject line</name>
  <files>(git commit only — no source changes in this task, unless Task 1 surfaced a source-side fix that was applied inline)</files>
  <read_first>
    - .planning/phases/07-lifecycle-compatibility/07-CONTEXT.md (D-11 step 6 — exact commit subject; D-13 — running `npm run lint` is the per-commit gate)
    - CLAUDE.md (Memory Reminders: NO Claude / AI references in commits)
  </read_first>
  <action>
    Stage `eslint.config.mjs` (and any source-side fix that was applied inline in Task 1 if the override deletion surfaced unresolved violations). Create a single commit with the exact subject line from D-11 step 6:

      chore(lint): remove Phase 7 ESLint overrides (DIR-05/06/07/08 acceptance)

    Commit body (3-5 sentences, NO Claude / AI references per CLAUDE.md):
      Paragraph 1: Names the override-block deletion at `eslint.config.mjs:65-91`. Lists the 8 rules previously suppressed (5 `obsidianmd/*` + 3 `@typescript-eslint/*`). Confirms the block was tracked by name per Phase 5 DOC-01 convention.
      Paragraph 2: Confirms Phase 5 / Phase 8 / `globalIgnores` blocks are untouched.
      Paragraph 3: Reports lint outcome: `npm run lint` exits 0 at HEAD. The 8 Phase 7 rules are now actively enforced; future regressions on DIR-05/DIR-06/DIR-07/DIR-08 will fail the build via the DOC-01 CI lint gate.
      Paragraph 4 (optional): If Task 1 required a source-side follow-up fix (rare; only if a violation was missed in plans 07-01/02/03/04), name the file and the rule that flagged it.

    Use HEREDOC for the commit message body. Do NOT use `--no-verify`. Do NOT include `Co-Authored-By:` or any trailer referencing Claude / AI / assistant.

    BEFORE committing, run `npm run lint` one more time and confirm exit 0. The lint hook MUST pass at this commit — that's the entire point of the commit.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; git log -1 --pretty=%s | grep -qxF 'chore(lint): remove Phase 7 ESLint overrides (DIR-05/06/07/08 acceptance)' &amp;&amp; ! git log -1 --pretty=%B | grep -iE 'claude|AI assist|Co-Authored-By' &amp;&amp; git diff HEAD~1 --name-only | grep -q '^eslint\.config\.mjs$' &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` exactly matches `chore(lint): remove Phase 7 ESLint overrides (DIR-05/06/07/08 acceptance)`
    - `git diff HEAD~1 --name-only` includes `eslint.config.mjs` (and optionally one or more source files if Task 1 surfaced a follow-up fix)
    - `git log -1 --pretty=%B` contains NO occurrence of "claude", "Claude", "AI", "assistant", "Co-Authored-By"
    - `npm run lint` exits 0 at HEAD (the post-commit state)
    - The full DIR-05/06/07/08 grep set still passes at HEAD:
      - `git ls-files src/ | xargs grep -nE 'this\.calendarView\b'` returns zero matches
      - `git ls-files src/ | xargs grep -n 'detachLeavesOfType'` returns zero matches
      - `git ls-files src/ | xargs grep -nE 'getComputedStyle\(document\.documentElement\)'` returns zero matches
      - `git ls-files src/ | xargs grep -nE '\b(setTimeout|setInterval|clearTimeout|clearInterval|requestAnimationFrame)\(' | grep -v 'window\.' | grep -vE '^[^:]*:[0-9]+:\s*(//|\*|/\*)'` returns zero non-comment matches
      - `git ls-files src/ | xargs grep -n 'as TFile'` returns zero matches
      - `grep -nE 'async onload\(\)' src/views/Embedded*.ts` returns zero matches
  </acceptance_criteria>
  <done>
    The override-removal commit exists at HEAD with the exact D-11 step 6 subject line, contains `eslint.config.mjs` (plus any source-side follow-up), has no Claude / AI / Co-Authored-By references, and `npm run lint` exits 0 at HEAD. ROADMAP Phase 7 success criterion #4 is now formally satisfied. CI will block future DIR-05/06/07/08 regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 7 ships no new attack surface. This commit ENABLES regression prevention:
- The 8 rules previously suppressed are now actively enforced across all of `src/` via the Phase 5 DOC-01 CI lint gate
- Any future PR that reintroduces `this.calendarView = view`, `detachLeavesOfType` in `onunload`, `getComputedStyle(document.documentElement)`, bare timer calls, `as TFile`, floating promises, or `async onload()` on `MarkdownRenderChild` subclasses will FAIL the build

| Boundary | Description |
|----------|-------------|
| ESLint config → CI lint gate (DOC-01) | The override-block deletion EXPOSES the 8 rules to the now-cleaned codebase. If plans 07-01 through 07-04 missed a violation, this plan's lint run catches it before commit. |

## STRIDE Threat Register

Severity: **none**. RESEARCH §"Security Domain" verified no security implication. This change is a regression-prevention improvement.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | The removed override block | mitigate | Plans 07-01 through 07-04 cleaned every violation site BEFORE the rules went active. Task 1 re-runs `npm run lint` to confirm zero leftovers BEFORE committing. The commit verification embeds `npm run lint` as the gate. Future regressions on the 8 rules are now blocked by the DOC-01 CI workflow. |
| T-07-09 | Repudiation | Commit message hygiene | mitigate | Acceptance criteria explicitly check for the absence of "claude", "Claude", "AI", "assistant", "Co-Authored-By" trailers per CLAUDE.md memory reminders. |
</threat_model>

<verification>
- `grep -nE 'Phase 7\b' eslint.config.mjs` returns zero matches at HEAD
- All 8 Phase 7 rule disables removed (grep returns zero for each)
- Phase 5 / Phase 8 / `globalIgnores` blocks preserved (grep returns hits for each)
- `npm run lint` exits 0 at HEAD
- `npx eslint --fix src/` is a no-op at HEAD (per RESEARCH verification probe)
- HEAD commit subject matches D-11 step 6 exactly
- HEAD commit message has no Claude / AI / Co-Authored-By references
- The full DIR-05/06/07/08 grep set at HEAD returns zero violations across `src/`
</verification>

<success_criteria>
- ROADMAP Phase 7 success criterion #4 (Phase 5 overrides for DIR-05/06/07/08 removed; `npm run lint` still passes) is satisfied
- The 8 rules are now actively enforced across the entire source tree
- DOC-01's CI lint gate (Phase 5) now actively blocks DIR-05/06/07/08 regressions on future PRs
- Phase 8 override blocks are preserved (Phase 8 owns their removal)
- HEAD commit matches D-11 step 6 subject line exactly; no Claude / AI references
</success_criteria>

<output>
After completion, create `.planning/phases/07-lifecycle-compatibility/07-05-SUMMARY.md` with:
- The exact `eslint.config.mjs` line range deleted (with the post-deletion `grep -nE 'Phase [578]\b' eslint.config.mjs` output showing 5/8 still present, 7 gone)
- The 8 deleted rule disables enumerated
- The `npm run lint` output at HEAD (must show 0 errors, 0 warnings)
- The `npx eslint --fix src/` no-op confirmation (`git status -s` empty after `--fix`)
- The full DIR-05/06/07/08 grep set output (all zero)
- The git commit SHA and subject line
- Confirmation that no Claude / AI references appear in the commit message
- Hand-off to plan 07-06 (`07-HUMAN-UAT.md` walkthrough)
</output>
