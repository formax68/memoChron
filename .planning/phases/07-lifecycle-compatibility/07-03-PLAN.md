---
phase: 07-lifecycle-compatibility
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - src/views/CalendarView.ts
  - src/views/EmbeddedCalendarView.ts
  - src/views/EmbeddedAgendaView.ts
autonomous: true
requirements: [DIR-07]
tags: [obsidian-plugin, type-narrowing, instanceof, TFile, daily-notes]

must_haves:
  truths:
    - "`git ls-files src/ | xargs grep -n 'as TFile'` returns zero matches across the entire src/ tree (ROADMAP Phase 7 success criterion #3 verbatim)"
    - "All 4 daily-note open sites (CalendarView.ts:148, CalendarView.ts:828, EmbeddedCalendarView.ts:234, EmbeddedAgendaView.ts:383) use `instanceof TFile` runtime narrowing per D-08"
    - "Shape A (Map.set in CalendarView.ts:148) uses a positive `if (file instanceof TFile)` guard before `this.dailyNotes.set(dateStr, file)` — matches PATTERNS Pattern 2 Shape A and the analog at NoteService.ts:73"
    - "Shape B (3 sites: CalendarView.ts:828, EmbeddedCalendarView.ts:234, EmbeddedAgendaView.ts:383) replaces the `if (dailyNote)` truthy check with `if (dailyNote instanceof TFile)`, so the subsequent `await leaf.openFile(dailyNote)` receives a narrowed `TFile` — matches PATTERNS Pattern 2 Shape B"
    - "`npx eslint src/` reports zero `obsidianmd/no-tfile-tfolder-cast` violations after this commit (Phase 7 override block at `eslint.config.mjs:65-91` still active at this commit; per-rule sampling per VALIDATION.md confirms count does not regress vs HEAD~1)"
    - "`npx eslint src/` reports zero `@typescript-eslint/no-unnecessary-type-assertion` violations — the 2 cascade findings noted in CONTEXT.md `<domain>` close automatically once the `as TFile` casts are gone"
    - "No helper (`assertIsFile`, `isTFile`, etc.) is introduced — the `if (... instanceof TFile)` guard at each site is the established pattern per D-08 + PATTERNS Pattern 2"
    - "`import { TFile } from \"obsidian\"` is already present in all 3 modified files (verified at planning time); no new imports added"
  artifacts:
    - path: "src/views/CalendarView.ts"
      provides: "Positive `instanceof TFile` guard around `this.dailyNotes.set(dateStr, file)` at the former 148 site; `instanceof TFile` guard replacing truthy check at the former 828 site"
      pattern: "file instanceof TFile|dailyNote instanceof TFile"
    - path: "src/views/EmbeddedCalendarView.ts"
      provides: "`instanceof TFile` guard replacing truthy check at the former 234 site"
      pattern: "dailyNote instanceof TFile"
    - path: "src/views/EmbeddedAgendaView.ts"
      provides: "`instanceof TFile` guard replacing truthy check at the former 383 site"
      pattern: "dailyNote instanceof TFile"
  key_links:
    - from: "daily-note open consumer in views"
      to: "TFile-typed leaf.openFile() call"
      via: "instanceof TFile narrowing (no `as TFile` cast)"
      pattern: "if\\s*\\(\\s*\\w+\\s+instanceof\\s+TFile\\s*\\)"
    - from: "getAllDailyNotes() return values"
      to: "this.dailyNotes Map<string, TFile>"
      via: "instanceof TFile positive guard inside forEach"
      pattern: "this\\.dailyNotes\\.set"
---

<objective>
Third commit of Phase 7. Close DIR-07 (`as TFile` cast removal) by replacing all 4 `as TFile` casts in the codebase with `instanceof TFile` runtime narrowing per D-08. The 4 sites split into 2 shapes: Shape A (Map.set at `CalendarView.ts:148` — positive guard wraps the `set` call) and Shape B (leaf.openFile at `CalendarView.ts:828`, `EmbeddedCalendarView.ts:234`, `EmbeddedAgendaView.ts:383` — the existing truthy check `if (dailyNote)` becomes a typed narrow `if (dailyNote instanceof TFile)`). No helper is added; each guard lives at the consumer per PATTERNS Pattern 2.

Purpose: ROADMAP Phase 7 success criterion #3 ("`git ls-files src/ | xargs grep -n 'as TFile'` returns zero matches; every consumer of `TAbstractFile` narrows via `instanceof TFile` first"). Per RESEARCH §"DIR-07" + Pattern 5, the in-tree analogs at `src/services/NoteService.ts:69-83` and `src/services/CalendarService.ts:515-524` already establish the `instanceof TFile` narrowing pattern; Phase 7 extends it to the 4 daily-note open sites. The 2 `@typescript-eslint/no-unnecessary-type-assertion` violations noted in CONTEXT.md `<domain>` close as a cascade.

Output: 3 source files modified (`CalendarView.ts`, `EmbeddedCalendarView.ts`, `EmbeddedAgendaView.ts`); one git commit with subject `refactor(views): narrow TAbstractFile via instanceof TFile (DIR-07)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-lifecycle-compatibility/07-CONTEXT.md
@.planning/phases/07-lifecycle-compatibility/07-RESEARCH.md
@.planning/phases/07-lifecycle-compatibility/07-PATTERNS.md
@.planning/phases/07-lifecycle-compatibility/07-02-SUMMARY.md
@CLAUDE.md
@src/views/CalendarView.ts
@src/views/EmbeddedCalendarView.ts
@src/views/EmbeddedAgendaView.ts
@src/services/NoteService.ts
@src/services/CalendarService.ts

<interfaces>
<!-- 4 target sites + their current code shapes. Captured 2026-05-15 at planning. -->

Site 1 — `src/views/CalendarView.ts:147-149` (Shape A — Map.set inside forEach):
```typescript
// Store them in our map with date as key
Object.entries(allDailyNotes).forEach(([dateStr, file]) => {
  this.dailyNotes.set(dateStr, file as TFile);
});
```
Replace with positive guard (matches PATTERNS Pattern 2 Shape A and NoteService.ts:73 analog):
```typescript
Object.entries(allDailyNotes).forEach(([dateStr, file]) => {
  if (file instanceof TFile) {
    this.dailyNotes.set(dateStr, file);
  }
});
```

Site 2 — `src/views/CalendarView.ts:825-829` (Shape B — leaf.openFile after truthy check):
```typescript
// Open the daily note
if (dailyNote) {
  const leaf = this.app.workspace.getLeaf("tab");
  await leaf.openFile(dailyNote as TFile);
}
```
Replace truthy check with typed narrow:
```typescript
if (dailyNote instanceof TFile) {
  const leaf = this.app.workspace.getLeaf("tab");
  await leaf.openFile(dailyNote);
}
```

Site 3 — `src/views/EmbeddedCalendarView.ts:231-235` (Shape B):
```typescript
// Open the daily note
if (dailyNote) {
  const leaf = this.plugin.app.workspace.getLeaf("tab");
  await leaf.openFile(dailyNote as TFile);
}
```
Same transformation: `if (dailyNote)` → `if (dailyNote instanceof TFile)`; drop `as TFile`.

Site 4 — `src/views/EmbeddedAgendaView.ts:381-384` (Shape B):
```typescript
if (dailyNote) {
  const leaf = this.plugin.app.workspace.getLeaf("tab");
  await leaf.openFile(dailyNote as TFile);
}
```
Same transformation.

In-tree analog (PATTERNS Pattern 2) — already-existing positive narrow in `NoteService.ts:69-83`:
```typescript
const existingFile = this.plugin.app.vault.getAbstractFileByPath(filePath);
if (existingFile instanceof TFile) {
  return { file: existingFile, cursor: null };
}
```

Imports verification — `TFile` is already imported in all 3 files at planning time:
  src/views/CalendarView.ts: `import { ..., TFile, ... } from "obsidian";` (already present)
  src/views/EmbeddedCalendarView.ts: `import { ..., TFile, ... } from "obsidian";` (already present)
  src/views/EmbeddedAgendaView.ts: `import { ..., TFile, ... } from "obsidian";` (already present)

If the executor finds `TFile` missing from any import at execution time, add it to the existing `obsidian` import statement (do NOT add a new import line).

Behavior preservation:
  - Shape A: if a non-`TFile` enters the forEach (e.g., daily-notes plugin returns an unexpected shape), the entry is SILENTLY DROPPED from the map. This matches the previous behavior under `as TFile` (which would have crashed later when consumed); the new behavior is strictly safer.
  - Shape B: if `dailyNote` is non-null but is not a `TFile` (e.g., a `TFolder`), the open path is SKIPPED. Previously the `as TFile` cast would have passed it through and `leaf.openFile` would likely have crashed. The new behavior is strictly safer — the daily-notes plugin returning a folder is a malformed state that should not propagate.

  The 3 surrounding `console.error` / `Notice` calls (`CalendarView.ts:830-834`, `EmbeddedCalendarView.ts:237-241`, `EmbeddedAgendaView.ts:386-390`) remain unchanged — they handle the `await leaf.openFile(...)` rejection path inside the existing `try/catch`. The new `instanceof TFile` guard does not raise, so the catch is unaffected.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace 4 `as TFile` casts with `instanceof TFile` guards across 3 view files</name>
  <files>
    src/views/CalendarView.ts,
    src/views/EmbeddedCalendarView.ts,
    src/views/EmbeddedAgendaView.ts
  </files>
  <read_first>
    - .planning/phases/07-lifecycle-compatibility/07-CONTEXT.md (D-08 — site list + Shape A / Shape B distinction; CONTEXT `<code_context>` Reusable Assets — codebase pattern reuse)
    - .planning/phases/07-lifecycle-compatibility/07-RESEARCH.md (Pattern 5 — verified shape; §"Common Pitfalls" Pitfall 2 — `instanceof TFile` returning false is exceptionally rare)
    - .planning/phases/07-lifecycle-compatibility/07-PATTERNS.md (Pattern 2 — 2 in-tree analogs at `NoteService.ts:69-83` and `CalendarService.ts:515-524`; both transformations sketched)
    - src/views/CalendarView.ts:140-160 and 820-840 (the 2 sites + surrounding context)
    - src/views/EmbeddedCalendarView.ts:225-245 (the 1 site + surrounding context)
    - src/views/EmbeddedAgendaView.ts:375-395 (the 1 site + surrounding context)
  </read_first>
  <action>
    Make 4 surgical edits across the 3 files. Each edit replaces an `as TFile` cast with an `instanceof TFile` runtime narrow.

    Edit 1 — `src/views/CalendarView.ts` Shape A around line 148:
      Current shape (3 lines):
        ```
        Object.entries(allDailyNotes).forEach(([dateStr, file]) => {
          this.dailyNotes.set(dateStr, file as TFile);
        });
        ```
      New shape (5 lines):
        ```
        Object.entries(allDailyNotes).forEach(([dateStr, file]) => {
          if (file instanceof TFile) {
            this.dailyNotes.set(dateStr, file);
          }
        });
        ```
      Net diff: +2 lines, removes the `as TFile` cast, adds a positive guard. Non-`TFile` entries are silently dropped from the map (strictly safer than the previous behavior).

    Edit 2 — `src/views/CalendarView.ts` Shape B around line 828:
      Current shape (4 lines):
        ```
        if (dailyNote) {
          const leaf = this.app.workspace.getLeaf("tab");
          await leaf.openFile(dailyNote as TFile);
        }
        ```
      New shape (4 lines):
        ```
        if (dailyNote instanceof TFile) {
          const leaf = this.app.workspace.getLeaf("tab");
          await leaf.openFile(dailyNote);
        }
        ```
      Net diff: same line count, replaces truthy check with typed narrow, drops `as TFile` cast.

    Edit 3 — `src/views/EmbeddedCalendarView.ts` Shape B around line 234:
      Current shape (4 lines):
        ```
        if (dailyNote) {
          const leaf = this.plugin.app.workspace.getLeaf("tab");
          await leaf.openFile(dailyNote as TFile);
        }
        ```
      New shape (4 lines):
        ```
        if (dailyNote instanceof TFile) {
          const leaf = this.plugin.app.workspace.getLeaf("tab");
          await leaf.openFile(dailyNote);
        }
        ```
      Net diff: same as Edit 2.

    Edit 4 — `src/views/EmbeddedAgendaView.ts` Shape B around line 383:
      Current shape (4 lines):
        ```
        if (dailyNote) {
          const leaf = this.plugin.app.workspace.getLeaf("tab");
          await leaf.openFile(dailyNote as TFile);
        }
        ```
      New shape (4 lines): same as Edit 2 / Edit 3 (positive `instanceof TFile` narrow, `dailyNote` is the narrowed identifier).

    Import verification:
      For each of the 3 files, confirm that the top-level `import { ... } from "obsidian";` statement already includes `TFile`. If a file is missing `TFile` from its `obsidian` import (unexpected at planning time, but possible if a prior `as TFile` cast survived without an explicit import), ADD `TFile` to the existing import — do NOT add a new import line. Run `grep -n 'import.*TFile' src/views/*.ts` to confirm presence.

    Behavior-preservation check: The surrounding `try/catch` blocks (`CalendarView.ts:837-841`, `EmbeddedCalendarView.ts:236-243`, `EmbeddedAgendaView.ts:385-392`) are UNCHANGED — the `await leaf.openFile(...)` call inside the new guard branch still throws on file-system errors, and the existing catch handles those (`console.error` + user-visible `Notice`). The `else` branch (when `dailyNote instanceof TFile` is false) is implicit no-op as before, since the previous `if (dailyNote)` truthy check also no-op'd on falsy `dailyNote`.

    SANITY CHECK after all 4 edits:
      `git ls-files src/ | xargs grep -n 'as TFile'` must return ZERO matches (ROADMAP Phase 7 success criterion #3).
      `git ls-files src/ | xargs grep -nE 'instanceof\s+TFile'` must return at least 6 matches: the 4 new edits + the 2 pre-existing analogs at `NoteService.ts:73` and `CalendarService.ts:520`.

    Do NOT add helper functions (`assertIsFile`, `isTFile`, `narrowToTFile`, etc.) — D-08 + PATTERNS Pattern 2 explicitly prohibits this; the `instanceof` operator IS the narrow and any wrapper defeats TypeScript's control-flow analysis (RESEARCH §"Don't Hand-Roll" — `instanceof TFile` row).
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; ! git ls-files src/ | xargs grep -n 'as TFile' &amp;&amp; [ "$(git ls-files src/ | xargs grep -cE 'instanceof\s+TFile' | awk -F: '{s+=$2} END {print s+0}')" -ge 6 ] &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files src/ | xargs grep -n 'as TFile'` returns ZERO matches (success criterion #3 verbatim)
    - `git ls-files src/ | xargs grep -nE 'instanceof\s+TFile'` returns at least 6 matches across `src/` (4 new + 2 pre-existing analogs at `NoteService.ts:73` and `CalendarService.ts:520`)
    - `grep -c 'instanceof TFile' src/views/CalendarView.ts` returns at least 2 (Edit 1 + Edit 2)
    - `grep -c 'instanceof TFile' src/views/EmbeddedCalendarView.ts` returns at least 1 (Edit 3)
    - `grep -c 'instanceof TFile' src/views/EmbeddedAgendaView.ts` returns at least 1 (Edit 4)
    - `npm run build` exits 0 (`tsc --noEmit` accepts the narrowed types; no `as TFile` cast required at `openFile(dailyNote)` because `instanceof TFile` narrows the type)
    - `npm run lint` exits 0 (Phase 7 override block at `eslint.config.mjs:65-91` still active; `obsidianmd/no-tfile-tfolder-cast` and `@typescript-eslint/no-unnecessary-type-assertion` suppressed at this commit; count must not regress vs HEAD~1 per VALIDATION.md sampling rate)
    - No new helper function added — verified by `git diff` not introducing any new exported function for TFile narrowing
    - `import { ..., TFile, ... } from "obsidian";` is present in all 3 view files (verified by `grep -E 'import.*\{[^}]*\bTFile\b[^}]*\}.*from\s+["\047]obsidian["\047]' src/views/CalendarView.ts src/views/EmbeddedCalendarView.ts src/views/EmbeddedAgendaView.ts` returning at least 3 matches)
  </acceptance_criteria>
  <done>
    All 4 `as TFile` casts replaced with `instanceof TFile` guards. Shape A (Map.set) wraps with positive guard; Shape B (leaf.openFile) replaces truthy check with typed narrow. No helper introduced. `npm run build && npm run lint` both exit 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit DIR-07 changes with the exact D-11 commit 3 subject line</name>
  <files>(git commit only — no source changes in this task)</files>
  <read_first>
    - .planning/phases/07-lifecycle-compatibility/07-CONTEXT.md (D-11 step 3 — exact commit subject)
    - CLAUDE.md (Memory Reminders: NO Claude / AI references in commits)
  </read_first>
  <action>
    Stage the 3 modified files from Task 1:
      - `src/views/CalendarView.ts`
      - `src/views/EmbeddedCalendarView.ts`
      - `src/views/EmbeddedAgendaView.ts`

    Create a single commit with the exact subject line from D-11 step 3:

      refactor(views): narrow TAbstractFile via instanceof TFile (DIR-07)

    Commit body (3-5 sentences, NO Claude / AI references per CLAUDE.md):
      Paragraph 1: Names DIR-07 closure. References D-08. Lists the 4 sites: `CalendarView.ts:148` (Shape A — Map.set), `CalendarView.ts:828`, `EmbeddedCalendarView.ts:234`, `EmbeddedAgendaView.ts:383` (Shape B — leaf.openFile). Names the pattern: `instanceof TFile` runtime narrowing replaces `as TFile` casts.
      Paragraph 2: Notes the cascade — the 2 `@typescript-eslint/no-unnecessary-type-assertion` violations identified in CONTEXT.md `<domain>` close automatically because they were the parallel `as TFile` casts.
      Paragraph 3: Optional — note the strictly-safer behavior change: non-`TFile` entries from `getAllDailyNotes()` are now silently dropped from the map (previously would have type-cast-then-crashed); non-`TFile` daily-note open paths are now skipped (previously would have type-cast-then-crashed at `openFile`).

    Use HEREDOC for the commit message body. Do NOT use `--no-verify`. Do NOT include `Co-Authored-By:` or any trailer referencing Claude / AI / assistant.

    BEFORE committing, run `npm run lint && npm run build` and confirm both exit 0.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; git log -1 --pretty=%s | grep -qxF 'refactor(views): narrow TAbstractFile via instanceof TFile (DIR-07)' &amp;&amp; ! git log -1 --pretty=%B | grep -iE 'claude|AI assist|Co-Authored-By' &amp;&amp; [ "$(git diff HEAD~1 --name-only | sort | tr '\n' ' ')" = "src/views/CalendarView.ts src/views/EmbeddedAgendaView.ts src/views/EmbeddedCalendarView.ts " ] &amp;&amp; ! git ls-files src/ | xargs grep -n 'as TFile' &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` exactly matches `refactor(views): narrow TAbstractFile via instanceof TFile (DIR-07)`
    - `git diff HEAD~1 --name-only | sort` lists exactly these 3 files: `src/views/CalendarView.ts`, `src/views/EmbeddedAgendaView.ts`, `src/views/EmbeddedCalendarView.ts`
    - `git log -1 --pretty=%B` contains NO occurrence of "claude", "Claude", "AI", "assistant", "Co-Authored-By"
    - `git ls-files src/ | xargs grep -n 'as TFile'` returns ZERO matches at HEAD
    - `npm run lint` exits 0 at HEAD
    - `npm run build` exits 0 at HEAD
  </acceptance_criteria>
  <done>
    The DIR-07 commit exists at HEAD with the exact D-11 step 3 subject line, contains exactly the 3 view files, has no Claude / AI / Co-Authored-By references, and `npm run lint && npm run build` exit 0 at HEAD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 7 ships no new attack surface. This commit STRENGTHENS type-safety against malformed daily-notes plugin returns:
- `instanceof TFile` is a runtime check — it doesn't lie. A `TFolder` returned by `getAllDailyNotes()` or `getDailyNote()` is now filtered out, not silently cast through.
- The 4 sites previously trusted the `TFile` shape implicitly via `as TFile`; an unexpected `TFolder` would have crashed `leaf.openFile()` or the Map consumer downstream. The new guard short-circuits cleanly.

| Boundary | Description |
|----------|-------------|
| Daily-notes plugin return values → views' consumer logic | The daily-notes plugin is third-party (`obsidian-daily-notes-interface`). Its return types declare `TFile`, but a future version change could return `TFolder` or null. The `instanceof TFile` guard makes this safe. |

## STRIDE Threat Register

Severity: **none**. RESEARCH §"Security Domain" confirmed Phase 7 has no security implications. This change is a strict robustness improvement.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | Daily-notes plugin returning unexpected shape | mitigate | The 4 sites now narrow via `instanceof TFile`. Non-`TFile` returns are filtered out (Shape A) or skipped (Shape B). Existing `try/catch` blocks around `await leaf.openFile(...)` still cover I/O errors. |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint` exits 0 (Phase 7 override block at `eslint.config.mjs:65-91` still active; `obsidianmd/no-tfile-tfolder-cast` + `@typescript-eslint/no-unnecessary-type-assertion` suppressed; count must not regress)
- `git ls-files src/ | xargs grep -n 'as TFile'` returns zero matches at HEAD
- `git ls-files src/ | xargs grep -nE 'instanceof\s+TFile'` returns at least 6 matches at HEAD
- `git log -1 --pretty=%s` matches D-11 step 3 subject exactly
- `git log -1 --pretty=%B` contains no Claude / AI / Co-Authored-By references
- `git diff HEAD~1 --name-only | sort` lists exactly 3 files
</verification>

<success_criteria>
- DIR-07 closed (ROADMAP Phase 7 success criterion #3 satisfied at source level)
- The 2 `@typescript-eslint/no-unnecessary-type-assertion` cascade violations close automatically
- HEAD commit matches D-11 step 3 subject line exactly; no Claude / AI references
- No helper introduced (per D-08 + PATTERNS Pattern 2)
</success_criteria>

<output>
After completion, create `.planning/phases/07-lifecycle-compatibility/07-03-SUMMARY.md` with:
- The 4 edit sites with `file:line` references showing shape A vs shape B
- Confirmation of zero `as TFile` matches at HEAD
- The git commit SHA and subject line
- The `npm run lint` violation-count delta vs HEAD~1 (must not regress; the 2 `no-unnecessary-type-assertion` cascades should disappear if the rule is active anywhere — note that for Phase 7 override block they are suppressed)
- Confirmation that the commit message has no Claude / AI references
- Hand-off to plan 07-04 (DIR-08 floating promises + MarkdownRenderChild lifecycle)
</output>
