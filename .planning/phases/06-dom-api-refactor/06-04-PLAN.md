---
phase: 06-dom-api-refactor
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - eslint.config.mjs
  - src/settings/SettingsTab.ts
autonomous: true
requirements: [DIR-02, DIR-03, DIR-04]
tags: [obsidian-plugin, eslint, sentence-case, dom-refactor-acceptance]

must_haves:
  truths:
    - "The Phase-6 ESLint override block at `eslint.config.mjs` lines 65-82 (DIR-02/03/04 rule disables for `SettingsTab.ts` and `CalendarView.ts`) is DELETED entirely (per D-13, D-16 commit 4)"
    - "The companion `obsidianmd/ui/sentence-case` override block at lines 83-98 is DELETED entirely (per D-13)"
    - "`npm run lint` exits 0 after both blocks are removed — no rule is silently re-disabled, no new override block is added for Phase-6 rules"
    - "Every `obsidianmd/ui/sentence-case` lint output for the touched files is resolved at the call site — either by lowercasing the string (preferred for non-proper-noun copy) or by adding a NARROWLY-SCOPED `// eslint-disable-next-line obsidianmd/ui/sentence-case` comment with a rationale referencing the proper noun (per D-13 + Claude's-Discretion line 106)"
    - "Phase 7 and Phase 8 ESLint override blocks (lines 100-126, 132-151, 152-168 per the pre-Phase-6 numbering) are LEFT UNTOUCHED — they belong to their owning phases"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Phase-6 override blocks removed; lint config no longer suppresses DIR-02/DIR-03/DIR-04 rules on the touched files"
      pattern: "Phase 7|Phase 8"
    - path: "src/settings/SettingsTab.ts"
      provides: "Per-site sentence-case resolutions (lowercase fixes OR scoped eslint-disable-next-line with rationale)"
      pattern: "eslint-disable-next-line obsidianmd/ui/sentence-case"
  key_links:
    - from: "lint output (zero violations)"
      to: "Phase 6 acceptance"
      via: "npm run lint exits 0 with no Phase-6 rule suppressions"
      pattern: "Phase 7|Phase 8"
---

<objective>
Delete the Phase-6 ESLint override blocks in `eslint.config.mjs` (lines 65-82 and 83-98) per D-13/D-16 commit 4, then resolve every `obsidianmd/ui/sentence-case` violation surfaced by the now-active rule by either lowercasing the flagged string or adding a narrowly-scoped `eslint-disable-next-line` with a proper-noun rationale per D-13 + CONTEXT Claude's-Discretion. Then run `npm run lint` and confirm it exits 0. This is the FOURTH atomic commit of Phase 6 per D-16, and it is the formal acceptance commit for DIR-02 / DIR-03 / DIR-04 (the rules now actively block any regression).

Purpose: ROADMAP Phase 6 success criterion #4 requires that "the Phase 5 ESLint overrides covering `no-inner-html`, the inline-style rule, and `document.createElement` are removed from `.eslintrc` and `npm run lint` still passes." This plan is that commit. The sentence-case resolution rides along because Phase 5 explicitly deferred it to Phase 6 ("Phase 6 will normalise copy when the DOM-API refactor touches these files") and the override block does NOT survive the phase per D-13.

Output: `eslint.config.mjs` shrunk by two override blocks (~34 lines); `src/settings/SettingsTab.ts` (and possibly the other 5 in-scope files if their strings are flagged) with sentence-case resolutions; one git commit with subject `chore(lint): remove Phase 6 ESLint overrides (DIR-02/03/04 acceptance)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dom-api-refactor/06-CONTEXT.md
@.planning/phases/06-dom-api-refactor/06-PATTERNS.md
@.planning/phases/06-dom-api-refactor/06-01-SUMMARY.md
@.planning/phases/06-dom-api-refactor/06-02-SUMMARY.md
@.planning/phases/06-dom-api-refactor/06-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The Phase-6 + sentence-case override blocks in eslint.config.mjs to delete (current line numbers, may have drifted by ±1 line — re-grep at task start). -->

Block A — Phase 6 DIR-02/03/04 rule disables (currently at eslint.config.mjs:65-82):

    // ---------------------------------------------------------------------------
    // Phase 6 — DIR-02 / DIR-03 / DIR-04 will remove these when the DOM-API
    // refactor lands. Also silences ui/sentence-case (discovered in dry-run)
    // across all UI-facing files until the text copy is normalised.
    // ---------------------------------------------------------------------------
    {
      files: [
        "src/settings/SettingsTab.ts",
        "src/views/CalendarView.ts",
      ],
      rules: {
        "@microsoft/sdl/no-inner-html": "off",
        "no-unsanitized/property": "off",
        "no-unsanitized/method": "off",
        "obsidianmd/no-static-styles-assignment": "off",
        "no-restricted-syntax": "off",
      },
    },

Block B — sentence-case override (currently at eslint.config.mjs:83-98):

    {
      files: [
        "src/services/CalendarService.ts",
        "src/settings/SettingsTab.ts",
        "src/utils/viewRenderers.ts",
        "src/views/CalendarView.ts",
        "src/views/EmbeddedAgendaView.ts",
        "src/views/EmbeddedCalendarView.ts",
      ],
      rules: {
        "obsidianmd/ui/sentence-case": "off",
      },
    },

DELETE both blocks entirely (including the comment header lines for block A and the blank lines between them). The remaining Phase 7 + Phase 8 override blocks (the next entries in the `tseslint.config(...)` array) MUST be left untouched.

The rule `obsidianmd/ui/sentence-case` flags strings that violate Obsidian's "first letter capitalized, rest lowercase except proper nouns" UI text convention. PATTERNS Commit 4 lists 15 candidate flagged sites in `SettingsTab.ts`. The lint output is the authoritative source — run `npx eslint src/settings/SettingsTab.ts` (and the other 5 files, scoped) AFTER deleting the blocks to enumerate the actual flagged sites.

D-13 + Claude's-Discretion (CONTEXT line 106) resolution rules:
  - Lowercase the string IF the capitalized word is NOT a proper noun / product name / acronym (preferred resolution)
  - Add a NARROWLY-SCOPED `// eslint-disable-next-line obsidianmd/ui/sentence-case` immediately above the flagged line, with a one-line trailing or preceding comment explaining the proper-noun rationale (e.g., `// proper noun: Google Calendar`), IF the capitalized word IS a proper noun (MemoChron, Google, Outlook, iCloud, Apple, GitHub, Microsoft, etc.)
  - DO NOT add a new file-level or block-level override — D-13 explicitly forbids re-disabling the rule wholesale

Candidate flagged sites from PATTERNS Commit 4 (starting-point inventory — the lint output is authoritative):

| Line ~ | String fragment | Likely resolution |
|--------|------------------|--------------------|
| 314 | `"How do I get the correct URL?"` | likely passes (only "How" capitalized at sentence start, "URL" is an acronym — acronym handling is rule-dependent) |
| 1172 | `"Preview: Notes will be saved..."` | LOWERCASE `Notes` → `notes` (post-colon, not a new sentence) |
| 1871 | `"How to Get the Correct Calendar URL"` | LOWERCASE to `"How to get the correct calendar URL"` — title case → sentence case |
| 1875 | `"Google Calendar"` | DISABLE-NEXT-LINE — proper noun (Google product name) |
| 1892 | `"Outlook / Microsoft 365"` | DISABLE-NEXT-LINE — proper nouns |
| 1905 | `"Apple iCloud Calendar"` | DISABLE-NEXT-LINE — proper nouns |
| 1917 | `"Common Mistakes"` | LOWERCASE to `"Common mistakes"` — title case → sentence case |
| 1928 | `"View full documentation on GitHub"` | DISABLE-NEXT-LINE only if rule flags "GitHub" — otherwise passes |

The remaining sites in PATTERNS Commit 4's table (1061, 1127, 1131, 1195, 1885, 1898, 1910) are marked "passes" — verify they do not flag in the actual lint run, and if they do, apply the same resolution rules.

Phase 7 and Phase 8 override blocks remain — those rules are still suppressed because their owning phases have not landed yet. Plan 04 ONLY removes Phase 6's two blocks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the Phase-6 + sentence-case ESLint override blocks</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (full file — to confirm current line numbers of the two blocks; line numbers in PATTERNS were captured at mapping time and may have drifted by 0-2 lines after Plans 01–03)
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-13 — exact deletion scope and rationale)
    - .planning/phases/06-dom-api-refactor/06-PATTERNS.md (Commit 4 section — structural analog with Phase 7/8 blocks)
  </read_first>
  <action>
    Before editing, run `grep -nE 'Phase [678]' eslint.config.mjs` to confirm the locations of the Phase 6/7/8 comment headers. The Phase 6 block currently begins at line 65 with a comment header `// Phase 6 — DIR-02 / DIR-03 / DIR-04 will remove these...`. The sentence-case override block immediately follows at line 83.

    Delete BOTH blocks entirely:
      1. The Phase-6 DIR-02/03/04 override (the comment header lines `// Phase 6 — ...` AND the `{ files: [...], rules: { ... } },` block that follows it — currently lines ~65-82)
      2. The sentence-case override (the `{ files: [...], rules: { "obsidianmd/ui/sentence-case": "off" } },` block that follows — currently lines ~83-98)

    After deletion, the file should flow directly from the closing `},` of the Phase 5 rules block (the one ending around line 63 with the `no-restricted-syntax` rule definition) to the comment header `// Phase 7 — DIR-05 / DIR-06 / DIR-07 / DIR-08 will remove these when the lifecycle / compatibility cleanup lands.` (currently at line 100).

    Leave the Phase 7 and Phase 8 override blocks COMPLETELY UNTOUCHED. Specifically:
      - The Phase 7 block (currently lines 100-126) — keep ALL rules suppressed: `obsidianmd/no-view-references-in-plugin`, `obsidianmd/no-tfile-tfolder-cast`, `obsidianmd/prefer-active-doc`, `obsidianmd/prefer-window-timers`, `obsidianmd/detach-leaves`, `@typescript-eslint/no-floating-promises`, `@typescript-eslint/no-misused-promises`, `@typescript-eslint/no-unnecessary-type-assertion`
      - The Phase 8 blocks (currently lines 128-168) — keep ALL rules suppressed: `obsidianmd/rule-custom-message`, `no-console`, `@typescript-eslint/no-explicit-any`, `@typescript-eslint/no-unsafe-*`, `no-case-declarations`, `no-useless-escape`, `@typescript-eslint/no-unused-vars` (narrow-files variant)

    Do NOT touch the `globalIgnores([...])` block at the bottom.

    After deletion, confirm:
      - `grep -nE 'Phase 6\b' eslint.config.mjs` returns zero matches (the comment header is gone)
      - `grep -n '"obsidianmd/ui/sentence-case"' eslint.config.mjs` returns zero matches
      - `grep -n '"@microsoft/sdl/no-inner-html": "off"' eslint.config.mjs` returns zero matches
      - `grep -n '"obsidianmd/no-static-styles-assignment": "off"' eslint.config.mjs` returns zero matches
      - The Phase 7 and Phase 8 sentinel rule names still appear (`grep -n 'no-floating-promises\|no-explicit-any' eslint.config.mjs` returns hits)

    Do NOT run `npm run lint` yet — Task 2 will run it AFTER the sentence-case resolutions. Running it now is expected to surface multiple `ui/sentence-case` violations and possibly nothing else (since Plans 01–03 already cleaned up the DOM-API findings).
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; ! grep -nE 'Phase 6\b' eslint.config.mjs &amp;&amp; ! grep -n '"obsidianmd/ui/sentence-case"' eslint.config.mjs &amp;&amp; ! grep -n '"@microsoft/sdl/no-inner-html": "off"' eslint.config.mjs &amp;&amp; ! grep -n '"obsidianmd/no-static-styles-assignment": "off"' eslint.config.mjs &amp;&amp; grep -q 'no-floating-promises' eslint.config.mjs &amp;&amp; grep -q 'no-explicit-any' eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Phase 6\b' eslint.config.mjs` returns zero matches
    - `grep -n '"obsidianmd/ui/sentence-case"' eslint.config.mjs` returns zero matches
    - `grep -n '"@microsoft/sdl/no-inner-html": "off"' eslint.config.mjs` returns zero matches
    - `grep -n '"obsidianmd/no-static-styles-assignment": "off"' eslint.config.mjs` returns zero matches
    - `grep -n '"no-unsanitized/property": "off"' eslint.config.mjs` returns zero matches
    - `grep -n '"no-unsanitized/method": "off"' eslint.config.mjs` returns zero matches
    - `grep -nE 'Phase 7\b' eslint.config.mjs` returns at least 1 match (Phase 7 block preserved)
    - `grep -nE 'Phase 8\b' eslint.config.mjs` returns at least 1 match (Phase 8 block preserved)
    - `grep -n '"@typescript-eslint/no-floating-promises": "off"' eslint.config.mjs` returns at least 1 match (Phase 7 rules preserved)
    - `grep -n '"@typescript-eslint/no-explicit-any": "off"' eslint.config.mjs` returns at least 1 match (Phase 8 rules preserved)
    - `wc -l eslint.config.mjs` shows a line-count delta of approximately -34 lines vs pre-Plan-04 (blocks A + B + their comment header total ~34 lines)
  </acceptance_criteria>
  <done>
    The Phase-6 override block and the sentence-case override block are both deleted from `eslint.config.mjs`. The Phase 7 and Phase 8 blocks are unchanged. The `globalIgnores` block at the bottom is unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Resolve every obsidianmd/ui/sentence-case violation surfaced by the now-active rule</name>
  <files>src/settings/SettingsTab.ts (likely the only file affected per PATTERNS), and any other in-scope file the lint output flags</files>
  <read_first>
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-13 — resolution rules: lowercase non-proper-nouns, narrowly-scoped disable-next-line for proper nouns; Claude's-Discretion line 106 — proper-noun examples)
    - .planning/phases/06-dom-api-refactor/06-PATTERNS.md (Commit 4 — starting-point inventory table of 15 candidate sites)
  </read_first>
  <action>
    Run the lint command and capture the actual `obsidianmd/ui/sentence-case` violations:

        npx eslint src/settings/SettingsTab.ts src/views/CalendarView.ts src/views/EmbeddedCalendarView.ts src/views/EmbeddedAgendaView.ts src/utils/viewRenderers.ts src/services/CalendarService.ts 2>&amp;1 | grep -E 'sentence-case' | tee /tmp/sentence-case-violations.txt

    For each flagged site, apply the resolution rule from D-13:

    Resolution A — LOWERCASE the string (preferred for non-proper-noun copy):
      Modify the string literal at the flagged location to use sentence case: only the first character is uppercase (and any genuine acronym or proper noun that is part of standard English), the rest are lowercase. Examples expected from PATTERNS Commit 4:
        - `"How to Get the Correct Calendar URL"` → `"How to get the correct calendar URL"`
        - `"Common Mistakes"` → `"Common mistakes"`
        - `"Preview: Notes will be saved..."` → `"Preview: notes will be saved..."`

    Resolution B — NARROWLY-SCOPED disable-next-line (for proper nouns / product names / domain acronyms only):
      Add a single line immediately above the flagged line, in the form:
        // eslint-disable-next-line obsidianmd/ui/sentence-case -- proper noun: <name>
      Where `<name>` is the specific proper noun or product name that triggered the rule. Examples:
        - For `"Google Calendar"` (heading): `// eslint-disable-next-line obsidianmd/ui/sentence-case -- proper noun: Google Calendar`
        - For `"Outlook / Microsoft 365"`: `// eslint-disable-next-line obsidianmd/ui/sentence-case -- proper noun: Microsoft 365`
        - For `"Apple iCloud Calendar"`: `// eslint-disable-next-line obsidianmd/ui/sentence-case -- proper noun: Apple iCloud`

      DO NOT use block-level (`/* eslint-disable */`) or file-level disables. DO NOT add a new override block in `eslint.config.mjs`. The whole point of D-13 is that the per-site `disable-next-line` with a rationale comment is the only acceptable narrow-scope escape.

    Proper-noun classification rule (from CLAUDE.md project conventions + product naming):
      - LOWERCASE these (NOT proper nouns when used in headings about generic concepts): "Mistakes", "Notes", "Preview", "Get", "Correct", "Calendar" (when used as a generic word, not part of a brand)
      - DISABLE-NEXT-LINE for these (genuine proper nouns / product names): "MemoChron", "Google", "Google Calendar", "Outlook", "Microsoft", "Microsoft 365", "Apple", "iCloud", "Apple iCloud", "GitHub", "Obsidian"
      - ACRONYMS that are part of standard English are usually accepted by the rule mid-sentence: "URL", "ICS", "iCal" — verify per-site whether the rule flags them; if it does and they are domain acronyms, use Resolution B with rationale `acronym: <NAME>`.

    For ambiguous cases (e.g., "Publish a calendar" appearing as a quoted UI button label from Outlook's UI), prefer Resolution B with rationale `external UI label: Outlook button name` if the rule flags it.

    After all per-site resolutions, re-run the lint:

        npm run lint

    It MUST exit 0. If it does not, inspect remaining violations and apply the same resolution rules. The grep `grep -c '// eslint-disable-next-line obsidianmd/ui/sentence-case' src/settings/SettingsTab.ts` will show how many proper-noun disables were added — record this count for the SUMMARY.

    Do NOT modify any string outside of the lint-flagged set. Do NOT touch `eslint.config.mjs` further in this task.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; npm run lint 2&gt;&amp;1 | tail -10 &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npx eslint src/settings/SettingsTab.ts 2>&1 | grep -c 'sentence-case'` returns 0
    - `npx eslint src/views/CalendarView.ts src/views/EmbeddedCalendarView.ts src/views/EmbeddedAgendaView.ts src/utils/viewRenderers.ts src/services/CalendarService.ts 2>&1 | grep -c 'sentence-case'` returns 0
    - All `// eslint-disable-next-line obsidianmd/ui/sentence-case` comments added in this task include a `-- <rationale>` suffix (no bare disable-next-line). Verified by `grep -E 'eslint-disable-next-line obsidianmd/ui/sentence-case$' src/settings/SettingsTab.ts | wc -l` returning 0 (every match has a rationale).
    - No new entry was added to `eslint.config.mjs` in this task (the file is identical to its post-Task-1 state). Verified by `git diff HEAD --name-only` NOT listing `eslint.config.mjs` if Task 1 was committed separately, OR by inspecting the staged diff if both tasks are in one working tree.
    - The full DIR-02/DIR-03/DIR-04 grep set still passes:
      - `git ls-files src/ | xargs grep -nE '\.(inner|outer)HTML\s*='` returns zero matches
      - `git ls-files src/ | xargs grep -nE '\.style\.(border|color|cursor|display|fontSize|height|left|margin|marginTop|opacity|padding|position|textAlign|top|width)\s*='` returns zero matches
      - `git ls-files src/ | xargs grep -n 'document\.createElement[^N]'` returns zero matches
  </acceptance_criteria>
  <done>
    Every `obsidianmd/ui/sentence-case` violation that the now-active rule surfaced is resolved either by lowercasing the string OR by a narrowly-scoped `eslint-disable-next-line` with a rationale referencing the proper noun. `npm run lint` exits 0. The Phase-6 DOM-API grep suite still returns zero matches.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit the override-block deletion + sentence-case resolutions</name>
  <files>(git commit only — no source changes in this task)</files>
  <read_first>
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-16 — exact commit subject line; D-13 — running `npm run lint` is part of the commit's local verification)
    - CLAUDE.md (Memory Reminders: NO Claude / AI references in commits)
  </read_first>
  <action>
    Stage `eslint.config.mjs` and any source files modified in Task 2 (likely `src/settings/SettingsTab.ts` and possibly other in-scope files if the lint flagged them). Create a single commit with the exact subject line from D-16:

      chore(lint): remove Phase 6 ESLint overrides (DIR-02/03/04 acceptance)

    Commit body: 3-5 sentences referencing (1) deletion of the Phase-6 DIR-02/03/04 override block, (2) deletion of the sentence-case override block per Phase 5's deferred-to-Phase-6 commitment, (3) per-site sentence-case resolutions (count of lowercase fixes vs disable-next-line additions), (4) `npm run lint` exits 0, (5) DIR-02/03/04 grep set still returns zero matches. NO Claude / AI references (per CLAUDE.md memory reminders).

    Use a HEREDOC for the commit message. Do NOT use `--no-verify`. The lint hook MUST pass at this commit — that's the entire point of the commit.

    BEFORE committing, run `npm run lint` one more time and confirm exit 0. If it fails, fix the remaining violations using the Task 2 rules and re-stage before committing.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; git log -1 --pretty=%s | grep -qxF 'chore(lint): remove Phase 6 ESLint overrides (DIR-02/03/04 acceptance)' &amp;&amp; ! git log -1 --pretty=%B | grep -iE 'claude|AI assist|Co-Authored-By' &amp;&amp; git diff HEAD~1 --name-only | grep -q '^eslint.config.mjs$' &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` exactly matches `chore(lint): remove Phase 6 ESLint overrides (DIR-02/03/04 acceptance)`
    - `git diff HEAD~1 --name-only` includes `eslint.config.mjs` and (if Task 2 made any source changes) at least one source file under `src/`
    - `git log -1 --pretty=%B` contains NO occurrence of "claude", "Claude", "AI", "assistant", "Co-Authored-By"
    - `npm run lint` exits 0 at HEAD (the post-commit state)
    - The 3 DIR-02/03/04 success-criterion greps still return zero matches at HEAD (canonical ROADMAP Phase 6 criteria #1, #2, #3)
  </acceptance_criteria>
  <done>
    The override-block-removal commit exists at HEAD with the exact D-16 subject line, contains `eslint.config.mjs` and any sentence-case-resolved source files, has no Claude / AI / Co-Authored-By references, and `npm run lint` exits 0 at HEAD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ESLint config → CI lint gate (DOC-01) | The override-block deletion EXPOSES the DIR-02/03/04 rules to the now-cleaned codebase. If Plans 01-03 missed a violation, Plan 04's lint run will catch it. The Phase 5 CI workflow runs `npm run lint` on every push and PR — DIR-02/03/04 regressions in future phases will block the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | The removed override blocks | mitigate | Plans 01-03 cleaned every violation site BEFORE the rules went active. Task 2 of Plan 04 re-runs the violation greps and `npm run lint` to confirm zero leftovers BEFORE committing. The commit verification embeds `npm run lint` as the gate. |
| T-06-10 | Tampering | Per-site `eslint-disable-next-line obsidianmd/ui/sentence-case` comments | accept | Each disable comment includes a `-- <rationale>` suffix naming the proper noun. The rule's narrow scope (single line) prevents accidental suppression of other lint findings. Future code reviewers can grep for `eslint-disable-next-line` to audit the suppressions. |
| T-06-11 | Repudiation | Commit message hygiene | mitigate | The acceptance criteria explicitly check for the absence of "claude", "Claude", "AI", "assistant", "Co-Authored-By" trailers per CLAUDE.md memory reminders. |
</threat_model>

<verification>
- `grep -nE 'Phase 6\b' eslint.config.mjs` returns zero matches (override block deleted)
- `grep -n '"obsidianmd/ui/sentence-case"' eslint.config.mjs` returns zero matches (override block deleted)
- `grep -nE 'Phase 7\b|Phase 8\b' eslint.config.mjs` returns multiple matches (other phases' override blocks preserved)
- `npm run lint` exits 0
- All 3 DIR-02/03/04 success-criterion greps still return zero matches
- HEAD commit subject matches D-16 commit 4 exactly
</verification>

<success_criteria>
- ROADMAP Phase 6 success criterion #4 (Phase 5 overrides for DIR-02/03/04 removed; `npm run lint` still passes) is satisfied.
- The companion sentence-case override block (Phase 5 D-04 + D-13 commitment) is also removed.
- Every `obsidianmd/ui/sentence-case` violation surfaced by the now-active rule is resolved per-site (lowercase OR narrow disable-next-line with rationale).
- DOC-01's CI lint gate (Phase 5) now actively blocks DIR-02/03/04 regressions on future PRs.
- HEAD commit matches D-16 commit 4 subject line exactly; no Claude / AI references.
- Phase 7 and Phase 8 override blocks are preserved (those phases own their removal).
</success_criteria>

<output>
After completion, create `.planning/phases/06-dom-api-refactor/06-04-SUMMARY.md` with:
- The exact eslint.config.mjs line range deleted (with the post-deletion `grep -nE 'Phase [678]\b'` output showing 6 is gone and 7/8 remain)
- The full inventory of `obsidianmd/ui/sentence-case` resolutions: per-site list with file:line, original string, resolution applied (lowercase vs disable-next-line + rationale)
- The `npm run lint` output (must show 0 errors, 0 warnings or an explicit acceptable warning count)
- The 3 DIR-02/03/04 grep outputs (must be empty)
- The git commit SHA and subject line
- Confirmation that no Claude / AI references appear in the commit message
- Hand-off to Plan 05 (HUMAN-UAT.md)
</output>
