---
phase: 06-dom-api-refactor
plan: 05
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - .planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md
autonomous: false
requirements: [DIR-02, DIR-03, DIR-04]
tags: [human-uat, visual-parity, verification]

must_haves:
  truths:
    - "`06-HUMAN-UAT.md` exists in the phase directory and follows the Phase 5 HUMAN-UAT.md template structure (per D-14, D-15)"
    - "The 5 mandatory UAT steps from CONTEXT D-14 are documented as a check-list with explicit pass/fail criteria each: sidebar calendar, embedded views, settings tab, hide-calendar toggle, no-layout-regressions at 350px/400px/default widths"
    - "The reviewer executes the UAT walk-through in a built/installed plugin and records pass/fail evidence inline in `06-HUMAN-UAT.md`"
    - "No before/after screenshot PNG artifacts are committed under `.planning/phases/06-*/` (per D-15)"
  artifacts:
    - path: ".planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md"
      provides: "Human-executed UAT evidence-of-record for Phase 6 visual-parity acceptance (ROADMAP Phase 6 success criterion #5)"
      pattern: "DIR-02|DIR-03|DIR-04|visual"
  key_links:
    - from: ".planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md"
      to: "Phase 6 acceptance closure"
      via: "5-step UAT walkthrough confirms visual identity with v1.14.0"
      pattern: "Pass|Fail"
---

<objective>
Create `.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md` and execute its 5-step walkthrough against a built/installed plugin to confirm visual parity with v1.14.0 per CONTEXT D-14 + ROADMAP Phase 6 success criterion #5. This is the FIFTH and FINAL atomic commit of Phase 6 per D-16, and it closes the visual-parity verification leg of DIR-02/DIR-03/DIR-04.

Purpose: ROADMAP Phase 6 success criterion #5 requires: "Manual UAT: opening the sidebar calendar, switching month/week view, opening an embedded calendar code block, and opening the settings tab all render visually identically to the v1.14.0 baseline (no layout, color, or interactivity regressions)." Per CONTEXT D-14, the verification artifact is `06-HUMAN-UAT.md` (no test suite this milestone per Phase 5 D-13 precedent; no PNG baselines per D-15). The reviewer is the user, and the user is the visual-parity oracle.

Output: `06-HUMAN-UAT.md` with the 5 mandatory UAT steps documented and executed (pass/fail evidence inline); one git commit with subject `docs(06): record Phase 6 human UAT`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-dom-api-refactor/06-CONTEXT.md
@.planning/phases/06-dom-api-refactor/06-01-SUMMARY.md
@.planning/phases/06-dom-api-refactor/06-02-SUMMARY.md
@.planning/phases/06-dom-api-refactor/06-03-SUMMARY.md
@.planning/phases/06-dom-api-refactor/06-04-SUMMARY.md
@.planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md
@CLAUDE.md

<interfaces>
<!-- Template structure: follow the Phase 5 HUMAN-UAT.md format. The Phase 5 file is the canonical template per CONTEXT D-15 + PATTERNS Commit 5. -->

Phase 5 HUMAN-UAT.md template (the in-repo precedent, per PATTERNS Commit 5 analog):
  - Frontmatter: `phase`, `date`, `status` (`pending` → `complete` after walkthrough)
  - One section per requirement closed by the phase
  - Each section: "Requirement" sentence + "Verification" steps + evidence (output, screenshots ARE NOT included per D-15)

For Phase 6, the structure is one section per UAT step (5 steps from D-14), each mapping back to the requirement(s) and CONTEXT decision(s) it verifies.

5 mandatory UAT steps from CONTEXT D-14 (authoritative):
  1. **Sidebar calendar:** open the calendar view, navigate one month forward and back, toggle month-view → week-view via drag-resize, confirm event dots render in source-coloured pairs (verifies D-04 dynamic-color path through `setCssProps`), confirm today / selected-day indicators look exactly as in v1.14.0
  2. **Embedded views:** open a note containing a `memochron-calendar` code block and a `memochron-agenda` code block, confirm rendering matches v1.14.0
  3. **Settings tab:** open settings, expand "Add a calendar" flow, verify the custom color picker overlay (the 24×24 hidden-input pattern from D-06) opens correctly on click and applies the chosen color; open the "iCal URL setup guide" section and confirm the 5 `<li>` lines render with bolded action text exactly as before (verifies D-01)
  4. **Hide-calendar toggle:** enable "Hide calendar" in settings, confirm the calendar, resize handle, and controls all hide; disable, confirm all reappear (verifies D-10 class-toggle migration via `.memochron-hidden`)
  5. **No layout regressions** at standard sidebar widths (350px, 400px, default) — visual eyeball check sufficient

Optional 6th step per CONTEXT specifics line 185:
  6. **Mobile-CSS sanity** — if reasonable, verify the plugin renders on Obsidian mobile (the `isDesktopOnly: false` requirement). If not reasonable in the reviewer's environment, the reviewer notes "desktop-only visual check; mobile audit deferred to v1.16 if regression reported" and moves on.

Per CONTEXT D-15: NO before/after screenshot PNG files are committed under `.planning/phases/06-*/`. The walkthrough is live; pass/fail evidence is text inline in the markdown file.

Per CLAUDE.md memory reminders: NO Claude / AI references in the file content, commit subject, commit body, or any trailer.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Build the plugin and prepare for UAT</name>
  <files>(no source changes — build verification only)</files>
  <read_first>
    - .planning/phases/06-dom-api-refactor/06-04-SUMMARY.md (confirms `npm run lint` exits 0 and the 3 DIR success-criterion greps are zero at HEAD)
  </read_first>
  <what-built>
    Plans 01-04 have completed:
    - All `.innerHTML` / `.outerHTML` writes removed from `src/` (DIR-02)
    - All `.style.<flagged-property>` writes removed from `src/` (DIR-03)
    - All `document.createElement` calls (non-NS) removed from `src/` (DIR-04)
    - `styles.css` augmented with new + augmented CSS classes
    - ESLint override blocks for DIR-02/03/04 + sentence-case deleted from `eslint.config.mjs`
    - `npm run lint` exits 0 with no Phase-6 rule suppressions

    What remains is the human visual-parity check.
  </what-built>
  <how-to-verify>
    1. Run `npm run build` and confirm exit 0 (sanity check — was verified in Plans 01-03 but re-run at the start of UAT for fresh `main.js`).
    2. Install the freshly built plugin into a test Obsidian vault:
        - Locate `main.js`, `manifest.json`, `styles.css` in the repo root
        - Copy them to `<test-vault>/.obsidian/plugins/memochron/` (or use the existing dev install location)
        - Reload Obsidian or use the developer "Reload app without saving" command
    3. Open Obsidian, enable MemoChron in Community Plugins if not already, and confirm the plugin loads without errors in the dev console (Cmd+Opt+I / Ctrl+Shift+I → Console tab).
    4. If the build, install, and load all succeed, proceed to Task 2 (the UAT walkthrough).

    Resume signal: Type "build ready" once the plugin is installed and loads cleanly in the test vault.
  </how-to-verify>
  <resume-signal>Type "build ready" once the plugin is installed and loads cleanly in the test vault; if build or load fails, describe the failure for diagnosis.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the 06-HUMAN-UAT.md file skeleton with the 5 mandatory UAT steps</name>
  <files>.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md (template structure: frontmatter, one section per requirement, evidence inline)
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-14 — exact 5 UAT steps; D-15 — no PNG screenshots)
  </read_first>
  <action>
    Create `.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md` with the following structure (use the exact heading levels and content; replace bracketed placeholders only after Task 3 walkthrough completes — leave them as placeholders for now):

    Frontmatter (exact field names matching Phase 5 precedent):
      ---
      phase: 06-dom-api-refactor
      date: 2026-05-13 (or the current date when the walkthrough runs)
      status: pending
      ---

    Body sections:
      # Phase 6 — Human UAT Evidence

      This file is the verification-of-record for Phase 6 (DOM API Refactor), consistent with Phases 1-5 practice. It captures the acceptance evidence for ROADMAP Phase 6 success criterion #5 (visual parity with v1.14.0 baseline) and closes the DIR-02 / DIR-03 / DIR-04 visual-acceptance leg.

      Per CONTEXT D-15, no PNG screenshots are committed; the walkthrough is live and the evidence is text inline.

      ---

      ## Pre-Conditions

      - Plans 01-04 of Phase 6 are merged.
      - `npm run lint` exits 0 (recorded in 06-04-SUMMARY.md).
      - All three DIR-02/DIR-03/DIR-04 success-criterion greps return zero matches (recorded in 06-04-SUMMARY.md).
      - Freshly built `main.js` + `manifest.json` + `styles.css` installed in the reviewer's test vault.
      - Obsidian version: [reviewer fills in — `Obsidian → Help → About → App version`]
      - Comparison baseline: v1.14.0 release (reviewer recalls or refers to the v1.14.0 binary for side-by-side check)

      ---

      ## UAT Step 1 — Sidebar Calendar

      **Verifies:** D-04 dynamic-color path through `setCssProps`; D-05 height application; today / selected-day indicators; month/week view switching; ROADMAP success criterion #5 for the sidebar surface.

      **Steps:**
      1. Open the sidebar calendar view (left-side ribbon icon or `Cmd/Ctrl+P → MemoChron: Open calendar view`).
      2. Confirm the month grid renders with event dots in source colors (each calendar source's configured color appears on its events).
      3. Navigate one month forward (arrow button); navigate back. Confirm transitions are instantaneous (no flicker, no layout reflow regression vs v1.14.0).
      4. Toggle to week view by dragging the resize handle DOWN; toggle back to month view by dragging UP. Confirm the height transition is smooth and the grid layout adapts cleanly.
      5. Confirm today's date has the v1.14.0 today-indicator styling (border / background per `.memochron-today` rule — unchanged in Phase 6).
      6. Confirm the selected day has the v1.14.0 selected-day styling (highlight per `.memochron-selected` rule — unchanged in Phase 6).
      7. Confirm event dots in the agenda below the grid also render in source colors (verifies the `viewRenderers.ts:320` `setCssProps` rewrite).

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in any observed deviation; if Pass, leave blank or write "no deviation observed"]

      ---

      ## UAT Step 2 — Embedded Views

      **Verifies:** `EmbeddedCalendarView` and `EmbeddedAgendaView` render unchanged (PATTERNS confirmed these views had zero direct violations; Phase 6 changes them only via shared `viewRenderers.ts` and shared CSS classes).

      **Steps:**
      1. Open or create a note containing a `memochron-calendar` code block (fenced with ` ```memochron-calendar ` ... ` ``` `) with default params.
      2. Confirm the embedded calendar renders as a month grid with event dots in source colors, identical to v1.14.0.
      3. In the same note (or a sibling), embed a `memochron-agenda` code block.
      4. Confirm the embedded agenda renders the day's events with per-source color indicators (verifies the `style.setProperty("--event-color", ...)` writes at `EmbeddedAgendaView.ts:263, 297` — left INTENTIONALLY UNCHANGED in Phase 6 per PATTERNS 3h, since CSS custom-property writes are not in DIR-03's banned list).
      5. Click an event in the embedded agenda; confirm it opens / creates the linked note correctly.

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in]

      ---

      ## UAT Step 3 — Settings Tab

      **Verifies:** D-01 setup-guide rewrites (bolded action text); D-06 custom color picker overlay; D-07/D-08/D-09 inline-style deletions for error message + help button + doc-link + button-container.

      **Steps:**
      1. Open `Settings → Community plugins → MemoChron` (or `MemoChron` directly in the Settings sidebar).
      2. Expand the "Add a calendar" / per-calendar configuration section. For an existing calendar source, click the color swatch. The 24×24 custom color overlay should open the native OS color picker on click.
      3. Pick a non-default color and confirm it applies — the swatch updates, the calendar grid event dots update to the new color (verifies D-06 collapse of the duplicated overlay clusters into shared CSS classes).
      4. Enter an invalid URL (e.g., `not-a-url`) in the add-calendar URL field and trigger validation. Confirm the error message renders in red with proper spacing — matches v1.14.0 (verifies D-07 deletion of inline styles; `.memochron-error-message` class still applies with the `--text-error, #c92424` fallback).
      5. Enter an invalid refresh interval (e.g., `-1` or `0`) and trigger validation; confirm the same error rendering.
      6. Click any inline "help" / "?" button next to a settings field. Confirm the help button has correct margin/font-size (verifies D-08 augmentation of `.memochron-help-btn`).
      7. Open the "iCal URL setup guide" modal (or expandable section, depending on UI location). Verify the 5 `<li>` lines render with bolded action text exactly as v1.14.0:
          - "Copy the **Secret address in iCal format**" (Google)
          - "Copy the **ICS link** (not the HTML link)" (Outlook)
          - "**Using the public link** - This opens a webpage, not calendar data" (mistakes)
          - "**Using the embed link** - This is for embedding in websites" (mistakes)
          - "**Missing the .ics extension** - The URL should end with .ics" (mistakes)
         Confirm the doc-link near the bottom has correct margin spacing (verifies D-09 `.memochron-help-doc-link` augmentation) and the button container at the bottom is right-aligned with correct margin (verifies D-09 new `.memochron-help-buttons` rule).

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in]

      ---

      ## UAT Step 4 — Hide-Calendar Toggle

      **Verifies:** D-10 / D-11 class-toggle migration in `updateCalendarVisibility` via `.memochron-hidden`.

      **Steps:**
      1. In MemoChron settings, enable "Hide calendar" toggle.
      2. Confirm in the sidebar calendar view: the month grid hides, the resize handle hides, the controls (month/year/today buttons) hide. The agenda remains visible (agenda-only layout reflow per the `agenda-only` class which is unchanged in semantics).
      3. Disable "Hide calendar".
      4. Confirm all three (grid, resize handle, controls) reappear and the agenda returns to its non-agenda-only layout.
      5. Toggle the setting back and forth 2-3 times to confirm no flicker, no layout glitch, and no console errors.

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in]

      ---

      ## UAT Step 5 — No Layout Regressions at Sidebar Widths

      **Verifies:** ROADMAP success criterion #5 across the common sidebar widths.

      **Steps:**
      1. Resize the left or right sidebar to approximately 350px width. Confirm the calendar grid + agenda layout has no overflow, no clipped content, no obviously broken alignment (visual eyeball check). Compare mentally against v1.14.0 if possible.
      2. Resize to approximately 400px width. Same check.
      3. Resize to the Obsidian default sidebar width. Same check.
      4. Briefly check the embedded calendar + agenda renderings inside a note at the standard editor width.

      **Result:** [ ] Pass / [ ] Fail
      **Notes:** [reviewer fills in]

      ---

      ## UAT Step 6 (optional) — Mobile sanity check

      **Verifies:** `isDesktopOnly: false` manifest flag still holds — the new CSS classes don't break mobile WebView rendering. Per CONTEXT specifics line 185, this is OPTIONAL — if the reviewer doesn't have a mobile vault set up, the recommended note is "desktop-only visual check; mobile audit deferred to v1.16 if regression reported."

      **Steps (if performed):**
      1. Install the built plugin in a mobile Obsidian vault.
      2. Open the calendar sidebar. Confirm grid + agenda render without overflow.
      3. Test the custom color picker overlay on mobile — note any deviation.

      **Result:** [ ] Pass / [ ] Fail / [ ] Skipped (with reason)
      **Notes:** [reviewer fills in]

      ---

      ## Overall Acceptance

      ROADMAP Phase 6 success criterion #5 status: [ ] PASS / [ ] FAIL

      If FAIL: [list the failing step(s) and what specifically deviated from v1.14.0 baseline; create gap-closure follow-up notes if needed]

      If PASS: Phase 6 is complete; Phase 7 may begin.

      ---

      *Phase: 06-dom-api-refactor*
      *UAT executed: [date]*
      *Reviewer: [reviewer handle]*

    Save the file. Do NOT execute the walkthrough yet — that's Task 3. Just create the skeleton.

    Do NOT include any Claude / AI references in the file content (per CLAUDE.md memory reminders).
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; [ -f .planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md ] &amp;&amp; grep -q '## UAT Step 1' .planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md &amp;&amp; grep -q '## UAT Step 5' .planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md &amp;&amp; grep -q 'status: pending' .planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md &amp;&amp; ! grep -iE 'claude|AI assist' .planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md` exists
    - The file contains exactly 5 mandatory step headings (`## UAT Step 1` through `## UAT Step 5`) and one optional step heading (`## UAT Step 6`)
    - The frontmatter contains `phase: 06-dom-api-refactor` and `status: pending`
    - The 5 setup-guide bolded strings from D-01 appear verbatim in UAT Step 3 (`"Secret address in iCal format"`, `"ICS link"`, `"Using the public link"`, `"Using the embed link"`, `"Missing the .ics extension"`)
    - `grep -iE 'claude|AI assist' .planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md` returns zero matches
    - No PNG / JPG / image binary files exist under `.planning/phases/06-dom-api-refactor/` (per D-15). Verified by `ls .planning/phases/06-dom-api-refactor/*.{png,jpg,jpeg,gif,bmp,webp} 2>/dev/null | wc -l` outputting 0.
  </acceptance_criteria>
  <done>
    `06-HUMAN-UAT.md` exists as a structured skeleton with all 5 mandatory + 1 optional UAT steps. Status is `pending`. No image artifacts are committed. No Claude / AI references.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute the 5-step UAT walkthrough and fill in pass/fail evidence</name>
  <files>.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md</files>
  <what-built>
    Task 2 created the `06-HUMAN-UAT.md` skeleton. Task 1 confirmed the freshly built plugin loads in the reviewer's test vault. The reviewer now executes the 5 mandatory UAT steps (plus the optional 6th if applicable), filling in:
    - **Result:** [x] Pass for steps that match v1.14.0 baseline; [x] Fail if any deviation observed
    - **Notes:** specific observation text for any deviation (or "no deviation observed" for Pass)
    - The Obsidian version in the Pre-Conditions section
    - The **Overall Acceptance** verdict at the bottom (PASS or FAIL)
    - The reviewer handle and date at the bottom
    - Change frontmatter `status: pending` → `status: complete` if Overall Acceptance is PASS

    If Overall Acceptance is FAIL, the reviewer documents the failing step(s) and the specific deviation(s). The Phase 6 closure then becomes contingent on either (a) a gap-closure plan (`/gsd-plan-phase 06 --gaps`) addressing the failure, or (b) the reviewer judging the deviation acceptable and recording the rationale.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md` in the editor.
    2. Execute UAT Step 1 (Sidebar Calendar) in the test vault. Fill in Result + Notes.
    3. Execute UAT Step 2 (Embedded Views). Fill in Result + Notes.
    4. Execute UAT Step 3 (Settings Tab) — the longest step, verifies D-01 setup-guide + D-06 color picker overlay + D-07/D-08/D-09 inline-style deletions. Fill in Result + Notes.
    5. Execute UAT Step 4 (Hide-Calendar Toggle) — verifies D-10. Fill in Result + Notes.
    6. Execute UAT Step 5 (Layout Regressions) at 350px / 400px / default widths. Fill in Result + Notes.
    7. (Optional) Execute UAT Step 6 (Mobile sanity check) or mark as Skipped with reason.
    8. Fill in the Pre-Conditions section: Obsidian version (`Obsidian → Help → About → App version`).
    9. Fill in the Overall Acceptance verdict.
    10. If PASS: change frontmatter `status: pending` → `status: complete`. Fill in date and reviewer handle.
    11. If FAIL: leave `status: pending`, document failing step(s), STOP and notify the orchestrator that gap closure may be needed.

    Resume signal: Type "UAT complete pass" (if all 5 steps pass), or "UAT complete fail: <brief>" (if any step fails). If the latter, the orchestrator decides next steps (gap closure plan, or accept the deviation with rationale).
  </how-to-verify>
  <resume-signal>Type "UAT complete pass" or "UAT complete fail: <brief reason>".</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit the HUMAN-UAT.md (assuming PASS verdict)</name>
  <files>(git commit only — no source changes in this task)</files>
  <read_first>
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-16 — exact commit subject line)
    - CLAUDE.md (Memory Reminders: NO Claude / AI references in commits)
  </read_first>
  <action>
    PRECONDITION: Task 3 returned "UAT complete pass". If it returned "UAT complete fail: ...", do NOT execute this task — the orchestrator handles the gap-closure path.

    Stage ONLY `.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md`. Create a single commit with the exact subject line from D-16:

      docs(06): record Phase 6 human UAT

    Commit body: 2-3 sentences referencing (1) the 5 UAT steps executed, (2) the Overall Acceptance verdict (PASS), (3) Phase 6 closure via ROADMAP success criterion #5. NO Claude / AI references.

    Use a HEREDOC for the commit message. Do NOT use `--no-verify`. Lint should still exit 0 at this commit (this is a docs-only change; the lint config is now active for DIR-02/03/04 rules, and no source files are touched).

    After commit, the Phase 6 commit graph at HEAD should show the 5-commit sequence from D-16 in reverse order:
      HEAD            docs(06): record Phase 6 human UAT
      HEAD~1          chore(lint): remove Phase 6 ESLint overrides (DIR-02/03/04 acceptance)
      HEAD~2          refactor(views): replace inline styles with CSS classes and setCssProps (DIR-03)
      HEAD~3          refactor(settings): replace document.createElement with createEl for color input (DIR-04)
      HEAD~4          refactor(settings): replace setup-guide innerHTML with createEl + appendText (DIR-02)

    (If Plan 03 was split per CONTEXT discretion, HEAD~2 and HEAD~3 may be DIR-03 / settings + DIR-03 / views in some order.)
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; git log -1 --pretty=%s | grep -qxF 'docs(06): record Phase 6 human UAT' &amp;&amp; ! git log -1 --pretty=%B | grep -iE 'claude|AI assist|Co-Authored-By' &amp;&amp; git diff HEAD~1 --name-only | grep -qx '.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md' &amp;&amp; git log --oneline -5</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` exactly matches `docs(06): record Phase 6 human UAT`
    - `git diff HEAD~1 --name-only` is exactly `.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md`
    - `git log -1 --pretty=%B` contains NO occurrence of "claude", "Claude", "AI", "assistant", "Co-Authored-By"
    - `git log --oneline -5` shows the 5-commit Phase-6 sequence matching D-16 ordering (allowing optional split of commit 3)
    - The file `06-HUMAN-UAT.md` at HEAD has `status: complete` in frontmatter and Overall Acceptance = PASS
  </acceptance_criteria>
  <done>
    The Phase-6 closure commit exists at HEAD with the exact D-16 subject line, contains only `06-HUMAN-UAT.md`, has no Claude / AI / Co-Authored-By references, and the 5-commit Phase-6 sequence matches D-16.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer's eyes → markdown evidence | The visual-parity verdict is human-judged. The risk is that subtle layout drift escapes a "looks fine" verdict. Mitigation: the UAT steps are scoped to the exact features Plan 03 modified (color dots, height application, error rendering, help button spacing, doc-link spacing, hide-calendar toggle, setup-guide bolding) so the reviewer is checking against a specific list, not a generic "everything still works." |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Repudiation | UAT evidence | mitigate | Each UAT step is documented with explicit Pass/Fail checkboxes, Notes field, and a per-step verification claim. The Overall Acceptance verdict at the bottom is the formal record. The reviewer handle and date are required fields. |
| T-06-13 | Tampering | UAT walkthrough completeness | mitigate | The 5 mandatory steps map 1:1 to CONTEXT D-14 + ROADMAP Phase 6 success criterion #5. Each step's "Verifies" line names the specific CONTEXT D-NN decision(s) being verified, so a missed step has a visible audit trail. |
</threat_model>

<verification>
- `.planning/phases/06-dom-api-refactor/06-HUMAN-UAT.md` exists at HEAD with `status: complete` and Overall Acceptance = PASS
- All 5 mandatory UAT step results are filled in (no remaining `[ ] Pass / [ ] Fail` placeholders)
- HEAD commit subject matches D-16 commit 5 exactly: `docs(06): record Phase 6 human UAT`
- The 5-commit Phase-6 sequence matches D-16 ordering
- ROADMAP Phase 6 success criterion #5 is recorded as satisfied in the HUMAN-UAT.md
</verification>

<success_criteria>
- ROADMAP Phase 6 success criterion #5 (manual UAT confirms visual parity with v1.14.0) is satisfied and documented.
- All 5 ROADMAP Phase 6 success criteria are now closed (grep #1, #2, #3 + lint passing #4 + UAT PASS #5).
- HEAD commit matches D-16 commit 5 exactly; no Claude / AI references.
- Phase 6 is fully complete; STATE.md and ROADMAP.md can be updated to reflect completion.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dom-api-refactor/06-05-SUMMARY.md` with:
- Link to `06-HUMAN-UAT.md` and the Overall Acceptance verdict
- The 5 step verdicts (each as one line: "Step N: PASS — <one-line summary>")
- The git commit SHA and subject
- Confirmation that the full 5-commit Phase-6 sequence exists in `git log --oneline -5`
- Confirmation that all 5 ROADMAP Phase 6 success criteria are now satisfied (with grep / lint / UAT evidence each)
- Hand-off to STATE.md update + Phase 7 planning
</output>
