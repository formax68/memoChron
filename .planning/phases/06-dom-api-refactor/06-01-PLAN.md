---
phase: 06-dom-api-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings/SettingsTab.ts
autonomous: true
requirements: [DIR-02]
tags: [obsidian-plugin, dom-refactor, createEl, appendText, innerHTML]

must_haves:
  truths:
    - "Every `.innerHTML` / `.outerHTML` write in `src/settings/SettingsTab.ts` is gone (5 sites at lines 1882, 1899, 1920, 1921, 1922)"
    - "The setup-guide `<li>` items still render with the same bolded action text as v1.14.0 (per D-01, D-02, D-14 step 3)"
    - "No helper function is introduced — each site is an inline `appendText` + `createEl('strong', { text })` rewrite per D-01"
  artifacts:
    - path: "src/settings/SettingsTab.ts"
      provides: "5 innerHTML sites rewritten using createEl + appendText"
      pattern: "li\\.appendText"
  key_links:
    - from: "src/settings/SettingsTab.ts (rewritten <li> sites)"
      to: "rendered Obsidian DOM"
      via: "createEl + appendText + createEl('strong', { text })"
      pattern: "createEl\\(\"strong\""
---

<objective>
Replace all 5 `<li>.innerHTML = "..."` writes in `SettingsTab.ts` with inline `appendText` + `createEl("strong", { text })` segment rewrites per CONTEXT D-01 and PATTERNS commit-1 spec. This is the FIRST atomic commit of Phase 6 per D-16, and it closes the DIR-02 finding for shipped code.

Purpose: DIR-02 is the simplest of the three Phase-6 findings and is isolated to one file. Landing it first (a) makes the per-requirement diff small and bisectable, (b) leaves the bulk DIR-03 commit (Plan 03) cleaner, (c) is independent of Plan 02 (DIR-04) so it could in principle parallelize — but per planning-context guidance we keep it in its own wave to avoid line-number drift in `SettingsTab.ts` when Plans 01–03 all edit the same file.

Output: 5 rewritten `<li>` blocks in `SettingsTab.ts`; one git commit with subject `refactor(settings): replace setup-guide innerHTML with createEl + appendText (DIR-02)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dom-api-refactor/06-CONTEXT.md
@.planning/phases/06-dom-api-refactor/06-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing in-file analog patterns the executor MUST follow exactly. -->
<!-- Source: SettingsTab.ts:1878-1886 (clean, shipping code that proves the target idiom). -->

Existing call-site idiom for plain-text `<li>` (already in the SAME function being modified):

    gcalSteps.createEl("li", { text: "Open Google Calendar in your browser" });
    gcalSteps.createEl("li", { text: "Click the gear icon (⚙️) → Settings" });

Existing call-site idiom for "bold within a sibling" (line 1885-1886, in the SAME function):

    gcalNote.createEl("strong", { text: "Correct URL looks like: " });
    gcalNote.createEl("code", { text: "https://calendar.google.com/calendar/ical/.../basic.ics" });

Obsidian HTMLElement extension signature:

    interface HTMLElement {
      appendText(text: string): void;
      createEl<K extends keyof HTMLElementTagNameMap>(tag: K, o?: DomElementInfo): HTMLElementTagNameMap[K];
    }

The `appendText` method is the documented Obsidian-API safe alternative for inserting raw text adjacent to child elements — it does NOT trigger `@microsoft/sdl/no-inner-html` or `no-unsanitized/property`. `appendText` is NEW to this codebase (zero existing uses); Phase 6 introduces it via these 5 sites.

Per-site segment spec (authoritative — PATTERNS commit-1 table):

| Line | Parent | Prefix text | Bold text | Suffix text |
|------|--------|-------------|-----------|-------------|
| 1882 | `gcalSteps` | `"Copy the "` | `"Secret address in iCal format"` | — |
| 1899 | `outlookSteps` | `"Copy the "` | `"ICS link"` | `" (not the HTML link)"` |
| 1920 | `mistakesList` | — | `"Using the public link"` | `" - This opens a webpage, not calendar data"` |
| 1921 | `mistakesList` | — | `"Using the embed link"` | `" - This is for embedding in websites"` |
| 1922 | `mistakesList` | — | `"Missing the .ics extension"` | `" - The URL should end with .ics"` |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite the 5 setup-guide innerHTML sites in SettingsTab.ts</name>
  <files>src/settings/SettingsTab.ts</files>
  <read_first>
    - src/settings/SettingsTab.ts (lines 1855-1947 — the entire "iCal URL setup guide" rendering block, so the executor sees the surrounding analogs at 1878-1886 before rewriting)
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-01, D-02, D-14 step 3 — exact per-site rewrite shape, no-helper rule, bolding-only confirmation)
    - .planning/phases/06-dom-api-refactor/06-PATTERNS.md (Commit 1 section, S-1 + S-2 patterns — analog idioms and per-site segment table)
  </read_first>
  <action>
    For each of the 5 violation sites in `src/settings/SettingsTab.ts`, replace the single-line `parent.createEl("li").innerHTML = "..."` write with a three-or-four-line per-site rewrite. Do NOT introduce a helper function — D-01 forbids it. Do NOT broaden into a generic rich-text helper (D-02). The `<strong>` tag is the ONLY inline tag in these strings — confirm via grep before rewriting.

    Per-site rewrites (use the exact strings from the segment table above; preserve bolding semantics exactly so visual output is identical to v1.14.0):

    Site 1 — line 1882 (parent: `gcalSteps`):
      - Replace the existing `gcalSteps.createEl("li").innerHTML = "Copy the <strong>Secret address in iCal format</strong>";` line.
      - Create a narrowly-scoped local (suggested name: `gcalStep5`) via `gcalSteps.createEl("li")`.
      - Call `gcalStep5.appendText("Copy the ");`
      - Call `gcalStep5.createEl("strong", { text: "Secret address in iCal format" });`
      - No suffix text on this site.

    Site 2 — line 1899 (parent: `outlookSteps`):
      - Local name suggestion: `outlookStep5`.
      - Prefix `"Copy the "`, bold `"ICS link"`, suffix `" (not the HTML link)"`.

    Site 3 — line 1920 (parent: `mistakesList`):
      - Local name suggestion: `mistakeItem1` (or `publicLinkMistake`).
      - No prefix; bold `"Using the public link"`; suffix `" - This opens a webpage, not calendar data"`.

    Site 4 — line 1921 (parent: `mistakesList`):
      - Local name suggestion: `mistakeItem2` (or `embedLinkMistake`).
      - No prefix; bold `"Using the embed link"`; suffix `" - This is for embedding in websites"`.

    Site 5 — line 1922 (parent: `mistakesList`):
      - Local name suggestion: `mistakeItem3` (or `missingExtMistake`).
      - No prefix; bold `"Missing the .ics extension"`; suffix `" - The URL should end with .ics"`.

    For the three `mistakesList` sites, the prefix is empty — start each `<li>` with the `createEl("strong", { text })` call, then `appendText(suffix)`. Match the existing in-file analog (SettingsTab.ts:1885-1886) ordering: bold first, plain text appended after, no leading `appendText("")` no-op.

    After the rewrite, the file must contain ZERO `.innerHTML` / `.outerHTML` write assignments. Run `grep -nE '\.(inner|outer)HTML\s*=' src/settings/SettingsTab.ts` to confirm zero matches before stopping (per D-16 commit 1 + ROADMAP success criterion #1).

    Do NOT touch lines outside the 5 rewrite sites. Do NOT modify the surrounding analog lines (1878-1881, 1885-1886, 1888-1898, 1904-1918). Do NOT modify `eslint.config.mjs` in this plan — the Phase-6 override block is removed in Plan 04 per D-13/D-16 commit 4.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; npm run build &gt; /tmp/build-06-01-t1.log 2&gt;&amp;1; rc=$?; tail -5 /tmp/build-06-01-t1.log; [ $rc -eq 0 ] &amp;&amp; [ "$(grep -cE '\.(inner|outer)HTML\s*=' src/settings/SettingsTab.ts)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\.(inner|outer)HTML\s*=' src/settings/SettingsTab.ts` outputs `0`
    - `git ls-files src/ | xargs grep -nE '\.(inner|outer)HTML\s*='` returns zero matches (the full repo success-criterion grep from ROADMAP)
    - `npm run build` exits 0 with no TypeScript errors
    - `grep -c 'appendText' src/settings/SettingsTab.ts` outputs at least `4` (sites 1, 2 use prefix appendText; sites 2, 3, 4, 5 use suffix appendText — minimum 4 expected calls; exact count depends on whether a prefix-only or suffix-only site collapses one call, but the minimum visual lower bound is 4)
    - `grep -c "createEl(\"strong\"" src/settings/SettingsTab.ts` outputs at least `6` (5 new sites + the existing analog at line 1885 = 6 minimum)
    - All 5 rewritten `<li>` blocks contain the EXACT strings from the segment table — verified by `grep -n "Secret address in iCal format" src/settings/SettingsTab.ts` returning 1 match, same for `"ICS link"`, `"Using the public link"`, `"Using the embed link"`, `"Missing the .ics extension"`
    - No new local function or helper is declared between the rewrites (per D-01 inline-only rule); confirm by reading the diff
  </acceptance_criteria>
  <done>
    All 5 innerHTML sites in `src/settings/SettingsTab.ts` are rewritten as per-segment `appendText` + `createEl("strong", { text })` blocks. `grep -nE '\.(inner|outer)HTML\s*='` returns zero matches across `src/`. `npm run build` passes. No helper function was introduced.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit the DIR-02 rewrite</name>
  <files>(git commit only — no source changes in this task)</files>
  <read_first>
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-16 — exact commit subject line; D-14 — no Claude/AI references rule reinforced from CLAUDE.md memory)
    - CLAUDE.md (Memory Reminders: "Do not include claude references on commits, or release notes")
  </read_first>
  <action>
    Stage ONLY `src/settings/SettingsTab.ts` (do not include any other modified file — Plan 01 is single-file by design). Create a single commit with the exact subject line from D-16:

      refactor(settings): replace setup-guide innerHTML with createEl + appendText (DIR-02)

    The commit body should be a 2-3 sentence summary explaining: (1) the 5 sites rewritten (file/line range), (2) the per-segment `appendText` + `createEl("strong", { text })` shape per D-01, (3) DIR-02 grep now returns zero matches across `src/`. Do NOT include any reference to Claude, AI, or assistant tooling in the commit message (per CLAUDE.md memory reminders).

    Use a HEREDOC for the commit message exactly as project convention requires:

      git commit -m "$(cat <<'EOF'
      refactor(settings): replace setup-guide innerHTML with createEl + appendText (DIR-02)

      [2-3 sentence body referencing DIR-02 success criterion #1 and the 5 sites at SettingsTab.ts:1882, 1899, 1920, 1921, 1922]
      EOF
      )"

    Do NOT use `--no-verify`. Pre-commit lint hook (if any) must pass — the file is still in the Phase-5 override block at this point so `npm run lint` is expected to pass cleanly.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; git log -1 --pretty=%s | grep -qxF 'refactor(settings): replace setup-guide innerHTML with createEl + appendText (DIR-02)' &amp;&amp; ! git log -1 --pretty=%B | grep -iE 'claude|AI assist|Co-Authored-By' &amp;&amp; git diff HEAD~1 --name-only | grep -qx 'src/settings/SettingsTab.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` exactly matches `refactor(settings): replace setup-guide innerHTML with createEl + appendText (DIR-02)`
    - `git diff HEAD~1 --name-only` is exactly `src/settings/SettingsTab.ts` (single-file commit)
    - `git log -1 --pretty=%B` contains NO occurrence of "claude", "Claude", "AI", "assistant" (case-insensitive grep returns no matches in body or trailer)
    - `git log -1 --pretty=%B` does NOT contain `Co-Authored-By:` lines referencing Claude or any AI tool
  </acceptance_criteria>
  <done>
    The DIR-02 commit exists at HEAD with the exact subject line from D-16, contains only `src/settings/SettingsTab.ts`, and has no Claude / AI references in subject, body, or trailers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied calendar URL → settings DOM | URL strings flow into the setup-guide rendering region but ARE NOT rewritten by Plan 01 — the 5 setup-guide strings are entirely static literals controlled by the plugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | The 5 rewritten `<li>` strings | accept | All 5 strings are compile-time literals in `SettingsTab.ts`; no user input flows into them. `appendText` + `createEl({ text })` are the documented safe alternatives to `innerHTML` (per `@microsoft/sdl/no-inner-html` rule rationale) — they treat their string argument as text, not HTML. Visual semantics preserved exactly. |
| T-06-02 | Information disclosure | Local variable names (`gcalStep5`, etc.) | accept | Function-scoped TypeScript locals; no information leakage. |
</threat_model>

<verification>
- `grep -nE '\.(inner|outer)HTML\s*=' src/settings/SettingsTab.ts` returns zero matches
- `git ls-files src/ | xargs grep -nE '\.(inner|outer)HTML\s*='` returns zero matches (canonical ROADMAP success-criterion #1 — verified at the repo level)
- `npm run build` exits 0
- `git log -1 --pretty=%s` matches the D-16 subject line exactly
- HUMAN UAT (deferred to Plan 05) will visually confirm the 5 `<li>` items still render with bolded action text identical to v1.14.0 (D-14 step 3)
</verification>

<success_criteria>
- DIR-02 success criterion (ROADMAP Phase 6 #1: zero `\.(inner|outer)HTML\s*=` matches across shipped `src/`) is satisfied repo-wide as a side-effect of this plan, because all 5 in-codebase violations are in `SettingsTab.ts` and Plan 01 removes them.
- The single commit at HEAD matches the D-16 subject line exactly.
- No regression to TypeScript compilation (`npm run build` clean).
- Plan 01 leaves `eslint.config.mjs` and all other files untouched — Plans 02–04 own the remaining Phase-6 changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dom-api-refactor/06-01-SUMMARY.md` with:
- The 5 sites rewritten (file + line range before edit, exact diff per site)
- `grep -nE '\.(inner|outer)HTML\s*=' src/settings/SettingsTab.ts` output (must be empty)
- The git commit SHA and subject
- Confirmation that no Claude / AI references appear in the commit message
- Hand-off to Plan 02 (DIR-04 / document.createElement rewrite)
</output>
