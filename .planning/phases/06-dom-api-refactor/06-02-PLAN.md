---
phase: 06-dom-api-refactor
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/settings/SettingsTab.ts
autonomous: true
requirements: [DIR-04]
tags: [obsidian-plugin, dom-refactor, createEl, document-createElement]

must_haves:
  truths:
    - "Every `document.createElement(...)` call in `SettingsTab.ts` is replaced with `createEl(...)` (2 sites at lines 652, 735) using the `createEl(\"input\", { type: \"color\", cls: ... })` shape per D-12"
    - "The 3 `document.createElementNS(SVG_NS, ...)` calls at lines 563, 568, 586 are UNCHANGED — SVG namespace is correctly excluded from DIR-04 per the `no-restricted-syntax` selector"
    - "The custom color-input UX is preserved exactly (24×24 overlay, click opens native color picker)"
  artifacts:
    - path: "src/settings/SettingsTab.ts"
      provides: "2 document.createElement sites rewritten using createEl({ type, cls })"
      pattern: "createEl\\(\"input\", \\{"
  key_links:
    - from: "customLabel.createEl('input', { type: 'color', cls: 'memochron-inline-color-input' })"
      to: "DOM color-input element under the swatch overlay"
      via: "Obsidian createEl helper (returns already-appended element)"
      pattern: "createEl\\(\"input\""
---

<objective>
Replace the 2 `document.createElement("input")` sites in `SettingsTab.ts` (lines 652 and 735) with `createEl("input", { type: "color", cls: "memochron-inline-color-input" })` per CONTEXT D-12 and PATTERNS commit-2 spec. This is the SECOND atomic commit of Phase 6 per D-16, and it closes the DIR-04 finding for shipped code (the only 2 violations live in this one file).

Purpose: DIR-04 has only 2 violation sites, both in the same file, both structurally identical. Landing it as its own commit keeps the per-requirement diff small. Plan 02 leaves the 10 inline-style lines per site (656-665, 739-748) in place — those go away in Plan 03 (DIR-03 bulk commit). This split is deliberate: each commit is independently reviewable and bisectable per D-16.

Output: 2 rewritten color-input creation sites in `SettingsTab.ts`; one git commit with subject `refactor(settings): replace document.createElement with createEl for color input (DIR-04)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dom-api-refactor/06-CONTEXT.md
@.planning/phases/06-dom-api-refactor/06-PATTERNS.md
@.planning/phases/06-dom-api-refactor/06-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- In-file analog the executor MUST follow exactly. -->
<!-- Source: SettingsTab.ts:1080 (clean, shipping code that proves the target idiom). -->

Existing call-site idiom (top-level `type:` option — the in-file dominant form, resolves CONTEXT D-12 Claude's-Discretion item):

    const checkbox = labelEl.createEl("input", { type: "checkbox" });

Obsidian `createEl` signature (relevant overload):

    interface HTMLElement {
      createEl<K extends keyof HTMLElementTagNameMap>(
        tag: K,
        o?: DomElementInfo | string,
        callback?: (el: HTMLElementTagNameMap[K]) => void,
      ): HTMLElementTagNameMap[K];
    }

    interface DomElementInfo {
      cls?: string | string[];
      text?: string | DocumentFragment;
      attr?: { [key: string]: string | number | boolean | null };
      type?: string;
      // ... (parent, prepend, etc.)
    }

`createEl(...)` returns an element ALREADY appended to its parent. The existing `customLabel.appendChild(colorInput)` calls at lines 666 and 749 become redundant after the rewrite and MUST be removed (per PATTERNS Commit 2 notes).

DIR-04 rule definition — from `eslint.config.mjs:53-61`, the `no-restricted-syntax` selector matches:

    CallExpression[callee.object.name='document'][callee.property.name='createElement']

Note: `createElement` (no `NS`). The 3 SVG sites at lines 563, 568, 586 use `document.createElementNS(SVG_NS, ...)` — the selector does NOT match. They stay (CONTEXT D-12 line 69, specifics line 181, PATTERNS commit-2 SVG-NOT-TO-TOUCH note).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite the 2 document.createElement sites in SettingsTab.ts</name>
  <files>src/settings/SettingsTab.ts</files>
  <read_first>
    - src/settings/SettingsTab.ts (lines 625-680 and lines 715-760 — the two color-picker rendering blocks, plus lines 555-590 to see the surrounding `createElementNS` SVG analog that MUST NOT be touched)
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-12 — exact rewrite shape; specifics line 181 — `createElementNS` exclusion)
    - .planning/phases/06-dom-api-refactor/06-PATTERNS.md (Commit 2 section — rewrite spec, in-file analog at line 1080, SVG-NOT-TO-TOUCH note)
    - eslint.config.mjs (lines 53-61 — the `no-restricted-syntax` selector that defines DIR-04's scope)
  </read_first>
  <action>
    Rewrite the 2 `document.createElement("input")` sites in `src/settings/SettingsTab.ts`. The 3 `document.createElementNS(SVG_NS, ...)` sites at lines 563, 568, 586 MUST be left UNTOUCHED — they are SVG namespace creations and are NOT flagged by DIR-04's `no-restricted-syntax` selector (which matches only `createElement`, not `createElementNS`). Verify before editing: run `grep -n 'document\.createElementNS' src/settings/SettingsTab.ts` and confirm the 3 SVG hits are at 563/568/586; do NOT modify any of those lines.

    Site 1 (currently around line 652, parent: `customLabel`, inside `renderInlineColorPicker`): replace the 4 setup lines (`document.createElement("input")` creation + `.type = "color"` assignment + `.className = "memochron-inline-color-input"` assignment + the trailing `customLabel.appendChild(colorInput)` call at line 666) with a single `customLabel.createEl("input", { type: "color", cls: "memochron-inline-color-input" })` call. The `colorInput.value = this.colorToHex(currentColor)` assignment STAYS — it is a property write, not a DOM creation, and is not flagged. Use the top-level `type:` option (matches the in-file dominant form at `SettingsTab.ts:1080`). Use the `cls:` option for the class name (folds the existing `.className =` assignment).

    Site 2 (currently around line 735, parent: `customLabel`, inside `renderDailyNoteColorPicker`): structurally identical to Site 1; apply the same rewrite. Remove the `customLabel.appendChild(colorInput);` call that follows the inline-style block (currently around line 749). The `colorInput.value = this.colorToHex(currentColor)` line STAYS.

    Confirm post-edit:
      - `grep -nE '\bdocument\.createElement\b[^N]' src/settings/SettingsTab.ts` returns zero matches (note the `\b` word boundary and the negated-N character class — this MUST NOT match `createElementNS`)
      - `grep -n 'document\.createElementNS' src/settings/SettingsTab.ts` STILL returns the 3 unchanged SVG hits at 563, 568, 586
      - `npm run build` exits 0

    Do NOT touch the inline styles at lines 648-651, 656-665, 731-734, 739-748 — those are the DIR-03 surface area, removed in Plan 03. Do NOT touch `eslint.config.mjs` — Plan 04 owns that. Do NOT touch any other file.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; npm run build &gt; /tmp/build-06-02-t1.log 2&gt;&amp;1; rc=$?; tail -5 /tmp/build-06-02-t1.log; [ $rc -eq 0 ] &amp;&amp; [ "$(grep -cE '\bdocument\.createElement\b[^N]' src/settings/SettingsTab.ts)" = "0" ] &amp;&amp; [ "$(grep -c 'document\.createElementNS' src/settings/SettingsTab.ts)" = "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\bdocument\.createElement\b[^N]' src/settings/SettingsTab.ts` returns zero matches (word-boundary + not-followed-by-N to exclude `createElementNS`)
    - `git ls-files src/ | xargs grep -n 'document\.createElement[^N]'` returns zero matches across the entire `src/` tree (the ROADMAP success-criterion #3 grep, made `createElementNS`-safe)
    - `grep -c 'document\.createElementNS' src/settings/SettingsTab.ts` outputs `3` (lines 563, 568, 586 unchanged)
    - `grep -c 'customLabel\.createEl("input"' src/settings/SettingsTab.ts` outputs `2` (the 2 new sites)
    - `grep -c 'customLabel\.appendChild(colorInput)' src/settings/SettingsTab.ts` outputs `0` (both legacy append calls removed)
    - `npm run build` exits 0 with no TypeScript errors
    - `git diff HEAD --name-only` lists ONLY `src/settings/SettingsTab.ts` (no other file modified)
  </acceptance_criteria>
  <done>
    Both `document.createElement("input")` sites in `SettingsTab.ts` are rewritten as `customLabel.createEl("input", { type: "color", cls: "memochron-inline-color-input" })`. The redundant `customLabel.appendChild(colorInput)` calls are removed. The 3 SVG `createElementNS` sites are unchanged. `npm run build` passes. DIR-04 success-criterion grep returns zero matches across `src/`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit the DIR-04 rewrite</name>
  <files>(git commit only — no source changes in this task)</files>
  <read_first>
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-16 — exact commit subject line)
    - CLAUDE.md (Memory Reminders: NO Claude / AI references in commits)
  </read_first>
  <action>
    Stage ONLY `src/settings/SettingsTab.ts`. Create a single commit with the exact subject line from D-16:

      refactor(settings): replace document.createElement with createEl for color input (DIR-04)

    Commit body: 2-3 sentences referencing (1) the 2 sites at SettingsTab.ts:652 and 735, (2) the `createEl({ type, cls })` shape per D-12 + PATTERNS S-1, (3) the SVG `createElementNS` sites at 563/568/586 explicitly left untouched per the DIR-04 selector scope, (4) DIR-04 grep now returns zero matches across `src/`. NO Claude / AI references (per CLAUDE.md memory reminders).

    Use a HEREDOC for the commit message (heredoc body must NOT mention Claude, AI, assistant, Co-Authored-By Claude, etc.). Do NOT use `--no-verify`. The file is in the Phase-5 override block for DIR-04's `no-restricted-syntax` rule (override still active in Plan 02 — Plan 04 removes it), so lint passes either way at this point.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; git log -1 --pretty=%s | grep -qxF 'refactor(settings): replace document.createElement with createEl for color input (DIR-04)' &amp;&amp; ! git log -1 --pretty=%B | grep -iE 'claude|AI assist|Co-Authored-By' &amp;&amp; git diff HEAD~1 --name-only | grep -qx 'src/settings/SettingsTab.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` exactly matches `refactor(settings): replace document.createElement with createEl for color input (DIR-04)`
    - `git diff HEAD~1 --name-only` is exactly `src/settings/SettingsTab.ts` (single-file commit)
    - `git log -1 --pretty=%B` contains NO occurrence of "claude", "Claude", "AI", "assistant", "Co-Authored-By" (case-insensitive grep returns no matches in body or trailer)
  </acceptance_criteria>
  <done>
    The DIR-04 commit exists at HEAD with the exact D-16 subject line, contains only `src/settings/SettingsTab.ts`, and has no Claude / AI / Co-Authored-By references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied color value → DOM `<input type="color">` element | The `colorInput.value = this.colorToHex(currentColor)` write feeds a hex string (validated upstream by Phase 2 `isValidColor`) into the native `<input type="color">`. The element is created by Obsidian's `createEl` helper (no innerHTML path); the value setter is safe by browser contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | The 2 rewritten color-input creation sites | accept | `createEl({ type, cls })` is the Obsidian-API canonical safe alternative to `document.createElement`. The string options (`"color"`, `"memochron-inline-color-input"`) are compile-time literals; no user input flows into the `type` or `cls` paths. |
| T-06-04 | Information disclosure | The 3 SVG `createElementNS` sites left untouched | accept | DIR-04's `no-restricted-syntax` selector does NOT match `createElementNS`. Per CONTEXT D-12 and PATTERNS commit-2 SVG-NOT-TO-TOUCH note, the SVG construction is the correct API for the namespace and stays. |
</threat_model>

<verification>
- `grep -nE '\bdocument\.createElement\b[^N]' src/settings/SettingsTab.ts` returns zero matches
- `git ls-files src/ | xargs grep -n 'document\.createElement[^N]'` returns zero matches (canonical ROADMAP success-criterion #3 — verified at the repo level, `createElementNS`-safe)
- `grep -c 'document\.createElementNS' src/settings/SettingsTab.ts` outputs `3`
- `npm run build` exits 0
- `git log -1 --pretty=%s` matches the D-16 subject line exactly
- HUMAN UAT (deferred to Plan 05) will visually confirm the custom color overlay still opens the native picker on click and applies the chosen color (D-14 step 3)
</verification>

<success_criteria>
- DIR-04 success criterion (ROADMAP Phase 6 #3: zero `document.createElement` matches across shipped `src/`) is satisfied repo-wide as a side-effect of this plan, because both in-codebase violations are in `SettingsTab.ts` and Plan 02 removes them.
- The single commit at HEAD matches the D-16 subject line exactly.
- No regression to TypeScript compilation (`npm run build` clean).
- The 3 SVG `createElementNS` sites remain unchanged (DIR-04 selector scope).
- Plan 02 leaves `eslint.config.mjs` and the DIR-03 inline-style lines untouched — Plans 03–04 own the remaining Phase-6 changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dom-api-refactor/06-02-SUMMARY.md` with:
- The 2 sites rewritten (file + line range before edit, exact diff per site)
- Confirmation that the 3 SVG `createElementNS` sites are unchanged (grep output verifying 3 hits at 563/568/586)
- `grep -nE '\bdocument\.createElement\b[^N]' src/settings/SettingsTab.ts` output (must be empty)
- The git commit SHA and subject
- Confirmation that no Claude / AI references appear in the commit message
- Hand-off to Plan 03 (DIR-03 bulk — CSS classes + setCssProps + class toggle)
</output>
