---
phase: 06-dom-api-refactor
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - src/utils/viewRenderers.ts
  - src/views/CalendarView.ts
  - src/views/EmbeddedCalendarView.ts
  - src/views/EmbeddedAgendaView.ts
  - src/settings/SettingsTab.ts
  - styles.css
autonomous: true
requirements: [DIR-03]
tags: [obsidian-plugin, dom-refactor, setCssProps, css-classes, toggleClass]

must_haves:
  truths:
    - "Zero `.style.<flagged-property>` assignments remain across shipped `src/` (the 15 properties: border, color, cursor, display, fontSize, height, left, margin, marginTop, opacity, padding, position, textAlign, top, width)"
    - "3 dynamic-color sites use `setCssProps({ color: ... })` — viewRenderers.ts:320, CalendarView.ts:661, CalendarView.ts:670 (D-04)"
    - "3 dynamic-height sites use `setCssProps({ height: ... })` — CalendarView.ts:202, 1184, 1227 (D-05)"
    - "`updateCalendarVisibility` (CalendarView.ts ~980-1000) uses a CSS class toggle on `.memochron-hidden` instead of `style.display` writes (D-10, D-11)"
    - "The 2 duplicated color-input overlay clusters (SettingsTab.ts ~648-665, ~731-748) collapse into shared CSS class usage (D-06) via augmentation of the EXISTING `.memochron-inline-color-custom-label` rule — NO new wrapper class is introduced"
    - "The 2 error-message inline-style sites (SettingsTab.ts ~304-306, ~890-892) are deleted — the existing `.memochron-error-message` class already applies and is augmented to preserve the `--text-error` fallback exactly (D-07)"
    - "The 2 help-button inline-style lines (SettingsTab.ts ~317-318) are deleted — the existing `.memochron-help-btn` rule is augmented with margin-top/font-size (D-08)"
    - "The 3 doc-link / button-container inline-style lines (SettingsTab.ts ~1926, ~1935-1936) are deleted — existing class rules augmented or new `.memochron-help-buttons` rule added (D-09)"
    - "The `EmbeddedAgendaView.ts` `style.setProperty('--event-color', ...)` writes at lines 263, 297 remain UNCHANGED — CSS custom-property writes are NOT in DIR-03's banned 15-property list (PATTERNS classification)"
  artifacts:
    - path: "src/utils/viewRenderers.ts"
      provides: "Dynamic dot color via setCssProps (1 site)"
      pattern: "setCssProps\\(\\s*\\{\\s*color"
    - path: "src/views/CalendarView.ts"
      provides: "Dynamic color (2 sites), dynamic height (3 sites), CSS-class toggle visibility (1 function)"
      pattern: "setCssProps\\(|memochron-hidden"
    - path: "styles.css"
      provides: "New `.memochron-hidden` utility class; `.memochron-error-message` fallback preservation; `.memochron-help-btn`/`.memochron-help-doc-link`/`.memochron-help-buttons` augmentations or additions; augmentation of the existing `.memochron-inline-color-custom-label` rule with wrapper geometry per D-06 (no new wrapper class is added)"
      pattern: "memochron-hidden|memochron-help-buttons"
    - path: "src/settings/SettingsTab.ts"
      provides: "All flagged inline-style assignments removed across error-message, help-button, color-input overlay, doc-link, button-container clusters"
      pattern: "memochron-inline-color-custom-label|memochron-inline-color-input"
  key_links:
    - from: "CalendarView.handleDragMove / snapToCurrentViewMode / initial-apply"
      to: "this.calendar height styling"
      via: "setCssProps({ height: `${px}px` })"
      pattern: "setCssProps\\(\\s*\\{\\s*height"
    - from: "viewRenderers.renderCalendarGrid + CalendarView per-event dot renderer"
      to: "event-color dot styling"
      via: "setCssProps({ color: event.color })"
      pattern: "setCssProps\\(\\s*\\{\\s*color"
    - from: "CalendarView.updateCalendarVisibility"
      to: ".memochron-hidden CSS class toggle on calendar / resizeHandle / controls"
      via: "classList.toggle('memochron-hidden', hide) (or el.toggleClass if supported)"
      pattern: "memochron-hidden"
---

<objective>
Single bulk commit replacing every `.style.<flagged-property> =` assignment across the 5 in-scope source files with either (a) `setCssProps({ ... })` for dynamic values per D-03/D-04/D-05, (b) CSS class toggles for boolean visibility per D-10/D-11, or (c) deletion of redundant inline styles in favor of existing or augmented CSS classes per D-06/D-07/D-08/D-09. Plus all required additions / augmentations to `styles.css`. This is the THIRD atomic commit of Phase 6 per D-16 — the bulk commit — and closes the DIR-03 finding for shipped code.

Purpose: DIR-03 has 51 violation sites across 3 files (8 in CalendarView, 1 in viewRenderers, 42 in SettingsTab). They cluster into 7 logical sub-rewrites (3a–3g in PATTERNS). One commit covers all 7 because they touch the same set of files and splitting per-sub-rewrite would multiply review surface for no gain. Per CONTEXT D-16 Claude's-Discretion, the planner has discretion to split this commit if the diff becomes unreviewable, but the default is single commit. We use single commit.

Output: 5 source files + `styles.css` modified; one git commit with subject `refactor(views): replace inline styles with CSS classes and setCssProps (DIR-03)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dom-api-refactor/06-CONTEXT.md
@.planning/phases/06-dom-api-refactor/06-PATTERNS.md
@.planning/phases/06-dom-api-refactor/06-01-SUMMARY.md
@.planning/phases/06-dom-api-refactor/06-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing in-file analogs and Obsidian-API contracts the executor MUST follow. -->

Obsidian HTMLElement extension (NEW APIs introduced by Phase 6):

    interface HTMLElement {
      setCssProps(props: Partial<CSSStyleDeclaration>): void;
      toggleClass(name: string, force?: boolean): void;
      addClass(...names: string[]): void;
      removeClass(...names: string[]): void;
    }

`setCssProps` is the Obsidian-API-documented substitute for direct `style.X =` assignments. It resolves to `setProperty` under the hood — wall-clock cost is identical to direct property assignment, including in the drag hot loop (per CONTEXT D-05).

Existing in-file analogs (target idioms — already shipping clean):

CSS class toggle with `force` argument (in the SAME function modified by D-10):

    // CalendarView.ts:991, 998 (existing, clean)
    this.agenda.classList.add("agenda-only");
    this.agenda.classList.remove("agenda-only");

    // SettingsTab.ts:244 (existing, clean) — single-call form
    itemEl.classList.toggle("disabled", !value);

    // CalendarView.ts:1175 (existing, clean) — Obsidian addClass form
    this.resizeHandle.addClass("dragging");

Existing CSS classes that ALREADY APPLY at the call sites (per PATTERNS):

    /* styles.css:1215-1227 (existing) — applied at SettingsTab.ts:315 */
    .memochron-help-btn {
      background: var(--interactive-accent);
      color: var(--text-on-accent);
      border: none;
      border-radius: var(--radius-s);
      padding: var(--size-4-1) var(--size-4-2);
      cursor: pointer;
      transition: background-color 0.15s ease;
    }

    /* styles.css:1279-1281 (existing) — applied at SettingsTab.ts:1925 */
    .memochron-help-doc-link {
      text-align: center;
    }

    /* styles.css:1292-1296 (existing) — applied at SettingsTab.ts:302 and 887 */
    .memochron-error-message {
      color: var(--text-error);
      font-size: 0.9em;
      margin-top: var(--size-4-2);
    }

    /* styles.css:1205-1208 (existing) — applied at SettingsTab.ts:632, 715 */
    .memochron-inline-color-custom-label {
      /* (existing properties — DO NOT REMOVE EXISTING; only augment if needed) */
    }

Per-cluster spec (PATTERNS commit 3, sub-sections 3a–3g — authoritative):

3a. Dynamic colors (3 sites):
  - src/utils/viewRenderers.ts:320  →  `dot.setCssProps({ color: event.color });`
  - src/views/CalendarView.ts:661   →  `dailyNoteDot.setCssProps({ color: dailyNoteColor });`
  - src/views/CalendarView.ts:670   →  `dot.setCssProps({ color: event.color });`

3b. Dynamic heights (3 sites in CalendarView.ts):
  - Line 202   (initial apply of saved `calendarHeight` setting)
  - Line 1184  (drag hot loop in `handleDragMove`)
  - Line 1227  (final snap in `snapToCurrentViewMode`)
  Uniform shape:
      this.calendar.setCssProps({ height: `${px}px` });

3c. Color-input overlay clusters (SettingsTab.ts ~648-666 and ~731-749 — 4 customLabel.style.* lines + 10 colorInput.style.* lines per site, ~26 lines deleted total):
  - **CSS STRATEGY (PINNED — lower-diff option per PATTERNS):** augment the EXISTING `.memochron-inline-color-custom-label` rule in `styles.css` with the wrapper geometry (`position: relative; display: inline-block; width: 24px; height: 24px;`). The wrapper class is ALREADY applied to `customLabel` at lines 632/715 — NO new class is introduced on the wrapper, and `customLabel.addClass(...)` is NOT called at the call site for the wrapper geometry.
  - **DO NOT introduce a new `.memochron-custom-color-wrapper` class.** The alternative wrapper-class approach mentioned in CONTEXT D-06 / PATTERNS line 167 is explicitly NOT chosen here per checker feedback — pin one strategy so Task 1 (CSS) and Task 2 (call sites) agree.
  - The colorInput element (created in Plan 02 with `cls: "memochron-inline-color-input"`) has a CSS rule needed for its 10 overlay properties. Confirm before editing whether `.memochron-inline-color-input` already exists in `styles.css` (grep at task start). If it does NOT contain the overlay geometry, add a new CSS rule (or augment the existing one) with the 10 properties (position/top/left/width/height/opacity/cursor/border/padding/margin) per PATTERNS spec.
  - Delete all 14 inline-style lines per site (4 on `customLabel`, 10 on `colorInput`) — 28 lines total across the 2 sites.

3d. Error-message styling (SettingsTab.ts ~304-306, ~890-892):
  - The `.memochron-error-message` class ALREADY EXISTS and is ALREADY APPLIED at both sites (lines 302, 887). The 3 inline-style lines per site (`color`, `fontSize`, `marginTop`) are pure duplication.
  - Action: DELETE the 6 inline-style lines (3 per site). Augment the existing `.memochron-error-message` CSS rule to use the `var(--text-error, #c92424)` fallback (was `var(--text-error)`) — single-line CSS change to preserve the fallback per CONTEXT D-07.

3e. Help-button styling (SettingsTab.ts ~317-318):
  - The `.memochron-help-btn` class ALREADY EXISTS and is ALREADY APPLIED at line 315. Augment the existing rule with `margin-top: 0.5em;` and `font-size: 0.85em;` per D-08. Delete the 2 inline-style lines.
  - PATTERNS notes that the class rule applies to ALL `.memochron-help-btn` instances (currently only one). CONTEXT D-08 calls this "trivial" — accept the augmentation as the lower-diff option.

3f. Doc-link + button-container spacing (SettingsTab.ts ~1926, ~1935-1936):
  - Augment the existing `.memochron-help-doc-link` CSS rule with `margin-top: 1em;`. Delete line 1926.
  - The `.memochron-help-buttons` class does NOT yet exist in `styles.css` (verified by PATTERNS grep). Add a NEW rule `.memochron-help-buttons { margin-top: 1.5em; text-align: right; }`. The class is already applied at line 1934. Delete lines 1935-1936.

3g. Display-toggle migration (CalendarView.ts ~980-1000, `updateCalendarVisibility`):
  - Add a NEW utility class `.memochron-hidden { display: none; }` to `styles.css`.
  - Rewrite the function to use `classList.toggle("memochron-hidden", hide)` on `this.calendar`, `this.resizeHandle` (if non-null), and `controls` (if non-null). The existing `this.agenda.classList.add/remove("agenda-only")` stays — `agenda-only` carries different semantics per CONTEXT D-10. (PATTERNS recommends collapsing both branches via the `force` argument; the existing `SettingsTab.ts:244` analog uses `classList.toggle(name, force)`.)
  - Prefer Obsidian's `el.toggleClass(name, force)` if `@types/obsidian` for the installed version exposes it; fall back to `classList.toggle(name, force)` otherwise. The in-file analog uses `classList.toggle` so the fallback path is already proven (no codebase precedent for `el.toggleClass(name, force)`).

3h. EmbeddedAgendaView CSS custom-property writes (DO NOT TOUCH):
  - `EmbeddedAgendaView.ts:263, 297` write `style.setProperty("--event-color", ...)`. CSS custom-property writes are NOT in DIR-03's banned 15-property list per PATTERNS classification. The DIR-03 grep at ROADMAP success-criterion #2 does NOT match them. They stay.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add CSS class definitions and augment existing rules in styles.css</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (lines 1175-1296 — full color picker + help button + help modal + error message sections; grep for `\.memochron-` namespace inventory to choose placement of new classes)
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-06 through D-11 — exact CSS rule bodies expected)
    - .planning/phases/06-dom-api-refactor/06-PATTERNS.md (Commit 3 sub-sections 3c, 3d, 3e, 3f, 3g — exact CSS additions/augmentations + existing-rule line numbers)
  </read_first>
  <action>
    Make CSS-only changes in `styles.css`. All changes are additive or single-line augmentations to existing rules. No existing rules are removed or restructured. **No new `.memochron-custom-color-wrapper` class is introduced — the wrapper geometry is added by augmenting the existing `.memochron-inline-color-custom-label` rule.**

    3d. Augment the existing `.memochron-error-message` rule (around styles.css:1292): change the `color: var(--text-error);` declaration to `color: var(--text-error, #c92424);` so the hex fallback from the deleted inline-style lines is preserved exactly per D-07.

    3e. Augment the existing `.memochron-help-btn` rule (around styles.css:1215): append `margin-top: 0.5em;` and `font-size: 0.85em;` to the rule body per D-08.

    3f. Augment the existing `.memochron-help-doc-link` rule (around styles.css:1279): append `margin-top: 1em;` to the rule body per D-09. Then ADD a new rule (place immediately after the existing `.memochron-help-doc-link` block, before `.memochron-error-message`):

        .memochron-help-buttons {
          margin-top: 1.5em;
          text-align: right;
        }

    3c (wrapper). Augment the existing `.memochron-inline-color-custom-label` rule (around styles.css:1205) to add the wrapper geometry per D-06: ensure the rule body contains `position: relative; display: inline-block; width: 24px; height: 24px;`. If the existing rule already sets any of these properties, preserve the existing value (do not duplicate). The intent is that `customLabel` is the wrapper after Plan 03 — NO second class on `customLabel`, and Task 2's call-site code MUST NOT call `customLabel.addClass("memochron-custom-color-wrapper")` (no such class exists).

    3c (input). Ensure `.memochron-inline-color-input` rule contains the overlay properties per D-06. Run `grep -nE '^\.memochron-inline-color-input\b' styles.css` BEFORE editing. If the rule exists, augment it to include any missing overlay properties (position: absolute; top: 0; left: 0; width: 24px; height: 24px; opacity: 0; cursor: pointer; border: none; padding: 0; margin: 0). If the rule does NOT exist, create it as a new block immediately after `.memochron-inline-color-custom-label`. All 10 properties must be present so that the inline-style deletions in Task 2 do not change visual output.

    3g. Add a NEW utility class at the bottom of `styles.css` (or in a new "Utilities" comment-section if the file ends with section comments):

        /* Utility classes */
        .memochron-hidden {
          display: none;
        }

    No other CSS changes. Do NOT remove or restructure any existing rule. Do NOT add `.memochron-custom-color-wrapper`.

    After edits, run `npx eslint styles.css 2>&amp;1 || true` to confirm CSS is well-formed (build will catch syntax errors, but a sanity check is cheap), and proceed.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; grep -q '^\.memochron-hidden' styles.css &amp;&amp; grep -q '^\.memochron-help-buttons' styles.css &amp;&amp; grep -q 'var(--text-error, #c92424)' styles.css &amp;&amp; grep -A2 '^\.memochron-help-btn ' styles.css | grep -q '0\.85em\|font-size' &amp;&amp; grep -A3 '^\.memochron-help-doc-link ' styles.css | grep -q '1em\|margin-top' &amp;&amp; ! grep -q 'memochron-custom-color-wrapper' styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^\.memochron-hidden\s*\{' styles.css` returns exactly 1 match
    - `grep -nE '^\.memochron-help-buttons\s*\{' styles.css` returns exactly 1 match
    - `grep -n 'var(--text-error, #c92424)' styles.css` returns at least 1 match (the augmented `.memochron-error-message` color line)
    - `grep -nE '^\.memochron-error-message\s*\{' styles.css` returns exactly 1 match (existing rule, unchanged count)
    - `grep -nE '^\.memochron-help-btn\s*\{' styles.css` returns exactly 1 match (existing rule, augmented body)
    - `grep -nE '^\.memochron-help-doc-link\s*\{' styles.css` returns exactly 1 match (existing rule, augmented body)
    - `grep -nE '^\.memochron-inline-color-input\s*\{' styles.css` returns exactly 1 match with the 10 overlay properties present in its rule body (verified by counting `position:|top:|left:|width:|height:|opacity:|cursor:|border:|padding:|margin:` lines within the rule)
    - `grep -nE '^\.memochron-inline-color-custom-label\s*\{' styles.css` returns exactly 1 match with `position: relative` and `width: 24px` present in its rule body
    - `grep -c 'memochron-custom-color-wrapper' styles.css` outputs `0` (the alternative wrapper-class strategy is NOT used)
    - No existing CSS rule was removed (line count delta is positive; verified by `git diff --stat styles.css`)
  </acceptance_criteria>
  <done>
    `styles.css` contains the new `.memochron-hidden` and `.memochron-help-buttons` classes plus augmentations to `.memochron-error-message` (fallback color), `.memochron-help-btn` (margin-top + font-size), `.memochron-help-doc-link` (margin-top), `.memochron-inline-color-custom-label` (wrapper geometry), and `.memochron-inline-color-input` (overlay geometry). No `.memochron-custom-color-wrapper` rule is added. All other rules are untouched. The file remains parseable (no syntax errors).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite all 51 inline-style sites and the visibility toggle across the 5 source files</name>
  <files>src/utils/viewRenderers.ts, src/views/CalendarView.ts, src/views/EmbeddedCalendarView.ts, src/views/EmbeddedAgendaView.ts, src/settings/SettingsTab.ts</files>
  <read_first>
    - src/utils/viewRenderers.ts (lines 300-330 — the `renderCalendarGrid` per-event dot site)
    - src/views/CalendarView.ts (lines 195-215, 650-685, 975-1010, 1175-1234 — all 8 violation regions)
    - src/views/EmbeddedCalendarView.ts (full file — re-scan for any new inline-style writes added since codebase map; PATTERNS confirms zero direct violations as of mapping date)
    - src/views/EmbeddedAgendaView.ts (full file — re-scan; the `style.setProperty("--event-color", ...)` writes at lines 263, 297 MUST be left untouched per PATTERNS 3h)
    - src/settings/SettingsTab.ts (lines 295-330, 640-760, 880-905, 1855-1947 — all violation regions)
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-03 through D-11 — exact rewrite shapes)
    - .planning/phases/06-dom-api-refactor/06-PATTERNS.md (Commit 3 sub-sections 3a–3g — per-cluster rewrite spec)
  </read_first>
  <action>
    Before starting, run the violation grep across the entire `src/` tree to capture the current state for hand-off comparison:

        git ls-files src/ | xargs grep -nE '\.style\.(border|color|cursor|display|fontSize|height|left|margin|marginTop|opacity|padding|position|textAlign|top|width)\s*='

    PATTERNS records 51 hits as of mapping date. Plan 03 reduces this to 0. If the grep returns hits in `EmbeddedCalendarView.ts` or `EmbeddedAgendaView.ts` that PATTERNS did NOT list, fold them into the appropriate sub-cluster below — those views are in scope per ROADMAP.

    Execute all 7 sub-cluster rewrites in a single coherent edit pass (the 5 files are file-level isolated; line-level edits within each file can be done one cluster at a time):

    3a. Dynamic colors (3 sites) — replace `dot.style.color = X;` with `dot.setCssProps({ color: X });`:
      - `src/utils/viewRenderers.ts:320` → `dot.setCssProps({ color: event.color });`
      - `src/views/CalendarView.ts:661` → `dailyNoteDot.setCssProps({ color: dailyNoteColor });`
      - `src/views/CalendarView.ts:670` → `dot.setCssProps({ color: event.color });`

    3b. Dynamic heights (3 sites in CalendarView.ts) — replace `this.calendar.style.height = "Npx";` with `this.calendar.setCssProps({ height: "Npx" });`:
      - Line 202   `this.calendar.setCssProps({ height: \`${this.plugin.settings.calendarHeight}px\` });`
      - Line 1184  `this.calendar.setCssProps({ height: \`${newHeight}px\` });`  (drag hot loop — wall-clock cost identical, per D-05)
      - Line 1227  `this.calendar.setCssProps({ height: \`${idealHeight}px\` });`

    3c. Color-input overlay clusters (SettingsTab.ts ~648-665, ~731-748) — DELETE 4 `customLabel.style.*` lines + 10 `colorInput.style.*` lines per site (~28 lines total). The class assignments are already in place: `customLabel` ALREADY has `cls: "memochron-inline-color-custom-label"` from lines 632/715 (the wrapper geometry is now carried by Task 1's augmentation of that existing rule), and `colorInput` was given `cls: "memochron-inline-color-input"` in Plan 02 (its overlay geometry is carried by Task 1's `.memochron-inline-color-input` rule). NO call-site additions are needed — specifically, **DO NOT** call `customLabel.addClass("memochron-custom-color-wrapper")` (no such class exists in `styles.css` per the pinned CSS strategy in Task 1). The 14 inline-style deletions per site are the entirety of the call-site change for cluster 3c.

    3d. Error-message styling — DELETE the 6 inline-style lines (3 per site) at SettingsTab.ts:304-306 and 890-892. The `.memochron-error-message` class is already applied at lines 302 and 887; Task 1's CSS augmentation preserves the `--text-error` fallback exactly.

    3e. Help-button styling — DELETE the 2 inline-style lines at SettingsTab.ts:317-318. Task 1 augmented `.memochron-help-btn` with the margin-top and font-size properties.

    3f. Doc-link + button-container spacing — DELETE lines 1926 and 1935-1936 in SettingsTab.ts. Task 1 added the `margin-top: 1em` to `.memochron-help-doc-link` and added the new `.memochron-help-buttons` rule.

    3g. Display-toggle migration in `CalendarView.updateCalendarVisibility` (around lines 980-1000):
      Replace the if/else block currently writing `style.display = "none"` / `style.display = ""` on `this.calendar`, `this.resizeHandle`, and `controls` with a single boolean-driven class toggle. Use this exact shape (matches the in-file `agenda-only` analog at the same call site, the `SettingsTab.ts:244` `classList.toggle` analog, and the new `.memochron-hidden` class from Task 1):

          const hide = this.plugin.settings.hideCalendar;
          this.calendar.classList.toggle("memochron-hidden", hide);
          if (this.resizeHandle) this.resizeHandle.classList.toggle("memochron-hidden", hide);
          if (controls) controls.classList.toggle("memochron-hidden", hide);
          this.agenda.classList.toggle("agenda-only", hide);

      The `this.agenda` line collapses the existing `add("agenda-only")` / `remove("agenda-only")` pair into a single `toggle(name, force)` call — same behavior, cleaner shape, matches the in-file `SettingsTab.ts:244` form. Per CONTEXT D-10 the `agenda-only` class semantics are preserved exactly (layout reflow, not visibility). Per CONTEXT D-11, do NOT use `el.show()` / `el.hide()` — those would re-trigger `obsidianmd/no-static-styles-assignment`.

      If the executor verifies that the installed Obsidian typings expose `el.toggleClass(name, force)` (NOT just `el.toggleClass(name)`), then `el.toggleClass("memochron-hidden", hide)` is acceptable as a stylistic preference. The `classList.toggle` form is the proven in-codebase fallback and is the recommended default.

    Sub-cluster 3h (EmbeddedAgendaView CSS custom-property writes) is INTENTIONALLY UNCHANGED — `style.setProperty("--event-color", ...)` is not in DIR-03's banned 15-property list and is correct as-is.

    After all rewrites, verify:
      - `git ls-files src/ | xargs grep -cnE '\.style\.(border|color|cursor|display|fontSize|height|left|margin|marginTop|opacity|padding|position|textAlign|top|width)\s*='` returns 0 (the canonical ROADMAP success-criterion #2)
      - `npm run build` exits 0
      - The 2 `style.setProperty("--event-color", ...)` writes in `EmbeddedAgendaView.ts:263, 297` are unchanged — verify with `grep -nE 'style\.setProperty\(\"--event-color\"' src/views/EmbeddedAgendaView.ts | wc -l` returns 2.
      - No `customLabel.addClass("memochron-custom-color-wrapper")` call appears anywhere in `src/settings/SettingsTab.ts` — verify with `grep -c 'memochron-custom-color-wrapper' src/settings/SettingsTab.ts` returns 0.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; npm run build &gt; /tmp/build-06-03-t2.log 2&gt;&amp;1; rc=$?; tail -5 /tmp/build-06-03-t2.log; [ $rc -eq 0 ] &amp;&amp; [ "$(git ls-files src/ | xargs grep -cnE '\.style\.(border|color|cursor|display|fontSize|height|left|margin|marginTop|opacity|padding|position|textAlign|top|width)\s*=' 2&gt;/dev/null | awk -F: '{s+=$2} END {print s+0}')" = "0" ] &amp;&amp; [ "$(grep -cE 'style\.setProperty\("--event-color"' src/views/EmbeddedAgendaView.ts)" = "2" ] &amp;&amp; [ "$(grep -c 'setCssProps' src/views/CalendarView.ts)" -ge "5" ] &amp;&amp; [ "$(grep -c 'setCssProps' src/utils/viewRenderers.ts)" -ge "1" ] &amp;&amp; grep -q 'memochron-hidden' src/views/CalendarView.ts &amp;&amp; [ "$(grep -c 'memochron-custom-color-wrapper' src/settings/SettingsTab.ts)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files src/ | xargs grep -nE '\.style\.(border|color|cursor|display|fontSize|height|left|margin|marginTop|opacity|padding|position|textAlign|top|width)\s*='` returns ZERO matches (canonical ROADMAP Phase 6 success criterion #2)
    - `grep -cE 'style\.setProperty\("--event-color"' src/views/EmbeddedAgendaView.ts` outputs exactly `2` (the 2 CSS custom-property writes left untouched per PATTERNS 3h)
    - `grep -c 'setCssProps' src/views/CalendarView.ts` outputs at least `5` (3 height sites + 2 color sites)
    - `grep -c 'setCssProps' src/utils/viewRenderers.ts` outputs at least `1` (the line-320 site)
    - `grep -c 'memochron-hidden' src/views/CalendarView.ts` outputs at least `3` (toggle calls on calendar / resizeHandle / controls)
    - `grep -c 'this\.calendar\.style\.display' src/views/CalendarView.ts` outputs `0`
    - `grep -c 'classList\.add("agenda-only")\|classList\.remove("agenda-only")' src/views/CalendarView.ts` outputs `0` (collapsed into the single `toggle("agenda-only", hide)` call)
    - `grep -c 'memochron-custom-color-wrapper' src/settings/SettingsTab.ts` outputs `0` (no new wrapper-class call-site path is introduced — pinned CSS strategy)
    - `npm run build` exits 0 with no TypeScript errors
    - `git diff HEAD --name-only` lists exactly: `styles.css`, `src/utils/viewRenderers.ts`, `src/views/CalendarView.ts`, `src/settings/SettingsTab.ts` (and `src/views/EmbeddedCalendarView.ts` / `src/views/EmbeddedAgendaView.ts` ONLY IF the executor's re-scan found new violations there since PATTERNS mapping — otherwise these two files have no diff in this plan)
  </acceptance_criteria>
  <done>
    All 51 inline-style violation sites are rewritten or deleted. The DIR-03 grep returns zero matches across `src/`. `setCssProps` is the substitute for the 6 dynamic-value sites. `.memochron-hidden` class toggle replaces the visibility branch in `updateCalendarVisibility`. The 2 EmbeddedAgendaView CSS custom-property writes are unchanged. No `.memochron-custom-color-wrapper` class is added or referenced. `npm run build` passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit the DIR-03 bulk rewrite</name>
  <files>(git commit only — no source changes in this task)</files>
  <read_first>
    - .planning/phases/06-dom-api-refactor/06-CONTEXT.md (D-16 — exact commit subject line; Claude's-Discretion note on optional commit split)
    - CLAUDE.md (Memory Reminders: NO Claude / AI references in commits)
  </read_first>
  <action>
    Stage the 5 source files modified in Task 2 PLUS `styles.css` from Task 1 (and ONLY those files). Create a single commit with the exact subject line from D-16:

      refactor(views): replace inline styles with CSS classes and setCssProps (DIR-03)

    Commit body: 4-6 sentences summarizing the 7 sub-cluster rewrites (dynamic colors via setCssProps, dynamic heights via setCssProps, color-input overlay via existing CSS classes, error-message inline-style deletion, help-button augmentation, doc-link + button-container spacing, display-toggle via .memochron-hidden class), the `styles.css` additions/augmentations, and DIR-03 grep now returns zero matches across `src/`. NO Claude / AI references (per CLAUDE.md memory reminders).

    Use a HEREDOC for the commit message. Do NOT use `--no-verify`. The `obsidianmd/no-static-styles-assignment` rule is still in the Phase-5 override block at this point (Plan 04 removes it), so lint passes either way at this commit.

    Note on optional split (CONTEXT D-16 Claude's Discretion): if `git diff HEAD --stat` shows the diff is exceptionally large and the executor judges that a reviewer would benefit from splitting, the bulk DIR-03 commit MAY be split into two commits (e.g., `refactor(settings): replace inline styles with CSS classes (DIR-03 / settings)` and `refactor(views): replace inline styles with setCssProps and class toggles (DIR-03 / views)`). Default is single commit; only split if the executor explicitly chooses to.
  </action>
  <verify>
    <automated>cd /Users/mike/code/memoChron &amp;&amp; git log -1 --pretty=%s | grep -qE 'refactor\((settings|views)\):.*DIR-03' &amp;&amp; ! git log -1 --pretty=%B | grep -iE 'claude|AI assist|Co-Authored-By' &amp;&amp; [ -z "$(git status --porcelain)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` matches the regex `refactor\((settings|views)\):.*DIR-03` AND in the single-commit default case equals `refactor(views): replace inline styles with CSS classes and setCssProps (DIR-03)` exactly
    - If single commit (default): `git diff HEAD~1 --name-only` lists the 5 source files (plus `styles.css`) that were modified — and ONLY those files. If split commit: each commit's name-only listing reflects its scope.
    - `git log -1 --pretty=%B` (and the prior commit if split) contains NO occurrence of "claude", "Claude", "AI", "assistant", or `Co-Authored-By`
    - `git status --porcelain` shows no uncommitted changes after the commit (verified by `[ -z "$(git status --porcelain)" ]` as an assertion, NOT as `|| true`)
  </acceptance_criteria>
  <done>
    The DIR-03 commit (or 2 commits if split per CONTEXT discretion) exists at HEAD with the exact D-16 subject line(s), contains only the Phase-6 source files plus `styles.css`, and has no Claude / AI / Co-Authored-By references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `event.color` / `dailyNoteColor` string → `setCssProps({ color })` | Color strings flow from user-edited settings through `isValidColor` (Phase 2 D-02 + `colorValidation.ts` load-time validation) into the `setCssProps` call. `setCssProps` is `setProperty` under the hood; the color string is never interpreted as HTML. |
| `calendarHeight` setting → `setCssProps({ height })` | Numeric height from settings; clamped by upstream Phase 1 / Phase 4 logic; no user-input HTML path. |
| `hide` boolean from settings → `classList.toggle("memochron-hidden", hide)` | Pure boolean; no string interpretation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | `setCssProps({ color: event.color })` (3 sites) | accept | Per CONTEXT code_context bullet 4: `event.color` is sanitized by `isValidColor` at settings load and at color-picker save. The 3 render sites inherit the upstream guarantee — no double-validation needed. `setCssProps` is the Obsidian-API-named safe alternative; it does NOT interpret its argument as HTML/JS. |
| T-06-06 | Tampering | `setCssProps({ height: '${px}px' })` (3 sites) | accept | Height values come from `settings.calendarHeight` (numeric, bounded) and live drag deltas (numeric, bounded). Template-literal interpolation yields `"<n>px"` — no string-escape risk. |
| T-06-07 | Information disclosure | `.memochron-hidden { display: none; }` | accept | The class controls visual visibility only. The hidden element remains in the DOM and is still accessible to screen readers — same behavior as the previous `style.display = "none"` write. Accessibility-specific hiding (`aria-hidden`, `visibility: hidden`) is out of scope per CLAUDE.md / REQUIREMENTS Out of Scope (accessibility deferred to a dedicated milestone). |
| T-06-08 | Denial of service | Drag-resize hot loop calling `setCssProps` per `mousemove` | accept | Per CONTEXT D-05: `setCssProps` resolves to `setProperty` — identical wall-clock cost to direct `.style.height =`. No throttling / RAF change in this phase. PERF-04 (deferred to v2 perf milestone) covers any future debounce. |
</threat_model>

<verification>
- `git ls-files src/ | xargs grep -nE '\.style\.(border|color|cursor|display|fontSize|height|left|margin|marginTop|opacity|padding|position|textAlign|top|width)\s*='` returns ZERO matches (canonical ROADMAP success-criterion #2)
- `grep -cE 'style\.setProperty\("--event-color"' src/views/EmbeddedAgendaView.ts` outputs `2` (custom-property writes preserved)
- `grep -c 'memochron-custom-color-wrapper' styles.css src/settings/SettingsTab.ts` outputs `0:0` (no new wrapper-class strategy used — pinned per checker feedback)
- `npm run build` exits 0
- HUMAN UAT (deferred to Plan 05) will visually confirm: (a) sidebar calendar event dots render with source colors (verifies 3a), (b) drag-resize and view-mode switching work without visual regression (verifies 3b), (c) custom color picker overlay opens on click (verifies 3c), (d) URL/refresh-interval validation errors render in red with correct spacing (verifies 3d), (e) help button has correct margin/font-size (verifies 3e), (f) "iCal URL setup guide" modal shows the doc-link and buttons with correct spacing (verifies 3f), (g) "Hide calendar" toggle hides/shows the grid + resize handle + controls (verifies 3g)
</verification>

<success_criteria>
- ROADMAP Phase 6 success criterion #2 (zero `.style.<flagged>` matches across shipped `src/`) is satisfied repo-wide.
- The 6 dynamic-value sites use `setCssProps` per D-03/D-04/D-05.
- The 2 color-input overlay clusters, 2 error-message clusters, 1 help-button site, 2 doc-link/button-container sites, and 1 visibility-toggle function are all migrated to CSS-class-driven styling.
- The 2 EmbeddedAgendaView CSS custom-property writes remain untouched.
- `styles.css` carries the new `.memochron-hidden`, new `.memochron-help-buttons`, and the augmentations to `.memochron-error-message`, `.memochron-help-btn`, `.memochron-help-doc-link`, `.memochron-inline-color-custom-label`, `.memochron-inline-color-input`. No `.memochron-custom-color-wrapper` rule is added.
- `npm run build` exits 0; no TypeScript errors.
- HEAD commit(s) match the D-16 subject line(s); no Claude / AI references.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dom-api-refactor/06-03-SUMMARY.md` with:
- Per-cluster summary of edits (one short paragraph per 3a–3g)
- The before/after grep count of the 51 inline-style sites (must be 0 after)
- The `styles.css` rules added or augmented (note explicitly: no new wrapper class)
- Confirmation that the 2 `style.setProperty("--event-color", ...)` writes in `EmbeddedAgendaView.ts` are unchanged
- The git commit SHA(s) and subject line(s)
- Confirmation that no Claude / AI references appear in any commit message
- Hand-off to Plan 04 (ESLint override-block deletion + sentence-case resolution + `npm run lint` clean)
</output>
