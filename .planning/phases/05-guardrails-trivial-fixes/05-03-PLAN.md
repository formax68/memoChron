---
phase: 05-guardrails-trivial-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.mjs
  - .github/workflows/lint.yml
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "`npm run lint` exists as a script in package.json and runs ESLint against `src/`"
    - "`npm run lint` exits 0 against the v1.15 starting tree (after phase-tagged override blocks are tuned)"
    - "`npm run lint` exits non-zero when a deliberate `innerHTML` write, `as TFile` cast, or floating promise is introduced into source code outside the override-protected files"
    - "ESLint enforces — at minimum — `@microsoft/sdl/no-inner-html`, `no-unsanitized/property`, `obsidianmd/no-static-styles-assignment`, `obsidianmd/no-tfile-tfolder-cast`, `obsidianmd/no-view-references-in-plugin`, `obsidianmd/prefer-window-timers`, `obsidianmd/prefer-active-doc`, `@typescript-eslint/no-floating-promises`, `@typescript-eslint/no-explicit-any`, `@typescript-eslint/no-unused-vars: error`, and a custom `no-restricted-syntax` selector for `document.createElement` (DIR-04 placeholder until eslint-plugin-obsidianmd ships `prefer-create-el`)"
    - "Every per-rule / per-file override block in `eslint.config.mjs` carries a leading `// Phase N — DIR-XX will remove this when ...` comment naming the phase that will remove it (Phase 6 / Phase 7 / Phase 8 — never silent)"
    - "`.github/workflows/lint.yml` exists, runs on `push` and `pull_request` for all branches, and a failing lint exits the job non-zero (blocks the build by default ESLint behavior)"
    - "TypeScript is bumped from 4.7.4 to ^5.9.3 (typescript-eslint@8 peer requirement), and `tsc -noEmit -skipLibCheck` still exits 0 on the bumped tree"
    - "The legacy `@typescript-eslint/eslint-plugin` and `@typescript-eslint/parser` 5.29.0 entries are removed from package.json devDependencies (replaced by the `typescript-eslint` meta package)"
  artifacts:
    - path: package.json
      provides: "lint and lint:fix scripts; refreshed devDependencies (eslint@^9.39.4, typescript-eslint@^8.59.3, eslint-plugin-obsidianmd@^0.3.0, globals@^14.0.0, typescript@^5.9.3)"
      exports:
        - lint
        - lint:fix
    - path: eslint.config.mjs
      provides: "ESLint 9 flat config; obsidianmd recommended config + Phase 5 tightenings + Phase 6/7/8 phase-tagged override blocks"
      contains: "obsidianmd.configs.recommended"
    - path: .github/workflows/lint.yml
      provides: "CI lint workflow running on push + pull_request"
      contains: "npm run lint"
    - path: package-lock.json
      provides: "Updated lockfile with new ESLint toolchain and bumped TypeScript"
  key_links:
    - from: package.json scripts.lint
      to: eslint.config.mjs
      via: "eslint CLI with default config-file resolution"
      pattern: "eslint src/"
    - from: .github/workflows/lint.yml
      to: package.json scripts.lint
      via: "npm run lint after npm ci"
      pattern: "npm run lint"
    - from: eslint.config.mjs (each phase-tagged override block)
      to: "future phase that removes the block (Phase 6 / Phase 7 / Phase 8)"
      via: "leading `// Phase N — DIR-XX ...` comment"
      pattern: "// Phase [678] — DIR-"
---

<objective>
Close DOC-01. Install ESLint 9 + `typescript-eslint@8` + `eslint-plugin-obsidianmd@0.3.0` + `globals@14`, bump TypeScript from 4.7.4 to ^5.9.3 (required by `typescript-eslint@8` peer constraint), author `eslint.config.mjs` (flat config) with phase-tagged per-rule override blocks, add `lint` / `lint:fix` scripts to `package.json`, and add `.github/workflows/lint.yml` that runs `npm run lint` on every push and pull request.

Purpose: Lands the lint gate FIRST so Phases 6–8 land against rules already enforced. Without this plan, the v1.15 milestone can't begin its bulk refactor work.
Output: A clean `npm run lint` exit-zero against the v1.15 starting tree (achieved via phase-named override blocks, NEVER via inline `eslint-disable`), and a new CI workflow that runs lint on every push and PR.

DEVIATION NOTE: Research (05-RESEARCH.md §1.1, §9.1) requires bumping `typescript` from `4.7.4` to `^5.9.3` because `typescript-eslint@8` peer-requires `typescript >=4.8.4 <6.1.0`. CONTEXT.md did not anticipate this. Without the bump, `npx eslint .` fails at runtime with a peer-version error. This deviation is mandatory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-guardrails-trivial-fixes/05-CONTEXT.md
@.planning/phases/05-guardrails-trivial-fixes/05-RESEARCH.md
@.planning/phases/05-guardrails-trivial-fixes/05-PATTERNS.md
@.planning/phases/05-guardrails-trivial-fixes/05-VALIDATION.md
@CLAUDE.md
@package.json
@tsconfig.json
@esbuild.config.mjs
@version-bump.mjs

<interfaces>
<!-- Toolchain (05-RESEARCH.md §1.1, §10 — pinned and verified against npm registry 2026-05-13): -->
<!--   eslint                      ^9.39.4   (maintenance line; Node ^18.18 || ^20.9 || >=21.1) -->
<!--   typescript-eslint           ^8.59.3   (meta package; peer typescript >=4.8.4 <6.1.0) -->
<!--   eslint-plugin-obsidianmd    ^0.3.0    (official obsidianmd/eslint-plugin; bundles no-unsanitized + @microsoft/sdl + import + depend as direct deps) -->
<!--   globals                     ^14.0.0   -->
<!--   typescript                  ^5.9.3    (bump from 4.7.4 — peer of typescript-eslint@8) -->
<!-- REMOVE:  @typescript-eslint/eslint-plugin 5.29.0 ; @typescript-eslint/parser 5.29.0 -->
<!-- DO NOT add eslint-plugin-no-unsanitized or @microsoft/eslint-plugin-sdl as direct devDeps — they come in via eslint-plugin-obsidianmd dependencies (05-RESEARCH.md §1.2 last row + §3.3). -->

<!-- eslint.config.mjs skeleton — full copy-paste-ready snippet in 05-RESEARCH.md §4.1. -->
<!-- Structural template: obsidianmd/obsidian-sample-plugin@master eslint.config.mts (05-RESEARCH.md §1.3). -->
<!-- Required structure (top-to-bottom order matters in flat config — later wins): -->
<!--   1. languageOptions block: globals.browser + parserOptions.projectService.allowDefaultProject=['eslint.config.mjs','manifest.json'] + tsconfigRootDir: import.meta.dirname + extraFileExtensions: ['.json'] -->
<!--   2. ...obsidianmd.configs.recommended  (spreads @typescript-eslint recommendedTypeChecked, no-unsanitized recommended, @microsoft/sdl no-inner-html + no-document-write, all 26 obsidianmd/* rules, registers Obsidian globals) -->
<!--   3. Phase 5 tightenings (src/**/*.ts): -->
<!--        '@typescript-eslint/no-unused-vars': ['error', { args: 'none' }]   (recommended config has it at "warn"; DOC-01 wants it as a real error) -->
<!--        'no-restricted-syntax' selector for CallExpression[callee.object.name='document'][callee.property.name='createElement']   (DIR-04 — eslint-plugin-obsidianmd@0.3.0 does NOT register `prefer-create-el`; this hand-rolled selector fills the gap per CONTEXT.md D-02 + 05-RESEARCH.md §3.5) -->
<!--   4. Phase 6 override block (DIR-02 / DIR-03 / DIR-04) — leading comment names the phase that removes it -->
<!--   5. Phase 7 override block (DIR-05 / DIR-06 / DIR-07 / DIR-08) — leading comment names the phase that removes it -->
<!--   6. Phase 8 override block (DIR-01 / DIR-09 / DIR-10) — leading comment names the phase that removes it -->
<!--   7. globalIgnores(['node_modules', 'main.js', 'esbuild.config.mjs', 'version-bump.mjs', 'versions.json']) -->

<!-- Phase 6/7/8 override files (05-PATTERNS.md "File-to-rule mapping"). Researcher's globs in 05-RESEARCH.md §4.1 are defensive best-guesses — planner's dry-run narrows them to the smallest workable set: -->
<!--   Phase 6: confirmed hot file is src/settings/SettingsTab.ts (innerHTML, inline-style, document.createElement). Researcher's wider list (src/views/*, src/utils/viewRenderers.ts) may also trip — confirm in dry-run before locking. -->
<!--   Phase 7: src/main.ts (window.setInterval, window.setTimeout), src/views/CalendarView.ts (as TFile, window.setTimeout), src/settings/SettingsTab.ts (setTimeout), plus floating-promises hotspots surfaced by dry-run. -->
<!--   Phase 8: broad — `src/**/*.ts` (console.* is scattered across 39 sites; `any` usage is across types + views + services). -->

<!-- lint.yml shape — full snippet in 05-RESEARCH.md §5.1 (sourced from obsidian-sample-plugin). Adapted for MemoChron: -->
<!--   name: Lint -->
<!--   on: push (branches: ['**']) + pull_request (branches: ['**']) -->
<!--   jobs.lint.runs-on: ubuntu-latest -->
<!--   matrix.node-version: [20.x, 22.x]   (sample-plugin canonical; CONTEXT.md D-06 says Node 20.x — matrix supersets and catches drift) -->
<!--   steps: actions/checkout@v4, actions/setup-node@v4 with cache: 'npm', npm ci, npm run lint -->
<!--   DO NOT add `npm run build` here (lint is a separate gate per CONTEXT.md D-05 + 05-RESEARCH.md §5.3). -->
<!--   DO NOT add `if:` gating (this is a true CI gate, not a Claude-event-gated workflow; 05-PATTERNS.md lint.yml section). -->

<!-- package.json conventions (05-PATTERNS.md "package.json"): -->
<!--   2-space indentation; alphabetical devDependencies key order; trailing-comma policy is "JSON-strict, no trailing commas" -->
<!--   New scripts (insert after `version`): -->
<!--     "lint": "eslint src/", -->
<!--     "lint:fix": "eslint src/ --fix" -->
<!--   (CONTEXT.md D-05 picks `eslint src/` over `eslint .` — more explicit; decouples from globalIgnores correctness.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump TypeScript and install ESLint toolchain (DOC-01 toolchain)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current devDependencies — confirm 5.29.0 entries to remove and 4.7.4 to bump)
    - .planning/phases/05-guardrails-trivial-fixes/05-RESEARCH.md §1.1 (pinned versions table), §1.2 (peer constraints), §9.1 (TS 4.7→5.9 risk), §10 (final pin recommendations)
    - .planning/phases/05-guardrails-trivial-fixes/05-PATTERNS.md "package.json" section (alphabetical key order, caret-range convention for new entries, DO-NOT-ADD list for unsanitized + sdl)
    - tsconfig.json (confirm `target: ES6`, `strictNullChecks: true`, `noImplicitAny: true` — TS 5.x will respect these)
  </read_first>
  <action>
    Two-step, with explicit isolation of TS upgrade fallout from ESLint installation per 05-RESEARCH.md §9.1 + §11 Open Question 1:

    Step A — TypeScript bump in isolation:
    1. In `package.json` devDependencies, change `"typescript": "4.7.4"` to `"typescript": "^5.9.3"`. Run `npm install`. This regenerates `package-lock.json` for the TS bump alone.
    2. Run `npx tsc -noEmit -skipLibCheck` to surface any TS 4.7 → 5.9 fallout BEFORE adding the ESLint deps. If errors appear: fix them in-place this phase (do NOT push to a later phase — guardrails first means the type-checker must be green before lint is wired). Most likely-safe outcomes per 05-RESEARCH.md §9.1 / §11 Q1: zero new errors (codebase uses straightforward types). If errors appear: report them in the task SUMMARY and either fix narrowly or document why a fix is being deferred and how.

    Step B — ESLint toolchain install:
    1. In `package.json` devDependencies, REMOVE these two entries (per 05-RESEARCH.md §1.1 last paragraph — superseded by the `typescript-eslint` meta package):
       - `"@typescript-eslint/eslint-plugin": "5.29.0"`
       - `"@typescript-eslint/parser": "5.29.0"`
    2. ADD these four entries, alphabetical, caret-ranged (per 05-PATTERNS.md "package.json" mixed-pin convention — new entries use caret):
       - `"eslint": "^9.39.4"`
       - `"eslint-plugin-obsidianmd": "^0.3.0"`
       - `"globals": "^14.0.0"`
       - `"typescript-eslint": "^8.59.3"`
    3. Run `npm install` to regenerate `package-lock.json`.

    Do NOT add separate `@typescript-eslint/eslint-plugin` / `@typescript-eslint/parser` v8 entries — the meta package re-exports them (05-RESEARCH.md §1.1, §10 note).
    Do NOT add `eslint-plugin-no-unsanitized` or `@microsoft/eslint-plugin-sdl` — they come in as direct deps of `eslint-plugin-obsidianmd` (05-RESEARCH.md §1.2, §3.3).
    Do NOT add a `test` or `typecheck` script (out of scope per CONTEXT.md `<deferred>`).
    Do NOT touch `package.json` `description`, `author`, `keywords`, or any field outside `devDependencies`.

    Per D-01.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=p.devDependencies; if(d['@typescript-eslint/eslint-plugin']||d['@typescript-eslint/parser']){console.error('5.29 entries still present');process.exit(1)} if(!d['eslint']||!d['typescript-eslint']||!d['eslint-plugin-obsidianmd']||!d['globals']){console.error('missing deps',d);process.exit(1)} if(d.typescript!=='^5.9.3'){console.error('typescript not bumped:',d.typescript);process.exit(1)} console.log('deps ok')" && npx tsc -noEmit -skipLibCheck && echo "tsc ok"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e ...` deps assertion exits 0 and prints `deps ok`.
    - `npx tsc -noEmit -skipLibCheck` exits 0 (no new TS errors from the 4.7→5.9 bump, or any new errors have been fixed in this task).
    - `package-lock.json` contains entries for `eslint@9.39.x`, `typescript-eslint@8.59.x` (or higher patch), `eslint-plugin-obsidianmd@0.3.x`, and resolves `typescript` to `5.9.x`.
    - `npm ls eslint-plugin-obsidianmd` exits 0 (package is installed at the top level).
    - The legacy `@typescript-eslint/eslint-plugin` and `@typescript-eslint/parser` entries are absent from `package.json` AND from the top-level `package-lock.json` `packages.""` block.
    - `package.json` devDependencies block is alphabetically sorted (matches the pre-edit convention).
  </acceptance_criteria>
  <done>
    Toolchain installed; TS bumped and type-check green; lockfile regenerated; devDependencies block alphabetical; no stray sub-plugins added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author eslint.config.mjs + add lint scripts + tune phase-tagged override blocks via dry-run (DOC-01 config)</name>
  <files>eslint.config.mjs, package.json</files>
  <read_first>
    - .planning/phases/05-guardrails-trivial-fixes/05-RESEARCH.md §3.3 (what `obsidianmd.configs.recommended` actually enables), §3.4 (DOC-01 → rule mapping), §3.5 (DIR-04 hand-rolled selector), §4.1 (full copy-paste-ready `eslint.config.mjs` skeleton), §4.2 (flat-config sibling-object pattern), §4.3 (dry-run discovery — planner's task), §9.2 (out-of-project .mjs files), §9.4 (no-console semantics)
    - .planning/phases/05-guardrails-trivial-fixes/05-PATTERNS.md "eslint.config.mjs" section (ESM style donor — `esbuild.config.mjs` / `version-bump.mjs`; double-quoted imports; 2-space indent; trailing commas) AND "File-to-rule mapping for the override-block globs" table
    - esbuild.config.mjs (ESM style reference — double quotes, 2 spaces, top-level `export default`)
    - version-bump.mjs (ESM style reference)
    - src/main.ts (Phase 7 override candidate — `window.setInterval`, `window.setTimeout`)
    - src/settings/SettingsTab.ts (Phase 6 hot file — innerHTML, inline-style, document.createElement)
    - src/views/CalendarView.ts (Phase 7 — `as TFile`, `window.setTimeout`)
  </read_first>
  <action>
    Three-step, with empirical override-glob tuning per CONTEXT.md D-04 + 05-RESEARCH.md §4.3:

    Step A — Author `eslint.config.mjs`:
    Create `eslint.config.mjs` at the repo root using the skeleton in 05-RESEARCH.md §4.1 as the literal starting point. Adapt to MemoChron's in-repo convention per 05-PATTERNS.md "ESM Configuration File Style":
    - Convert single-quoted import paths to double-quoted (matches `esbuild.config.mjs` / `version-bump.mjs`)
    - Preserve 2-space indentation, trailing commas on multi-line objects, top-level `export default tseslint.config(...)`
    - Use `//` line comments (no `/* block */`)
    - The required structural order (later wins in flat config) is the seven-section layout in `<interfaces>` above. Do NOT skip the Phase 5 tightening block (section 3) — it is what makes `@typescript-eslint/no-unused-vars` an error (DOC-01 wording) and what installs the DIR-04 hand-rolled `no-restricted-syntax` selector.
    - Each phase-tagged override block (sections 4, 5, 6) MUST carry a leading `//` comment naming the phase and DIR IDs that will remove it, in the form `// Phase 6 — DIR-02 / DIR-03 / DIR-04 will remove these when the DOM-API refactor lands.` — ROADMAP success criterion #3 + CONTEXT.md `<specifics>`. No `eslint-disable` inline comments anywhere in this phase (CONTEXT.md D-04).

    Step B — Add `lint` and `lint:fix` scripts to `package.json`:
    In the `scripts` block, after `"version"`, insert:
    - `"lint": "eslint src/"`
    - `"lint:fix": "eslint src/ --fix"`
    (Per CONTEXT.md D-05 — `eslint src/` over `eslint .`. 2-space indent, comma-separated, JSON-strict no trailing commas.)

    Step C — Dry-run and tune override-block file globs:
    1. Run `npx eslint src/` and capture the full output. The initial run will use the researcher's defensive override globs from 05-RESEARCH.md §4.1.
    2. If the initial run exits 0, leave the globs alone (defensive globs are acceptable for the closing diff — Phases 6/7/8 will tighten when they remove the blocks).
    3. If the initial run exits non-zero with violations OUTSIDE the override-protected files (e.g., a rule trips in `src/services/CalendarService.ts` that the Phase 7 block didn't include), expand the relevant phase block's `files` array to include the offending path. Re-run. Repeat until exit 0.
    4. After exit 0 is reached, run `npx eslint src/ --quiet` again to confirm there are zero error-level diagnostics (warnings — like `obsidianmd/prefer-active-doc` at `warn` severity in the recommended config — are acceptable since the Phase 7 override silences them anyway; double-check no `error` remains).
    5. Document the final override-block file globs in the plan SUMMARY along with the chain of dry-runs that led to them. Per 05-PATTERNS.md "File-to-rule mapping" — Phase 6 hot file is `src/settings/SettingsTab.ts`; Phase 7 needs `src/main.ts`, `src/views/CalendarView.ts`, and possibly `src/settings/SettingsTab.ts` and `src/services/CalendarService.ts`; Phase 8 stays broad.

    Per D-01, D-04, D-05, ROADMAP success criterion #3.
  </action>
  <verify>
    <automated>test -f eslint.config.mjs && grep -q "obsidianmd.configs.recommended" eslint.config.mjs && grep -q "projectService" eslint.config.mjs && grep -q "no-restricted-syntax" eslint.config.mjs && grep -cE "^\s*//\s*Phase\s+[678]\s+—\s+DIR-" eslint.config.mjs | awk '{exit ($1>=3)?0:1}' && node -e "const p=require('./package.json'); if(p.scripts.lint!=='eslint src/'||p.scripts['lint:fix']!=='eslint src/ --fix'){console.error('scripts wrong',p.scripts);process.exit(1)} console.log('scripts ok')" && npm run lint --silent && echo "lint exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.mjs` exists at repo root.
    - File contains `obsidianmd.configs.recommended` (recommended config spread).
    - File contains `projectService` (type-aware lint enabled per CONTEXT.md D-03 + 05-RESEARCH.md §2.1).
    - File contains the `no-restricted-syntax` selector targeting `document.createElement` (DIR-04 hand-roll per 05-RESEARCH.md §3.5).
    - File contains AT LEAST 3 leading-comment lines matching `^\s*//\s*Phase\s+[678]\s+—\s+DIR-` (one per phase-tagged override block — ROADMAP success criterion #3).
    - File contains `@typescript-eslint/no-unused-vars` set to `error` somewhere in the Phase 5 tightening block (DOC-01 wording — recommended config has it at `warn`; we tighten to `error` per 05-RESEARCH.md §3.4).
    - File contains `globalIgnores(` with entries for `node_modules`, `main.js`, `esbuild.config.mjs`, `version-bump.mjs`.
    - File contains NO `eslint-disable` inline comments (`grep -c "eslint-disable" eslint.config.mjs` returns 0). Inline disables in src/**/*.ts are also prohibited this phase (CONTEXT.md D-04).
    - `package.json` `scripts.lint` is exactly `"eslint src/"`; `scripts.lint:fix` is exactly `"eslint src/ --fix"`.
    - `npm run lint` exits 0 against the v1.15 starting tree (this is the empirical proof that the override blocks cover the existing violations).
    - Regression smoke check: introduce a temporary `el.innerHTML = "x"` in any file under `src/` that is NOT in the Phase 6 override block (e.g., `src/services/CalendarService.ts`), run `npm run lint`, expect non-zero exit AND at least one error containing `no-inner-html` or `no-unsanitized`; then revert the edit. This is a manual UAT step per 05-VALIDATION.md — record the captured output in the plan SUMMARY.
  </acceptance_criteria>
  <done>
    `eslint.config.mjs` enforces the DOC-01 rule set with phase-tagged override blocks (each named in a leading comment), `npm run lint` exits 0 on the starting tree, the lint:fix script is wired, and the regression smoke check confirms a deliberate `innerHTML` write trips the gate.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add lint.yml CI workflow (DOC-01 CI gate)</name>
  <files>.github/workflows/lint.yml</files>
  <read_first>
    - .planning/phases/05-guardrails-trivial-fixes/05-RESEARCH.md §5.1 (full lint.yml reference implementation), §5.2 (notes — action pins, Node matrix, `cache: 'npm'`, npm ci vs install), §5.3 (do NOT chain into build)
    - .planning/phases/05-guardrails-trivial-fixes/05-PATTERNS.md "lint.yml" section (release.yml shape donor; "no `if:` gating"; "`npm ci`, not `npm install`"; "do NOT add `npm run build`")
    - .github/workflows/release.yml (in-repo workflow shape reference — for indentation and step pattern)
    - .github/workflows/claude.yml (do NOT use as template — Claude-event-gated workflow; wrong shape for a true CI gate)
    - package.json (after Task 2 — confirm `npm run lint` exists)
  </read_first>
  <action>
    Create `.github/workflows/lint.yml` using the snippet in 05-RESEARCH.md §5.1 as the literal starting point. Adapted for MemoChron:
    - Workflow name: `Lint`
    - Triggers: `on: { push: { branches: ['**'] }, pull_request: { branches: ['**'] } }` (lints every branch push and every PR — CONTEXT.md D-06)
    - `jobs.lint.runs-on: ubuntu-latest`
    - `strategy.matrix.node-version: [20.x, 22.x]` (mirrors obsidian-sample-plugin canonical; supersets CONTEXT.md D-06's Node 20.x ask). If the planner prefers single-job for simpler PR-check display, use a single `node-version: 20.x` — both are valid per 05-PATTERNS.md "lint.yml" "Node matrix vs single-job". Default recommendation: use the matrix to mirror the sample plugin.
    - Steps: `actions/checkout@v4`, `actions/setup-node@v4` with `node-version: ${{ matrix.node-version }}` and `cache: 'npm'`, `npm ci`, `npm run lint`

    Two-space YAML indentation throughout. NO `if:` gating (this is a true CI gate, not a Claude-event-gated workflow). NO `npm run build` step (lint is a separate gate per CONTEXT.md D-05 + 05-RESEARCH.md §5.3).

    Do NOT touch `release.yml`, `claude.yml`, or `claude-code-review.yml`.

    Per D-06, D-07.
  </action>
  <verify>
    <automated>test -f .github/workflows/lint.yml && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/lint.yml')); assert d['name']=='Lint', d['name']; assert 'push' in d['on'] and 'pull_request' in d['on'], d['on']; j=d['jobs']['lint']; assert j['runs-on']=='ubuntu-latest'; steps=j['steps']; uses=[s.get('uses','') for s in steps]; assert any('actions/checkout' in u for u in uses), uses; assert any('actions/setup-node' in u for u in uses), uses; runs=[s.get('run','') for s in steps]; assert any('npm ci' in r for r in runs), runs; assert any('npm run lint' in r for r in runs), runs; assert not any('npm run build' in r for r in runs), 'build should not be in lint.yml'; print('lint.yml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/lint.yml` exists.
    - YAML parses cleanly (Python yaml.safe_load).
    - `name` is `Lint`.
    - Triggers include BOTH `push` AND `pull_request`.
    - Runs on `ubuntu-latest`.
    - Steps include `actions/checkout`, `actions/setup-node` (with `cache: 'npm'`), `npm ci`, `npm run lint`, in that order.
    - Does NOT include `npm run build` (per CONTEXT.md D-05 + 05-RESEARCH.md §5.3 — lint is a separate gate).
    - Does NOT include any `if:` step-gating (per 05-PATTERNS.md "lint.yml" — true CI gate).
    - `release.yml`, `claude.yml`, `claude-code-review.yml` are NOT modified (`git status` shows only `lint.yml` as new).
  </acceptance_criteria>
  <done>
    `lint.yml` exists, parses, runs `npm run lint` after `npm ci` on push and pull_request, no other workflows touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → npm registry | New devDependencies are installed; supply-chain risk surface for ESLint plugins |
| Source code on disk → `npm run lint` (local) | ESLint reads source files and reports rule violations |
| Pushed branch / opened PR → `lint.yml` (CI) | CI runs the same `npm run lint` on the GitHub-hosted runner; result blocks merge by default |
| eslint.config.mjs override blocks → future-phase reviewers | Each phase-tagged block's leading comment is the contract that the block will be removed in the named phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Lint rule silenced via inline `eslint-disable` comment in source | mitigate | CONTEXT.md D-04 + ROADMAP success criterion #3 prohibit inline disables for scorecard violations. All suppression is in `eslint.config.mjs` phase-tagged override blocks. Verify via `grep -c "eslint-disable" src/` returns 0 in the plan SUMMARY. |
| T-05-09 | Tampering | Override block forgotten in a later phase (e.g., Phase 6 ships but the Phase 6 override block stays) | mitigate (process) | Each phase-tagged block carries a leading comment naming the DIR IDs and the phase that owns the removal. Phase 6/7/8 closing checks include "remove the named override block" as an explicit success criterion in their roadmap entries. |
| T-05-10 | Supply-chain (tampering/spoofing) | Compromised ESLint plugin in `eslint-plugin-obsidianmd` transitive deps | accept | `eslint-plugin-obsidianmd@0.3.0` is maintained by `joethei` on behalf of the official `obsidianmd` org and published 2026-05-12 (one day before research). Pinned `^0.3.0` allows patch updates. Fallback to `^0.2.9` documented in 05-RESEARCH.md §9.9 if 0.3.0 surfaces a critical bug during the dry-run. Standard npm trust model; no custom hardening this phase. |
| T-05-03 (residual) | Tampering | Lint workflow bypassed by merging to `main` without required-status-check | accept | Branch-protection-rule on `main` is out-of-scope per CONTEXT.md D-07 (repo-settings concern). User enables required-status-check on `lint.yml` in GitHub settings after Phase 5 ships. The workflow itself is the technical mitigation; merge-time enforcement is a follow-up. |
| T-05-11 | Tampering / Information Disclosure | TS bump 4.7→5.9 introduces silent behavioral changes in strict-null narrowing | mitigate | Task 1 isolates the TS bump and runs `tsc -noEmit -skipLibCheck` BEFORE installing ESLint. Any new TS errors are fixed (or documented) in Task 1's SUMMARY rather than masked. 05-RESEARCH.md §9.1 + §11 Q1. |
</threat_model>

<verification>
- `npm run lint` exits 0 (full automated verification in Task 2)
- `npx tsc -noEmit -skipLibCheck` exits 0 (TS bump did not introduce new errors)
- `eslint.config.mjs` contains the three phase-tagged override blocks with leading comments matching `// Phase [678] — DIR-`
- Regression smoke check from Task 2 confirms a deliberate `el.innerHTML = "x"` outside the override-protected files trips the lint gate (manual UAT — output captured in SUMMARY)
- `.github/workflows/lint.yml` exists and parses
- `grep -c "eslint-disable" eslint.config.mjs src/` returns 0 (no inline disables this phase)
- `git diff --stat` shows ONLY the four files in `files_modified` (plus `package-lock.json` regeneration)
</verification>

<success_criteria>
- ROADMAP success criterion #1 satisfied: `npm run lint` exists and exits non-zero when DOC-01's required rules are violated
- ROADMAP success criterion #2 satisfied: A GitHub Actions workflow under `.github/workflows/` runs `npm run lint` on every push and PR, and a failing lint fails the build
- ROADMAP success criterion #3 satisfied: ESLint runs cleanly on the v1.15 starting tree via phase-named override blocks; no silent rule disables; no inline `eslint-disable` comments for scorecard violations
- Phase 5 commit 3 (`chore(lint): add ESLint 9 + obsidianmd + per-phase overrides + CI gate (DOC-01)`) is ready to author (NO Claude/AI references per CLAUDE.md and D-12; commit body should briefly note the TS 4.7→5.9 deviation rationale per 05-RESEARCH.md §1.1)
</success_criteria>

<output>
After completion, create `.planning/phases/05-guardrails-trivial-fixes/05-03-SUMMARY.md` with:
- Full `npm run lint` exit-0 output (final state, after override-block tuning)
- `npx tsc -noEmit -skipLibCheck` exit-0 confirmation (after TS bump)
- The chain of dry-run iterations: what each iteration's violations were and which override-block `files` array was expanded to reach exit 0
- Final per-phase override-block `files` arrays (Phase 6, Phase 7, Phase 8) — the exact globs that ended up in `eslint.config.mjs`
- Regression smoke-check stdout: deliberate `innerHTML` write → `npm run lint` non-zero exit + offending rule ID
- Full `git diff` summary (file list + lines added/removed per file)
- The commit hash and message body (per D-11: `chore(lint): add ESLint 9 + obsidianmd + per-phase overrides + CI gate (DOC-01)`; body notes the TS 4.7→5.9 deviation per 05-RESEARCH.md §1.1; NO Claude/AI references per CLAUDE.md and D-12)
- Confirmation that no files outside `files_modified` were touched
</output>
