---
phase: 05-guardrails-trivial-fixes
plan: 04
type: execute
wave: 4
depends_on:
  - 02
  - 03
files_modified:
  - package.json
  - manifest.json
  - versions.json
  - .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md
autonomous: false
requirements:
  - DIR-12
user_setup:
  - service: github
    why: "Tag push triggers release.yml; gh CLI required for tag push and attestation verification"
    env_vars:
      - name: GH_TOKEN
        source: "Local gh auth (gh auth status) — already configured for the repo"
    dashboard_config:
      - task: "Verify gh CLI version is >= 2.46.0 (gh attestation subcommand availability)"
        location: "Local shell — `gh --version`"
      - task: "Confirm formax68/memoChron is a public repo (artifact attestations require public on GitHub Free/Pro/Team plans)"
        location: "https://github.com/formax68/memoChron/settings"
must_haves:
  truths:
    - "A pre-release tag `1.15.0-beta.1` is pushed to GitHub"
    - "release.yml runs to completion on the tag push, producing a draft release with three assets (main.js, manifest.json, styles.css) AND a Sigstore attestation entry in the GitHub attestations API keyed by each asset's SHA-256 digest"
    - "`gh attestation verify` exits 0 against each of the three downloaded assets"
    - "The published manifest.json artifact has description ending in `.` AND version `1.15.0-beta.1` (BRAT-compat: version-bump.mjs has synced manifest.json + versions.json from package.json)"
    - "Evidence (all three `gh attestation verify` stdouts + the test-tag release URL) is captured in 05-HUMAN-UAT.md"
  artifacts:
    - path: package.json
      provides: "Version bumped to 1.15.0-beta.1 for the test pre-release"
      contains: '"version": "1.15.0-beta.1"'
    - path: manifest.json
      provides: "Version synced to 1.15.0-beta.1 (via version-bump.mjs); description still ends with `.`"
      contains: '"version": "1.15.0-beta.1"'
    - path: versions.json
      provides: "1.15.0-beta.1 mapped to minAppVersion 1.8.9 (via version-bump.mjs)"
      contains: '"1.15.0-beta.1"'
    - path: .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md
      provides: "Captured stdout of the three gh attestation verify commands and the test-tag release URL"
      contains: "DIR-12 Verification"
  key_links:
    - from: "package.json version (1.15.0-beta.1)"
      to: "manifest.json version + versions.json"
      via: "npm version 1.15.0-beta.1 → version-bump.mjs"
      pattern: "1.15.0-beta.1"
    - from: "git tag 1.15.0-beta.1"
      to: "release.yml on push.tags"
      via: "GitHub Actions tag-push trigger"
      pattern: "refs/tags/1.15.0-beta.1"
    - from: "release.yml attest-build-provenance@v3 step"
      to: "GitHub attestations API"
      via: "Sigstore-signed attestation keyed by asset SHA-256"
      pattern: "subject-path:.*main\\.js"
    - from: "gh attestation verify <asset> --repo formax68/memoChron"
      to: "GitHub attestations API"
      via: "online attestation lookup by digest"
      pattern: "successfully verified"
---

<objective>
Close DIR-12's acceptance half. Cut a real pre-release tag (`1.15.0-beta.1` — matches the only prior pre-release convention `1.3.0-beta.1` per 05-RESEARCH.md §7.1), watch `release.yml` complete, then run `gh attestation verify` against all three release assets and capture the evidence in `05-HUMAN-UAT.md`.

Purpose: DIR-12 acceptance is explicit per ROADMAP success criterion #5 — "verified by inspecting the workflow YAML **AND** the next pre-release output". Plan 02 closed the workflow half; this plan closes the output half. Without an actual attested pre-release, DIR-12 is half-acceptable.
Output: A pushed git tag `1.15.0-beta.1`, a completed `release.yml` run with a draft pre-release at https://github.com/formax68/memoChron/releases, and `05-HUMAN-UAT.md` containing the three `gh attestation verify` stdouts.

The test pre-release tag can be kept as v1.15's first beta or deleted after evidence capture — the attestation registry entry persists either way (05-RESEARCH.md §7.3). Researcher's recommendation: keep `--draft` in release.yml as currently configured, run `gh attestation verify` against the locally-downloaded draft assets (sidesteps the immutable-releases edge case), then optionally promote the draft to a real GitHub-UI pre-release.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-guardrails-trivial-fixes/05-CONTEXT.md
@.planning/phases/05-guardrails-trivial-fixes/05-RESEARCH.md
@.planning/phases/05-guardrails-trivial-fixes/05-PATTERNS.md
@.planning/phases/05-guardrails-trivial-fixes/05-VALIDATION.md
@CLAUDE.md
@version-bump.mjs
@manifest.json
@package.json
@versions.json
@.github/workflows/release.yml

<interfaces>
<!-- Pre-flight checks (05-RESEARCH.md §8.5, §9.8): -->
<!--   gh --version  →  must be >= 2.46.0 (gh attestation subcommand). -->
<!--   gh auth status  →  must show authenticated as a user with push access to formax68/memoChron. -->
<!--   formax68/memoChron repo visibility  →  must be public (05-RESEARCH.md §6.7). -->

<!-- BRAT-compat tag flow (05-RESEARCH.md §7.2, CLAUDE.md "Beta Release Strategy"): -->
<!--   1. `npm version 1.15.0-beta.1`  →  npm sets package.json version, then triggers `version` script (`node version-bump.mjs && git add manifest.json versions.json`). -->
<!--      version-bump.mjs reads package.json version and writes it to manifest.json `version` and to versions.json (mapped to the manifest's current minAppVersion 1.8.9). -->
<!--      `npm version` then commits the three files with message `1.15.0-beta.1` and tags `1.15.0-beta.1`. -->
<!--   2. `git push origin main && git push origin 1.15.0-beta.1`  →  pushes the commit AND the tag. -->
<!--   3. release.yml fires on the tag push (on.push.tags = '*') and: build → attest-build-provenance@v3 → gh release create --draft. -->

<!-- gh attestation verify (05-RESEARCH.md §8): -->
<!--   gh release download 1.15.0-beta.1 --repo formax68/memoChron --pattern 'main.js' -->
<!--   gh release download 1.15.0-beta.1 --repo formax68/memoChron --pattern 'manifest.json' -->
<!--   gh release download 1.15.0-beta.1 --repo formax68/memoChron --pattern 'styles.css' -->
<!--   gh attestation verify main.js       --repo formax68/memoChron   →  exit 0 -->
<!--   gh attestation verify manifest.json --repo formax68/memoChron   →  exit 0 -->
<!--   gh attestation verify styles.css    --repo formax68/memoChron   →  exit 0 -->
<!--   `--repo` is stricter than `--owner` and the recommended invocation per 05-RESEARCH.md §8.1. -->

<!-- Manifest.json description must STILL end with `.` after the version bump — version-bump.mjs only touches `version`, NOT `description` (05-PATTERNS.md confirms; 05-RESEARCH.md §9.11). Plan 01's edit is durable. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight checks before cutting the test pre-release</name>
  <what-built>
    Plans 01 / 02 / 03 have shipped to `main`:
    - manifest.json description ends with `.`
    - release.yml has the attestation step + expanded permissions
    - eslint.config.mjs + lint.yml + lint scripts are in place; `npm run lint` exits 0
  </what-built>
  <how-to-verify>
    Run these commands locally and paste output into the resume signal:

    1. `gh --version`  →  confirm `gh version` is >= 2.46.0 (05-RESEARCH.md §9.8). If older, install/update gh: https://github.com/cli/cli#installation.
    2. `gh auth status`  →  confirm authenticated as a user with push access to formax68/memoChron.
    3. `gh repo view formax68/memoChron --json visibility -q .visibility`  →  must print `PUBLIC` (artifact attestations require public on GitHub Free/Pro/Team — 05-RESEARCH.md §6.7).
    4. `git -C /Users/mike/code/memoChron log --oneline -5`  →  confirm Plans 01/02/03 commits are on the current branch and pushed to `origin/main`.
    5. `git -C /Users/mike/code/memoChron status`  →  must be clean (no uncommitted changes).
    6. `cat /Users/mike/code/memoChron/manifest.json | grep description`  →  must show description ending in `.`,
    7. `grep -c "attest-build-provenance@v3" /Users/mike/code/memoChron/.github/workflows/release.yml`  →  must return 1.
    8. `cat /Users/mike/code/memoChron/package.json | grep '"version"'`  →  note the current version (likely `1.14.0`).
  </how-to-verify>
  <resume-signal>
    Paste the stdouts of the eight commands above. Reply "ready" if all eight pass, or describe the blocker if any fail.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Cut and push test pre-release tag 1.15.0-beta.1 (DIR-12 acceptance — release flow)</name>
  <files>package.json, manifest.json, versions.json</files>
  <read_first>
    - .planning/phases/05-guardrails-trivial-fixes/05-RESEARCH.md §7.1 (tag convention — `1.15.0-beta.1` matches prior `1.3.0-beta.1`), §7.2 (BRAT-compat tag flow)
    - .planning/phases/05-guardrails-trivial-fixes/05-PATTERNS.md "manifest.json" section (version-bump.mjs durability — only touches `version`, never `description`)
    - CLAUDE.md "Beta Release Strategy" (BRAT-compat — version in manifest.json must match release tag exactly)
    - version-bump.mjs (confirm it preserves manifest.json description and only mutates `version` + `versions.json`)
    - package.json (current version field)
    - manifest.json (current version field + the `.`-terminated description from Plan 01)
    - versions.json (current shape)
  </read_first>
  <action>
    Cut and push the test pre-release tag. Per D-10 and 05-RESEARCH.md §7.2:

    1. From the repo root, run `npm version 1.15.0-beta.1`. This:
       - Updates `package.json` `version` to `1.15.0-beta.1`
       - Triggers the `version` npm script (`node version-bump.mjs && git add manifest.json versions.json`)
       - version-bump.mjs reads `package.json` version and writes it to `manifest.json` `version` field AND to `versions.json` (mapping `1.15.0-beta.1` → the current `manifest.json.minAppVersion`, `1.8.9`)
       - npm commits `package.json` (and the staged `manifest.json` + `versions.json`) with message `1.15.0-beta.1` and creates an annotated tag `1.15.0-beta.1`
    2. Sanity-check the resulting tree BEFORE pushing:
       - `node -e "console.log(require('./package.json').version)"`  →  `1.15.0-beta.1`
       - `node -e "console.log(require('./manifest.json').version)"`  →  `1.15.0-beta.1`
       - `node -e "console.log(require('./manifest.json').description)"`  →  must still end with `.` (Plan 01's edit durable)
       - `node -e "console.log(require('./versions.json')['1.15.0-beta.1'])"`  →  `1.8.9`
       - `git log -1 --oneline`  →  shows the version-bump commit
       - `git tag -l 1.15.0-beta.1`  →  shows the tag
    3. Push the commit and the tag:
       - `git push origin main`
       - `git push origin 1.15.0-beta.1`
       The tag push triggers `release.yml` which: checks out, builds (`npm install && npm run build`), runs `attest-build-provenance@v3` against `main.js` + `manifest.json` + `styles.css`, then `gh release create --draft 1.15.0-beta.1 ...`.

    Per D-11 this is the fourth atomic commit in the locked phase sequence and its title must align with the illustrative D-11 shape. The default `npm version 1.15.0-beta.1` commit message is `1.15.0-beta.1` (the npm default; matches all prior version commits), which DOES NOT match D-11's illustrative title. After `npm version` creates the commit and tag and BEFORE pushing:

       - Amend the auto-generated commit message to `chore(release): cut 1.15.0-beta.1 to verify attestation flow (DIR-12 acceptance)` via `git commit --amend -m "chore(release): cut 1.15.0-beta.1 to verify attestation flow (DIR-12 acceptance)"`.
       - Re-tag if needed: `npm version` creates the annotated tag pointing at the original commit. After `--amend`, run `git tag -f 1.15.0-beta.1` (annotated) to repoint the tag at the amended commit. Verify with `git tag -l --format='%(objectname) %(refname:strip=2)' 1.15.0-beta.1 | head -1`  →  the SHA should equal `git rev-parse HEAD`.
       - NO Claude/AI references anywhere in the amended message (per D-12 / CLAUDE.md).

    Do NOT touch `release.yml`, `eslint.config.mjs`, `lint.yml`, source code, or any other file.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'),m=require('./manifest.json'),v=require('./versions.json'); if(p.version!=='1.15.0-beta.1'){console.error('package.json',p.version);process.exit(1)} if(m.version!=='1.15.0-beta.1'){console.error('manifest.json',m.version);process.exit(1)} if(!/[.!?]$/.test(m.description)){console.error('description regression',m.description);process.exit(1)} if(v['1.15.0-beta.1']!=='1.8.9'){console.error('versions.json',v);process.exit(1)} console.log('version state ok')" && git tag -l 1.15.0-beta.1 | grep -q "^1.15.0-beta.1$" && echo "tag exists" && git ls-remote --tags origin 1.15.0-beta.1 | grep -q "refs/tags/1.15.0-beta.1" && echo "tag pushed" && [ "$(git rev-list -n 1 1.15.0-beta.1)" = "$(git rev-parse HEAD)" ] && echo "tag points at HEAD" && git log -1 --format=%s | grep -qF "chore(release): cut 1.15.0-beta.1 to verify attestation flow (DIR-12 acceptance)" && echo "commit message matches D-11"</automated>
  </verify>
  <acceptance_criteria>
    - All four version files agree on `1.15.0-beta.1` (package.json, manifest.json, versions.json key).
    - manifest.json `description` still ends with `.` (Plan 01's edit was not regressed by version-bump.mjs).
    - Tag `1.15.0-beta.1` exists locally AND on `origin`.
    - `git log -1 --format=%s` shows `chore(release): cut 1.15.0-beta.1 to verify attestation flow (DIR-12 acceptance)` (the amended message — aligns with D-11 illustrative commit-4 title; NO Claude/AI references per D-12).
    - Annotated tag `1.15.0-beta.1` points at the amended commit (`git rev-list -n 1 1.15.0-beta.1` equals `git rev-parse HEAD`).
    - `git status` is clean post-push.
  </acceptance_criteria>
  <done>
    Tag `1.15.0-beta.1` pushed to GitHub; `release.yml` has been triggered (run is in progress or complete).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wait for release.yml to complete and verify attestation</name>
  <what-built>
    Tag `1.15.0-beta.1` is pushed; `release.yml` is running or has completed; the attestation registry entry should be persisted.
  </what-built>
  <how-to-verify>
    1. Watch the workflow run to completion:
       `gh run watch --repo formax68/memoChron $(gh run list --repo formax68/memoChron --branch 1.15.0-beta.1 --workflow release.yml --limit 1 --json databaseId -q .[0].databaseId)`
       OR open https://github.com/formax68/memoChron/actions and wait until the "Release Obsidian plugin" run for tag `1.15.0-beta.1` shows green.

    2. Confirm a draft release was created:
       `gh release view 1.15.0-beta.1 --repo formax68/memoChron --json url,isDraft,assets`
       Expect: `isDraft: true`, three assets (`main.js`, `manifest.json`, `styles.css`), `url` shaped like https://github.com/formax68/memoChron/releases/tag/untagged-... or .../releases/tag/1.15.0-beta.1.

    3. Download the three assets to a tmp working dir and run `gh attestation verify` against each:
       ```
       mkdir -p /tmp/dir12-uat && cd /tmp/dir12-uat
       gh release download 1.15.0-beta.1 --repo formax68/memoChron --pattern 'main.js' --pattern 'manifest.json' --pattern 'styles.css'
       gh attestation verify main.js       --repo formax68/memoChron
       gh attestation verify manifest.json --repo formax68/memoChron
       gh attestation verify styles.css    --repo formax68/memoChron
       ```
       Each `verify` command MUST exit 0 (per 05-RESEARCH.md §8.2 + §8.3 — successful verification prints `successfully verified` / `all attestations verified` / per-predicate `sigstore: ✓ Verified...`).

    4. Confirm the downloaded `manifest.json` artifact still has `description` ending in `.` AND `version: "1.15.0-beta.1"`:
       `node -e "const m=require('/tmp/dir12-uat/manifest.json'); console.log({description:m.description, version:m.version, descOk:/[.!?]$/.test(m.description)})"`
       Expect: `descOk: true`, `version: '1.15.0-beta.1'`.

    Capture ALL of the following into clipboard and paste into the resume signal:
    - The draft release URL from step 2
    - The full stdout of each of the three `gh attestation verify` commands
    - The `node -e ...` output from step 4
  </how-to-verify>
  <resume-signal>
    Paste the captured outputs. Reply "all verified" if all three `gh attestation verify` commands exited 0 and the manifest.json artifact is correct, or describe the failure mode if any verification failed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Author 05-HUMAN-UAT.md with the captured DIR-12 evidence</name>
  <files>.planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-guardrails-trivial-fixes/05-VALIDATION.md "Manual-Only Verifications" table (`gh attestation verify` row + `BRAT-compatible release artifacts unchanged` row + `Fresh-clone lint flow` row + `Deliberate innerHTML regression catches` row + `GitHub PR check status visible` row)
    - The captured stdout from Task 3 (from the resume signal)
    - .planning/phases/05-guardrails-trivial-fixes/05-CONTEXT.md D-13 (what the UAT file is for) + D-10 (what evidence DIR-12 acceptance requires)
  </read_first>
  <action>
    Create `.planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md` with these sections, populated from the captured evidence in Task 3:

    1. `# Phase 5 — Human UAT Evidence` header with phase / date / status metadata.
    2. `## DIR-11 Verification` — the `node -e ...` assertion stdout from Plan 01's SUMMARY confirming `description` ends with `.` (cross-reference; can be a single line).
    3. `## DIR-12 Verification` — populate with:
       - The test pre-release URL (draft release URL from Task 3 step 2)
       - The exact tag string used (`1.15.0-beta.1`) and the commit SHA the tag points to
       - The full stdout of `gh attestation verify main.js --repo formax68/memoChron`
       - The full stdout of `gh attestation verify manifest.json --repo formax68/memoChron`
       - The full stdout of `gh attestation verify styles.css --repo formax68/memoChron`
       - The `node -e ...` confirmation that the downloaded `manifest.json` artifact has description ending in `.` AND version `1.15.0-beta.1`
    4. `## DOC-01 Verification` — populate with:
       - `npm ci && npm run lint` exit-0 confirmation (fresh-clone flow — per 05-VALIDATION.md "Manual-Only Verifications" row 1)
       - The regression smoke-check stdout from Plan 03 Task 2 (deliberate `el.innerHTML` write → non-zero exit + offending rule ID — per 05-VALIDATION.md row 2)
       - The `lint.yml` workflow's first PR-check appearance (link to a PR's checks view, or a note if no PR has been opened yet — per 05-VALIDATION.md row 3 — this can be deferred to the first real PR if no test PR is available, in which case note that explicitly)
    5. `## Disposition of test pre-release tag` — record the planner's call per D-10: KEEP `1.15.0-beta.1` as v1.15's first beta, OR DELETE the draft release and tag after evidence capture (the attestation registry entry persists either way per 05-RESEARCH.md §7.3). Default recommendation: KEEP the draft as v1.15's first beta (one less moving part).
    6. `## Sign-off` — single-line "DIR-11 ✅, DIR-12 ✅, DOC-01 ✅ — Phase 5 acceptance evidence captured 2026-MM-DD".

    Use markdown fenced code blocks (```) for the captured stdouts so they are unambiguous.

    NO Claude/AI references anywhere in the UAT file (per CLAUDE.md and CONTEXT.md D-12 — applies to all phase artifacts, not just commits).

    Per D-10, D-13.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md && grep -q "^## DIR-12 Verification" .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md && grep -q "gh attestation verify main.js" .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md && grep -q "gh attestation verify manifest.json" .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md && grep -q "gh attestation verify styles.css" .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md && grep -q "1.15.0-beta.1" .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md && ! grep -iE "claude|AI[- ]assist|generated with" .planning/phases/05-guardrails-trivial-fixes/05-HUMAN-UAT.md && echo "UAT ok"</automated>
  </verify>
  <acceptance_criteria>
    - `05-HUMAN-UAT.md` exists at the phase directory.
    - Contains all three `gh attestation verify` command stdouts in fenced code blocks.
    - Contains the test pre-release tag string (`1.15.0-beta.1`) and the draft release URL.
    - Contains the `## DIR-11 Verification`, `## DIR-12 Verification`, and `## DOC-01 Verification` sections.
    - Contains zero matches for `claude`, `AI assist`, `AI-assisted`, or `generated with` (case-insensitive).
    - The disposition of the test pre-release tag (KEEP or DELETE) is explicitly recorded.
  </acceptance_criteria>
  <done>
    `05-HUMAN-UAT.md` captures the full DIR-12 acceptance evidence + cross-references DIR-11 and DOC-01 evidence; ready to commit alongside the Plan 04 SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub Actions runner | Tag push triggers release.yml on the GitHub-hosted runner |
| GitHub Actions runner → Sigstore | OIDC-mediated signing of the multi-subject attestation |
| GitHub attestations API → `gh attestation verify` | Online attestation lookup by SHA-256 digest |
| Captured stdout → 05-HUMAN-UAT.md | Plain-text evidence record; written to the planning directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 (acceptance) | Spoofing | Release artifact provenance | mitigate | This plan PROVES T-05-01's mitigation works end-to-end — three `gh attestation verify` commands MUST exit 0 against the actual draft release assets. Captured in 05-HUMAN-UAT.md. |
| T-05-12 | Tampering | manifest.json `description` regression via version-bump.mjs | mitigate | Task 2 explicitly asserts (`node -e ...`) that the `.` terminating punctuation survives the `npm version` flow. Backed by source-read of version-bump.mjs in 05-PATTERNS.md and 05-RESEARCH.md §9.11 confirming the script only mutates `version` and `versions.json`. |
| T-05-13 | Tampering | Tag pushed without `release.yml` having been merged (wrong workflow runs) | mitigate (process) | Plan 04 depends_on Plan 02 (release.yml change). Task 1's pre-flight check #7 asserts `attest-build-provenance@v3` is present in `release.yml` on the branch BEFORE the tag is cut. |
| T-05-14 | Information Disclosure | UAT file contains repo-internal info (commit SHAs, tag URLs) | accept | All captured info is from a public repo; nothing is secret. Recording the SHAs is the point — it makes the evidence reproducible. |
</threat_model>

<verification>
- All three `gh attestation verify` commands exit 0 (captured in Task 3 resume signal and Task 4's UAT file)
- `package.json` / `manifest.json` / `versions.json` all agree on `1.15.0-beta.1`
- manifest.json `description` still ends with `.` after the version bump (Plan 01's edit is durable)
- `05-HUMAN-UAT.md` exists, contains the three verification stdouts, the tag string, the release URL, and the per-requirement sections
- No Claude/AI references anywhere in the UAT file or in the version-bump commit message
</verification>

<success_criteria>
- ROADMAP success criterion #5 fully satisfied: release workflow + next pre-release output both verify the attestation flow end-to-end
- DIR-12 marked complete in `.planning/REQUIREMENTS.md` (Requirements / Traceability sections)
- Phase 5 commit 4 (the `npm version 1.15.0-beta.1` auto-commit, amended to the D-11 illustrative title `chore(release): cut 1.15.0-beta.1 to verify attestation flow (DIR-12 acceptance)` per Task 2) is in `origin/main`
- `05-HUMAN-UAT.md` is committed alongside the Plan 04 SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/05-guardrails-trivial-fixes/05-04-SUMMARY.md` with:
- The `git log -1 --format=fuller` of the version-bump commit (showing commit SHA + message — NO Claude/AI references in the message)
- The draft pre-release URL
- The full stdout of all three `gh attestation verify` commands (or a pointer to `05-HUMAN-UAT.md` where the same content is captured)
- The disposition of the `1.15.0-beta.1` tag (KEEP / DELETE)
- A note that all five ROADMAP Phase 5 success criteria (#1, #2, #3, #4, #5) are now satisfied
- The commit hash(es) used to record `05-HUMAN-UAT.md` (which is a `docs(05): ...` commit separate from the `npm version` commit — per D-11 commit hygiene, NO Claude/AI references)
</output>
