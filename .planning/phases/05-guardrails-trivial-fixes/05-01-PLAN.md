---
phase: 05-guardrails-trivial-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
autonomous: true
requirements:
  - DIR-11
must_haves:
  truths:
    - "D-08: manifest.json description ends with `.`, `!`, or `?` — new value `\"Calendar integration and note creation with support for public iCalendar URLs.\"` (single-character append per CONTEXT.md D-08)"
    - "D-08: manifest.json indentation, key order, and all other fields are byte-identical to the pre-edit file (tab indent preserved per D-08 'no other wording change')"
    - "D-11: this is commit 1 of 4 in the locked phase sequence (DIR-11 → DIR-12 → DOC-01 → pre-release verification), title `fix(manifest): end description with a period (DIR-11)`"
    - "D-12: commit message contains NO Claude / AI references; DIR-11 ID appears in title or body"
    - "version-bump.mjs still produces a no-op diff against the new manifest.json description (the new punctuation is durable across `npm version`)"
  artifacts:
    - path: manifest.json
      provides: "Obsidian plugin manifest with terminating punctuation on description"
      contains: 'description.*\\.\"'
  key_links:
    - from: manifest.json
      to: "Obsidian community-plugin scorecard DIR-11"
      via: "description field byte-equality check"
      pattern: '"description":\\s*"[^"]*[.!?]"'
---

<objective>
Close DIR-11. Append a single `.` to the `description` value in `manifest.json` so the field ends with terminating punctuation.

Purpose: Removes the smallest scorecard finding before the bulk DOC-01 work begins, so the milestone's mechanical fixes land separately from the toolchain change.
Output: A one-character diff on `manifest.json`, line 6, byte-equal to the rest of the file (tab indentation preserved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-guardrails-trivial-fixes/05-CONTEXT.md
@.planning/phases/05-guardrails-trivial-fixes/05-RESEARCH.md
@.planning/phases/05-guardrails-trivial-fixes/05-PATTERNS.md
@CLAUDE.md
@manifest.json
@version-bump.mjs

<interfaces>
<!-- The only consumer of manifest.json's `description` field is Obsidian's community-plugin Review scorecard. -->
<!-- version-bump.mjs (line 9) writes manifest.json via JSON.stringify(manifest, null, "\t") and ONLY mutates the `version` key — confirmed in 05-PATTERNS.md and 05-RESEARCH.md §9.11. The `.` we append is therefore durable across future `npm version` runs. -->

Current manifest.json (verified `od -c`, tab indentation):
  Line 6: `\t"description": "Calendar integration and note creation with support for public iCalendar URLs",`

Required after this plan:
  Line 6: `\t"description": "Calendar integration and note creation with support for public iCalendar URLs.",`

No other line changes. Field order, tab indentation, and trailing-newline state must be preserved.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append terminating period to manifest.json description (DIR-11)</name>
  <files>manifest.json</files>
  <read_first>
    - manifest.json (the file being modified — confirm current value byte-for-byte)
    - .planning/phases/05-guardrails-trivial-fixes/05-RESEARCH.md §6.2 (exact before/after string)
    - .planning/phases/05-guardrails-trivial-fixes/05-PATTERNS.md (tab-indentation preservation note, version-bump.mjs durability confirmation)
    - version-bump.mjs (confirm it never rewrites `description`)
  </read_first>
  <action>
    Edit manifest.json line 6. Change the value of the `description` key from `"Calendar integration and note creation with support for public iCalendar URLs"` to `"Calendar integration and note creation with support for public iCalendar URLs."` (single trailing `.` appended inside the closing quote). Per D-08 / DIR-11. Preserve tab indentation, key order, the trailing comma after the value, and the file's trailing-newline state. Do NOT touch any other field. Do NOT reformat. Do NOT update `package.json` `description` (different field, not in DIR-11 scope per CONTEXT.md D-08).
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('manifest.json','utf8')).description; if(!/[.!?]$/.test(d)){console.error('DIR-11 fail: description does not end with .!?',JSON.stringify(d));process.exit(1)} if(d!=='Calendar integration and note creation with support for public iCalendar URLs.'){console.error('DIR-11 fail: unexpected description value',JSON.stringify(d));process.exit(1)} console.log('DIR-11 ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e ...` assertion above exits 0 and prints `DIR-11 ok`.
    - `git diff manifest.json` shows exactly one line changed and the diff hunk introduces exactly one `.` character (no other byte changes).
    - `grep -cP "^\t" manifest.json` returns >= 7 (tab indentation preserved across all 7 indented keys).
    - JSON parses cleanly: `node -e "JSON.parse(require('fs').readFileSync('manifest.json','utf8'))"` exits 0.
  </acceptance_criteria>
  <done>
    manifest.json `description` value ends with `.`; tab indentation and all other fields byte-equal to pre-edit file; JSON still parses; diff hunk is one character.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File-on-disk → Obsidian plugin loader | manifest.json is read by Obsidian on plugin load; malformed JSON breaks the plugin |
| File-on-disk → community-plugin Review scorecard | scorecard reads `description` field as a string; only content (terminating punctuation) is checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | manifest.json JSON validity | mitigate | Automated `JSON.parse` check in `<verify>` runs after the edit |
| T-05-05 | Repudiation | DIR-11 acceptance claim | mitigate | Source-assertion `node -e ...` check in `<verify>` proves the byte-level state; result captured in plan SUMMARY |
</threat_model>

<verification>
- `node -e "process.exit(JSON.parse(require('fs').readFileSync('manifest.json','utf8')).description.match(/[.!?]$/)?0:1)"` exits 0
- `git diff --shortstat manifest.json` shows `1 file changed, 1 insertion(+), 1 deletion(-)` (single line replaced) — or `1 file changed, 1 insertion(+)` if the editor performs a pure insertion
- `grep -c "\"description\":" manifest.json` returns 1 (no duplication of the key)
</verification>

<success_criteria>
- ROADMAP success criterion #4 satisfied: the `description` field in `manifest.json` ends with `.`, `!`, or `?`
- No diff-noise on tab indentation, key order, or any other manifest field
- Phase 5 commit 1 (`fix(manifest): end description with a period (DIR-11)`) is ready to author
</success_criteria>

<output>
After completion, create `.planning/phases/05-guardrails-trivial-fixes/05-01-SUMMARY.md` with:
- The exact `git diff manifest.json` output (1 line changed)
- The `node -e ...` assertion stdout (`DIR-11 ok`)
- Confirmation that `git status` is clean except for `manifest.json`
- The commit hash and message body (per D-11: `fix(manifest): end description with a period (DIR-11)`; NO Claude/AI references per CLAUDE.md and D-12)
</output>
