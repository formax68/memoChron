---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/services/CalendarService.ts
  - src/utils/constants.ts
  - src/views/EmbeddedCalendarView.ts
  - src/views/EmbeddedAgendaView.ts
autonomous: true
requirements: [CLEAN-01]
tags: [cleanup, dead-code, imports]

must_haves:
  truths:
    - "The codebase contains no reference to calculateEndDate, DEFAULT_TEMPLATE_PATH, or TEMPLATE_VARIABLES anywhere under src/"
    - "The unused App import in EmbeddedCalendarView.ts is gone"
    - "The unused TFile and renderAgendaList imports in EmbeddedAgendaView.ts are gone"
    - "The renderAgendaList FUNCTION in viewRenderers.ts is intact (D-11 — only the import was unused)"
  artifacts:
    - path: "src/services/CalendarService.ts"
      provides: "calculateEndDate private method removed"
      contains: ""
    - path: "src/utils/constants.ts"
      provides: "DEFAULT_TEMPLATE_PATH and TEMPLATE_VARIABLES constants removed"
      contains: ""
    - path: "src/views/EmbeddedCalendarView.ts"
      provides: "App removed from obsidian named-import"
      contains: "import { MarkdownRenderChild, Notice } from \"obsidian\""
    - path: "src/views/EmbeddedAgendaView.ts"
      provides: "TFile dropped from obsidian import; renderAgendaList dropped from viewRenderers import"
      contains: "import { MarkdownRenderChild, Notice } from \"obsidian\""
    - path: "src/utils/viewRenderers.ts"
      provides: "renderAgendaList function definition INTACT (D-11 — out of scope to remove)"
      contains: "export function renderAgendaList"
  key_links:
    - from: "src/views/EmbeddedAgendaView.ts imports"
      to: "src/utils/viewRenderers.ts (parseDate, RenderOptions only)"
      via: "named import"
      pattern: "import \\{\\s*parseDate,\\s*RenderOptions,?\\s*\\} from \"\\.\\./utils/viewRenderers\""
---

<objective>
CLEAN-01: Remove the four dead-code targets named in the phase success criteria — `CalendarService.calculateEndDate` (private method), `DEFAULT_TEMPLATE_PATH` and `TEMPLATE_VARIABLES` (constants), the unused `App` import in `EmbeddedCalendarView.ts`, and the unused `TFile`/`renderAgendaList` imports in `EmbeddedAgendaView.ts`. Per D-11, leave the `renderAgendaList` FUNCTION in `viewRenderers.ts` intact (only the IMPORT in EmbeddedAgendaView.ts is in scope).

Purpose: Reduce noise for all subsequent phases. After this plan, `grep -rn "calculateEndDate|DEFAULT_TEMPLATE_PATH|TEMPLATE_VARIABLES" src/` returns no output, and the four import statements are minimal and accurate.

Output: Modified `src/services/CalendarService.ts` (delete one method), `src/utils/constants.ts` (delete two constants), `src/views/EmbeddedCalendarView.ts` (one import edit), `src/views/EmbeddedAgendaView.ts` (two import edits). Atomic commit per requirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

@src/services/CalendarService.ts
@src/utils/constants.ts
@src/views/EmbeddedCalendarView.ts
@src/views/EmbeddedAgendaView.ts
@src/utils/viewRenderers.ts

<interfaces>
Existing CalendarService.calculateEndDate (BEFORE — to be DELETED), per src/services/CalendarService.ts lines 758-762:
```typescript
private calculateEndDate(start: Time, duration: any): Date {
  const end = start.clone();
  end.addDuration(duration);
  return end.toJSDate();
}
```
Verified zero call sites: `grep -n "calculateEndDate" src/services/CalendarService.ts` returns ONLY the definition line. The two recurring/single processing paths (lines 655-708 and 715-738) inline equivalent logic via `endTime.clone()` + `addDuration`, so no replacement is needed.

The `Time` type imported from `ical.js` at line 2 of CalendarService.ts is still referenced elsewhere in the file (e.g., `let next: Time | null` at line 652) — do NOT auto-prune the `Time` import.

Existing constants.ts (BEFORE — DEFAULT_TEMPLATE_PATH and TEMPLATE_VARIABLES to be DELETED), per src/utils/constants.ts lines 6 and 17-24:
```typescript
// Line 6:
export const DEFAULT_TEMPLATE_PATH: string = 'templates/defaultTemplate.md';

// Lines 17-24:
export const TEMPLATE_VARIABLES = {
    TITLE: '{{title}}',
    START_TIME: '{{startTime}}',
    END_TIME: '{{endTime}}',
    DATE: '{{date}}',
    DESCRIPTION: '{{description}}',
    LOCATION: '{{location}}',
};
```
Verified zero importers via `grep -rn "DEFAULT_TEMPLATE_PATH\|TEMPLATE_VARIABLES" src/` returning only the definitions. Other constants in this file (`DEFAULT_NOTE_TITLE_FORMAT`, `DEFAULT_FRONTMATTER`, `MEMOCHRON_VIEW_TYPE`, color palette, etc.) MUST be preserved — D-10 is strictly scoped to the two named symbols.

Existing EmbeddedCalendarView.ts imports (BEFORE), per src/views/EmbeddedCalendarView.ts line 1:
```typescript
import { MarkdownRenderChild, Notice, App } from "obsidian";
```
Verified `App` is referenced only at line 1 (the import) — `grep -n "\bApp\b" src/views/EmbeddedCalendarView.ts` returns just line 1. `TFile` is NOT imported here — do not invent a removal.

Existing EmbeddedAgendaView.ts imports (BEFORE), per src/views/EmbeddedAgendaView.ts lines 1-7:
```typescript
import { MarkdownRenderChild, Notice, TFile } from "obsidian";
import MemoChron from "../main";
import {
  renderAgendaList,
  parseDate,
  RenderOptions,
} from "../utils/viewRenderers";
```
Both `TFile` (line 1) and `renderAgendaList` (line 4) are unused per the grep verification in research. The other imports (`MarkdownRenderChild`, `Notice`, `MemoChron`, `parseDate`, `RenderOptions`) are all still used — DO NOT remove them.

D-11 constraint: The `renderAgendaList` FUNCTION at `src/utils/viewRenderers.ts:81` is OUT OF SCOPE for this phase. Only the IMPORT in EmbeddedAgendaView.ts is removed. The function remains exported and available for future callers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete CalendarService.calculateEndDate private method</name>
  <files>src/services/CalendarService.ts</files>
  <read_first>
    - src/services/CalendarService.ts (search for `calculateEndDate` to confirm it's still at the same approximate line range — Plans 01 and 03 may have shifted line numbers slightly; the method body is unambiguous)
    - .planning/phases/01-foundation/01-RESEARCH.md (Common Pitfalls — Pitfall 5 about calculateEndDate being a private method, not an import; Code Examples — "Dead-Code Verification Commands")
    - .planning/phases/01-foundation/01-PATTERNS.md (section "src/services/CalendarService.ts / Current calculateEndDate" — confirms zero callers and the inline-logic note)
    - .planning/phases/01-foundation/01-CONTEXT.md (decision D-10)
  </read_first>
  <action>
    Delete the `calculateEndDate` private method from `src/services/CalendarService.ts` per D-10.

    Locate the method (was at lines 758-762 in BEFORE state; line numbers may have shifted slightly):
    ```typescript
    private calculateEndDate(start: Time, duration: any): Date {
      const end = start.clone();
      end.addDuration(duration);
      return end.toJSDate();
    }
    ```

    Delete the entire method, including any blank line preceding it that becomes orphaned by the deletion. Do NOT modify any surrounding methods. Do NOT touch the `Time` import at line 2 — it is still used elsewhere in the file (e.g., `let next: Time | null` around line 652 in BEFORE state).

    Verify by searching for `calculateEndDate` in the file: there should be ZERO matches after the deletion (the call-site verification step in research already confirmed there are no callers, only the definition).

    Do NOT touch any other method in CalendarService.ts. Do NOT modify imports. Do NOT touch `scheduleBackgroundRefresh` (Plan 03 finalized it). Do NOT touch `needsRefresh` or the constructor (Plan 01 finalized them).
  </action>
  <verify>
    <automated>node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck &amp;&amp; node esbuild.config.mjs production &amp;&amp; ! grep -nE "calculateEndDate" src/services/CalendarService.ts &amp;&amp; grep -cE "import \{[^}]*\bTime\b[^}]*\} from" src/services/CalendarService.ts | grep -vE '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck` exits 0
    - `node esbuild.config.mjs production` exits 0
    - `grep -nE "calculateEndDate" src/services/CalendarService.ts` returns NO match
    - `grep -E "import \{[^}]*\bTime\b[^}]*\} from" src/services/CalendarService.ts` returns at least one match (the `Time` import is preserved because it is used elsewhere)
    - File still has `private async fetchCalendars` method (sanity — `grep -c "private async fetchCalendars" src/services/CalendarService.ts` >= 1)
    - File still has `scheduleBackgroundRefresh` method (sanity — `grep -c "private scheduleBackgroundRefresh" src/services/CalendarService.ts` >= 1)
    - File still has `needsRefresh` method (sanity — `grep -c "private needsRefresh" src/services/CalendarService.ts` >= 1)
  </acceptance_criteria>
  <done>
    `calculateEndDate` is deleted from `CalendarService.ts`. The `Time` import is preserved. TypeScript and esbuild succeed. Other methods are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete DEFAULT_TEMPLATE_PATH and TEMPLATE_VARIABLES from constants.ts</name>
  <files>src/utils/constants.ts</files>
  <read_first>
    - src/utils/constants.ts (full file — small file; confirm the two targets and verify the remaining constants like `MEMOCHRON_VIEW_TYPE`, `DEFAULT_NOTE_TITLE_FORMAT`, `DEFAULT_FRONTMATTER`, color palette etc. are preserved)
    - .planning/phases/01-foundation/01-PATTERNS.md (section "src/utils/constants.ts" — exact lines and the strict-scope rule)
    - .planning/phases/01-foundation/01-CONTEXT.md (decision D-10 — only the two named constants)
  </read_first>
  <action>
    Delete exactly two constants from `src/utils/constants.ts` per D-10:

    1. Delete the line:
    ```typescript
    export const DEFAULT_TEMPLATE_PATH: string = 'templates/defaultTemplate.md';
    ```
    (was at line 6 in BEFORE state)

    2. Delete the entire `TEMPLATE_VARIABLES` block (was at lines 17-24 in BEFORE state):
    ```typescript
    export const TEMPLATE_VARIABLES = {
        TITLE: '{{title}}',
        START_TIME: '{{startTime}}',
        END_TIME: '{{endTime}}',
        DATE: '{{date}}',
        DESCRIPTION: '{{description}}',
        LOCATION: '{{location}}',
    };
    ```
    Including the closing `};` and any orphaned blank line immediately following.

    Preserve everything else in this file:
    - `MEMOCHRON_VIEW_TYPE`
    - `DEFAULT_NOTE_TITLE_FORMAT` (if present)
    - `DEFAULT_FRONTMATTER` (if present)
    - `DEFAULT_NOTE_LOCATION` (if present)
    - `DEFAULT_REFRESH_INTERVAL` (if present)
    - `CALENDAR_COLOR_PALETTE` (if present)
    - Any other CUTYPE / color / default constants

    Do NOT remove blank lines between OTHER constants (only orphaned blank lines from the deletions). Do NOT modify any other constant's value.
  </action>
  <verify>
    <automated>node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck &amp;&amp; node esbuild.config.mjs production &amp;&amp; ! grep -nE "DEFAULT_TEMPLATE_PATH|TEMPLATE_VARIABLES" src/utils/constants.ts &amp;&amp; grep -cE "MEMOCHRON_VIEW_TYPE" src/utils/constants.ts | grep -vE '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck` exits 0
    - `node esbuild.config.mjs production` exits 0
    - `grep -nE "DEFAULT_TEMPLATE_PATH" src/utils/constants.ts` returns NO match
    - `grep -nE "TEMPLATE_VARIABLES" src/utils/constants.ts` returns NO match
    - `grep -cE "MEMOCHRON_VIEW_TYPE" src/utils/constants.ts` is at least 1 (preserved)
    - `grep -rnE "DEFAULT_TEMPLATE_PATH|TEMPLATE_VARIABLES" src/` returns NO match (no other file imported them)
  </acceptance_criteria>
  <done>
    `DEFAULT_TEMPLATE_PATH` and `TEMPLATE_VARIABLES` are deleted from `src/utils/constants.ts`. All other constants in this file are preserved. TypeScript and esbuild succeed. No other file under `src/` references either of the deleted symbols.
  </done>
</task>

<task type="auto">
  <name>Task 3: Drop unused App import from EmbeddedCalendarView.ts</name>
  <files>src/views/EmbeddedCalendarView.ts</files>
  <read_first>
    - src/views/EmbeddedCalendarView.ts (full file — confirm App appears only on line 1 and is not used in the class body; small file)
    - .planning/phases/01-foundation/01-PATTERNS.md (section "src/views/EmbeddedCalendarView.ts" — straight import deletion)
    - .planning/phases/01-foundation/01-CONTEXT.md (decision D-10)
  </read_first>
  <action>
    Edit `src/views/EmbeddedCalendarView.ts` line 1 per D-10.

    Replace:
    ```typescript
    import { MarkdownRenderChild, Notice, App } from "obsidian";
    ```
    with:
    ```typescript
    import { MarkdownRenderChild, Notice } from "obsidian";
    ```

    `MarkdownRenderChild` and `Notice` are still used in this file — do NOT remove them. `TFile` is NOT imported here — do not invent a removal.

    Do NOT touch any other code in this file.
  </action>
  <verify>
    <automated>node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck &amp;&amp; node esbuild.config.mjs production &amp;&amp; ! grep -nE "\bApp\b" src/views/EmbeddedCalendarView.ts &amp;&amp; grep -cE "import \{ MarkdownRenderChild, Notice \} from \"obsidian\"" src/views/EmbeddedCalendarView.ts | grep -E '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck` exits 0
    - `node esbuild.config.mjs production` exits 0
    - `grep -nE "\bApp\b" src/views/EmbeddedCalendarView.ts` returns NO match (App fully gone — confirms it was only in the import)
    - `grep -cE "import \{ MarkdownRenderChild, Notice \} from \"obsidian\"" src/views/EmbeddedCalendarView.ts` is exactly 1
    - File still parses: `grep -c "MarkdownRenderChild" src/views/EmbeddedCalendarView.ts` >= 2 (import line + at least one usage in `class X extends MarkdownRenderChild`)
  </acceptance_criteria>
  <done>
    `App` is removed from EmbeddedCalendarView.ts's obsidian import. `MarkdownRenderChild` and `Notice` are preserved. TypeScript and esbuild succeed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Drop unused TFile and renderAgendaList imports from EmbeddedAgendaView.ts</name>
  <files>src/views/EmbeddedAgendaView.ts</files>
  <read_first>
    - src/views/EmbeddedAgendaView.ts (full file — confirm TFile is only on line 1 and renderAgendaList is only on the named-import block at lines 4-7; verify parseDate and RenderOptions ARE still used)
    - src/utils/viewRenderers.ts (just confirm the renderAgendaList function definition is still present — it MUST remain per D-11)
    - .planning/phases/01-foundation/01-PATTERNS.md (section "src/views/EmbeddedAgendaView.ts" — exact replacement)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-10 and D-11 — function stays, only import goes)
  </read_first>
  <action>
    Edit `src/views/EmbeddedAgendaView.ts` lines 1-7 per D-10. Two import edits in this file.

    Edit 1 — drop `TFile` from the obsidian named-import. Replace:
    ```typescript
    import { MarkdownRenderChild, Notice, TFile } from "obsidian";
    ```
    with:
    ```typescript
    import { MarkdownRenderChild, Notice } from "obsidian";
    ```

    Edit 2 — drop `renderAgendaList` from the viewRenderers named-import. Replace:
    ```typescript
    import {
      renderAgendaList,
      parseDate,
      RenderOptions,
    } from "../utils/viewRenderers";
    ```
    with:
    ```typescript
    import {
      parseDate,
      RenderOptions,
    } from "../utils/viewRenderers";
    ```

    Do NOT touch the `import MemoChron from "../main";` line. Do NOT touch any other code in this file. Do NOT touch `src/utils/viewRenderers.ts` — D-11 explicitly preserves the `renderAgendaList` function there.
  </action>
  <verify>
    <automated>node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck &amp;&amp; node esbuild.config.mjs production &amp;&amp; ! grep -nE "\bTFile\b" src/views/EmbeddedAgendaView.ts &amp;&amp; ! grep -nE "renderAgendaList" src/views/EmbeddedAgendaView.ts &amp;&amp; grep -cE "parseDate" src/views/EmbeddedAgendaView.ts | grep -vE '^0$' &amp;&amp; grep -cE "RenderOptions" src/views/EmbeddedAgendaView.ts | grep -vE '^0$' &amp;&amp; grep -cE "export function renderAgendaList" src/utils/viewRenderers.ts | grep -E '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck` exits 0
    - `node esbuild.config.mjs production` exits 0
    - `grep -nE "\bTFile\b" src/views/EmbeddedAgendaView.ts` returns NO match
    - `grep -nE "renderAgendaList" src/views/EmbeddedAgendaView.ts` returns NO match (the import is gone)
    - `grep -cE "parseDate" src/views/EmbeddedAgendaView.ts` is at least 2 (still imported AND still used in the body)
    - `grep -cE "RenderOptions" src/views/EmbeddedAgendaView.ts` is at least 2 (still imported AND still used)
    - `grep -cE "import MemoChron from \"\.\./main\"" src/views/EmbeddedAgendaView.ts` is exactly 1 (untouched)
    - `grep -cE "export function renderAgendaList" src/utils/viewRenderers.ts` is exactly 1 (D-11 — function intact)
  </acceptance_criteria>
  <done>
    `TFile` is removed from EmbeddedAgendaView.ts's obsidian import. `renderAgendaList` is removed from the viewRenderers named-import. `parseDate` and `RenderOptions` imports are preserved. The `renderAgendaList` function in viewRenderers.ts is intact (D-11). TypeScript and esbuild succeed.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run final phase-wide dead-code grep gate</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples — "Dead-Code Verification Commands"; Verification Approach — CLEAN-01 static checks)
    - .planning/phases/01-foundation/01-CONTEXT.md (Phase 1 success criterion #4 — "The codebase contains no reference to calculateEndDate, DEFAULT_TEMPLATE_PATH, TEMPLATE_VARIABLES, the unused App/TFile imports, or the dead renderAgendaList import in embedded views")
  </read_first>
  <action>
    Run the phase-wide dead-code verification grep gate from RESEARCH.md. This task creates no source-file changes — it only verifies the cumulative state after Tasks 1-4.

    Run these commands in order. ALL must produce the expected output:

    ```bash
    # Gate 1: No reference to calculateEndDate, DEFAULT_TEMPLATE_PATH, or TEMPLATE_VARIABLES anywhere under src/
    grep -rn "calculateEndDate\|DEFAULT_TEMPLATE_PATH\|TEMPLATE_VARIABLES" src/
    # Expected: NO output (exit code from grep is 1, which `! grep ...` flips to success)

    # Gate 2: renderAgendaList import gone from EmbeddedAgendaView.ts
    grep -n "renderAgendaList" src/views/EmbeddedAgendaView.ts
    # Expected: NO output

    # Gate 3: App import gone from EmbeddedCalendarView.ts
    grep -nE "\bApp\b" src/views/EmbeddedCalendarView.ts
    # Expected: NO output

    # Gate 4: TFile import gone from EmbeddedAgendaView.ts
    grep -nE "\bTFile\b" src/views/EmbeddedAgendaView.ts
    # Expected: NO output

    # Gate 5: D-11 — renderAgendaList FUNCTION still in viewRenderers.ts
    grep -n "export function renderAgendaList" src/utils/viewRenderers.ts
    # Expected: ONE match (line 81 in BEFORE state)

    # Gate 6: Build clean
    node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck && \
      node esbuild.config.mjs production && echo "BUILD OK"
    ```

    If any gate fails, do NOT commit. Diagnose which earlier task left a residual reference and fix it.

    This task does NOT modify any file. It is purely a verification gate. The acceptance_criteria below encode the gate as a single automated command for the executor.
  </action>
  <verify>
    <automated>! grep -rn "calculateEndDate\|DEFAULT_TEMPLATE_PATH\|TEMPLATE_VARIABLES" src/ &amp;&amp; ! grep -n "renderAgendaList" src/views/EmbeddedAgendaView.ts &amp;&amp; ! grep -nE "\bApp\b" src/views/EmbeddedCalendarView.ts &amp;&amp; ! grep -nE "\bTFile\b" src/views/EmbeddedAgendaView.ts &amp;&amp; grep -cE "export function renderAgendaList" src/utils/viewRenderers.ts | grep -E '^1$' &amp;&amp; node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck &amp;&amp; node esbuild.config.mjs production</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "calculateEndDate\|DEFAULT_TEMPLATE_PATH\|TEMPLATE_VARIABLES" src/` returns NO match
    - `grep -n "renderAgendaList" src/views/EmbeddedAgendaView.ts` returns NO match
    - `grep -nE "\bApp\b" src/views/EmbeddedCalendarView.ts` returns NO match
    - `grep -nE "\bTFile\b" src/views/EmbeddedAgendaView.ts` returns NO match
    - `grep -cE "export function renderAgendaList" src/utils/viewRenderers.ts` is exactly 1 (D-11 honored)
    - `node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck` exits 0
    - `node esbuild.config.mjs production` exits 0
    - This task has no modified files — confirm with `git diff --name-only` returning the same files as Tasks 1-4 (the four files in this plan's `files_modified` frontmatter)
  </acceptance_criteria>
  <done>
    All five dead-code gates pass. Build is clean. The cumulative state of Tasks 1-4 satisfies Phase 1 success criterion #4.
  </done>
</task>

<task type="auto">
  <name>Task 6: Commit CLEAN-01 atomically</name>
  <files>src/services/CalendarService.ts, src/utils/constants.ts, src/views/EmbeddedCalendarView.ts, src/views/EmbeddedAgendaView.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (Claude's Discretion — per-requirement atomic commits is the GSD default)
    - CLAUDE.md (Memory Reminders section — commit messages MUST NOT reference Claude or AI assistance)
  </read_first>
  <action>
    Stage exactly the four modified files and create an atomic commit for CLEAN-01. Do NOT stage any other files.

    Run:
    ```bash
    git add src/services/CalendarService.ts src/utils/constants.ts src/views/EmbeddedCalendarView.ts src/views/EmbeddedAgendaView.ts
    git status
    ```

    Confirm only those four files are staged, then commit using a HEREDOC body. No co-author line, no Claude reference, no AI reference (per CLAUDE.md project instructions):

    ```bash
    git commit -m "$(cat <<'EOF'
    chore(cleanup): remove dead code (calculateEndDate, template constants, unused imports)

    CLEAN-01: Strict to the four named targets in the phase 1 success
    criteria — CalendarService.calculateEndDate (private, zero callers),
    DEFAULT_TEMPLATE_PATH and TEMPLATE_VARIABLES constants (zero
    importers), the unused App import in EmbeddedCalendarView, and the
    unused TFile and renderAgendaList imports in EmbeddedAgendaView.
    The renderAgendaList function in viewRenderers.ts is left intact
    per D-11 (out of scope to remove).
    EOF
    )"
    ```

    After the commit, run `git log -1 --stat` to confirm only the four intended files were touched and that the commit message contains no Claude/AI references.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:'%s' | grep -E '^chore\(cleanup\): remove dead code' &amp;&amp; git log -1 --name-only --pretty=format: | grep -cE '^(src/services/CalendarService\.ts|src/utils/constants\.ts|src/views/EmbeddedCalendarView\.ts|src/views/EmbeddedAgendaView\.ts)$' | grep -E '^4$' &amp;&amp; ! git log -1 --pretty=format:'%B' | grep -iE 'claude|anthropic|ai-(generated|assist)'</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject starts with `chore(cleanup): remove dead code`
    - Latest commit touches exactly 4 files: `src/services/CalendarService.ts`, `src/utils/constants.ts`, `src/views/EmbeddedCalendarView.ts`, `src/views/EmbeddedAgendaView.ts`
    - `git log -1 --pretty=format:'%B' | grep -iE 'claude|anthropic|ai-(generated|assist)'` returns no match (CLAUDE.md commit hygiene)
    - `git status` reports a clean working tree after the commit
    - `src/utils/viewRenderers.ts` is NOT in the commit's file list (D-11 honored — function preserved)
  </acceptance_criteria>
  <done>
    CLEAN-01 lands as a single atomic commit. Working tree is clean. Build passes. The commit message follows conventional-commit style and contains zero AI/Claude references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure dead-code removal. No behavior change, no new attack surface, no new dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Accidentally removing the renderAgendaList FUNCTION (instead of just the import) | mitigate | D-11 explicitly forbids removing the function. Acceptance criteria for Task 4 and Task 5 grep for `export function renderAgendaList` in viewRenderers.ts and require exactly 1 match — the gate fails if the function is removed. |
| T-05-02 | Tampering | Accidentally removing OTHER constants from constants.ts (e.g., MEMOCHRON_VIEW_TYPE) | mitigate | D-10 strictly scopes the deletion to the two named symbols. Task 2 acceptance criteria grep for MEMOCHRON_VIEW_TYPE preservation. The TypeScript compiler would fail the build if any other consumer's import broke. |
| T-05-03 | Tampering | Accidentally removing the `Time` import from CalendarService.ts (it's named near calculateEndDate but used elsewhere) | mitigate | Pitfall in PATTERNS.md called this out. Task 1 acceptance criteria explicitly grep for the `Time` import preservation. The TypeScript compiler would also fail at the `let next: Time | null` declaration. |
| T-05-04 | Information Disclosure / Spoofing / Repudiation / Elevation / DoS | N/A | N/A | No identity, network, data, or availability boundaries are crossed by removing dead code. |
</threat_model>

<verification>
After all six tasks land, verify the phase 1 / CLEAN-01 acceptance gate from RESEARCH.md §"Verification Approach":

```bash
# Build clean
node ./node_modules/typescript/lib/tsc.js -noEmit -skipLibCheck && \
  node esbuild.config.mjs production && echo "BUILD OK"

# CLEAN-01 final gate
grep -rn "calculateEndDate\|DEFAULT_TEMPLATE_PATH\|TEMPLATE_VARIABLES" src/   # expect: no output
grep -n "renderAgendaList" src/views/EmbeddedAgendaView.ts                    # expect: no output
grep -nE "\bApp\b" src/views/EmbeddedCalendarView.ts                          # expect: no output
grep -nE "\bTFile\b" src/views/EmbeddedAgendaView.ts                          # expect: no output

# D-11: renderAgendaList function preserved
grep -n "export function renderAgendaList" src/utils/viewRenderers.ts          # expect: 1 match
```

This is also the verification gate for Phase 1 success criterion #4 ("The codebase contains no reference to calculateEndDate, DEFAULT_TEMPLATE_PATH, TEMPLATE_VARIABLES, the unused App/TFile imports, or the dead renderAgendaList import in embedded views").
</verification>

<success_criteria>
CLEAN-01 is complete when:
- `CalendarService.calculateEndDate` private method is deleted from `src/services/CalendarService.ts`
- `DEFAULT_TEMPLATE_PATH` and `TEMPLATE_VARIABLES` are deleted from `src/utils/constants.ts`
- `App` is removed from EmbeddedCalendarView.ts's obsidian import
- `TFile` is removed from EmbeddedAgendaView.ts's obsidian import
- `renderAgendaList` is removed from EmbeddedAgendaView.ts's viewRenderers import
- The `renderAgendaList` function in `src/utils/viewRenderers.ts` is INTACT (D-11)
- The `Time` import in `src/services/CalendarService.ts` is INTACT
- All other constants in `src/utils/constants.ts` (MEMOCHRON_VIEW_TYPE, etc.) are INTACT
- TypeScript type-check exits 0
- esbuild production bundle exits 0
- A single atomic commit exists with subject `chore(cleanup): remove dead code (calculateEndDate, template constants, unused imports)` and contains no Claude/AI references
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` documenting:
- The four files modified
- The single commit SHA
- The output of the dead-code gate (each grep returning empty)
- Confirmation that D-11 was honored: `grep -n "export function renderAgendaList" src/utils/viewRenderers.ts` returns exactly 1 match
- Confirmation that other constants in constants.ts were preserved (specifically MEMOCHRON_VIEW_TYPE)
- Confirmation that the Time import in CalendarService.ts was preserved
- Note that this completes Phase 1 / Foundation if Plans 01-04 also landed cleanly
</output>
